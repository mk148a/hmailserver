# Current State
- UTC/local timestamp: `2026-08-25T17:35:00Z` / `2026-08-25T20:35:00+03:00`
- Current branch and upstream: `net10-installer-rollback-guard` / no upstream
- Current HEAD at last content update: `6a83d5401`; this documentation-only refresh is the subsequent state update.
- Last successfully pushed commit: `5d89e85c4` on `origin/net10-modernization`
- Latest focused-test result: SURBL COM `17 passed, 0 skipped, 0 failed`; SURBL SQL `6 passed, 0 skipped, 0 failed`; SecurityRanges COM `28/28`; whitelist COM `19/19`; related SQL whitelist `11/11`; TCP/IP `25/25`; SSL `14/14`; blocked attachments `17/17`
- Latest full Net10 result: standard Debug `2726 passed, 96 skipped, 0 failed` (`2822` total); opt-in Debug remains unavailable because required environment variables are absent; prerequisites passed with .NET 10 SDK 10.0.301 and MSBuild 17.x
- Opt-in tests passed/skipped/blocked: SQL integration `94 passed, 2 skipped` against local MSSQLSERVER Developer with GUID-scoped databases; live SQL/Data diagnostics `6 passed, 0 skipped, 0 failed` on exact 1000-message benchmark fixtures with marker cleanup verified; Full-Text SQL/Data acceptance `32 passed, 0 skipped, 0 failed`; offline SEC-18 boundary tests `47 passed, 1 skipped`; installer/registration/rollback guard tests `10 passed, 1 skipped`; staging inventory remains blocked because the repository evidence root was removed; reparse-point tests remain skipped because disposable symlink creation is not permitted; registered COM/SEC-18, installer/service rollback, paired C++ load, and long-soak infrastructure remain unavailable
- Current bounded slice: code/test commit `6a83d5401` adds generation-bound authorization leases to authenticated SURBL insert/update/delete callbacks through `AntiSpam.SURBLServers`, preserving COM identity, direct activation denial, owner scope, and snapshots. Legacy anchors are `InterfaceSURBLServer::Save/Delete` (`hmailserver/source/Server/COM/InterfaceSURBLServer.cpp:12,187`) and `InterfaceSURBLServers::DeleteByDBID/Add` (`hmailserver/source/Server/COM/InterfaceSURBLServers.cpp:88,135`). Focused tests are `17/17` COM and `6/6` SQL; standard full Debug is `2726/96/0`. Cache `Clear` remains blocked because no real cache-container/backend abstraction exists.
- Completed milestones: native Data restore rename; authenticated AntiVirus mutations; handle-relative recursive raw `DataBackup` traversal, digesting, nested destination creation, and private snapshot containment; process-local CrashSimulationMode COM/Admin parity; authenticated production-shaped backup dispatch evidence; legacy salted-SHA256 administrator-password setter with INI write result and live verifier publication; legacy JScript line-separator escaping with WSH regression coverage; retained Settings.Scripting parent reauthentication guard; raw non-DB-only backup and legacy-version host-start refusal; Full-Text-capable localhost SQL migration and SQL/Data round-trip acceptance; disposable Data root ACL/write/read evidence; authenticated Application.Start/Stop lifecycle delegation with ordered rollback; ServerState bootstrap/readiness/shutdown transition bridge; detailed history remains in `hmailserver/source/Server.Net10/REWRITE_BACKLOG.md` and `CODEX_HANDOFF.md`
- Open production blockers: process-kill/power-loss reboot proof for INI replacement; registered/out-of-process COM/SCM wake validation; real script override and AD/SSPI credential evidence; exact native SMTP security-range behavior and service lifecycle semantics; true atomic backup snapshot/quiescence and crash-consistency contract; same-name replacement and remaining destination/binding ancestor TOCTOU review; target-identity native filesystem review; SEC-18; plaintext PHP session credentials; AD/master-user; DKIM/DMARC/SPF; installer/service/data rollback; paired C++ performance; SMTP/delivery thresholds; and 24-hour leak soak. Legacy `PasswordValidator::ValidatePassword` is anchored at `hmailserver/source/Server/Common/Util/PasswordValidator.cpp:105-164`; script override precedes empty-password, AD, and hash checks, and Net10 now retains nullable stored fields until that decision. Account/domain active admission is covered; real script/AD credential execution remains unproven. Legacy `InterfaceSettings::SetAdministratorPassword` is anchored at `hmailserver/source/Server/COM/InterfaceSettings.cpp:1014-1032`; the durability change adds directory metadata flush but does not prove actual crash/reboot survival. Legacy `Events.cpp:209-248` exposes fetch-account Password to scripts, but the Net10 runner now transports it through stdin rather than runner source. Legacy DISPID 76 is `IInterfaceSettings::SetAdministratorPassword`, not an Application member. `SubmitEMail` remains unauthenticated because legacy `InterfaceApplication::SubmitEMail` is also unauthenticated. The offline 100k benchmark is diagnostic only: its expected result uses the same implementation and it is not a C++ comparison.
- Environment-blocked work: local MSSQLSERVER Developer is usable for isolated GUID-scoped test databases and benchmark fixtures; C++ paired load requires an independently isolated registry/configuration path or disposable VM because the legacy preflight rejects the current installed Application registration; a separate disposable SQL VM/instance, live WSH/AD/SSPI credentials, worker-token/registered-COM, cloned installer rollback, target-identity filesystem, DNS, and soak evidence remain unavailable; no production service, existing hMailServer database, Data directory, COM registration, DCOM ACL, IIS, or firewall state was changed. The earlier native rename failure is no longer reproduced by the current focused test.
- Protected/do-not-touch areas: production service/SQL/Data, installed Application COM registration/DCOM, production IIS, dirty `AGENTS.md`, and pre-existing untracked SEC-18/benchmark/migration/disposable artifacts
- Next three independent slices: implement a read-only SQL backup projection snapshot factory with transaction-context wiring for every production projection store; installer/service/Data rollback acceptance on cloned legacy state; isolated registered COM/SEC-18 caller evidence when independent worker-token infrastructure is available. Cache `Clear` requires a separately designed real cache backend before implementation.
