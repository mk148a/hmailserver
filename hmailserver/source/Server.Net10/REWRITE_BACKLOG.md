
## Current Audit Note (2026-08-09, OBSOLETE ANTISPAM TARPIT SETTER PARITY)

Code/test commit `508d35d17` closes the bounded legacy `AntiSpam.TarpitDelay`/`TarpitCount` setter gap. Legacy `InterfaceAntiSpam::get_TarpitDelay`, `put_TarpitDelay`, `get_TarpitCount`, and `put_TarpitCount` (`hmailserver/source/Server/COM/InterfaceAntiSpam.cpp:729-792`) return `0` for getters and authenticated `S_OK` no-op for setters; the historical settings were removed by `Upgrade5320to5400MSSQL.sql`. The .NET `AntiSpam` facade (`hmailserver/source/Server.Net10/src/HMailServer.ComInterop/AntiSpam.cs:292-294,406-408`) now preserves that behavior without adding SQL fields, persistence, protocol behavior, or broader AntiSpam mutation.

Focused `AntiSpamComContractTests` coverage is `15 passed, 0 failed, 0 skipped`; full Net10 is `1961 passed, 31 skipped, 2 failed`, with the two failures caused by host-AV locks on generated scanner `.eml` cleanup. Direct activation remains `E_ACCESSDENIED`; IID/vtable/DISPID/class identity is unchanged. The retained AntiSpam facade continues to match the legacy cached `config_` lifetime established by `InterfaceAntiSpam::LoadSettings` (`InterfaceAntiSpam.cpp:28-35`); no unrelated retained-auth refactor is included.

The legacy IMAP domain-alias/default-domain authentication item is stale: `PasswordValidator::ValidatePassword` (`hmailserver/source/Server/Common/Util/PasswordValidator.cpp:45-51`) and `IMAPCommandAUTHENTICATE` (`hmailserver/source/Server/IMAP/IMAPCommandAuthenticate.cpp:85-91`) are represented by `SqlServerImapAccountAuthenticator.AccountLookupSql` and `AuthenticateNormalAsync` (`hmailserver/source/Server.Net10/src/HMailServer.Storage.SqlServer/SqlServerImapAccountAuthenticator.cs:3-60,194-226`), with existing AD/alias integration coverage. Do not restart it without a newly proven gap.

The next highest-priority gate remains populated SQL/Data restore acceptance with an approved disposable connection and isolated-create opt-in. Independent environment-gated work remains live SQL/FTS or protocol performance acceptance and a clean scanner run on an AV-compatible isolated path. Release remains RED.

## Current Audit Note (2026-08-09, BACKUP CREATION REVALIDATION)

The formerly recorded raw non-DB-only `BODomains|BOMessages` `DataBackup` staging item is complete in `50d8cefc3`, and the full option matrix is covered by `d210c5611`. Legacy anchors are `BackupExecuter::StartBackup`/`BackupDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:57-147,172-217`), `FileUtilities::CopyDirectory`/`DeleteFilesInDirectory`, and `Compression::AddDirectory`; current implementation is `SevenZipBackupArchiveRuntime.CreateAsync`. Raw mode leaves the external sibling `DataBackup`, compressed mode archives staged content, and DB-only mode emits metadata without physical message staging.

Focused backup creation/restore containment revalidation passed `150/150`; `build/check-net10-prereqs.ps1 -RequireMsBuild` passed. This is evidence refresh only; no production code changed. Do not restart the stale raw staging backlog item. The next gate is disposable SQL/Data restore acceptance when its approved isolated connection and opt-in are available.

## Current Audit Note (2026-08-09, CLAMAV LOCAL-TARGET REBIND HARDENING)

Code/test commit `414b1e9e0` closes the bounded ClamAV hostname re-resolution window in the COM test path. Legacy `InterfaceAntiVirus::TestClamAVScanner` (`hmailserver/source/Server/COM/InterfaceAntiVirus.cpp:577-596`) passes the hostname through `VirusScannerTester::TestClamAVConnect` (`hmailserver/source/Server/Common/AntiVirus/VirusScannerTester.cpp:22-45`) to `ClamAVVirusScanner::Scan`/`SynchronousConnection::Connect` (`hmailserver/source/Server/Common/AntiVirus/ClamAVVirusScanner.cpp:48-64`). The .NET `LegacyLocalScannerTargetGuard.TryGetValidatedLocalAddress` resolves once and proves all answers are local, then `AntiVirus.TestClamAVScanner` passes the selected validated IP literal to `IClamAvScannerTestRuntime`. This prevents the existing `ClamAvInstreamClient` from performing a second hostname lookup in this COM test flow.

Focused guard/ClamAV/AntiVirus tests are `20 passed, 0 failed, 0 skipped`. Filtered full Net10 is `1954 passed, 0 failed, 31 skipped`; default full is `1959 passed, 2 failed, 31 skipped`. The default failures are the known host-AV locks during generated scanner `.eml` cleanup. No installed COM identity, IDL, direct activation boundary, authentication, SMTP trust, SQL/Data, service, registry, DCOM, IIS, or production state changed.

The slice is intentionally limited to the authenticated COM scanner-test path. The general configured-message scanning path still accepts its configured ClamAV host through `ClamAvInstreamClient` and is not changed here. The next independent slice is approved disposable SQL/Data restore acceptance when its isolated connection and opt-in exist; release remains RED.

## Current Audit Note (2026-08-09, RETAINED ANTIVIRUS AUTHORIZATION)

Code/test commit `3c8b58981` closes the retained AntiVirus authorization gap. Legacy `InterfaceSettings::get_AntiVirus` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:387-405`) exposes the object only to a server administrator, and `InterfaceAntiVirus` checks `GetIsServerAdmin` at every public getter, setter, attachment-blocking operation, and scanner test (`hmailserver/source/Server/COM/InterfaceAntiVirus.cpp:20-581`). The .NET `AntiVirus.Snapshot` guard now evaluates the live administrator callback for retained scalar and scanner operations. `BlockedAttachments.GetBlockedAttachments` now guards retained collection access and `DeleteByDBID` as well. This collection guard is intentional fail-closed hardening: legacy `InterfaceBlockedAttachments::DeleteByDBID` checked only its attached parent pointer, while the current .NET child Save/Delete paths already carry the live callback.

Focused AntiVirus/BlockedAttachments tests are `27 passed, 0 failed, 0 skipped`. Filtered full Net10 is `1951 passed, 0 failed, 31 skipped`; default full is `1956 passed, 2 failed, 31 skipped`. The default failures are reproducible host-AV locks on generated scanner `.eml` cleanup, isolated to `ClamWinScannerTestRuntimeTests` and `CustomScannerTestRuntimeTests`. No installed COM identity, IDL, direct activation boundary, SMTP trust, SQL/Data, service, registry, DCOM, IIS, or production state changed.

The next code slice is a parity-confirmed ClamAV local-target connection hardening slice. `AntiVirus.TestClamAVScanner` (`hmailserver/source/Server.Net10/src/HMailServer.ComInterop/AntiVirus.cs:255`) and `LegacyLocalScannerTargetGuard` validate the hostname, but `ClamAvInstreamClient` (`hmailserver/source/Server.Net10/src/HMailServer.Security/ClamAvInstreamClient.cs:43`) can resolve it again when opening the socket, leaving a DNS-rebind window. Keep SMTP trust and broader scanner behavior unchanged. The highest-priority environment-gated work remains disposable SQL/Data restore acceptance; release remains RED.

## Current Audit Note (2026-08-09, RETAINED MESSAGEINDEXING AUTHORIZATION PARITY)

Code/test commit `e2109f422` completes the bounded retained MessageIndexing authorization slice. Legacy `InterfaceSettings::get_MessageIndexing` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:1974-1990`) requires server-admin access; `InterfaceMessageIndexing::get_TotalMessageCount`, `get_TotalIndexedCount`, `Clear`, and `Index` (`hmailserver/source/Server/COM/InterfaceMessageIndexing.cpp:64-137`) recheck the attached authentication, while `get_Enabled`/`put_Enabled` (`:30-62`) intentionally do not. The .NET path propagates the live callback into `MessageIndexingComClass` and guards retained legacy methods plus the extended status properties and destructive `Rebuild`. Direct activation remains denied and installed IID/vtable/DISPID/type-library identity is unchanged.

Focused MessageIndexing/Settings coverage is `25 passed, 0 failed, 0 skipped`; filtered full Net10 is `1949 passed, 0 failed, 31 skipped`; default full is `1954 passed, 2 failed, 31 skipped`. The two default failures are host-AV cleanup locks in the ClamWin and custom scanner tests, not this slice. Security review initially identified the extended retained-operation bypass; the guarded status and `Rebuild` path now close that finding. Reality status remains RED for release. No SQL/Data, SMTP trust, live reconfiguration, service, registry, DCOM, IIS, or production state changed.

The subsequent parity audit found no valid code slice. `InterfaceServerMessages::{LoadSettings,Refresh,get_Count,get_Item,get_ItemByDBID,get_ItemByName}` (`hmailserver/source/Server/COM/InterfaceServerMessages.cpp:13-146`) and `InterfaceServerMessage` (`InterfaceServerMessage.cpp:13-80`) match the current .NET acquisition-scoped behavior. `InterfaceGlobalObjects::get_Languages` (`InterfaceGlobalObjects.cpp:58-76`), `InterfaceLanguages::{get_Count,get_Item,get_ItemByName}` (`InterfaceLanguages.cpp:12-73`), and `InterfaceLanguage` (`InterfaceLanguage.cpp:13-80`) do not recheck authentication; adding a callback would be a compatibility regression. The performance agent reran the existing offline gate at `565175aff` with p50/p95/p99 `6.839/13.904/16.184 ms`; this is not live performance evidence.

The next code slice is a fresh parity audit of remaining COM/Admin child-facade paths; do not restart older entries without confirming an actual gap. The highest-priority environment-gated slice remains approved disposable SQL/Data restore acceptance, whose required integration connection and isolated-create opt-in are still unset. Release remains RED.

## Historical Audit Note (2026-08-09, RETAINED ACCOUNT CHILD AUTHORIZATION PARITY)

Code/test commit `9b93e9c34` completes the bounded retained Account child-facade guard. Legacy `InterfaceAccount::SetAuthentication` (`hmailserver/source/Server/COM/InterfaceAccount.cpp:53-56`) is attached to `get_Messages` (`:420-445`), `get_FetchAccounts` (`:694-721`), `get_Rules` (`:790-815`), `get_IMAPFolders` (`:817-845`), and scalar `get_AdminLevel` (`:723-738`). The .NET path now carries the live authentication callback through Account `AdminLevel`, Messages and returned Message facades, FetchAccounts and returned FetchAccount facades/drafts, Rules, and folder Messages.

Focused Account/Links coverage is `123 passed, 0 failed, 0 skipped`; `git diff --check` passed. The default full run reached `1947 passed, 2 failed, 31 skipped`; both failures are scanner cleanup `UnauthorizedAccessException` caused by host AV locking generated `.eml` files in `ClamWinScannerTestRuntimeTests` and `CustomScannerTestRuntimeTests`. Excluding those two scanner classes, the full assembly is `1942 passed, 0 failed, 31 skipped`. Installed COM identity, direct activation, standalone callback-null factories, SQL ownership, SMTP trust, and live reconfiguration are unchanged.

Residual test gap for the next slice: explicit retained-child denial for Rules and IMAPFolders, post-logout draft setter denial, and a clean scanner run on a host where AV does not lock test files. Nested Domain collection callback propagation, SQL/Data restore, service/COM, SEC-18, live performance, and soak gates remain open; release is RED.

## Current Audit Note (2026-08-09, RETAINED LINKS CHILD AUTHORIZATION PARITY)

Code/test commit `52d000029` closes the bounded direct-child propagation gap left by root retained `Links`. Legacy `InterfaceLinks::{get_Domain,get_Account,get_Alias,get_DistributionList}` (`hmailserver/source/Server/COM/InterfaceLinks.cpp:22-138`) attaches the shared `COMAuthentication` object to each child; the corresponding .NET `Links` factories now pass the live `ApplicationAuthorizationAuthority.IsServerAdministrator` guard into Domain, Alias, DistributionList, and Account facades. `DistributionList` uses a separate read callback so existing standalone collection scalar-read semantics are unchanged while direct Links children deny retained reads after logout.

Focused Links/related tests are `78 passed, 0 failed, 0 skipped`; full Net10 is `1945 passed, 0 failed, 31 skipped`; `git diff --check` passed. Installed `IInterfaceLinks`, child IID/CLSID/ProgID/DISPID/vtable/type-library identity, direct activation denial, owner-scoped SQL lookup, SMTP trust, and live reconfiguration are unchanged. This slice does not claim nested `Account.Messages`, `FetchAccounts`, `Rules`, `IMAPFolders`, or nested Domain collection parity; those are the next code slice and must include negative retained-object tests. Release remains RED because SQL/Data restore, service/COM, SEC-18, installer, live protocol/load, and soak gates remain open.

## Current Audit Note (2026-08-09, RETAINED LINKS AUTHORIZATION PARITY)

Code/test commit `e5b441b36` completes the bounded root `Links` authorization slice. Legacy `InterfaceApplication::get_Links` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:457-479`) requires server-admin access, and `InterfaceLinks::{get_Domain,get_Account,get_Alias,get_DistributionList}` (`hmailserver/source/Server/COM/InterfaceLinks.cpp:22-138`) rechecks the shared `COMAuthentication` state before each lookup. The .NET `Application.Links` now passes a live `ApplicationAuthorizationAuthority.IsServerAdministrator` guard into `Links`; all four getters fail before store reads after failed reauthentication and work again after successful reauthentication.

Focused `LinksComContractTests` coverage is `8 passed, 0 failed, 0 skipped`; full Net10 is `1944 passed, 0 failed, 31 skipped`. Direct activation remains `E_ACCESSDENIED`; retained root `Links` denial preserves legacy `0x800403E9` and the standard `COMAuthentication::GetAccessDenied` message. Installed `IInterfaceLinks` IID/CLSID/ProgID/DISPIDs/vtable and existing cross-domain server-admin lookup semantics are unchanged. This slice intentionally covers the retained `Links` root getters only. Child facades returned by `Links` (notably `Domain`, `Alias`, `DistributionList`, and `Account` nested access) retain separate authorization propagation gaps and are the next code slice; no broader child-graph refactor was included.

The highest-priority environment-gated slice remains approved disposable SQL/Data restore acceptance; its integration connection and isolated-create opt-in are still unset. No production SQL/Data, service, SMTP, registry, DCOM, or WebAdmin state changed. Release remains RED.

## Current Audit Note (2026-08-09, GLOBALOBJECTS DELIVERY-QUEUE AUTHORIZATION PARITY)

Code/test commit `89cc2c860` closes one retained-object authorization gap without changing installed COM identity, SQL, SMTP, service, registry, or DCOM state. Legacy `InterfaceApplication::get_GlobalObjects` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:163`), `InterfaceGlobalObjects::get_DeliveryQueue` (`InterfaceGlobalObjects.cpp:34-54`), and `InterfaceDeliveryQueue::{Clear,ResetDeliveryTime,Remove,StartDelivery}` (`InterfaceDeliveryQueue.cpp:14-91`) use the shared mutable `COMAuthentication::GetIsServerAdmin` state at property/method entry. The .NET `Application.GlobalObjects` now passes a live `ApplicationAuthorizationAuthority.IsServerAdministrator` guard into retained `GlobalObjects` and `DeliveryQueue` objects.

Focused `GlobalObjectsComContractTests` coverage is `8 passed, 0 failed, 0 skipped`; full Net10 is `1943 passed, 0 failed, 31 skipped`. Unauthorized `Clear` preserves the legacy error text and HRESULT `0x800403E9`; the other three queue methods preserve raw `S_FALSE`. Existing direct activation denial and IIDs/CLSIDs/ProgIDs/DISPIDs/vtable shape remain covered. The guard is intentionally method-entry based, matching legacy check-then-use behavior; concurrent invalidation after the check and asynchronous `Clear` scheduling remain residual risks, not an atomic revocation guarantee.

The next smallest code candidate is retained `Links` live authorization parity, after parity confirmation. The highest-priority environment-gated slice remains approved disposable SQL/Data restore acceptance; its integration connection and isolated-create opt-in are still unset. Release remains RED.

## Current Audit Note (2026-08-09, RELEASE-GATE AND ACCOUNT PASSWORD PARITY REVIEW)

The latest parity/security/reality audit found no independently executable production slice on this host. Legacy `InterfaceAccount::ValidatePassword` (`hmailserver/source/Server/COM/InterfaceAccount.cpp:350-364`) calls `PasswordValidator::ValidatePassword` on an attached account, covering empty-password rejection, plaintext/MD5/SHA256/Blowfish verification, AD validation, and `OnClientValidatePassword`. The .NET `Account.ValidatePassword` (`hmailserver/source/Server.Net10/src/HMailServer.ComInterop/AccountComClass.cs:417-426`) correctly remains fenced at `E_NOTIMPL` for SQL-backed snapshots: implementing it requires a fresh domain-scoped credential verifier, retained-object reauthentication, and reviewed COM-specific AD/script behavior. Do not reuse the protocol authenticator or add credential fields to the normal administration snapshot as a shortcut.

The existing short offline soak is intentionally not promoted to a release gate because it repeats an in-process synthetic LINQ workload and reports host-wide TCP snapshots. The commandable 100,000-message offline SEARCH/SORT gate remains valid only as offline evidence. No production code or benchmark gate was changed by this audit.

The authoritative next slice remains approved disposable SQL/Data restore acceptance. Required evidence is isolated populated-store commit/rollback, staged file cleanup, and failure containment. The approved integration connection and isolated-create opt-in are unset; production SQL/Data remains prohibited.

## Current Audit Note (2026-08-09, SUPERSEDED COM/WEBADMIN NEXT-SLICE ENTRIES)

Parity review confirms that older entries naming `RuleCriteria.MatchValue`, `hm_status.php`, `hm_backup.php`, `background_servermessage_save.php`, and DistributionLists Add/Save as next work are superseded by current code/tests. `RuleCriteria.MatchValue` is implemented in `d95ce9c69`; the status and backup handlers use POST-only mutation inputs plus `hmailRequirePostCsrfToken()`; the server-message handler has the same boundary; and `DistributionLists.Add()` plus new-item `DistributionList.Save()` is implemented with owner-scoped defaults, six-field parameterized insert, identity readback, failure retention, and direct activation denial. Do not restart these slices.

The authoritative next slice remains approved disposable SQL/Data restore acceptance. Required evidence is isolated populated-store commit/rollback, staged file cleanup and failure containment. The SQL service is present on this host, but the approved integration connection and isolated-create opt-in are unset; production SQL/Data remains prohibited. Live SQL identity/rollback, out-of-process COM activation, and connection-loss reconciliation for DistributionLists remain evidence gaps, not reasons to alter the already implemented path.

## Current Next Slice (2026-08-09, APPROVED DISPOSABLE SQL/DATA RESTORE ACCEPTANCE)

Code/test commit `2925427d2` adds only the internal `ReinitializationAdmission` gate: atomic single-flight admission, duplicate suppression while running, and release after success or exception. It is not wired to COM or service lifecycle. Legacy anchors are `Application::Reinitialize` (`hmailserver/source/Server/Common/Application/Application.cpp:437-450`), `Reinitializator::ReInitialize`/`WorkerFunc` (`hmailserver/source/Server/Common/Application/Reinitializator.cpp:35-57`), and `InterfaceApplication::Reinitialize` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:91-108`).

The bounded seam is complete with `3 passed, 0 failed, 0 skipped`; default full Net10 is `1942 passed, 0 failed, 31 skipped`. The next slice is the existing raw non-DB and public-cleanup restore acceptance against an explicitly approved disposable SQL database and isolated Data directory. It must prove commit, rollback, populated-store scope, staged file cleanup, and failure containment without production resources. Do not wire actual reinitialization, stop listeners, reload SQL/configuration, reset readiness, change COM identity/ACLs, or claim full restore parity. Native handle-relative restore remains blocked on this host by `ERROR_INVALID_PARAMETER` for the safe relative rename path.

## Current Next Slice (2026-08-09, DISPOSABLE SQL/FTS AND LIVE PROTOCOL BENCHMARK ACCEPTANCE)

Code/test commit `5d0e62192` adds `build/test-net10-benchmarks.ps1`, a commandable Release-mode gate around the existing offline synthetic IMAP SEARCH/SORT benchmark. The gate fixes the scenario to 100,000 messages, seed `5700`, search term `needle`, and `DATE DESC, UID ASC`; it validates JSON/CSV/Markdown presence and consistency, correctness, threshold, timestamps, runtime metadata, and exact pre-build Git commit identity.

Legacy anchors are `IMAPCommandSEARCH::ExecuteCommand`/`DoesMessageMatch_` (`hmailserver/source/Server/IMAP/IMAPCommandSearch.cpp:40`), `IMAPSearchParser::ParseCommand` (`hmailserver/source/Server/IMAP/IMAPSearchParser.cpp:118`), `IMAPSortParser::Parse` (`hmailserver/source/Server/IMAP/IMAPSortParser.cpp:24`), and `IMAPSort::Sort`/`CacheHeaderFields_` (`hmailserver/source/Server/IMAP/IMAPSort.cpp:32`). The accepted run is only an offline correctness/performance gate: p50 `6.695 ms`, p95 `7.261 ms`, p99 `7.322 ms`, `9091/9091`, threshold passed. It is not evidence of live SQL FTS, live IMAP behavior, C++ equivalence, 1,000 concurrent connections, delivery performance, or long-duration leak freedom.

The next bounded slice is to add an approved disposable SQL/FTS and live protocol acceptance path with explicit thresholds and JSON/CSV/Markdown hardware/runtime/configuration metadata. Do not use production SQL/Data, do not claim C++ parity without a comparable legacy run, and do not broaden into installer, restore, SEC-18, or service cutover work. Release remains RED. Native restore containment also remains blocked on this host because the safe RootDirectory-relative rename path returns `ERROR_INVALID_PARAMETER`; the unsafe absolute-path fallback was intentionally not retained.

## Current Next Slice (2026-08-08, FULL-RESTORE PUBLIC-FOLDER ORDERING)

Code/test commit `3e912982a` adds a native Windows source-handle-backed directory swap seam to `BackupRestoreDataDirectoryRuntime`. `WindowsBackupRestoreDataDirectoryMutation.MoveDirectory` opens the source directory with `CreateFileW`, performs a non-overwriting `FILE_RENAME_INFO` rename, and is used for both the target-to-rollback swap and rollback. Focused runtime coverage is `17 passed, 0 failed, 0 skipped`; default full Net10 is `1939 passed, 0 failed, 29 skipped`.

Legacy anchors are `BackupExecuter::RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:339-388`) and `FileUtilities::CopyDirectory` (`hmailserver/source/Server/Common/Util/FileUtilities.cpp:370-402`), which are path-based. This bounded hardening does not change COM/IDL identity, SQL, protocol, service, or recovery-journal contracts. It is not full handle-relative containment: the destination uses an absolute path, recursive `CopyTree` remains path-based, and cleanup still uses path-based deletion. The next slice is full-restore public-folder deletion with staged message-file cleanup and reinitialization ordering on isolated disposable SQL/Data; native destination-parent/copy containment remains an open security slice.

Release remains RED: populated SQL/Data acceptance, full public-folder/settings/reinitialization ordering, destination/copy TOCTOU, process-kill/power-loss, SQL/filesystem atomicity, service/COM, SEC-18, installer, migration, AD/DC, and soak evidence remain open.

## Current Next Slice (2026-08-08, FULL-RESTORE PUBLIC-FOLDER ORDERING)

The transaction-scoped public-folder cleanup capability is complete in code/test commit `5d9ad666c`. `IBackupRestoreMetadataTransaction.DeleteAllPublicFoldersForRestoreAsync` and `SqlServerImapFolderAdministrationStore.DeleteAllPublicFoldersForRestoreSql` select only legacy public folders (`folderaccountid = 0`) and their public messages (`messageaccountid = 0`), delete recipients for non-Delivered messages, search rows, metadata, ACLs, messages, and non-Inbox folders inside the caller-owned SQL transaction, and leave commit/rollback to that owner. Focused store coverage is `11 passed, 0 failed, 0 skipped`; default full Net10 is `1937 passed, 0 failed, 29 skipped`.

Legacy anchors are `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`), the public-folder `DeleteAll()` call (`BackupExecuter.cpp:287-289`), `PersistentIMAPFolder::DeleteObject`, and `Reinitializator::ReInitialize` (`hmailserver/source/Server/Common/Application/Reinitializator.cpp:35-57`). The capability is intentionally not wired into DB-only restore because legacy skips public-folder deletion for `bMessagesDBOnly`, and the current DB-only path does not restore public-folder metadata/files. The next bounded slice is full-restore public-folder deletion ordering with staged message-file cleanup and reinitialization ordering on an isolated disposable SQL/Data target. Do not broaden DB-only restore, alter COM identity, or touch production state.

Release remains RED: approved populated SQL/Data acceptance, filesystem atomicity, handle-relative containment, process-kill/power-loss, service/COM, SEC-18, installer, migration, AD/DC, and soak evidence remain open.

## Historical Next Slice (2026-08-08, WIRE DOMAIN CLEANUP INTO FULL RESTORE; SUPERSEDED)

The transaction-scoped domain cleanup capability is complete in code/test commit `74ca89853`, but it is not yet called by restore orchestration. Legacy `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`) deletes domain-owned state before replacing the Data directory; `Collection<T,P>::DeleteAll` (`hmailserver/source/Server/Common/BO/Collection.h:85-215`) supplies collection deletion semantics, `PersistentAccount::DeleteObject` removes account-owned folders/groups/ACLs, and `Reinitializator::ReInitialize` (`hmailserver/source/Server/Common/Application/Reinitializator.cpp:36-53`) performs the later application restart/reinitialization.

The next bounded slice is to invoke `IBackupRestoreMetadataTransaction.DeleteAllDomainsForRestoreAsync` at the full-restore boundary, preserving the existing authorization lease and recovery journal, then prove ordering and rollback against an approved disposable SQL/Data target. Do not add public-folder/settings deletion, reinitialization, live reconfiguration, service/COM activation, or production targeting in that slice. Current focus is `6 passed, 0 failed, 3 skipped`; default full Net10 is `1934 passed, 0 failed, 29 skipped`; SQL integration remains skipped because the approved disposable SQL environment is not configured.

## Current Next Slice (2026-08-08, DISPOSABLE POPULATED-STORE DB-ONLY RESTORE ACCEPTANCE)

DB-only restore replacement wiring is complete in code/test commit `a2b030d82`. `BackupRestoreExecution.RestoreMetadataAsync` now performs archive-internal duplicate validation, preserves the existing authorization lease, opens the SQL transaction, calls `DeleteAllDomainsForRestoreAsync` once, and inserts the archive metadata through the same transaction-scoped stores. Legacy anchors are `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`), `Collection<T,P>::XMLLoad`/`DeleteAll` (`hmailserver/source/Server/Common/BO/Collection.h:85-215`), `PersistentAccount::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:55-100`), and `Reinitializator::ReInitialize` (`hmailserver/source/Server/Common/Application/Reinitializator.cpp:35-57`).

The next bounded slice is an approved disposable SQL/Data acceptance test with a populated existing domain graph, same-name replacement, commit, disposal rollback, and injected metadata failure. It must not target production SQL/Data, add public-folder/settings deletion, invoke reinitialization, alter COM identity, or change non-DB empty-store behavior. Current focused restore execution/round-trip coverage is `15 passed, 0 failed, 11 skipped`; default full Net10 is `1936 passed, 0 failed, 29 skipped`; SQL integration remains skipped because the approved disposable SQL environment is not configured.

## Current Completed Slice (2026-08-08, FINAL RESTORE CONTAINMENT REVALIDATION)

Code/test commit `0d08e2c47` adds a final path-based containment revalidation immediately before non-DB restore staging, after metadata parse and the authorization lease. `BackupRestoreExecutionTests` mutates the raw source or target at the lease boundary and verifies fail-closed behavior with zero data copy and zero domain mutation. The slice does not claim handle-relative safety and does not alter installed COM identity, direct activation, SMTP trust, or machine state.

Focused coverage is `13 passed, 0 failed, 0 skipped`; default full Net10 is `1933 passed, 0 failed, 27 skipped`. The remaining path substitution race in `BackupRestoreDataDirectoryRuntime.Directory.Move`/`CopyTree` requires native handle-relative operations. Process-kill/power-loss, live SQL/Data, normal-installation deletion/reinitialization, service/COM, SEC-18, installer, AD/DC, migration, and soak gates remain open. Next slice: native handle-relative restore swap/copy containment on disposable targets.

## Current Completed Slice (2026-08-08, RECOVERY JOURNAL FINALIZATION DURABILITY)

Code/test commit `cc1d0f6a5` closes the journal parent-directory finalization gap in the bounded non-DB restore runtime. `BackupRestoreRecoveryJournal.Persist` flushes after replacement; `Remove` flushes after deletion and restores/flushed evidence when finalization fails. `BackupRestoreDataDirectoryRuntime` tracks rollback-artifact deletion and fails closed with a pending journal instead of attempting rollback after the artifact is gone. The Windows implementation is private native `CreateFileW` with `FILE_FLAG_BACKUP_SEMANTICS` and `FlushFileBuffers`; no COM contract or machine-wide state changes.

Focused recovery coverage is `15 passed, 0 failed, 0 skipped`; default full Net10 is `1931 passed, 0 failed, 27 skipped`. The tests simulate directory flush failures; process-kill/power-loss restart evidence remains unavailable, and path-based swap/copy remains a separate handle-relative containment gap. Normal-installation deletion/reinitialization, crash-safe SQL/filesystem atomicity, service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Next slice: implement handle-relative restore swap/copy containment on an isolated disposable target.

## Current Completed Slice (2026-08-08, ARCHIVED ACCOUNT CREDENTIAL RESTORE PARITY)

Code/test commit `d039b8ed8` closes the account credential restore parity gap. Legacy `Account::XMLLoad` (`hmailserver/source/Server/Common/BO/Account.cpp:335-346`) copies the archive `Password` and `PasswordEncryption` values into the account, and `PersistentAccount::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:263-280`) writes those values directly to `hm_accounts`. `BackupRestoreMetadataWriter.RestoreAccountsAsync` now invokes `InsertAccountForRestoreAsync`; `SqlServerAccountAdministrationStore` preserves the archived password and encryption type, and the default restore operation fails closed for unsupported stores. Normal Administrator insert/update behavior is unchanged.

Focused account/parser/store coverage is `26 passed, 0 failed, 0 skipped`; default full Net10 is `1928 passed, 0 failed, 27 skipped`. The disposable SQL credential round-trip is implemented but skipped because approved SQL environment variables are unset. This slice does not validate or normalize missing/unsupported legacy metadata, does not add live reconfiguration, and does not broaden restore scope. Release remains RED. Next slice: harden recovery-journal durability and handle-relative containment, then implement legacy deletion/reinitialization ordering on disposable targets.

## Current Completed Slice (2026-08-08, NON-DB RESTORE RECOVERY JOURNAL)

Code/test commit `904000f85` adds a bounded recovery journal around the existing non-DB Data directory swap. `BackupRestoreRecoveryJournal` persists a size-bounded absolute-path manifest with phase transitions before the move, after filesystem replacement, around metadata commit, and during rollback. Normal success removes the journal and rollback directory; rollback failure or ambiguous metadata outcome preserves evidence. `Program.cs` refuses service startup when a pending or malformed journal is present, and a later restore refuses to mutate until manual review. The journal is `[ComVisible(false)]`; installed COM identities, vtables, DISPIDs, authentication boundaries, SMTP trust, and live reconfiguration are unchanged.

Legacy anchors are `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`), `Collection<T,P>::XMLLoad`/`DeleteAll` (`hmailserver/source/Server/Common/BO/Collection.h:85-220`), and `Reinitializator::ReInitialize` (`hmailserver/source/Server/Common/Application/Reinitializator.cpp:36-53`). Legacy deletes domains/public folders before replacing data and reinitializes asynchronously; current .NET remains disposable-target-only and does not claim that SQL and NTFS commit atomically. Current symbols are `BackupRestoreRecoveryJournal`, `BackupRestoreDataDirectoryRuntime.RestoreAsync`, `MetadataBackupRestoreExecutor.ExecuteNonDbDataRestoreAsync`, and service `Program.cs`. Focused recovery coverage is `12 passed, 0 failed, 0 skipped`; default full Net10 is `1914 passed, 0 failed, 25 skipped`.

This is evidence preservation and fail-closed detection, not automatic crash reconciliation. Power-loss directory-entry durability, journal ACL/authentication and handle-relative safety, extracted-temp cleanup after process kill, SQL connection-loss/commit ambiguity, full schema constraints, normal-installation deletion/reinitialization, queued service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Security and reality reviews remain RED. Next slice: legacy domain/public-folder deletion and application reinitialization ordering on isolated disposable targets.

## Historical Slice (2026-08-08, DB-ONLY RESTORE SQL TRANSACTION)

Code/test commit `41d81cca0` adds a production-wired SQL transaction boundary for the bounded DB-only `RestoreDomains` metadata restore. `IBackupRestoreMetadataTransactionFactory` opens one `SqlConnection`/`SqlTransaction`; domain, account, alias, distribution-list, and recipient inserts share it; successful restore commits once, failure/disposal rolls back, and the service composition fails closed if the factory is absent. The installed COM identity and authenticated Backup/BackupManager access boundary are unchanged.

Legacy anchors are `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`) and `Collection<T,P>::XMLLoad`/`DeleteAll` (`hmailserver/source/Server/Common/BO/Collection.h:85`); legacy has no equivalent shared SQL/filesystem transaction. Current anchors are `MetadataBackupRestoreExecutor.RestoreMetadataAsync`, `BackupXmlPayloadRuntime`, `SqlServerBackupRestoreMetadataTransactionFactory`, and service wiring in `Host.cs`/`Program.cs`. Focused LocalDB coverage is `10 passed, 0 failed, 0 skipped`; default full Net10 is `1908 passed, 0 failed, 25 skipped`; SQL-enabled full is `1926 passed, 5 failed, 2 skipped`, with five unrelated message-indexing fixture failures.

This closes only DB-only metadata atomicity. Non-DB restore still compensates across SQL and NTFS without crash recovery; the transaction-scoped adapter is intentionally used for inserts only. Commit-failure/connection-loss/process-kill evidence, full schema constraints, normal-installation deletion/reinitialization, queued service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Release status remains RED. Next slice: durable non-DB restore journal/recovery evidence, then legacy deletion/reinitialization ordering.

## Historical Slice (2026-08-08, PARTIAL RECIPIENT ROLLBACK ACCEPTANCE)

Test-only code/test commit `ec9b71ed0` adds disposable LocalDB acceptance for a failure after one real distribution-list recipient is inserted. The second recipient insert is injected to fail; the test verifies the first recipient, list, alias, account, and domain are removed through the real SQL stores and that the original Data directory is restored. Focused restore coverage is `5 passed, 0 failed, 0 skipped`; default full Net10 is `1908 passed, 0 failed, 20 skipped`; SQL-enabled full Net10 is `1921 passed, 5 failed, 2 skipped` with five unrelated message-indexing fixture failures.

Legacy anchors are `DistributionList::XMLLoad`/`XMLLoadSubItems`, `DistributionListRecipients::PreSaveObject`, and persistent list/list-recipient `SaveObject`; legacy persists the list before recipients and leaves partial rows after a later recipient failure. Current .NET anchors are `BackupRestoreMetadataWriter.RestoreDistributionListRecipientsAsync` and `MetadataBackupRestoreExecutor.RollbackAsync`, which record generated IDs and compensate in reverse order. No production code or machine state changed.

The next independent slice is a shared SQL transaction or durable restore journal with crash/recovery tests. Queued COM acceptance remains environment-blocked; normal-installation deletion/reinitialization ordering, full restore, SEC-18, installer, native AD/DC, and lifecycle gates remain open.

## Historical Slice (2026-08-08, DISPOSABLE NON-DB RESTORE ACCEPTANCE)

Test-only code/test commit `1b479dfac` adds a real LocalDB executor acceptance test for the bound raw non-DB restore path. Against a unique disposable database and temporary Data directory, `MetadataBackupRestoreExecutor` restores the legacy domain graph through the SQL administration stores and replaces the old data tree from the bound raw sibling; the test drops the database and removes the temporary tree in `finally`. Legacy behavior remains anchored by `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`).

The focused restore round-trip suite is `2 passed, 0 failed, 0 skipped` with LocalDB enabled. The default full Net10 suite remains `1908 passed, 0 failed, 16 skipped`. The SQL-enabled full run reached `1918 passed, 5 failed, 2 skipped`; all five failures are pre-existing `SqlServerMessageIndexingIntegrationTests` fixture failures (message schema/ACL/cache assumptions), not restore failures. No service, production SQL/Data directory, COM registration, DCOM ACL, or IIS state was changed.

The next service/COM queued acceptance remains environment-blocked: no approved isolated out-of-process COM/service composition is available, and registering or changing the installed Application COM graph is fenced. Next independent slices are the shared SQL/filesystem transaction or durable restore journal, then legacy domain/public-folder deletion and application reinitialization ordering.

## Current Completed Slice (2026-08-08, RAW DATABACKUP SIBLING BINDING)

Code/test commit `124acfc0c` closes the raw sibling path gap identified after the bounded non-DB restore slice. Legacy `BackupExecuter::RestoreDataDirectory_` resolves raw `DataFiles/@FolderName` beside the archive (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:339-388`), while `BackupManager::LoadBackup` retains only the caller archive path (`hmailserver/source/Server/Common/Application/BackupManager.cpp:101-135`). .NET now copies the `DataBackup` sibling into the service-owned archive binding directory, computes a stable deterministic tree identity, rejects source changes during the copy, and verifies the bound tree before restore execution. The installed Backup/BackupManager COM identity and authenticated access boundary are unchanged.

Focused archive/raw restore coverage is `5 passed, 0 failed, 0 skipped`; the preceding executor/runtime focus is `13 passed, 0 failed, 0 skipped`; full Net10 is `1908 passed, 0 failed, 16 skipped`. Tests cover source replacement after binding, snapshot tampering rejection, bound raw restore, compressed restore, metadata failure rollback, containment, cancellation, and rollback-artifact preservation.

Residual risk remains: the private temp binding directory uses inherited ACLs and copy/restore still uses path-based operations rather than handle-relative traversal; the SQL/filesystem boundary is compensating rather than crash-safe; normal-installation domain/public-folder deletion ordering and application reinitialization are not implemented; isolated service/COM and SQL/Data end-to-end acceptance is still missing. Next three independent slices: execute queued restore through isolated service/COM against disposable SQL/Data; add a shared SQL transaction or durable restore journal; implement legacy domain/public-folder deletion and reinitialization ordering only after those boundaries are available.

## Current Completed Slice (2026-08-08, BOUNDED NON-DB RESTORE STAGING)

Code/test commit `68d447861` wires `BackupRestoreDataDirectoryRuntime` behind the authenticated restore executor for `RestoreDomains|RestoreMessages` non-DB-only archives. Legacy anchors are `BackupManager::StartRestore` and `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupManager.cpp:74-135`, `hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`): raw mode reads the `DataFiles/@FolderName` sibling and compressed mode extracts `DataBackup` before replacing the data directory. The .NET path validates archive metadata, DTD/path containment and revalidation, stages raw or 7z data, restores the old disposable target on metadata failure, and preserves the rollback artifact if recovery itself fails.

Focused executor/runtime coverage is `13 passed, 0 failed, 0 skipped`; full Net10 is `1907 passed, 0 failed, 16 skipped`. Tests cover raw sibling staging, compressed mode `14`, SQL metadata failure with filesystem rollback, non-directory/reparse-root rejection, cancellation/runtime containment, and rollback-artifact preservation. COM identity, authenticated `Settings.IncomingRelays` and other Admin identities, direct activation boundaries, SMTP trust, live reconfiguration, production service/SQL/Data directories, registration, DCOM ACLs, and IIS remain untouched.

This is a bounded disposable-target slice, not a release claim: the current archive snapshot binds only the 7z file, so raw external sibling restore fails closed when invoked through a bound `LoadBackup`; raw sibling snapshot/hash binding is the next slice. Existing metadata writes use compensating deletes rather than one crash-safe SQL/filesystem transaction, normal-installation domain deletion/public-folder ordering is not yet implemented, and service/COM end-to-end restore remains unproven. Next three independent slices: bind/hash raw `DataBackup` beside the archive snapshot; execute the queued restore through isolated service/COM against disposable SQL/Data resources; design the shared SQL/filesystem transaction or durable recovery journal.

## Current Completed Slice (2026-08-08, BACKUP AUTHORIZATION REVALIDATION)

Code/test commit `edd01f557` closes the stale retained-object authorization gap for Application-created `BackupManager` and `Backup` instances. `Application.Authenticate` advances an internal generation and clears Administrator state before each attempt; BackupManager and loaded Backup objects capture that generation and fail closed after later reauthentication. A stale concurrent authentication result cannot overwrite a newer generation. Focused coverage is `30 passed, 0 failed, 0 skipped`; full Net10 is `1897 passed, 0 failed, 16 skipped`.

Legacy anchors: `COMAuthentication::Authenticate` (`hmailserver/source/Server/COM/COMAuthentication.cpp:31`), `InterfaceApplication::get_BackupManager`/`Authenticate` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:136`), `InterfaceBackupManager::LoadSettings`, `StartBackup`, and `LoadBackup`, and `InterfaceBackup::StartRestore` (`hmailserver/source/Server/COM/InterfaceBackupManager.cpp`, `InterfaceBackup.cpp`). The legacy wrappers only check loaded child state after acquisition; the new generation guard is an explicit security hardening. No installed COM identity or activation boundary changed.

Remaining risk and next slice: queued operations already past method-entry authorization are not revoked, and archive replacement can still race after the current integrity/containment revalidation. Next implement archive-owned path/content identity binding for `LoadBackup`/restore execution, with deletion/replacement failure tests. Then wire the existing raw/7z `DataBackup` staging runtime into non-DB-only `RestoreDomains|RestoreMessages` against disposable targets. Settings, public folders, message restore, shared SQL transactionality, live reinitialization, SEC-18, out-of-proc COM/DCOM, installer, AD/DC, and 24-hour lifecycle gates remain open.

## Current Completed Slice (2026-08-08, ARCHIVE BINDING HARDENING FOLLOW-UP)

Code/test commit `23d428569` closes the remaining correctness findings in the archive snapshot binding: real-reader `LoadBackup` rejects missing/non-file sources without a binding, the SHA-256 is calculated in the source-to-snapshot copy, and a second `StartRestore` on the same Backup object cannot clean the snapshot used by the first queued task. Focused coverage is `30 passed, 0 failed, 0 skipped`; full Net10 is `1902 passed, 0 failed, 16 skipped`.

Legacy behavior remains documented in the preceding snapshot entry: C++ retains a path and reopens it, while this is deliberate security hardening. The installed COM identities and direct activation boundary are unchanged. Next slice: connect `BackupRestoreDataDirectoryRuntime` to bound non-DB-only `RestoreDomains|RestoreMessages`, with raw/7z, containment, cancellation, SQL failure, and rollback tests against disposable targets. Settings, public folders, message restore, shared SQL transactionality, live reinitialization, SEC-18, COM/DCOM, installer, AD/DC, and 24-hour lifecycle gates remain open.

## Current Completed Slice (2026-08-08, QUEUED RESTORE ARCHIVE SNAPSHOT BINDING)

Code/test commit `435532ad0` binds a loaded archive to a private snapshot before metadata parsing. `BackupArchiveBinding` copies the source while denying concurrent write/delete sharing, holds the snapshot under read-sharing lock through queued execution, carries a SHA-256 identity, and cleans the snapshot after dispatch failure or task completion. A replacement or deletion of the caller path after `LoadBackup` cannot change the archive consumed by the current DB-only restore executor. Focused coverage is `38 passed, 0 failed, 0 skipped`; full Net10 is `1901 passed, 0 failed, 16 skipped`.

Legacy anchors: `BackupManager::LoadBackup` and `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupManager.cpp:38-135`, `BackupExecuter.cpp:230-388`), plus `BackupTask::DoWork` (`hmailserver/source/Server/Common/Application/BackupTask.cpp:26-47`). Legacy stores only the caller path and accepts a valid replacement; the snapshot is deliberate security hardening. Installed backup COM identity and authenticated access boundaries remain unchanged.

Remaining risk and next slice: the current binding protects the archive consumed by the DB-only executor, but raw sibling `DataBackup` content and full non-DB restore ordering remain unimplemented. Next wire `BackupRestoreDataDirectoryRuntime` into authenticated non-DB-only `RestoreDomains|RestoreMessages` using the bound archive and a disposable target, with raw/7z, SQL failure, cancellation, containment, and rollback tests. Settings, public folders, message restore, shared SQL transactionality, live reinitialization, SEC-18, out-of-proc COM/DCOM, installer, AD/DC, and 24-hour lifecycle gates remain open.

## Current Completed Slice (2026-08-08, ISOLATED DATABACKUP STAGING)

Code/test commit `a4b9dfe9e` adds an isolated filesystem restore primitive for the legacy `BackupExecuter::RestoreDataDirectory_` behavior. `BackupRestoreDataDirectoryRuntime` accepts only integrity-validated Raw or 7z `DataFiles`, stages the raw sibling or compressed `DataBackup` subtree into a disposable target, preserves the old target behind a rollback artifact, restores it on copy failure, rejects reparse points, kills canceled 7z extraction, and cleans successful temporary state. It is deliberately not wired into the production restore executor yet.

Legacy references: `hmailserver/source/Server/Common/Application/BackupExecuter.cpp` (`RestoreDataDirectory_`), `BackupExecuter::StartRestore`, and `BackupManager::StartRestore` in `hmailserver/source/Server/Common/Application/BackupManager.cpp`. Current symbols: `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/BackupRestoreDataDirectoryRuntime.cs`, `BackupRestoreContainmentPreflight.cs`, and `BackupRestoreIntegrityRuntime.cs`; focused `BackupRestoreDataDirectoryRuntimeTests` coverage is `4 passed, 0 failed, 0 skipped`; full Net10 is `1893 passed, 0 failed, 16 skipped`.

The previous DB-only metadata restore remains the last production-wired restore slice (`26b660ff8`). This staging slice changes no production behavior and uses no production service, SQL database, Data directory, COM registration, DCOM ACL, or IIS state. Next slice: wire raw/7z staging into the authenticated restore executor for non-DB-only `RestoreDomains|RestoreMessages` and prove disposable target replacement/rollback through the service composition. Full settings/public-folder/IMAP/message restore, one shared SQL transaction, application reinitialization, real COM/DCOM, SEC-18, native AD/DC, installer, and 24-hour lifecycle gates remain open.

# hMailServer .NET 10 Remaining Work

## Current Completed Slice (2026-08-08, QUEUED DB-ONLY METADATA RESTORE)

Code/test commit `26b660ff8` opens the first production restore execution slice. The authenticated `BackupManager.LoadBackup`/`Backup.StartRestore` path retains the archive path, queues through the existing serialized maintenance operation, and preserves the installed `IInterfaceBackup` IID/vtable/DISPID/class identity. `MetadataBackupRestoreExecutor` accepts only `RestoreDomains` for a `BODomains`-only archive, runs `BackupRestoreIntegrityRuntime`, `BackupRestoreDryRunPlanner`, and containment revalidation, then restores domains, accounts, aliases, distribution lists, and recipients through existing administration stores. Generated domain/account/alias/list/recipient IDs are captured and compensating rollback deletes them in reverse order; settings, public folders, IMAP folders/messages, message files, raw/7z DataBackup, reinitialization, and live SMTP behavior remain out of scope and fail closed.

Legacy behavior is anchored by `hmailserver/source/Server/Common/Application/BackupManager.cpp` (`StartRestore`, `LoadBackup`), `hmailserver/source/Server/Common/Application/BackupExecuter.cpp` (`StartRestore`, `RestoreDataDirectory_`), `hmailserver/source/Server/COM/InterfaceBackup.cpp` (`StartRestore`), and `hmailserver/source/Server/hMailServer/hMailServer.idl` (`IInterfaceBackup` restore DISPIDs). Current symbols are `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/BackupManager.cs`, `Backup.cs`, `BackupOperationRuntime.cs`, `BackupRestoreExecution.cs`, `BackupRestoreMetadataWriter.cs`, and `BackupArchiveXmlSnapshotParser.cs`; focused tests are `BackupManagerComContractTests`, `BackupRestoreExecutionTests`, `BackupRestoreMetadataWriterTests`, and `BackupRestoreRoundTripIntegrationTests`.

Acceptance recorded for this slice: focused restore/COM `25 passed, 0 failed, 0 skipped`; disposable Local SQL round-trip plus restore/COM `24 passed, 0 failed, 0 skipped`; full Net10 `1889 passed, 0 failed, 16 skipped`. The 16 skipped tests remain environment/opt-in gates, including native AD/DC, installer, and registry/COM evidence. The next independent restore gate is an actual disposable service/COM queued restore run against an isolated SQL target; after that, complete raw/compressed DataBackup staging and rollback. The previous historical “raw non-DB-only DataBackup staging is next” paragraph is superseded by this entry.

Open production blockers remain full restore ordering/settings/filesystem replacement, one-transaction SQL rollback proof, isolated out-of-process COM/DCOM activation, SEC-18 cutover, InnoSetup build, native AD/DC evidence, and 24-hour service/COM lifecycle soak. No production service, SQL database, Data directory, installed COM identity, DCOM ACL, or IIS state was changed.

## Current Completed Slice (2026-08-08, IMAP DOMAIN-ALIAS LAST-AT PARSING)

Corrective code/test commit `ea1299638` closes the remaining quoted-local-part gap in the domain-alias authentication slice from `a5e250557`. The SQL lookup now splits the username at the last `@`, matching legacy `StringParser::ExtractDomain`/`ExtractAddress` and C++ `ReverseFind`, while preserving deterministic `daid` ordering and the explicit case-insensitive SQL comparison. Focused SQL/shape coverage is `4 passed, 0 skipped`; full Net10 is `1884 passed, 0 failed, 16 skipped` excluding the two AV-locked EICAR cleanup methods. The disposable fixture covers case-insensitive alias input, a quoted local-part containing `@`, and ordinary non-AD password authentication through an alias.

Legacy anchors: `hmailserver/source/Server/Common/Util/StringParser.cpp` (`ExtractDomain`/`ExtractAddress`), `hmailserver/source/Server/Common/BO/DomainAliases.cpp:43-64`, and `hmailserver/source/Server/Common/Util/PasswordValidator.cpp:44-51`. No COM identity, SMTP trust, live reconfiguration, production service, database, or Data directory changed.

Next slice: isolated SQL/Data-directory restore execution and round-trip evidence. Real native AD/DC validation, 24-hour service/COM lifecycle soak, SEC-18 cutover, real COM/DCOM activation, and installer build remain environment-gated.

## Current Completed Slice (2026-08-08, IMAP DOMAIN-ALIAS LOGIN LOOKUP)

Code/test commit `a5e250557` implements legacy normal IMAP domain-alias lookup parity. `SqlServerImapAccountAuthenticator.AccountLookupSql` joins `hm_domain_aliases`, maps an alias mailbox to the owning `hm_domains.domainname`, preserves direct-address lookup, and orders alias matches deterministically by `daid`. SQL Server `Latin1_General_100_CI_AS` comparisons preserve the legacy case-insensitive behavior even under a Turkish database collation. The disposable local SQL fixture proves case-insensitive `ALIASUSER@ALIAS.TEST` authentication returns `aliasuser@example.test` and passes the owning AD validator boundary. Focused coverage is `4 passed, 0 skipped`; the full Net10 run is `1884 passed, 0 failed, 16 skipped` excluding the two AV-locked EICAR cleanup methods.

Legacy anchors: `PasswordValidator::ValidatePassword` (`hmailserver/source/Server/Common/Util/PasswordValidator.cpp:44-51`) applies aliases before default-domain lookup; `DomainAliases::ApplyAliasesOnAddress` (`hmailserver/source/Server/Common/BO/DomainAliases.cpp:43-64`) matches aliases case-insensitively and returns the owning domain address; `CreateTablesMSSQL.sql:250-256` defines `hm_domain_aliases`. No COM identity, SMTP trust, live reconfiguration, production service, database, or Data directory changed.

Next slice: isolated SQL/Data-directory restore execution and round-trip evidence. Real native AD/DC validation, 24-hour service/COM lifecycle soak, SEC-18 cutover, real COM/DCOM activation, and installer build remain environment-gated.

## Current Completed Slice (2026-08-08, DEFAULT DOMAIN LOGIN LOOKUP)

Code/test commit `c0d9294b6` applies the configured `Settings.DefaultDomain` to normal IMAP username lookup when the supplied username has no `@`, using the existing `ISettingsAdministrationStore` boundary and `Canonicalize` helper. The disposable local SQL test proves `default` authenticates as `default@example.test`; focused coverage is `1 passed, 0 skipped`, and the full Net10 run is `1882 passed, 0 failed, 16 skipped` excluding the two AV-locked EICAR cleanup methods. No domain-alias SQL behavior, COM identity, SMTP trust, production service, database, or Data directory changed.

Legacy anchor: `hmailserver/source/Server/Common/Util/PasswordValidator.cpp:44-51` applies aliases and then the configured default domain before account lookup. The remaining normal-login gap is domain-alias translation (`hm_domain_aliases`, legacy `DomainAliases::ApplyAliasesOnAddress`); real AD/DC evidence and release gates remain open.

Next slice: legacy domain-alias translation for normal IMAP authentication, preserving owner/schema boundaries and existing direct email lookup.

## Current Completed Slice (2026-08-08, LOGIN SCRIPT ORDERING)

Code/test commit `d2c24d2c8` restores the legacy normal `LOGIN` ordering in `SqlServerImapAccountAuthenticator`: the account is materialized, `IClientPasswordValidationScriptExecutor` runs first, script `Accept` can authorize an empty password, script `Reject` fails immediately, and only `Continue` reaches the empty-password rejection. The existing `AUTHENTICATE PLAIN` parser still rejects an empty password with `BAD Command is missing password.` before the authenticator. Focused SQL/IMAP coverage is `40 passed, 0 skipped`; the full Net10 run is `1882 passed, 0 failed, 16 skipped` excluding the two AV-locked EICAR cleanup methods.

Legacy anchors are `hmailserver/source/Server/IMAP/IMAPCommandLogin.cpp:52-57`, `hmailserver/source/Server/IMAP/IMAPCommandAuthenticate.cpp:77-79`, and `hmailserver/source/Server/Common/Util/PasswordValidator.cpp:109-133`. No COM identity, SQL schema, AD native boundary, SMTP trust, live reconfiguration, production service, database, or Data directory changed. Remaining auth blockers are domain aliases/default-domain lookup and real domain-controller/native `LogonUser` evidence; release blockers remain SEC-18/COM/DCOM, installer build, migration/restore acceptance, and the 24-hour lifecycle soak.

Next slice: legacy domain-alias/default-domain authentication lookup, preserving the current authenticated protocol and SQL boundaries.

## Current Completed Slice (2026-08-08, SQL ACTIVE DIRECTORY AUTHENTICATION EVIDENCE)

Code/test commit `4072dbf50` completes the isolated SQL-backed portion of the AD authentication slice. The opt-in `SqlServerImapActiveDirectoryIntegrationTests` fixture creates a unique local SQL Server database, uses production-compatible MSSQL account types, proves active-account/active-domain filtering, records the exact `CORP`/username/password validator call, verifies success and rejection last-logon behavior, and proves inactive domains never reach the validator. Local SQL evidence is `7 passed, 0 skipped`; the normal full Net10 run is `1880 passed, 0 failed, 16 skipped` excluding the two AV-locked EICAR cleanup methods. The authenticator projection now explicitly converts MSSQL `tinyint` flags and `datetime` fields before materialization, matching the existing `SqlServerAccountAdministrationStore` projection pattern.

Legacy anchors remain `hmailserver/source/Server/Common/Util/PasswordValidator.cpp:34-147` and `hmailserver/source/Server/Common/Util/SSPIValidation.cpp:13-22`: account/domain activity is checked before password validation, script override precedes empty-password rejection, and AD uses `LogonUser` network semantics. Real domain-controller credentials and native `LogonUser` acceptance remain environment-gated; domain-alias/default-domain lookup and script-before-empty-password parity are separately open behavioral slices. No production database, Data directory, service, COM registration, DCOM ACL, SMTP trust, or live reconfiguration was used.

Next slice: isolate the 24-hour service restart/COM lifecycle soak if a disposable host is available; otherwise continue with the next unblocked legacy authentication/parity slice. Keep SEC-18 registration/DCOM, installer build, and production replacement fenced.

## Current Completed Slice (2026-08-08, AD VALIDATION CONNECTION LIFETIME)

Code/test commit `eec9752e8` closes the SQL connection and reader before invoking the synchronous AD validator, and performs successful-authentication last-logon updates through a separate short-lived connection. The live SQL fixture sets `Max Pool Size=1` and opens a second SQL connection from the validator; focused local SQL coverage remains `7 passed, 0 skipped`, proving the validator no longer holds the account lookup connection. Master-user target lookup follows the same disposal-before-update boundary. No AD credentials, COM identity, SMTP trust, or production state changed.

Legacy references are `hmailserver/source/Server/Common/Util/AccountLogon.cpp:37-75` and `hmailserver/source/Server/Common/Util/PasswordValidator.cpp:109-147`; the legacy path validates through `LogonUser` and updates last-logon only after successful validation. Security review found no credential-retention or projection-cast regression in this change. Remaining open gaps are real domain-controller/native AD evidence, legacy domain-alias/default-domain lookup, legacy LOGIN script-before-empty-password ordering, SEC-18/COM/DCOM, installer build, and 24-hour lifecycle soak.

Next slice: legacy LOGIN script-before-empty-password ordering, preserving the existing AUTHENTICATE PLAIN parser rejection. Keep production service, database, Data directory, COM registration, DCOM ACL, and SMTP trust fenced.

## Current Completed Slice (2026-08-08, IMAP MASTER USER)

Code/test commit `ef7e5ec65` implements the bounded legacy IMAP AUTHENTICATE PLAIN master-user path. Legacy references are `hmailserver/source/Server/IMAP/IMAPCommandAuthenticate.cpp:27-122`, `hmailserver/source/Server/Common/Util/AccountLogon.cpp:37-75`, and `hmailserver/source/Server/Common/Util/PasswordValidator.cpp:78-164`: authcid is the configured master user, authzid is the active target mailbox, only the master credential is checked, and master-policy failures do not enter auto-ban accounting. The .NET path carries authzid through `ClientAuthenticationRequest`/`IImapAccountAuthenticator`, validates the configured master identity through the SQL-backed authenticator, returns the target mailbox, preserves ordinary auth, and does not retain credentials. Focused coverage is `43 passed, 0 skipped`; full Net10 is `1877 passed, 0 failed, 15 skipped` excluding the two AV-locked EICAR cleanup methods. AD/SSPI authentication and live SQL master-user evidence remain separate gaps; no COM identity, SMTP behavior, settings setter, or live reconfiguration changed.

Next slice: isolated SQL/AD authentication evidence and fail-closed AD boundary review. Keep live service/COM lifecycle soak, SEC-18 registration/DCOM, installer build, and production replacement fenced.

## Current Completed Slice (2026-08-08, SHORT OFFLINE SOAK)

Code/test commit `53680b0d2` adds `ShortSoakBenchmark` and `ShortSoakBenchmarkTests`. The commandable bounded synthetic IMAP SEARCH/SORT soak emits JSON/CSV/Markdown with p50/p95/p99, errors, process memory, handles, threads, TCP connections, GC deltas, thresholds, timestamps, runtime, and git commit. Focused coverage is `4 passed, 0 skipped`; a smoke run completed 3/3 cycles with 0 errors and threshold pass. This remains an offline synthetic gate and does not replace the mandatory 24-hour live service leak/handle/thread/socket soak or COM lifecycle acceptance. Next slice: run the live 24-hour restart/COM lifecycle soak on an isolated host, then continue SEC-18/COM activation and the approved-toolchain InnoSetup build gates.

## Current Completed Slice (2026-08-08, INSTALLER SOURCE GATE)

Code/test commit `8cc67112b` adds `InstallerSourceGateTests`. The source gate checks the legacy InnoSetup `hMailServer64.iss` include graph and the x64 C++ payload wiring in `hmailserver/installation/section_files_64.iss`. The real `ISCC.exe` build is explicit opt-in and is not reported as passed when prerequisites are absent. Focused coverage is `1 passed, 1 skipped`; the full suite excluding the two AV-locked EICAR cleanup methods is `1872 passed, 0 failed, 15 skipped`. This host has no `ISCC.exe` and no `hmailserver/source/server/hMailServer/x64/Release/hMailServer.exe`, so the actual installer build remains environment-blocked. No installer, service, registry, DCOM, database, or production state changed.

Next slice: commandable short soak/leak acceptance with explicit thresholds and JSON/CSV/Markdown artifacts. Keep the 24-hour leak run, installer build, COM/DCOM activation, SEC-18, and production replacement fenced.

## Current Completed Slice (2026-08-05, RELEASE ARTIFACT GATE)

The release artifact gate is complete in code/test commit ``4191ac3d1``. ``ReleaseArtifactGateTests`` asserts the Net10 Service output (``HMailServer.Service\bin\Debug\net10.0-windows``) contains the 13 required release artifacts (``hMailServer.exe``, ``HMailServer.ComInterop.dll`` + ``.comhost.dll``, ``Core``/``Protocols``/``Storage.SqlServer``/``Delivery``/``Security``, ``7za.exe``, ``BouncyCastle``) and that ``hMailServer.runtimeconfig.json`` targets ``Microsoft.NETCore.App``. Focused ``1/1``; full suite excluding the two AV-locked EICAR cleanup methods ``1871 passed, 0 failed, 14 opt-in skips`` (1885 total). Next slice: InnoSetup installer build gate.

## Current Completed Slice (2026-08-05, COM HOST ACTIVATION EVIDENCE)

COM host activation feasibility evidence is complete in code/test commit ``c09fcf435``. ``ComHostActivationIntegrationTests`` loads ``HMailServer.ComInterop.comhost.dll`` next to the test host, verifies the ``DllGetClassObject`` export exists, and records HRESULT ``0x80008093`` when invoked in-process — the .NET comhost cannot bootstrap its runtime from inside another .NET process, so genuine activation requires the service host/registration (registry/DCOM state, fenced). Focused ``1/1``; full suite excluding the two AV-locked EICAR cleanup methods ``1870 passed, 0 failed, 14 opt-in skips`` (1884 total). Next slice: installer/release artifact gate.

## Current Completed Slice (2026-08-05, 1K-CONCURRENT SMTP ACCEPTANCE)

Live 1k-concurrent connection acceptance is complete in code/test commit ``59623bb20``. ``SmtpTcpListenerTests.LoopbackConcurrency_AcceptsOneThousandClients`` opens 1000 concurrent loopback clients against ``SmtpTcpListener`` (backlog 1024) and asserts every client receives the ``220 hMailServer .NET 10 ESMTP ready`` banner. Focused ``1/1`` (stable across repeated runs); full suite excluding the two AV-locked EICAR cleanup methods ``1869 passed, 0 failed, 14 opt-in skips`` (1883 total). Next slice: real COM activation evidence.

## Current Completed Slice (2026-08-05, IMAP/POP3 ACCEPT LATENCY)

Live IMAP and POP3 accept-latency acceptance harnesses are complete in code/test commit ``c965cf2b0``, mirroring the SMTP harness: 200 sequential loopback clients per protocol, banner assertion (``* OK hMailServer .NET 10 IMAP ready`` / ``+OK hMailServer .NET 10 POP3 ready``), p50/p95/p99 measured, 5s p95 budget. Focused listener coverage ``15/15``; full suite excluding the two AV-locked EICAR cleanup methods ``1868 passed, 0 failed, 14 opt-in skips`` (1882 total). Next slice: 1k-concurrent loopback connection acceptance.

## Current Completed Slice (2026-08-05, SMTP ACCEPT LATENCY)

Live SMTP accept-latency acceptance harness is complete in code/test commit ``21b63cd13``. ``SmtpAcceptLatencyIntegrationTests`` binds ``SmtpTcpListener`` on loopback, connects 200 sequential clients, asserts each ``220 hMailServer .NET 10 ESMTP ready`` banner, and measures connect→banner p50/p95/p99 with a 5s p95 budget; the summary is emitted to the test log. Focused ``1/1``; full suite excluding the two AV-locked EICAR cleanup methods ``1866 passed, 0 failed, 14 opt-in skips`` (1880 total). Next slice: IMAP/POP3 loopback accept-latency harnesses, then 1k-concurrent connection acceptance.

## Current Completed Slice (2026-08-05, DB VERSION GATE EVIDENCE)

Isolated database version gate and upgrade rollback evidence is complete in code/test commit ``98433db25``. The opt-in ``SqlServerDatabaseAdministrationStoreIntegrationTests`` fixture seeds ``hm_dbversion`` at 5000 (``CurrentVersion < RequiredVersion``), simulates the upgrade write to 5708 (gate clears), then rolls back to 5000 (gate returns, exactly one version row remains). Live LocalDB ``1/1``; full suite excluding the two AV-locked EICAR cleanup methods ``1865 passed, 0 failed, 14 opt-in skips`` (1879 total). Data-directory message-file restore, protocol acceptance, real COM activation, SEC-18, and release gates remain open. Next slice: live protocol acceptance harness.

## Current Completed Slice (2026-08-05, RESTORE ROUND-TRIP EVIDENCE)

Isolated backup metadata restore round-trip evidence is complete in code/test commit ``495ddb974``. The opt-in ``BackupRestoreRoundTripIntegrationTests`` fixture restores a crafted legacy archive (domain + account + alias + distribution list + recipient XML) through ``BackupArchiveXmlSnapshotParser`` + ``BackupRestoreMetadataWriter`` into a disposable LocalDB target and verifies every restored row via the SQL admin stores (domains, accounts, aliases, distribution lists, recipients). Live LocalDB evidence ``1/1``; full suite excluding the two AV-locked EICAR cleanup methods ``1865 passed, 0 failed, 13 opt-in skips`` (1878 total). Data-directory message-file restore, upgrade rollback, protocol acceptance, and release gates remain open. Next slice: upgrade rollback evidence.

## Current Completed Slice (2026-08-05, RESTORE RECIPIENT XML)

Backup archive distribution-list recipient XML parsing with transactional restore is complete in code/test commit ``19456d549``. ``BackupArchiveXmlSnapshotParser.ParseDistributionListRecipients`` reads ``<Recipient Name=...>`` into ``DistributionListRecipientAdministrationSnapshot``, and ``BackupRestoreMetadataWriter.RestoreDistributionListRecipientsAsync`` replays the parsed snapshots through ``InsertDistributionListRecipientAsync`` (scoped to the owning list) inside ``BackupRestoreTransactionBoundary`` with caller rollback. ``BackupArchiveXmlSnapshotParserTests`` asserts field reconstruction. Focused ``1/1`` (plus existing parser/writer tests, total ``9/9``); full suite excluding the two AV-locked EICAR cleanup methods ``1865 passed, 0 failed, 12 opt-in skips`` (1877 total). The restore payload XML surface (domains, accounts, aliases, distribution lists, recipients) is now fully parsed. The full restore round-trip into a temp DB/Data and upgrade rollback remain open. Next slice: full restore round-trip into temp DB/Data.

## Current Completed Slice (2026-08-05, RESTORE ALIAS/LIST XML)

Backup archive alias and distribution-list XML parsing with transactional restore is complete in code/test commit ``8e5bfa01f``. ``BackupArchiveXmlSnapshotParser.ParseAliases`` reads ``Name``/``Value``/``Active`` into ``AliasAdministrationSnapshot``; ``ParseDistributionLists`` reads ``Name``/``Active``/``RequiresAuth``/``RequiresAuthAddress``/``ListMode`` into ``DistributionListAdministrationSnapshot``; ``BackupRestoreMetadataWriter.RestoreAliasesAsync``/``RestoreDistributionListsAsync`` replay the parsed snapshots through ``InsertAliasAsync``/``InsertDistributionListAsync`` inside ``BackupRestoreTransactionBoundary`` with caller rollback. ``BackupArchiveXmlSnapshotParserTests`` asserts field reconstruction and the XML→writer→store round trip for both. Focused ``2/2`` (plus existing parser/writer tests); full suite excluding the two AV-locked EICAR cleanup methods ``1864 passed, 0 failed, 12 opt-in skips`` (1876 total). Distribution-list recipient XML parsing and the full restore round-trip into a temp DB/Data remain open. Next slice: distribution-list recipient XML parsing to complete the restore payload.

## Current Completed Slice (2026-08-05, RESTORE ACCOUNT XML)

Backup archive account XML parsing with transactional restore is complete in code/test commit ``fc8efb819``. ``BackupArchiveXmlSnapshotParser.ParseAccounts`` reads the legacy ``<Account>`` attribute set (``Name``, ``PersonFirstName/LastName``, ``Active``, ``Password``, ``PasswordEncryption``, ``MaxAccountSize``, ``ADUsername/ADDomain/ADActive``, vacation fields, ``AdminLevel``, forward fields, signature fields) into ``RestoreAccountEntry``, and ``BackupRestoreMetadataWriter.RestoreAccountsAsync`` replays the entries through ``IAccountAdministrationStore.InsertAccountAsync`` inside ``BackupRestoreTransactionBoundary`` with caller rollback on partial failure. ``BackupArchiveXmlSnapshotParserTests`` asserts field reconstruction and the XML→writer→store round trip. Focused ``2/2`` (plus existing domain/parser tests); full suite excluding the two AV-locked EICAR cleanup methods ``1862 passed, 0 failed, 12 opt-in skips`` (1874 total). Alias/distribution-list XML parsing and the full restore round-trip into a temp DB/Data remain open. Next slice: alias/distribution-list XML parsing to complete the restore payload.

## Current Completed Slice (2026-08-05, RESTORE XML PARSER)

Backup archive XML→snapshot parser with restore wiring is complete in code/test commit ``9e2d44daf``. ``BackupArchiveXmlSnapshotParser.ParseDomains`` reads the legacy ``<Domain>`` attribute set (``Name``, ``Postmaster``, ``Active``, ``MaxMessageSize``, ``MaxSize``, ``MaxAccountSize``, ``UsePlusAddressing``, ``PlusAddressingChar``, ``AntiSpamOptions``, ``EnableSignature``, ``SignatureMethod``, signature text/html, ``AddSignaturesToLocalMail``/``Replies``, ``MaxNoOfAccounts/Aliases/Lists``, ``LimitationsEnabled``, ``DKIMSelector/PrivateKeyFile``) into ``DomainAdministrationSnapshot``, and the parsed snapshots feed ``BackupRestoreMetadataWriter.RestoreDomainsAsync`` for the transactional restore. ``BackupArchiveXmlSnapshotParserTests`` asserts field reconstruction (including anti-spam/DKIM and limitations bit packing) and the XML→writer→store round trip. Focused ``2/2`` (plus the restore writer ``2/2``); full suite excluding the two AV-locked EICAR cleanup methods ``1860 passed, 0 failed, 12 opt-in skips`` (1872 total). Account/alias/distribution-list XML parsing and the full restore round-trip into a temp DB/Data remain open. Next slice: account/alias/distribution-list XML parsing to complete the restore payload.

## Current Completed Slice (2026-08-05, RESTORE WRITER SEAM)

The first restore-execution seam is complete in code/test commit ``887521659``. ``BackupRestoreMetadataWriter.RestoreDomainsAsync`` replays a ``DomainAdministrationSnapshot`` batch through ``IDomainAdministrationStore.InsertDomainAsync`` inside ``BackupRestoreTransactionBoundary`` so a mid-batch failure invokes the caller ``rollbackAsync`` and rethrows, leaving no partial commit. ``BackupRestoreMetadataWriterTests`` covers full replay (all domains persisted, rollback not called) and partial-failure rollback (rollback invoked, exception rethrown, only the pre-failure inserts recorded). Focused ``2/2``; full suite excluding the two AV-locked EICAR cleanup methods ``1858 passed, 0 failed, 12 opt-in skips`` (1870 total). Archive XML→snapshot parsing and the full restore round-trip (into temp DB/Data) remain open. Next slice: wire archive XML→snapshot restore for domains/accounts (the remainder of the round-trip).

## Current Completed Slice (2026-08-05, BACKUP PROJECTION SQL EVIDENCE)

Isolated SQL backup-projection evidence is complete in code/test commit ``f8fa925f0``. The opt-in ``SqlServerBackupProjectionIntegrationTests`` fixture seeds a disposable LocalDB database (domain, account with BlowFish-encrypted password, rule) and proves the archive-feed seam: ``GetBackupAccountsAsync`` returns the account identity with ``Password`` decrypting to the original plaintext and ``PasswordEncryption=1``, ``GetBackupRulesAsync`` returns the seeded rule, and ``GetDomainsAsync`` returns the seeded domain. Live LocalDB evidence ``1/1``; full suite excluding the two AV-locked EICAR cleanup methods ``1856 passed, 0 failed, 12 opt-in skips`` (1868 total). Restore execution (writes into a temp DB/Data), upgrade rollback, and the full round-trip queue remain open. Next slice: full isolated backup/restore round-trip (restore execution into temp DB/Data).

## Current Completed Slice (2026-08-05, BENCHMARK ARTIFACT SEAM)

Offline acceptance seam for the performance benchmark pack is complete in test-only commit ``bd168169d``. ``SyntheticBenchmarkArtifactWriterTests`` asserts the deterministic JSON/CSV/Markdown artifact serialization (``offline-imap-search-sort.json/.csv/.md``), including CSV header/value columns and Markdown p50/p95/correctness rows, so the offline 100k SEARCH/SORT harness has an automated regression check without running the heavy benchmark or a live server. Focused ``1/1``; full suite excluding the two AV-locked EICAR cleanup methods ``1856 passed, 0 failed, 11 opt-in skips`` (1867 total). Next slice: isolated backup/restore round-trip + upgrade rollback evidence (temp DB/Data, disposable).

## Current Completed Slice (2026-08-05, COM SCANNER SSRF GUARD)

COM-path SSRF guard for scanner-test methods is complete in code/test commit ``0429fa1f1``. ``LegacyLocalScannerTargetGuard`` resolves an IP literal or DNS hostname and requires every returned address to be loopback or a local interface address (mirroring the legacy WebAdmin ``IsLocalHost`` policy), applied on ``AntiVirus.TestClamAVScanner`` and ``AntiSpam.TestSpamAssassinConnection`` before the runtime connects; non-local or unresolvable targets fail closed with ``E_FAIL``. Unit tests cover loopback accept / public reject; COM contract tests assert denial happens before the runtime and that local targets still delegate. Existing tests that drove non-local hostnames through the online runtime now use ``127.0.0.1``. Focused coverage ``26/26``; full suite excluding the two AV-locked EICAR cleanup methods ``1855 passed, 0 failed, 11 opt-in skips`` (1866 total). Next slice: real COM activation evidence for completed COM identities.

## Current Completed Slice (2026-08-05, EGRESS/SSRF AUDIT)

Read-only egress/SSRF posture audit (no production code changed). Verified: external fetch enforces ``ExternalFetchEndpointPolicy`` (metadata/cloud addresses, special-use ranges, private CIDR allow-list); WebAdmin scanner AJAX handlers enforce ``hmailResolveLocalScannerTarget``/``hmailRejectScannerTarget`` (``IsLocalHost`` only) plus POST-CSRF (commit ``437612a13``); the COM ``TestClamAVScanner``/``TestSpamAssassinConnection`` methods still pass an arbitrary host+port to their runtimes (residual gap). A COM-level SSRF guard would change behavior exercised by existing committed tests that drive non-local hostnames through an online runtime and require DNS resolution, so it is deferred as a security-policy slice requiring an explicit allow-list design (loopback-only vs private-LAN) rather than forced. Next slice: COM-path SSRF guard design decision for scanner-test methods; then real COM activation evidence.

## Current Completed Slice (2026-08-05, WEBADMIN AJAX POST/CSRF)

SEC-14 WebAdmin AJAX scanner-test POST/CSRF hardening is complete in code/test commit ``437612a13``. ``hmailserver/source/WebAdmin/background_ajax_virustest.php`` and ``background_ajax_spamassassintest.php`` now require ``hmailRequirePostCsrfToken()`` after the server-admin ``hmailGetAdminLevel() != ADMIN_SERVER`` guard, keep their existing local-scanner-target restrictions (``hmailResolveLocalScannerTarget``/``hmailRejectScannerTarget``) and POST-only reads, and the calling forms already submit ``csrftoken``. New source tests ``WebAdminAjaxVirusTestPostOnlySourceTests`` and ``WebAdminAjaxSpamAssassinPostOnlySourceTests`` assert guard-before-CSRF-before-reads and no GET/plain-POST reads. Focused source coverage ``2/2``; full suite excluding the two AV-locked EICAR cleanup methods ``1851 passed, 0 failed, 11 opt-in skips`` (1862 total). Next slice: shared egress/SSRF policy review for external fetch and diagnostics/network tests.

## Current Completed Slice (2026-08-05, MESSAGE DELETE)

Authenticated ``Messages.DeleteByDBID`` plus ``Clear`` parity (DB-only) is complete in code/test commit ``795bd3b93``, completing the message mutation trio (insert ``85e5a143a``, update ``f06a199b4``, delete). Legacy ``InterfaceMessages::DeleteByDBID``/``Clear`` delegate to ``PersistentMessage`` deletion which removes the data-directory file and the ``hm_messages`` row. .NET path preserves installed Messages/Message COM identity/direct activation denial, treats unknown/stale IDs as successful no-ops, maps store failure to ``E_FAIL`` retaining the owner snapshot, clears the folder snapshot only after successful whole-collection deletion, and removes only the selected snapshot after an owner+folder-scoped delete succeeds. Data-directory message-file deletion remains fenced. Focused Messages COM/store coverage ``31/31``; full suite excluding the two AV-locked EICAR cleanup methods ``1849 passed, 0 failed, 11 opt-in skips`` (1860 total). Next slice: SEC-14 WebAdmin remaining POST-only handlers.

## Current Completed Slice (2026-08-05, MESSAGE UPDATE)

Authenticated existing-row ``Message.Save()`` UPDATE parity (DB-only) is complete in code/test commit ``f06a199b4``, following message insert ``85e5a143a``. Legacy ``InterfaceMessage::Save`` delegates to ``PersistentMessage::SaveObject``, whose existing-row branch updates ``hm_messages`` columns by ``messageid``. .NET path preserves installed Messages/Message COM identity/direct activation denial, rechecks live authentication, stages retained-message From/header setters, persists via a parameterized owner-scoped UPDATE (``WHERE messageid AND messageaccountid AND messagefolderid``), maps failed or no-row updates to ``E_FAIL`` retaining the staged state, and replaces only the matching snapshot after success. Data-directory message-file creation and content rewrites remain fenced. Focused Messages COM/store coverage ``26/26``; full suite excluding the two AV-locked EICAR cleanup methods ``1844 passed, 0 failed, 11 opt-in skips`` (1855 total). Next slice: message delete/Clear parity (DB-only).

## Current Completed Slice (2026-08-05, MESSAGE INSERT SQL EVIDENCE)

Isolated SQL identity/readback and rollback evidence for the message insert is complete in code/test commit ``22c206330``. The new opt-in ``SqlServerMessageAdministrationStoreIntegrationTests`` fixture creates a disposable isolated database mirroring the legacy ``hm_messages`` schema and proves ``OUTPUT INSERTED.messageid`` identity readback with per-insert increments through ``GetFolderMessagesAsync``, and a UNIQUE ``messagefilename`` violation on INSERT throwing ``SqlException`` with no partial row. Live LocalDB evidence is ``1/1``; the focused Messages suite is ``24/24``. The full suite excluding the two AV-locked EICAR cleanup methods is ``1841 passed, 0 failed, 11 opt-in skips`` (1852 total) with the SQL connection unset. Data-directory message-file creation remains fenced. Next slice: existing-row ``Message.Save()`` UPDATE parity (DB-only).

## Current Completed Slice (2026-08-05, MESSAGE INSERT)

Folder-scoped ``Messages.Add()`` plus new-item ``Message.Save()`` INSERT parity (DB row only) is complete in code/test commit ``85e5a143a``. Legacy ``InterfaceMessages::Add`` (``hmailserver/source/Server/COM/InterfaceMessages.cpp:102``) rejects the account message-cache context (``DISP_E_BADINDEX``), creates a State-Created message scoped to the owning account/folder, and ``InterfaceMessage::Save`` routes through ``PersistentMessage::SaveObject`` which writes a data-directory file and inserts the ``hm_messages`` row. .NET path preserves installed Messages/Message COM identity/direct activation denial, rejects the account-cache Add with ``DISP_E_BADINDEX``, stages a folder-scoped draft with Subject/From/Date/header setters, persists via a parameterized insert with ``OUTPUT INSERTED.messageid`` and a generated partial ``.eml`` filename, maps store failure to ``E_FAIL`` retaining the draft, and publishes only the saved snapshot after success. Data-directory message-file creation remains fenced. Focused Messages COM/store coverage ``23/23``; full suite excluding the two AV-locked EICAR cleanup methods ``1841 passed, 0 failed, 10 opt-in skips`` (1851 total). Next slice: message SQL identity/readback evidence for the insert, then existing-row ``Message.Save()`` UPDATE parity.

## Current Completed Slice (2026-08-05, TCPIP PORT SQL EVIDENCE)

Isolated SQL identity/readback and rollback evidence for the completed TCP/IP port mutations is complete in code/test commit ``29d90ca9d``. The new opt-in ``SqlServerTcpIpPortAdministrationStoreIntegrationTests`` fixture creates a disposable isolated database mirroring the legacy ``hm_tcpipports`` schema and proves ``OUTPUT INSERTED.portid`` identity readback with per-insert increments, identity-preserving UPDATE, delete-by-id, ``DeleteAllTcpIpPorts``, and a UNIQUE ``portnumber`` violation on INSERT throwing ``SqlException`` with no partial row. Live LocalDB evidence is ``1/1``; the focused TCP/IP port suite is ``25/25``. The full suite excluding the two AV-locked EICAR cleanup methods is ``1838 passed, 0 failed, 10 opt-in skips`` (1848 total) with the SQL connection unset. Real COM activation, backup/restore, migration, SEC-18, release gates remain open; ``Messages`` COM mutation remains the only large Admin collection not yet opened (data-directory/file coupled, fenced). Next slice: bounded ``Messages.Add()``/``Message.Save()`` INSERT parity with data-directory file creation fenced.

## Current Completed Slice (2026-08-05, ACCOUNT SQL EVIDENCE)

Isolated SQL identity/readback and rollback evidence for the completed account mutations is complete in code/test commit ``e12dbe24a``. The new opt-in ``SqlServerAccountAdministrationStoreIntegrationTests`` fixture creates a disposable isolated database mirroring the legacy ``hm_accounts`` schema plus dependents and proves ``OUTPUT INSERTED.accountid`` identity readback with per-insert increments, BlowFish password round-trip through the encrypted ``accountpassword`` column, owner-scoped UPDATE/DELETE that no-op against foreign domain IDs, conditional password update (unchanged when ``null`` is passed, re-encrypted when a new password is passed), transactional cascade DELETE removing rules, messages, and fetch accounts before the account row, and NOT NULL address violations on INSERT/UPDATE throwing ``SqlException`` with no partial row and the original row intact. The fixture also surfaced a latent defect in the account admin read: ``ReadAccountsAsync`` reads ``maxsize``/size ordinals before ``id``, which is invalid under ``CommandBehavior.SequentialAccess``; the store read now uses ``CommandBehavior.Default``. Live LocalDB evidence is ``1/1``; the focused Accounts suite is ``57/57``. The full suite excluding the two AV-locked EICAR cleanup methods is ``1838 passed, 0 failed, 9 opt-in skips`` (1847 total) with the SQL connection unset. Running the full suite with the SQL connection set still surfaces the ``5`` pre-existing message-indexing opt-in failures unrelated to this test-only slice. Real COM activation, backup/restore, migration, SEC-18, and release gates remain open. Next slice: next authenticated Admin collection mutation after accounts.

## Current Completed Slice (2026-08-05, ACCOUNT UPDATE)

Authenticated existing-row ``Account.Save()`` UPDATE parity is complete in code/test commit ``2fbc3a085``, completing the account mutation trio (insert ``43ab59b74``, delete ``84fa764e3``, update). Legacy ``InterfaceAccount::Save`` delegates to ``PersistentAccount::SaveObject`` (``hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:220-300``), whose existing-row branch updates ``hm_accounts`` columns by ``accountid``. .NET path preserves installed Accounts/Account COM identity/direct activation denial, rechecks live domain/server authentication, stages retained-account setters through a mutable ``_currentSaveSnapshot`` overlay, persists via a parameterized owner-scoped UPDATE (``WHERE accountid AND accountdomainid``) with conditional password columns only when the password setter was used, maps failed or no-row updates to ``E_FAIL`` retaining the staged snapshot and collection state, and replaces only the matching snapshot after success. Retained account setters are now staged instead of ``E_NOTIMPL``; secret-free reads and direct activation denial remain unchanged. Focused Accounts COM/store coverage ``56/56``; full suite excluding the two AV-locked EICAR cleanup methods ``1838 passed, 0 failed, 8 opt-in skips`` (1846 total). Next slice: isolated SQL identity/readback rollback evidence for account mutations, then the next authenticated Admin collection mutation after accounts.

## Current Completed Slice (2026-08-05, ACCOUNT DELETE)

Authenticated domain-owned ``Accounts.DeleteByDBID``/``Delete`` plus attached ``Account.Delete()`` parity is complete in code/test commit ``84fa764e3``, following account insert ``43ab59b74``. Legacy ``InterfaceAccounts::Delete``/``DeleteByDBID`` require domain-admin and delegate to ``PersistentAccount::DeleteObject`` (``hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:30-70``), which deletes account rules, messages, fetch accounts, group members, ACL permissions, and IMAP folders before the ``hm_accounts`` row. .NET path preserves installed Accounts/Account COM identity/direct activation denial, rechecks live domain/server authentication, treats unknown/stale IDs as successful no-ops, maps store failure to ``E_FAIL`` retaining the owner snapshot, and removes only the selected snapshot after a transactional cascade delete succeeds. Data-directory folder deletion remains fenced. Focused Accounts COM/store coverage ``51/51``; full suite excluding the two AV-locked EICAR cleanup methods ``1833 passed, 0 failed, 8 opt-in skips`` (1841 total). Next slice: authenticated existing-row ``Account.Save()`` UPDATE parity, then isolated SQL identity/readback rollback evidence for account mutations.

## Current Completed Slice (2026-08-05, ACCOUNT INSERT)

Authenticated domain-owned ``Accounts.Add()`` plus new-item ``Account.Save()`` INSERT parity is complete in code/test commit ``43ab59b74``. Legacy anchors are ``InterfaceAccounts::Add`` (``hmailserver/source/Server/COM/InterfaceAccounts.cpp:42``) creating an ID-zero draft scoped to the owning domain, and ``PersistentAccount::SaveObject`` (``hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:220-300``) inserting ``hm_accounts`` columns with ``accountid`` identity and BlowFish password encryption. .NET path preserves installed Accounts/Account IID/vtable/DISPID/ProgID shape plus direct activation denial, rechecks live domain/server authentication on Add/Save, stages the draft against the owning domain, stages all account setters, persists via a parameterized insert with ``OUTPUT INSERTED.accountid`` and ``LegacyBlowfishPasswordCipher`` password encryption (``accountpwencryption=1``), retains the failed draft on store failure mapping to ``E_FAIL``, and publishes only the saved snapshot with child-state adapters after success. Existing-row Save/Delete remain ``E_NOTIMPL``. Focused Accounts COM/store coverage ``46/46``; full suite excluding the two AV-locked EICAR cleanup methods ``1828 passed, 0 failed, 8 opt-in skips`` (1836 total). Next slice: authenticated existing-row ``Account.Save()`` UPDATE parity, then owner-scoped account deletion, then isolated SQL identity/readback rollback evidence for account mutations.

## Current Completed Slice (2026-08-05, TCPIP SetDefault)

Authenticated ``TCPIPPorts.SetDefault()`` parity is complete in code/test commit ``794d93a3c``. Legacy anchors are ``InterfaceTCPIPPorts::SetDefault`` (``hmailserver/source/Server/COM/InterfaceTCPIPPorts.cpp:37``) and ``TCPIPPorts::SetDefault`` (``hmailserver/source/Server/Common/BO/TCPIPPorts.cpp:37-80``), which refresh, return early when the current ports already match the four legacy defaults (25/SMTP, 110/POP3, 143/IMAP, 587/SMTP on 0.0.0.0 with connection security None), otherwise delete all and reinsert those defaults. .NET path preserves installed TCPIPPorts COM identity/direct activation denial, rechecks live server-administrator authentication, performs the same no-op detection, deletes all then reinserts the four defaults and reloads the snapshot, and maps store failure to ``E_FAIL`` retaining the prior snapshot. Focused TCP/IP port coverage ``24/24``; full suite excluding the two AV-locked EICAR cleanup methods ``1824 passed, 0 failed, 8 opt-in skips`` (1832 total). Next slice: authenticated ``Accounts.Add()`` plus new-item ``Account.Save()`` INSERT parity.

## Current Completed Slice (2026-08-05, DOMAIN SQL EVIDENCE)

Isolated SQL identity/readback and rollback evidence for the completed domain mutations is complete in code/test commit ``f85bf4681``. The new opt-in ``SqlServerDomainAdministrationStoreIntegrationTests`` fixture creates a disposable isolated database mirroring the legacy ``hm_domains`` schema plus dependent tables and proves ``OUTPUT INSERTED.domainid`` identity readback with per-insert increments, anti-spam/limitations bit-packing round-trip through GetDomains readback (greylisting/DKIM flags, per-type limitation flags), owner-scoped UPDATE/DELETE that no-op against unknown IDs, transactional cascade DELETE removing domain aliases, distribution lists + recipients, aliases, account rules chain, account message chain, and accounts before the domain row, and NOT NULL name violations on INSERT/UPDATE throwing ``SqlException`` with no partial row and the original row intact. Live LocalDB evidence is ``1/1``; the focused Domains suite is ``23/23``. The full suite excluding the two AV-locked EICAR cleanup methods is ``1819 passed, 0 failed, 8 opt-in skips`` (1827 total) with the SQL connection unset. Running the full suite with the SQL connection set still surfaces the ``5`` pre-existing message-indexing opt-in failures unrelated to this test-only slice. Real COM activation, backup/restore, migration, SEC-18, and release gates remain open. Next slice: next authenticated Admin collection mutation after domains.

## Current Completed Slice (2026-08-05, DOMAIN DELETE)

Authenticated owner-scoped ``Domains.DeleteByDBID`` plus attached ``Domain.Delete()`` parity is complete in code/test commit ``aacbabb99``, completing the domain mutation trio (insert ``444d4f777``, update ``1778f619d``, delete). Legacy ``InterfaceDomains::DeleteByDBID`` delegates to ``Collection::DeleteItemByDBID`` (``hmailserver/source/Server/Common/BO/Collection.h:181-200``); ``InterfaceDomain::Delete`` rechecks server-admin authentication and routes through ``PersistentDomain::DeleteObject`` (``hmailserver/source/Server/Common/Persistence/PersistentDomain.cpp:46-70``), which deletes domain dependents (domain aliases, distribution lists, aliases, accounts plus their rules/messages) then the ``hm_domains`` row and the data-directory folder. .NET path preserves installed Domains/Domain COM identity/direct activation denial, rechecks live server-admin authentication, treats unknown/stale IDs as successful no-ops, maps store failure to ``E_FAIL`` retaining the owner snapshot, and removes only the selected snapshot after a transactional cascade delete succeeds. Data-directory folder deletion remains fenced. Focused Domains COM/store coverage ``22/22``; full suite excluding the two AV-locked EICAR cleanup methods ``1819 passed, 0 failed, 7 opt-in skips`` (1826 total). Next slice: isolated SQL identity/readback rollback evidence for domain mutations.

## Current Completed Slice (2026-08-05, DOMAIN UPDATE)

Authenticated existing-row ``Domain.Save()`` UPDATE parity is complete in code/test commit ``1778f619d``, following domain insert commit ``444d4f777``. Legacy ``InterfaceDomain::Save`` delegates to ``PersistentDomain::SaveObject`` (``hmailserver/source/Server/Common/Persistence/PersistentDomain.cpp:167-230``), whose existing-row branch updates ``hm_domains`` columns by ``domainid``. .NET path preserves installed Domains/Domain COM identity/direct activation denial, rechecks live server-admin authentication on setters/Save, stages setters on retained items, persists via a parameterized identity-constrained UPDATE with the same legacy anti-spam/limitations bit packing as insert, maps failed or no-row updates to ``E_FAIL`` retaining the staged snapshot and collection state, and replaces only the matching snapshot after success. Focused Domains COM/store coverage ``17/17``; full suite excluding the two AV-locked EICAR cleanup methods ``1814 passed, 0 failed, 7 opt-in skips`` (1821 total). Next slice: owner-scoped ``Domains.DeleteByDBID`` plus attached ``Domain.Delete()`` parity, then isolated SQL identity/readback rollback evidence for domain mutations.

## Current Completed Slice (2026-08-05, DOMAIN INSERT)

Authenticated ``Domains.Add()`` plus new-item ``Domain.Save()`` INSERT parity is complete in code/test commit ``444d4f777``. Legacy anchors are ``InterfaceDomains::Add`` (``hmailserver/source/Server/COM/InterfaceDomains.cpp:99``) creating an ID-zero child after server-admin check, ``InterfaceDomain::Save``, and ``PersistentDomain::SaveObject`` (``hmailserver/source/Server/Common/Persistence/PersistentDomain.cpp:167-230``) inserting ``hm_domains`` columns with ``domainid`` identity. .NET path preserves installed Domains/Domain IID/vtable/DISPID/ProgID shape plus direct activation denial, rechecks live server-admin authentication on Add/setters/Save, stages legacy defaults (inactive, ``+`` plus-addressing char, signature method 1, local-mail signatures, relaxed/relaxed DKIM) on the Add child, stages all 29 setters, persists via a parameterized insert with ``OUTPUT INSERTED.domainid`` including legacy anti-spam/limitations bit packing, retains the failed draft on store failure mapping to ``E_FAIL``, and appends only the saved snapshot after success. Existing-row Save/Delete remain ``E_NOTIMPL``. Focused Domains COM/store coverage ``13/13``; full suite excluding the two AV-locked EICAR cleanup methods ``1810 passed, 0 failed, 7 opt-in skips`` (1817 total). Next slice: authenticated existing-row ``Domain.Save()`` UPDATE parity, then ``Domains.DeleteByDBID`` plus ``Domain.Delete()``, then isolated SQL identity/readback rollback evidence for domain mutations.

## Current Completed Slice (2026-08-05, RULE SQL EVIDENCE)

Isolated SQL identity/readback and rollback evidence for the completed rule mutations is complete in code/test commit ``dbcbc346a``. The new opt-in ``SqlServerRuleAdministrationStoreIntegrationTests`` fixture creates a disposable isolated database mirroring the legacy ``hm_rules``/``hm_rule_criterias``/``hm_rule_actions`` schema and proves ``OUTPUT INSERTED.ruleid`` identity readback with per-insert increments, owner-scoped UPDATE/DELETE that no-op against foreign account IDs, transactional cascade DELETE removing criteria/action rows before the rule row, and NOT NULL name violations on INSERT/UPDATE throwing ``SqlException`` with no partial row and the original row intact. Live LocalDB evidence is ``1/1``; the focused Rules suite is ``113/113``. The full suite excluding the two AV-locked EICAR cleanup methods is ``1805 passed, 0 failed, 7 opt-in skips`` (1812 total) with the SQL connection unset. Running the full suite with the SQL connection set still surfaces the ``5`` pre-existing message-indexing opt-in failures unrelated to this test-only slice. Real COM activation, backup/restore, migration, SEC-18, and release gates remain open. Next slice: next authenticated Admin collection mutation after rules.

## Current Completed Slice (2026-08-05, RULE UPDATE)

Authenticated existing-row ``Rule.Save()`` UPDATE parity is complete in code/test commit ``d7694c227``, following rule insert commit ``0239f30a1``. Legacy ``InterfaceRule::Save`` delegates to ``PersistentRule::SaveObject`` (``hmailserver/source/Server/Common/Persistence/PersistentRule.cpp:73-120``), whose existing-row branch updates ``hm_rules`` columns by ``ruleid``. .NET path preserves installed Rules/Rule COM identity/direct activation denial, rechecks live account/server authentication on setters/Save, stages setters on retained items, persists via a parameterized owner-scoped UPDATE (``WHERE ruleid AND ruleaccountid``), maps failed or no-row updates to ``E_FAIL`` retaining staged snapshot and generation, and replaces only the matching snapshot in the generation after success. Focused Rules COM/store coverage ``112/112``; full suite excluding the two AV-locked EICAR cleanup methods ``1805 passed, 0 failed, 6 opt-in skips`` (1811 total). Next slice: isolated SQL identity/readback rollback evidence for rule mutations, then the next authenticated Admin collection mutation after rules.

## Current Completed Slice (2026-08-05, RULE INSERT)

Authenticated account-owned ``Rules.Add()`` plus new-item ``Rule.Save()`` INSERT parity is complete in code/test commit ``0239f30a1``. Legacy anchors are ``InterfaceRules::Add`` (``hmailserver/source/Server/COM/InterfaceRules.cpp:91``) creating an ID-zero child scoped to the owning account, ``InterfaceRule::Save``, and ``PersistentRule::SaveObject`` (``hmailserver/source/Server/Common/Persistence/PersistentRule.cpp:73-120``) inserting ``hm_rules`` columns with ``ruleid`` identity. .NET path preserves installed Rules/Rule IID/vtable/DISPID/ProgID shape plus direct activation denial, rechecks live account/server authentication on Add/setters/Save, stages legacy defaults (``Active=true``, ``UseAnd=true``, empty name, sort order 0) on the Add child, stages all four setters, uses a parameterized insert with ``OUTPUT INSERTED.ruleid``, retains the failed draft on store failure mapping to ``E_FAIL``, and publishes only the saved snapshot into the generation after success. Existing-row Save/setters remain ``E_NOTIMPL``. Focused Rules COM/store coverage ``108/108``; full suite excluding the two AV-locked EICAR cleanup methods ``1801 passed, 0 failed, 6 opt-in skips`` (1807 total). Next slice: authenticated existing-row ``Rule.Save()`` UPDATE parity, then isolated SQL identity/readback rollback evidence for rule mutations.

## Current Completed Slice (2026-08-05, ROUTE SQL EVIDENCE)

Isolated SQL identity/readback and rollback evidence for the completed route mutations is complete in code/test commit ``ad97b391b``. The new opt-in ``SqlServerRouteAdministrationStoreIntegrationTests`` fixture creates a disposable isolated database mirroring the legacy ``hm_routes``/``hm_routeaddresses`` schema and proves ``OUTPUT INSERTED.routeid`` identity readback with per-insert increments, identity-preserving UPDATE with BlowFish password round-trip, cascade DELETE removing ``hm_routeaddresses`` rows before the ``hm_routes`` row, unknown-ID UPDATE/DELETE returning false with rows intact, and NOT NULL domain-name violations on INSERT/UPDATE throwing ``SqlException`` with no partial row and the original row intact. Live LocalDB evidence is ``1/1``; the focused Routes suite is ``36/36``. The full suite excluding the two AV-locked EICAR cleanup methods is ``1796 passed, 0 failed, 6 opt-in skips`` (1802 total) with the SQL connection unset. Running the full suite with the SQL connection set still surfaces the ``5`` pre-existing message-indexing opt-in failures unrelated to this test-only slice. Real COM activation, backup/restore, migration, SEC-18, and release gates remain open. Next slice: next authenticated Admin collection mutation after routes.

## Current Completed Slice (2026-08-05, ROUTE DELETE)

Authenticated owner-scoped ``Routes.DeleteByDBID`` plus attached ``Route.Delete()`` parity is complete in code/test commit ``24510aafa``, completing the route mutation trio (insert ``264995c17``, update ``84135364e``, delete). Legacy ``InterfaceRoutes::DeleteByDBID`` delegates to ``Collection::DeleteItemByDBID`` (``hmailserver/source/Server/Common/BO/Collection.h:181-200``); ``InterfaceRoute::Delete`` (``hmailserver/source/Server/COM/InterfaceRoute.cpp:582``) rechecks server-admin authentication and routes through ``PersistentRoute::DeleteObject`` (``hmailserver/source/Server/Common/Persistence/PersistentRoute.cpp:31-40``), which first deletes ``hm_routeaddresses`` rows then the ``hm_routes`` row by ``routeid``. .NET path preserves installed Routes/Route COM identity/direct activation denial, rechecks live server-administrator authentication, treats unknown/stale IDs as successful no-ops, maps store failure to ``E_FAIL`` retaining the owner snapshot, and removes only the selected snapshot after the cascaded delete succeeds. Focused Routes COM/store coverage ``35/35``; full suite excluding the two AV-locked EICAR cleanup methods ``1796 passed, 0 failed, 5 opt-in skips`` (1801 total). Next slice: isolated SQL identity/readback rollback evidence for route mutations, then the next authenticated Admin collection mutation after routes.

## Current Completed Slice (2026-08-05, ROUTE UPDATE)

Authenticated existing-row ``Route.Save()`` UPDATE parity is complete in code/test commit ``84135364e``, following route insert commit ``264995c17``. Legacy ``InterfaceRoute::Save`` (``hmailserver/source/Server/COM/InterfaceRoute.cpp:243``) delegates to ``PersistentRoute::SaveObject`` (``hmailserver/source/Server/Common/Persistence/PersistentRoute.cpp:53-88``), whose existing-row branch updates all legacy ``hm_routes`` columns by ``routeid``. .NET path preserves installed Routes/Route COM identity/direct activation denial, rechecks live server-administrator authentication on setters/Save, stages setters on retained items (including ``SetRelayerAuthPassword``), persists via a parameterized identity-constrained UPDATE, maps failed or no-row updates to ``E_FAIL`` retaining the staged snapshot and collection state, and replaces only the matching snapshot in the collection after success. Focused Routes COM/store coverage ``30/30``; full suite excluding the two AV-locked EICAR cleanup methods ``1791 passed, 0 failed, 5 opt-in skips`` (1796 total). Next slice: owner-scoped ``Routes.DeleteByDBID`` plus attached ``Route.Delete()`` parity, then isolated SQL identity/readback rollback evidence for route mutations.

## Current Completed Slice (2026-08-05, ROUTE INSERT)

Authenticated ``Routes.Add()`` plus new-item ``Route.Save()`` INSERT parity is complete in code/test commit ``264995c17``. Legacy anchors are ``InterfaceRoutes::Add`` (``hmailserver/source/Server/COM/InterfaceRoutes.cpp``) creating an ID-zero child scoped to the owning collection, ``InterfaceRoute::Save`` (``hmailserver/source/Server/COM/InterfaceRoute.cpp:243``), and ``PersistentRoute::SaveObject`` (``hmailserver/source/Server/Common/Persistence/PersistentRoute.cpp:53-88``) which inserts all legacy ``hm_routes`` columns and assigns ``routeid``. .NET path preserves installed Routes/Route IID/vtable/DISPID/ProgID shape plus direct activation denial, rechecks live server-administrator authentication on Add/setters/Save, stages legacy defaults (``AllAddresses=true``, zeros, empty strings) on the Add child, stages all twelve setters including ``SetRelayerAuthPassword``, uses a parameterized insert with ``OUTPUT INSERTED.routeid`` persisting the BlowFish-encrypted relayer password, retains the failed draft on store failure mapping to ``E_FAIL``, and appends only the saved snapshot after success. Existing-row Save and Delete remain ``E_NOTIMPL``. Focused Routes COM/store coverage ``26/26``; full suite excluding the two AV-locked EICAR cleanup methods ``1787 passed, 0 failed, 5 opt-in skips`` (1792 total). Next slice: authenticated existing-row ``Route.Save()`` UPDATE parity, then ``Routes.DeleteByDBID`` plus ``Route.Delete()``, then isolated SQL identity/readback rollback evidence for route mutations.

## Current Completed Slice (2026-08-05, RECIPIENT SQL EVIDENCE)

Isolated SQL identity/readback and rollback evidence for the completed distribution-list recipient COM mutations is complete in code/test commit `36270f965`. The new opt-in `SqlServerDistributionListRecipientAdministrationStoreIntegrationTests` fixture (`StoreMutations_ExhibitLegacyIdentityReadbackOwnerScopingAndStatementRollback`) creates a disposable isolated database mirroring the legacy schema (`hmailserver/source/DBScripts/CreateTablesMSSQL.sql:309-340`), then proves `OUTPUT INSERTED.distributionlistrecipientid` identity readback with per-insert increments, owner-scoped UPDATE/DELETE predicates that no-op against foreign list IDs, identity-preserving UPDATE, and statement-level rollback where NOT NULL address violations on INSERT/UPDATE throw `SqlException` with no partial row and the original row intact. SqlClient truncates over-length NVarChar parameter values client-side, so the legacy column length is not a server-side failure seam; the NOT NULL constraint is the natural injection point. Live LocalDB evidence is `1/1`; the focused recipient suite is `24/24` (contract + store + live SQL). The full suite excluding the two AV-locked EICAR cleanup methods is `1782 passed, 0 failed, 5 opt-in skips` (1787 total) with the SQL connection unset. Running the full suite with the SQL connection set executes the previously skipped message-indexing opt-in fixtures for the first time in this environment and surfaces `5` pre-existing failures unrelated to this test-only slice (for example `SqlServerMessageIndexingIntegrationTests.AuthenticatedComPath_ReadsBoundedSettingsScalarsFromIsolatedDatabase` expects `blockedAttachments[0].Save` to throw `COMException` but no exception is raised). Real COM activation, backup/restore, migration, SEC-18, and release gates remain open. Next slice: next authenticated Admin collection mutation.

## Current Completed Slice (2026-08-05, RECIPIENT DELETE)

Authenticated owner-scoped distribution-list recipient `DeleteByDBID` plus attached `DistributionListRecipient.Delete()` parity is complete in code/test commit `20ec7a285`, following recipient UPDATE commit `259cf0867`. Legacy `InterfaceDistributionListRecipients::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceDistributionListRecipients.cpp:37-51`) and `InterfaceDistributionListRecipient::Delete` (`hmailserver/source/Server/COM/InterfaceDistributionListRecipient.cpp:115-137`) route through `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`), while `PersistentDistributionListRecipient::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentDistributionListRecipient.cpp:25-43`) deletes the row by recipient ID and invalidates the owning list cache. The .NET path preserves installed recipient COM identity/direct activation denial, live owner authentication, unknown/foreign/stale no-op behavior, owner-scoped SQL predicates, failed-delete snapshot retention, and post-success removal only. Focused coverage is `20/20`; the full suite excluding the two AV-locked EICAR cleanup methods is `1782 passed, 0 failed, 4 opt-in skips`. Direct full execution remains environment-blocked by those unrelated scanner cleanup failures. SQL identity/readback, real COM activation, rollback injection, backup/restore, migration, SEC-18, and release gates remain open.

Next slice: isolated SQL identity/readback and rollback evidence for completed COM mutations.

## Current Completed Slice (2026-08-05, RECIPIENT UPDATE)

Authenticated owner-scoped existing-row `DistributionListRecipient.Save()` UPDATE parity is complete in code/test commit `259cf0867`, following recipient INSERT commit `91645dc3a`. Legacy `InterfaceDistributionListRecipient::Save` (`hmailserver/source/Server/COM/InterfaceDistributionListRecipient.cpp:133-157`) delegates to `PersistentDistributionListRecipient::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentDistributionListRecipient.cpp:103-139`), whose existing-row branch updates `distributionlistrecipientlistid` and `distributionlistrecipientaddress` by `distributionlistrecipientid`; the schema is `hmailserver/source/DBScripts/CreateTablesMSSQL.sql:331-340`. The .NET path preserves installed recipient COM identity/direct activation denial, live owner authentication, owner-scoped SQL predicates, failed staged-state retention, and post-success replacement of only the matching snapshot. Focused coverage is `15/15`; the full suite excluding the two AV-locked EICAR cleanup methods is `1777 passed, 0 failed, 4 opt-in skips`. Direct full execution remains environment-blocked by those unrelated scanner cleanup failures. Recipient deletion, SQL identity/readback, real COM activation, rollback injection, backup/restore, migration, SEC-18, and release gates remain open.

Next slice: authenticated owner-scoped recipient `DeleteByDBID` plus attached `DistributionListRecipient.Delete()` parity.

This backlog tracks the remaining production-parity work for the side-by-side .NET 10 rewrite. Keep it current as each slice lands.

## Current Completed Slice (2026-08-05, RECIPIENT INSERT)

Authenticated owner-scoped distribution-list recipient `Add()` plus new-item `DistributionListRecipient.Save()` INSERT parity is complete in code/test commit `91645dc3a`. Legacy anchors are `hmailserver/source/Server/COM/InterfaceDistributionListRecipients.cpp:53-83`, `InterfaceDistributionListRecipient.cpp:93-157`, and `hmailserver/source/Server/Common/Persistence/PersistentDistributionListRecipient.cpp:103-139`; the table is `hmailserver/source/DBScripts/CreateTablesMSSQL.sql:331-340`. The .NET path preserves recipient COM identity/direct activation denial, owner live authentication, list-ID binding, staged address behavior, parameterized identity insert, failed-draft retention, and post-success append. Focused coverage is `11/11`; the full suite excluding the two AV-locked EICAR cleanup methods is `1773 passed, 0 failed, 4 opt-in skips`. Direct full execution remains environment-blocked by those unrelated scanner cleanup failures. Existing-row recipient update/delete, SQL identity/readback, real COM activation, rollback injection, backup/restore, migration, SEC-18, and release gates remain open.

Next slice: authenticated existing-row `DistributionListRecipient.Save()` UPDATE parity.

## Current Completed Slice (2026-08-05, DELETE)

Authenticated owner-scoped `DistributionLists.DeleteByDBID` plus attached `DistributionList.Delete()` parity is complete in code/test commit `fb6de84f7`. Legacy `InterfaceDistributionLists::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceDistributionLists.cpp:38-53`) delegates to `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`); `PersistentDistributionList::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentDistributionList.cpp:35-54`) deletes recipient rows first and then the parent list row. The .NET path preserves installed COM identity/direct activation denial, live authorization, owner containment, unknown/stale no-op behavior, failed-delete snapshot retention, and post-success snapshot removal. Focused coverage is `27/27`; the full suite excluding the two AV-locked EICAR cleanup methods is `1768 passed, 0 failed, 4 opt-in skips`. Direct full execution remains environment-blocked by those unrelated scanner cleanup failures. Recipient COM mutation, SQL identity/readback, real COM activation, rollback injection, backup/restore, migration, SEC-18, and release gates remain open.

Next slice: authenticated owner-scoped distribution-list recipient mutation.

## Current Completed Slice (2026-08-05, UPDATE)

The authenticated existing-row `DistributionList.Save()` UPDATE slice is complete in code/test commit `852aa1586`, after the INSERT slice `f2d33c348`. Legacy anchors are `hmailserver/source/Server/COM/InterfaceDistributionList.cpp:252-271` and `hmailserver/source/Server/Common/Persistence/PersistentDistributionList.cpp:118-157`; the latter updates all six `hm_distributionlists` fields by `distributionlistid`. The .NET path preserves the installed distribution-list COM identity and direct activation `E_ACCESSDENIED`, rechecks live authorization, stages the update on the owning facade, uses parameterized `WHERE distributionlistid = @ID`, retains staged state and the owner snapshot on failure, and replaces only the matching owner snapshot after success. Focused coverage is `20/20`; the full suite excluding the two AV-locked EICAR cleanup methods is `1761 passed, 0 failed, 4 opt-in skips`. Direct full execution remains environment-blocked by those unrelated scanner cleanup failures. Delete, recipients, SQL identity/readback, real COM activation, rollback injection, backup/restore, migration, SEC-18, and release gates remain open.

Next slice: authenticated owner-scoped `DistributionLists.DeleteByDBID` plus attached `DistributionList.Delete()` parity.

## Current Completed Slice (2026-08-05, working tree)

The bounded authenticated domain-owned `DistributionLists.Add()` plus new-item `DistributionList.Save()` INSERT slice is complete in code/test commit `f2d33c348`. Legacy references are `hmailserver/source/Server/COM/InterfaceDomain.cpp:574-603`, `InterfaceDistributionLists.cpp:55-84`, `InterfaceDistributionList.cpp:81-277`, and `hmailserver/source/Server/Common/Persistence/PersistentDistributionList.cpp:118-157`. The .NET path preserves the installed `IInterfaceDistributionLists`/`IInterfaceDistributionList` IID, CLSID, ProgID, DISPID, vtable, and type-library shape plus direct activation `E_ACCESSDENIED`; it passes the domain authentication callback into the child facades, binds new drafts to the owning domain, stages the five editable fields, inserts all six legacy columns with parameterized `OUTPUT INSERTED.distributionlistid`, retains failed drafts, and appends the generated-ID owner snapshot only after success. Focused coverage is `14/14`; the full suite excluding the two AV-locked EICAR cleanup methods is `1755 passed, 0 failed, 4 opt-in skips` (1759 total). Direct full execution remains environment-blocked by those two unrelated scanner-runtime cleanup failures. Existing-row update/delete, recipients mutation, SMTP list policy, live SQL identity/readback, real COM activation, rollback injection, backup/restore, migration, SEC-18, and release gates remain open. No production service, database, Data directory, IIS, COM registration, or DCOM state was used.

Historical next slice superseded by the DELETE completion above.

## Historical Next Slice (2026-08-04)

Code/test commit `b8025f2fe` completes authenticated owner-scoped SURBL deletion parity. Legacy `InterfaceSURBLServers::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceSURBLServers.cpp:88-101`) calls `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`), which invokes `PersistentSURBLServer::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentSURBLServer.cpp:25-33`) only for an in-memory contained ID and leaves unknown IDs as successful no-ops. Legacy `InterfaceSURBLServer::Delete` (`hmailserver/source/Server/COM/InterfaceSURBLServer.cpp:187-208`) rechecks server-admin authorization and routes attached items through the owning collection. The .NET path preserves installed SURBL IID/vtable/DISPID/ProgID shape and direct activation denial, rechecks live authorization, preserves stale-item no-op behavior, maps store failure to `E_FAIL`, and removes only the matching snapshot after successful parameterized SQL deletion. Focused coverage is `21/21`; the direct full run is `1748 passed, 2 unrelated scanner-runtime cleanup failures, 4 opt-in skips` (1754 total); excluding those two scanner classes passes `1743` with `4` skips. SQL identity/readback, real COM activation, live SURBL/SMTP behavior, backup/restore, migration, SEC-18, and release gates remain open. Next slice: obtain approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence; if unavailable, map the next independent authenticated Admin mutation. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `835e73804` completes authenticated existing-row `SURBLServer.Save()` UPDATE parity after `cd627826e`. Legacy `InterfaceSURBLServer::Save` (`hmailserver/source/Server/COM/InterfaceSURBLServer.cpp:11-36`) rechecks server-admin authentication and routes the attached object through `PersistentSURBLServer::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentSURBLServer.cpp:55-90`), whose existing-row branch updates `surblactive`, `surblhost`, `surblrejectmessage`, and `surblscore` by `surblid`. The .NET path preserves installed SURBL IIDs/vtable/DISPID/ProgID shape and direct activation denial, scopes setters and Save to the owning collection, rechecks retained-item authorization, maps failed updates to `E_FAIL`, retains staged values and the owner snapshot on failure, and replaces only the matching owner snapshot after success. Delete, SQL identity/readback, real COM activation, live SURBL/SMTP behavior, backup/restore, migration, SEC-18, and release gates remain open. Focused coverage is `15/15`; full Net10 is `1744 passed, 0 failed, 4 opt-in skips` (1748 total). Next slice: obtain approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence; if unavailable, map the next independent authenticated Admin mutation. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `cd627826e` completes authenticated `SURBLServers.Add()` plus new-item `SURBLServer.Save()` INSERT parity. Legacy `InterfaceSURBLServers::Add` (`hmailserver/source/Server/COM/InterfaceSURBLServers.cpp:134-163`) creates an ID-zero child; `InterfaceSURBLServer::Save` (`hmailserver/source/Server/COM/InterfaceSURBLServer.cpp:11-36`) rechecks server-admin authentication and attaches only after `PersistentSURBLServer::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentSURBLServer.cpp:55-90`) assigns `surblid`. The .NET path preserves installed SURBL IIDs/vtable/DISPIDs/ProgIDs and direct activation denial, stages the four persisted fields, uses parameterized `OUTPUT INSERTED.surblid`, retains failed drafts, and publishes only the new owner snapshot after success. Focused coverage is `12/12`; full Net10 is `1741 passed, 0 failed, 4 opt-in skips` (1745 total). Existing-row Save, Delete, SQL identity/readback, real COM activation, live SURBL/SMTP behavior, rollback injection, backup/restore, migration, SEC-18, and release gates remain open. Next slice: obtain approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence; if unavailable, map the next independent authenticated Admin mutation. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `811ce0300` completes authenticated owner-scoped `DNSBlackLists.DeleteByDBID` plus attached `DNSBlackList.Delete()` parity after `f6033de3b` completed existing-row UPDATE. Legacy `InterfaceDNSBlackLists::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceDNSBlackLists.cpp:91-106`) delegates to `Collection::DeleteItemByDBID` and returns `S_OK` for unknown IDs; `InterfaceDNSBlackList::Delete` (`hmailserver/source/Server/COM/InterfaceDNSBlackList.cpp:221-242`) rechecks server-admin authentication and routes attached objects through the parent, while `PersistentDNSBlackList::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentDNSBlacklist.cpp:25-32`) deletes by `sblid`. The .NET path preserves installed DNSBL IIDs/vtable/DISPIDs/ProgIDs and direct activation denial, treats unknown IDs as owner-scoped no-ops, maps store failure to `E_FAIL`, retains the prior snapshot on failure, and removes only the selected snapshot after successful deletion. Focused coverage is `20/20`; full Net10 is `1737 passed, 0 failed, 4 opt-in skips` (1741 total). Live DNSBL reconfiguration, SMTP trust, SQL identity/readback, real COM activation, rollback injection, backup/restore, migration, SEC-18, and release gates remain open. Next slice: obtain approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence; if unavailable, map the next independent authenticated Admin mutation. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `f6033de3b` completes authenticated existing-row `DNSBlackList.Save()` UPDATE parity after `e956dcd3d` completed Add/INSERT. Legacy `InterfaceDNSBlackList::Save` (`hmailserver/source/Server/COM/InterfaceDNSBlackList.cpp:14-37`) rechecks server-admin authentication and calls `PersistentDNSBlackList::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentDNSBlacklist.cpp:55-90`), whose existing-row branch updates `sblactive`, `sbldnshost`, `sblresult`, `sblrejectmessage`, and `sblscore` by `sblid`. The .NET path preserves installed DNSBL IIDs/vtable/DISPIDs/ProgIDs and direct activation denial, stages attached fields, uses parameterized `UPDATE hm_dnsbl ... WHERE sblid = @id`, retains the staged item and owner snapshot on failed update, and replaces only the matching owner snapshot after success. Focused coverage is `16/16`; full Net10 is `1733 passed, 0 failed, 4 opt-in skips` (1737 total). Existing-row Delete, SQL identity/readback, real COM activation, live DNSBL/SMTP behavior, rollback injection, backup/restore, migration, SEC-18, and release gates remain open. Next slice: obtain approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence; if unavailable, map the next independent authenticated Admin mutation. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `e956dcd3d` completes authenticated `DNSBlackLists.Add()` plus new-item `DNSBlackList.Save()` INSERT parity. Legacy `InterfaceDNSBlackLists::Add` (`hmailserver/source/Server/COM/InterfaceDNSBlackLists.cpp:138-165`) creates an ID-zero child; `InterfaceDNSBlackList::Save` (`hmailserver/source/Server/COM/InterfaceDNSBlackList.cpp:14-34`) persists first and attaches only after `PersistentDNSBlacklist::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentDNSBlacklist.cpp:55-90`) assigns `sblid`. The .NET path preserves installed DNSBL COM identity/vtable/DISPIDs/ProgIDs, direct activation denial, authenticated Settings access, staged new-item fields, parameterized `OUTPUT INSERTED.sblid`, failed-draft retention, and post-success owner publication. Focused coverage is `12/12`; full Net10 is `1729 passed, 0 failed, 4 opt-in skips` (1733 total). Existing-row Save, Delete, live DNSBL reconfiguration, SMTP trust, SQL identity/readback, real COM activation, backup/restore, migration, SEC-18, and release gates remain open. Next slice: obtain approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence; if unavailable, implement authenticated DNSBlackList existing-row Save UPDATE parity. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `fdfdc6c42` completes authenticated owner-scoped `GreyListingWhiteAddresses.DeleteByDBID` plus attached `GreyListingWhiteAddress.Delete()` parity after `6ba86e16b` completed existing-row UPDATE. Legacy `InterfaceGreyListingWhiteAddresses::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceGreyListingWhiteAddresses.cpp:85-102`) delegates membership to `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`); `InterfaceGreyListingWhiteAddress::Delete` (`hmailserver/source/Server/COM/InterfaceGreyListingWhiteAddress.cpp:117-137`) rechecks Administrator authentication and routes attached items through the owner, while direct persistence is `PersistentGreyListingWhiteAddress::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentGreyListingWhiteAddress.cpp:26-32`). The .NET implementation preserves installed IIDs/vtable/DISPIDs/ProgIDs and direct activation denial (`hmailserver/source/Server/hMailServer/hMailServer.idl:2356-2387`), treats unknown IDs as owner-scoped no-ops, maps store failure to `E_FAIL`, retains the prior snapshot on failure, and removes only the selected snapshot after success. Focused coverage is `21/21`; full Net10 is `1725 passed, 0 failed, 4 opt-in skips` (1729 total). SQL identity/readback, real COM activation, greylisting live reconfiguration, rollback injection, SMTP whitelist E2E, backup/restore, migration, SEC-18, and release gates remain open. Next slice: approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence remains environment-blocked; if unavailable, implement DNSBlackLists.Add plus new-item DNSBlackList.Save INSERT parity. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `6ba86e16b` completes authenticated existing-row `GreyListingWhiteAddress.Save()` UPDATE parity after `b31ce86c1` completed Add/INSERT. Legacy `InterfaceGreyListingWhiteAddress::Save` (`hmailserver/source/Server/COM/InterfaceGreyListingWhiteAddress.cpp:10-31`) calls `PersistentGreyListingWhiteAddress::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentGreyListingWhiteAddress.cpp:52-84`), which updates the legacy `hm_greylisting_whiteaddresses` columns by `whiteid`; `GreyListingWhiteAddress::SetUserEditableIPAddress` (`hmailserver/source/Server/Common/BO/GreyListingWhiteAddress.cpp:64-74`) continues to accept invalid text. The .NET implementation preserves installed IIDs/vtable/DISPIDs/ProgIDs and direct activation denial (`hmailserver/source/Server/hMailServer/hMailServer.idl:2356-2387`), rechecks live Administrator access, retains failed staged values, maps persistence failure to `E_FAIL`, and replaces only the matching owner snapshot after success. Focused coverage is `17/17`; full Net10 is `1721 passed, 0 failed, 4 opt-in skips` (1725 total). Existing-row Delete, SQL identity/readback, real COM activation, greylisting live reconfiguration, rollback injection, SMTP whitelist E2E, backup/restore, migration, SEC-18, and release gates remain open. Next slice: approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence remains environment-blocked; if unavailable, implement greylisting white-address DeleteByDBID/item Delete parity. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `b31ce86c1` completes authenticated `GreyListingWhiteAddresses.Add()` plus new-item `GreyListingWhiteAddress.Save()` INSERT parity. Legacy `InterfaceGreyListingWhiteAddresses::Add` (`hmailserver/source/Server/COM/InterfaceGreyListingWhiteAddresses.cpp:162-187`) creates an unsaved ID-zero child; `InterfaceGreyListingWhiteAddress::Save` (`hmailserver/source/Server/COM/InterfaceGreyListingWhiteAddress.cpp:10-31`) checks server-admin authentication, invokes `PersistentGreyListingWhiteAddress::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentGreyListingWhiteAddress.cpp:52-84`), receives the generated `whiteid`, and attaches only after successful persistence. Legacy user-editable IP conversion is `GreyListingWhiteAddress::SetUserEditableIPAddress` (`hmailserver/source/Server/Common/BO/GreyListingWhiteAddress.cpp:64-74`) through `SQLStatement::ConvertWildcardToLike` (`hmailserver/source/Server/Common/SQL/SQLStatement.cpp:591-610`); it does not parse or reject invalid input, which is covered by the new test. Installed IIDs/vtable/DISPIDs/ProgIDs and direct activation denial remain unchanged (`hmailserver/source/Server/hMailServer/hMailServer.idl:2356-2387`). Focused coverage is `14/14`; full Net10 is `1718 passed, 0 failed, 4 opt-in skips` (1722 total). The implementation stages the stored wildcard value, assigns the generated ID, retains failed drafts, and publishes only the new owner snapshot after success. Existing-row Save/Delete, SQL identity/readback, real COM activation, greylisting live reconfiguration, rollback injection, SMTP whitelist E2E, backup/restore, migration, SEC-18, and release gates remain open. Next slice: approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence remains environment-blocked; if unavailable, implement greylisting white-address existing-row Save parity. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `ae6239f54` completes authenticated owner-scoped `BlockedAttachments.DeleteByDBID` plus attached `BlockedAttachment.Delete()` parity after `2324b0131` completed existing-row UPDATE. Legacy `InterfaceBlockedAttachments::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceBlockedAttachments.cpp:76-89`) delegates membership to `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`), and `InterfaceBlockedAttachment::Delete` (`hmailserver/source/Server/COM/InterfaceBlockedAttachment.cpp:122-141`) rechecks server-admin authentication before parent deletion; direct objects use `PersistentBlockedAttachment::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentBlockedAttachment.cpp:26-32`). The installed COM contract and direct activation boundary remain unchanged (`hmailserver/source/Server/hMailServer/hMailServer.idl:2285-2312`). Focused coverage is `19/19`; full Net10 is `1714 passed, 0 failed, 4 opt-in skips` (1718 total). Unknown IDs are no-ops, failed deletes retain the owner snapshot, and successful deletes remove only the selected snapshot. SQL readback, real COM activation, scanner/live reconfiguration, and rollback injection remain open. Next slice: approved disposable SQL Group/member insert/update/delete/readback plus injected rollback evidence remains environment-blocked; if unavailable, map the next independent authenticated Admin mutation. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `843360528` completes the bounded SMTP global-whitelist evaluator wiring. `ISmtpGlobalWhitelistEvaluator` loads current `hm_whitelist` rows per listener-created SMTP request and uses `WhiteListMatcher` with fail-closed store faults. Legacy `WhiteListCache::IsWhitelisted` and `SMTPConnection::InitializeSpamProtectionType_` are the behavioral anchors. The receiver bypasses the selected legacy anti-spam checks but keeps URL blocklist, scripts, rules, attachment, antivirus, queue, and upstream recipient/auth/relay controls active. `SmtpTcpListener`/`SmtpSession` provide socket-peer identity; `ExternalFetchProcessor` marks its request `IsExternalFetch`, so POP3 fetch endpoint/MIME values are never treated as SMTP trust. Focused evaluator/receiver/external-fetch coverage is `71/71`; full Net10 is `1632 passed, 0 failed, 4 opt-in skips` (1636 total). This remains a code/test parity slice: no live disposable SQL readback, mutation visibility, SMTP socket E2E, rollback evidence, or sender default-domain canonicalization proof exists. Next slice: approved disposable SQL parent/UID readback and rollback evidence; then performance acceptance beyond offline synthetic SEARCH/SORT; then disposable SQL whitelist mutation/readback and SMTP acceptance. No production listener, SQL, Data directory, COM registration, or DCOM state was used.

Code/test commit `08901d2d9` adds the unwired `HMailServer.Core.WhiteListMatcher` seam. Legacy `WhiteListCache::IsWhitelisted` (`hmailserver/source/Server/Common/AntiSpam/WhiteListCache.cpp:52-88`) matches inclusive same-family IP ranges and empty/`*`/case-insensitive `*`/`?` sender patterns; invalid IP or malformed row data is not trusted. Focused matcher coverage is `6/6`; full Net10 is `1624 passed, 0 failed, 4 opt-in skips` (1628 total). This is a pure matcher slice: it does not wire SMTP, bypass non-spam controls, add a cache, or change COM/SQL behavior. Next slice: wire a fail-closed, transaction-scoped evaluator with socket-peer identity, live mutation invalidation, and preservation of authentication, relay/recipient, scripts, rules, attachment, URL, antivirus, and queue controls. No production listener, SQL, or data directory was used.

Code/test commit `298088e31` corrects the offline synthetic SEARCH/SORT benchmark to report the measured result count and include expected-versus-actual count equality in `Correct` (`hmailserver/source/Server.Net10/benchmarks/HMailServer.Net10.Benchmarks/SyntheticImapSearchSortBenchmark.cs:121-174`). Focused benchmark coverage is `3/3`; full Net10 is `1618 passed, 0 failed, 4 opt-in skips` (1622 total). Legacy anchors are `IMAPSearchParser::ParseCommand`, `IMAPSortParser::Parse`, and `IMAPSort::Sort`; this is a benchmark instrumentation fix only and does not close SQL FTS, live IMAP, C++ baseline, concurrency, or soak gates. Next slice: obtain approved disposable SQL parent/UID ordering/failure evidence; then map and implement the bounded SMTP whitelist bypass and extend performance acceptance beyond offline synthetic SEARCH/SORT. No production listener, SQL, or data directory was used.

Code/test commit `008e949dd` adds focused false-result and exception-path coverage for `ExternalFetchProcessor.DeleteMissingKnownUidsAsync` (`hmailserver/source/Server.Net10/src/HMailServer.Protocols/Pop3/ExternalFetchProcessor.cs:270-290`). Legacy `FetchAccountUIDList::RemoveMissingUIDs` and `PersistentFetchAccountUID::DeleteObject` (`hmailserver/source/Server/ExternalFetcher/FetchAccountUIDList.cpp:90-123`; `hmailserver/source/Server/Common/Persistence/PersistentFetchAccountUID.cpp:74-85`) do not count a false persistence result, while an exception fails the current account processing path. The tests preserve the current remote-success-before-local-UID deletion ordering and change no production behavior. Focused ExternalFetchProcessor coverage is `34/34`; full Net10 is `1617 passed, 0 failed, 4 opt-in skips` (1621 total). Residual risk is no disposable SQL parent/UID readback or rollback evidence, quota admission, SMTP whitelist evaluator/cache parity, and performance/release gates. Next slice: obtain approved disposable SQL parent/UID ordering/failure evidence; then map and implement the bounded SMTP whitelist bypass and extend performance acceptance beyond offline synthetic SEARCH/SORT. No production listener, SQL, or data directory was used.

Code/test commit `6a415afd1` aligns IMAP `GETQUOTA`/`GETQUOTAROOT` usage with legacy `AccountSizeCache::GetSize` and `PersistentAccount::GetMessageBoxSize` (`hmailserver/source/Server/Common/Cache/AccountSizeCache.cpp:59-72`; `hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:341-355`). Legacy sums all account-owned `hm_messages.messagesize` rows; the former Net10 query incorrectly filtered to `messagetype = 2`. The .NET query now uses account-scoped `COALESCE(SUM(m.messagesize), 0)` while preserving existing `/1024` truncation and account-limit conversion. Focused QUOTA coverage is `5/5`; full Net10 is `1584 passed, 0 failed, 4 opt-in skips`. Stateful AccountSizeCache lifecycle, quota admission, mixed-type SQL readback, and performance acceptance remain open. Next: bounded legacy `GETQUOTAROOT` no-quota/domain-limit/mailbox-quoting parity; then the AccountSize semantic/writer matrix and no-schema post-commit invalidation seam. No production SQL, protocol listener, or data directory was used.

Code/test commit `1c201c3c4` completes authenticated `FetchAccounts.Add()` and new-item `FetchAccount.Save()` INSERT parity. Legacy `InterfaceFetchAccounts::Add` (`hmailserver/source/Server/COM/InterfaceFetchAccounts.cpp:139-165`) creates an ID-zero child scoped to the owning account; `InterfaceFetchAccount::Save` (`hmailserver/source/Server/COM/InterfaceFetchAccount.cpp:341-360`) calls `PersistentFetchAccount::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentFetchAccount.cpp:153-207`) and publishes only after the generated `faid` is assigned. The .NET path preserves the installed FetchAccounts/FetchAccount COM identity/vtable/DISPID shape and direct activation denial, stages new fields, uses legacy Blowfish ciphertext for the password, enforces the owning-account boundary, uses parameterized `OUTPUT INSERTED.faid`, retains failed drafts, and appends only after insert success. Existing-row mutation, FetchAccountUID mutation/cleanup, retry/live-fetch behavior, and sibling-facade freshness remain open. Focused FetchAccounts/SQL coverage is `27/27`; full Net10 is `1584 passed, 0 failed, 4 opt-in skips`. Next slice: approved disposable SQL identity/readback plus injected rollback evidence remains environment-blocked; then AccountSize semantic/writer coverage and a no-schema post-commit invalidation seam, with quota admission explicitly deferred. No production SQL, COM activation, registration, or external-fetch service was used.

Code/test commit `cc53c77eb` completes authenticated `WhiteListAddresses.Clear()` parity. Legacy `InterfaceWhiteListAddresses::Clear` (`hmailserver/source/Server/COM/InterfaceWhiteListAddresses.cpp:42-61`) requires an attached collection and delegates to `Collection::DeleteAll`; `PersistentWhiteListAddress::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentWhiteListAddress.cpp:27-34`) deletes each `hm_whitelist` row. The .NET implementation preserves whitelist COM identity/direct activation denial, uses a scoped parameterized whole-collection delete, empties the owner snapshot only after store success, and maps failure to `E_FAIL` while retaining the prior snapshot. Focused whitelist/SQL coverage is `27/27`; full Net10 is `1580 passed, 0 failed, 4 opt-in skips`. SMTP evaluator/cache invalidation, sibling-facade freshness, live reconfiguration, and broader Admin mutation remain open. Next slice: approved disposable SQL identity/readback plus injected rollback evidence; if unavailable, continue IMAP account-size/cache lifecycle parity, then isolated FetchAccounts SQL/UID evidence. No production SQL, COM activation, or installed registration was used.

Code/test commit `cd91d276a` completes authenticated whitelist deletion parity. Legacy `InterfaceWhiteListAddresses::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceWhiteListAddresses.cpp:109-124`) delegates to the owning `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`); `InterfaceWhiteListAddress::Delete` (`hmailserver/source/Server/COM/InterfaceWhiteListAddress.cpp:33-54`) rechecks server-admin authorization and routes attached items through that owner, while `PersistentWhiteListAddress::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentWhiteListAddress.cpp:27-34`) deletes `hm_whitelist` by `whiteid`. The .NET path preserves installed whitelist COM identity/direct activation denial, uses parameterized identity deletion, removes only after a successful store result, maps store failure to `E_FAIL`, and treats unknown/foreign/stale IDs as owner-scoped no-ops. Focused whitelist/SQL coverage is `24/24`; full Net10 is `1577 passed, 0 failed, 4 opt-in skips`. SMTP evaluator/cache invalidation, sibling-facade freshness, Clear, live reconfiguration, and broader Admin mutation remain open. Next slice: approved disposable SQL identity/readback plus injected rollback evidence; if unavailable, continue IMAP account-size/cache lifecycle parity, then isolated FetchAccounts SQL/UID evidence. No production SQL, COM activation, or installed registration was used.

Code/test commit `d79b84ae8` completes authenticated existing-row `WhiteListAddress.Save()` UPDATE parity. Legacy `InterfaceWhiteListAddress::Save` (`hmailserver/source/Server/COM/InterfaceWhiteListAddress.cpp:8-31`) persists the attached object through `PersistentWhiteListAddress::SaveObject`; the legacy persistence implementation (`hmailserver/source/Server/Common/Persistence/PersistentWhiteListAddress.cpp:57-93`) updates `hm_whitelist` by `whiteid`, writes the four IP columns plus email/description, and requests `WhiteListCache::SetNeedRefresh`. The .NET path preserves installed whitelist COM identity/direct activation denial, owner-scoped staged setters, invalid-IP retention, and post-success replacement of only the matching owner snapshot; SQL is parameterized and constrained by `WHERE whiteid = @id`. Focused whitelist/SQL coverage is `22/22`; full Net10 is `1573 passed, 0 failed, 4 opt-in skips`. SMTP evaluator/cache invalidation, sibling-facade freshness, Delete/Clear, live reconfiguration, and broader Admin mutation remain open. Next slice: approved disposable SQL identity/readback plus injected rollback evidence; if unavailable, continue IMAP account-size/cache lifecycle parity, then isolated FetchAccounts SQL/UID evidence. No production SQL, COM activation, or installed registration was used.

The preceding security follow-up code/test commit `3165b3cab` closed the retained-collection reauthentication gap: `WhiteListAddresses.Add()` now rechecks the live administrator predicate before creating a staged child, matching `InterfaceWhiteListAddresses::Add`. Its focused/full evidence was `16/16` and `1569` passed with `4` opt-in skips. Whitelist-cache/SMTP evaluator integration and sibling-facade snapshot freshness remain open and are not silently claimed by the persistence slice.

Code/test commit `5ff2f4ab7` completes authenticated `AntiSpam.WhiteListAddresses.Add()` plus new-item `WhiteListAddress.Save()` INSERT parity. Legacy `InterfaceWhiteListAddresses::Add` (`hmailserver/source/Server/COM/InterfaceWhiteListAddresses.cpp:186-215`) creates an unsaved item scoped to the owning collection; `InterfaceWhiteListAddress::Save` (`hmailserver/source/Server/COM/InterfaceWhiteListAddress.cpp:8-31`) requires server-admin authentication, invokes `PersistentWhiteListAddress::SaveObject`, and adds the item to the parent only after the persistence call; `PersistentWhiteListAddress::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentWhiteListAddress.cpp:57-93`) inserts `whiteloweripaddress1/2`, `whiteupperipaddress1/2`, `whiteemailaddress`, and `whitedescription`, assigns `whiteid`, and requests `WhiteListCache::SetNeedRefresh`. The .NET implementation preserves whitelist COM identity/direct activation boundaries, rechecks retained-facade administrator authentication, preserves failed/invalid IP setter values, uses parameterized `OUTPUT INSERTED.whiteid`, and publishes only the new snapshot after successful insert. Existing-row Save/Delete/Clear, SMTP trust/cache/live reconfiguration, and other collections remain fenced. Focused whitelist/SQL tests pass `16/16`; full Net10 passes `1569` with `4` opt-in skips. Next slice: approved disposable SQL identity/readback plus injected rollback evidence for IMAP deletion/Rules/ACL; then whitelist existing-row Save parity and IMAP account-size/cache lifecycle evidence. The SQL opt-in gate remains unavailable; no production SQL/COM activation was used.

Code/test commit `1a98e88a8` completes the bounded legacy IMAP `SUBSCRIBE`/`UNSUBSCRIBE` protocol slice. `IMAPCommandSUBSCRIBE::ExecuteCommand` (`hmailserver/source/Server/IMAP/IMAPCommandSubscribe.cpp:23-67`) authenticates, uses the first argument, silently accepts the public-folder root, requires `PermissionLookup` for other folders, and saves the subscription flag. `IMAPCommandUNSUBSCRIBE::ExecuteCommand`/`ConfirmPossibleToUnsubscribe` (`hmailserver/source/Server/IMAP/IMAPCommandUnsubscribe.cpp:23-67`) require exactly one argument, reject missing/public folders, and save private-folder unsubscription. The .NET implementation adds a narrow protocol subscription store, reuses `SqlServerImapMailboxPath` and public ACL resolution, updates only the account/folder-scoped `hm_imapfolders.folderissubscribed` row, and dispatches both commands only from an authenticated session. Focused subscription/session/store coverage is `46/46`; full Net10 is `1564 passed, 0 failed, 4 opt-in skips`. COM identity/direct activation, SMTP, IMAP DELETE/RENAME, SQL schema, and live reconfiguration remain unchanged. Next slice: approved disposable SQL identity/readback plus injected rollback evidence for IMAP deletion/Rules/ACL; if unavailable, continue IMAP account-size/cache lifecycle parity, then isolated FetchAccounts SQL/UID evidence. Protocol DELETE, failed-file-cleanup repair/alert semantics, restore/upgrade rollback, SEC-18, and performance/release gates remain open.

The following IMAP folder deletion paragraph is historical and superseded by this update.

Code/test commit `0677320ad` completes authenticated parent-scoped IMAP folder deletion COM/store wiring. Legacy `InterfaceIMAPFolder::Delete` (`hmailserver/source/Server/COM/InterfaceIMAPFolder.cpp:249-262`) delegates attached items to the owning collection, `InterfaceIMAPFolders::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceIMAPFolders.cpp:145-161`) returns `DISP_E_BADINDEX` for absent IDs, `Collection<T,P>::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`) owns membership removal, and `PersistentIMAPFolder::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentIMAPFolder.cpp:48-97`) recursively removes children/messages/ACLs while preserving the root Inbox row. The .NET path wires both item and collection deletion to the existing owner/account/parent-scoped transactional SQL store, removes only the selected subtree from the shared snapshot, invokes the configured message-file cleanup runtime after successful SQL, preserves direct activation denial, and carries live Application authentication through Domains -> Accounts -> IMAPFolders so retained deletion is denied after failed reauthentication. Installed IIDs/CLSIDs/ProgIDs/DISPIDs/vtable shape remain unchanged. Focused IMAPFolders/Application coverage passes `27/27`; the deletion/store/helper set passes `33/33`; full Net10 has `1556` passes, `0` failures, and `4` opt-in skips. Next slice: approved disposable SQL identity/readback and injected rollback evidence for IMAP deletion/Rules/ACL; if that gate remains unavailable, continue with IMAP deletion cache/account-size lifecycle parity and isolated FetchAccounts SQL/UID evidence. Protocol `DELETE`, account-size cache, live cache notifications, file-cleanup repair/alert semantics, real COM activation, restore/upgrade rollback, SEC-18, and performance gates remain open.

The following RuleActions Add/Save paragraph is historical and superseded by this update.

Code/test commit `ce80fad48` completes authenticated `RuleActions.Add()` and new-item `RuleAction.Save()` INSERT parity. Legacy `InterfaceRuleActions::Add` (`hmailserver/source/Server/COM/InterfaceRuleActions.cpp:93-119`) returns an ID-zero child scoped to the owning rule; `InterfaceRuleAction::Save` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:30-72`) enforces the ScriptFunction administrator guard, assigns sort order, persists, receives the generated ID, and attaches only after success; `PersistentRuleAction::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleAction.cpp:65-116`) inserts all `hm_rule_actions` fields. The .NET implementation preserves the installed COM identity/vtable/DISPID shape and direct activation denial, stages new-item fields, binds INSERT to the immutable owning rule, returns `OUTPUT INSERTED.actionid`, retains failed items for retry, and publishes the parent snapshot only after success. Focused RuleActions/store coverage is `56/56`; full Net10 is `1550 passed, 4 skipped`. Disposable SQL identity/readback, real COM activation, retained-facade reauthentication, concurrent sort allocation, SMTP rule execution, rollback, and delivery evidence remain open. Next slice: disposable SQL RuleActions/ACL/deletion readback evidence; then IMAP folder deletion COM/store wiring and FetchAccounts SQL/UID cleanup evidence.

Code/test commit `808692ef5` follows `d5b25e701` and completes authenticated existing-row public-folder ACL `Save()` UPDATE parity. Legacy `InterfaceIMAPFolderPermission` setters/bit setters and `Save` (`hmailserver/source/Server/COM/InterfaceIMAPFolderPermission.cpp:75-208,227-242`) stage the complete row, while `PersistentACLPermission::Validate/SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentACLPermission.cpp:76-164`) validates the holder and updates all `hm_acl` columns by identity. The .NET path propagates an owner-captured update callback through index, DBID, and synthetic-name wrappers, validates User/Group/Anyone holder combinations and permission flags, uses parameterized SQL constrained by `aclid`, `aclsharefolderid`, and public-folder `folderaccountid = 0`, maps false/exception to `E_FAIL`, and replaces snapshots only after success. Existing insert behavior, direct activation/public-folder scope, and installed COM identity/vtable/DISPID shape remain unchanged. Focused IMAP permission/store coverage is `36/36`; full Net10 passes `1546` with `4` opt-in skips. Live SQL, COM activation, protocol ACL, duplicate-conflict, rollback fault injection, and round-trip evidence remain unproven. Next slice: approved disposable SQL insert/update/readback evidence for ACL Add/Save plus Rules/public-folder deletion, then the hardened IMAP folder and FetchAccounts SQL fixtures.

Code/test commit `cd1d2c210` follows `518617180` and completes authenticated public-folder ACL `Add()`, staged holder/value/permission flags, and new-row `Save()` INSERT parity. Legacy `InterfaceIMAPFolderPermissions::Add` (`hmailserver/source/Server/COM/InterfaceIMAPFolderPermissions.cpp:99-125`) creates an ID-0 child bound to the owning folder; `InterfaceIMAPFolderPermission` setters/bit setters and `Save` (`hmailserver/source/Server/COM/InterfaceIMAPFolderPermission.cpp:75-208,227-242`) stage fields and publish to the parent only after `PersistentACLPermission::SaveObject` succeeds. `PersistentACLPermission::Validate/SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentACLPermission.cpp:76-164`) defines User/Group/Anyone holder invariants and identity-backed `hm_acl` INSERT columns. The .NET path preserves installed COM identity/vtable/DISPID shape, direct activation denial, and the existing authenticated public-folder boundary; SQL enforces the owning folder and `folderaccountid = 0`, uses typed `SCOPE_IDENTITY()`, validates returned owner/fields, and appends only after success. Existing-row UPDATE remains explicitly fenced as `E_NOTIMPL`. Focused IMAP permission/store coverage is `31/31`; full Net10 passes `1541` with `4` opt-in skips. Live SQL, COM activation, protocol ACL, rollback fault injection, and existing-row update remain unproven. Next slice: approved disposable SQL insert/readback evidence for ACL Add/Save plus Rules/public-folder deletion, then the hardened IMAP folder and FetchAccounts SQL fixtures.

Code/test commit `4429bd0b1` follows `f53ec05e4` and completes authenticated public-folder `IMAPFolderPermission.Delete()` parity. Legacy `InterfaceIMAPFolderPermission::Delete` (`hmailserver/source/Server/COM/InterfaceIMAPFolderPermission.cpp:244-262`) routes the attached item's ID through `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`), returning public `S_OK` for stale/repeated or foreign items. The .NET `IMAPFolderPermissions` item factories pass the owning folder ID and callback; successful deletion alone removes the owning snapshot, callback failures map to `E_FAIL`, and a false result retains the snapshot as a fail-closed policy. Installed COM identity/vtable/DISPID shape, direct activation denial, and public-folder scope remain unchanged. Focused IMAP permission/store coverage is `25/25`; full Net10 passes `1535` with `4` opt-in skips. Live SQL, COM activation, rollback fault injection, and protocol ACL acceptance remain unproven. Next slice: approved disposable SQL evidence for Rules and public-folder permission deletion, then the hardened IMAP folder and FetchAccounts SQL fixtures.

Code/test commit `bf8c43037` follows `158774b65` and completes authenticated public-folder `IMAPFolderPermissions.Delete(index)` parity. Legacy `InterfaceIMAPFolderPermissions::Delete` (`hmailserver/source/Server/COM/InterfaceIMAPFolderPermissions.cpp:40-53`) forwards the zero-based index to `ACLPermissions::DeleteItem` and returns `S_OK`; negative and out-of-range values fail the unsigned bounds check and are no-ops, while valid entries use the attached folder collection. The .NET path reuses the existing owner/public-folder callback, preserves direct activation and public-folder boundaries, maps callback failures to `E_FAIL`, removes snapshots only after success, and leaves item Delete/Add/Save/setters, protocol ACL behavior, and IMAP folder deletion fenced. Focused IMAP permission/store coverage is `21/21`; full Net10 passes `1531` with `4` opt-in skips. No live SQL, rollback fault injection, or protocol ACL acceptance ran. Next slice: approved disposable SQL evidence for Rules and public-folder permission deletion, then the hardened IMAP folder and FetchAccounts SQL fixtures.

Code/test commit `158774b65` follows `28bdc36d4` and completes authenticated public-folder `IMAPFolderPermissions.DeleteByDBID` parity. Legacy `InterfaceIMAPFolderPermissions::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceIMAPFolderPermissions.cpp:166-182`) calls the attached `ACLPermissions::DeleteItemByDBID` and returns `S_OK` even for unknown IDs; `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`) scopes the lookup to the attached folder, and `PersistentACLPermission::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentACLPermission.cpp:28-36`) deletes the selected `hm_acl` row. The .NET path preserves the public-folder-only `IMAPFolder.Permissions` boundary, installed ACL COM identity/DISPIDs, and direct activation denial. Its SQL delete requires `aclid`, `aclsharefolderid`, and `hm_imapfolders.folderaccountid = 0`; it removes the snapshot only after one affected row, maps store failure to `E_FAIL`, and treats unknown/foreign/repeated IDs as no-ops. Focused IMAP permission/store coverage is `17/17`; full Net10 passes `1527` with `4` opt-in skips. No live SQL, rollback fault injection, or protocol ACL acceptance ran. Next slice: run approved disposable SQL evidence for Rules and public-folder permission deletion; then the hardened IMAP folder deletion and FetchAccounts SQL fixtures.

Code/test commit `28bdc36d4` follows `1a501b0cd` and completes the bounded authenticated Rules parent-delete slice. Legacy `InterfaceRules::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceRules.cpp:126-135`) and `InterfaceRule::Delete` (`hmailserver/source/Server/COM/InterfaceRule.cpp:298-310`) route through `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`), which only selects an object already in the owning collection; the COM wrappers return `S_OK` even for unknown IDs. `PersistentRule::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentRule.cpp:175-188`) removes the rule and its `hm_rule_actions`/`hm_rule_criterias` children, with no schema cascade (`hmailserver/source/Server/DBScripts/CreateTablesMSSQL.sql:471-523`). The .NET path preserves installed Rules/Rule IIDs, CLSIDs, ProgIDs, DISPIDs, direct activation denial, and authenticated Account/Application access. It performs one owner-scoped SQL transaction with `UPDLOCK, HOLDLOCK`, deletes the dependent rows and parent, removes shared snapshots only after commit, maps failures to `E_FAIL`, and keeps unknown/foreign/repeated/stale objects as no-ops. Focused Rules COM/store coverage is `15/15`; the full suite passes `1523` with `4` opt-in skips. No live SQL or rollback fault-injection evidence ran. Next slice: run a disposable local Rules deletion/rollback harness when the approved connection and isolated-create gate are supplied; continue with the hardened IMAP and FetchAccounts SQL fixtures, then select the next authenticated COM/Admin mutation.

Code/test commit `310086e66` follows `8167856b4` and completes authenticated parent-account-scoped `FetchAccounts.Delete(index)` and `DeleteByDBID` parity. Legacy `InterfaceFetchAccounts::Delete`/`DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceFetchAccounts.cpp:88-116`) invoke `Collection::DeleteItem`/`DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-231`) and return `S_OK` for unknown IDs/indexes; `PersistentFetchAccount::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentFetchAccount.cpp:116-140`) deletes the fetch row and UID children. The .NET implementation reuses the owner-scoped store callback, removes snapshots only after success, maps store failures to `E_FAIL`, and preserves direct activation denial and installed COM identity. Focused FetchAccounts COM coverage is `16/16`; the full suite passes `1517` with `4` opt-in skips. Live SQL/UID cleanup, rollback, broader FetchAccount mutation, and other Admin collection gaps remain open. The SQL fixtures remain blocked by the absent approved disposable connection and isolated-create opt-in. Next slice: run the IMAP and FetchAccounts SQL fixtures only after that gate is supplied.

The authenticated existing-row `IMAPFolder.Subscribed` staging slice is complete in code/test commit `8e3bf68d8`, following pushed documentation commit `7b07d7d35`. Legacy `InterfaceIMAPFolder::put_Subscribed` (`hmailserver/source/Server/COM/InterfaceIMAPFolder.cpp:144-159`) changes the attached boolean in memory; the existing `PersistentIMAPFolder::SaveObject` update persists it with the account, parent, and name fields. The .NET path keeps snapshot-only facades read-only, stages the value only through shared account state, and composes it into the existing owner-scoped update. Focused IMAPFolders/SQL coverage passes `19/19`; the full suite passes `1506` with `3` opt-in skips. Live SQL, rollback, concurrency, protocol/cache notification, deletion, and ACL mutation remain open. Next slice: authenticated parent-scoped existing-folder deletion parity; restore SQL/round-trip remains environment-blocked.

The authenticated existing-row `IMAPFolder.Name` staging and owning `Save` update slice is complete in code/test commit `3f64cd731`, following pushed documentation commit `d0e343106`. Legacy `InterfaceIMAPFolder::put_Name`/`Save` (`hmailserver/source/Server/COM/InterfaceIMAPFolder.cpp:47-67,92-124`) stages Modified UTF-7 and persists the existing row through `PersistentIMAPFolder::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentIMAPFolder.cpp:100-151`), which updates only account, parent, name, and subscribed fields. The .NET path adds an owner/parent-scoped parameterized update, preserves snapshot-only read facades and direct activation denial, and replaces shared account state only after a successful update. Focused IMAPFolders/SQL coverage passes `18/18`; the full suite passes `1505` with `3` opt-in skips. Live SQL, rollback after post-update snapshot failure, concurrency, protocol/cache notification, `Subscribed`, delete, and ACL mutation remain open. Next slice: authenticated existing-row `IMAPFolder.Subscribed` setter through the owning `Save` path; restore SQL/round-trip remains environment-blocked.

The authenticated account-owned `IMAPFolders.Add` insert slice is complete in code/test commit `e073b6ba7`, following pushed documentation commit `33095c5a1`. Legacy `InterfaceIMAPFolders::Add` (`hmailserver/source/Server/COM/InterfaceIMAPFolders.cpp:165-209`) Modified-UTF7-encodes the name, checks the owning collection for duplicates, creates the folder with account/parent scope, auto-subscribes public folders, persists through `PersistentIMAPFolder::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentIMAPFolder.cpp:100-151`), and appends the saved object to that collection. The .NET path adds a mutation-store boundary for `hm_imapfolders`, validates returned owner scope before snapshot append, and preserves direct activation denial and installed IIDs/DISPIDs. Focused IMAPFolders/SQL coverage passes `15/15`; the full suite has `1500` passes, `2` unrelated scanner temporary-file cleanup failures, and `3` opt-in skips. SQL integration, malformed-store rollback, concurrency, protocol/cache notifications, `IMAPFolder.Save`, setters, delete, and ACL mutation remain open. Next slice: authenticated existing-row `IMAPFolder.Name` setter plus owning `Save` update parity; restore SQL/round-trip remains environment-blocked.

The execution-time restore message/file correspondence freshness slice is complete in code/test commit `e38372a80`, following pushed documentation commit `cf08d1733`. `BackupRestoreContainmentPreflight.RevalidateAsync` performs fresh integrity inspection through `BackupRestoreIntegrityRuntime.InspectAsync` before rerunning containment checks, requires fresh `MessageFilesValidated` evidence for message-bearing restores, and fails closed for deleted raw files or a changed compressed message graph at the same archive path. Legacy `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`) performs no such check. Focused restore integrity/preflight coverage passes `100/100`; the full suite has `1498` passes, `2` unrelated scanner temporary-file cleanup failures, and `3` opt-in skips. This is an unwired read-only boundary: it does not make revalidation atomic with extraction or target replacement, does not validate the post-extraction tree, and does not implement SQL mutation, rollback, or round-trip equivalence. Next slice: isolated disposable SQL restore transaction harness/wiring when approved; the environment remains blocked by the absent approved integration connection and isolated-create opt-in.

The read-only restore message/file correspondence slice is complete in code/test commit `77e6ad723`, following `cdd5daedd`. Legacy `Message::XMLStore`/`XMLLoad` (`hmailserver/source/Server/Common/BO/Message.cpp:200-232`) stores only the message basename, while `PersistentMessage::GetFileName` (`hmailserver/source/Server/Common/Persistence/PersistentMessage.cpp:1120-1187`) derives private-account files under `DataDirectory/<domain>/<local-part>/<Filename[1..2]>/<Filename>`. The .NET integrity runtime now applies the equivalent `DataBackup` path for private-account messages, rejects rooted/separator/traversal/device/ADS-style names and unsafe owner segments, requires referenced files for compressed/raw non-DB-only payloads, rejects raw reparse-point components, and allows unreferenced files plus DB-only metadata without physical files. Focused restore integrity coverage passes `89/89`; the full suite has `1496` passes, `2` unrelated scanner temporary-file cleanup failures, and `3` opt-in skips; excluding those scanner classes, `1491` tests passed with `3` skips. This is read-only archive/source inspection: execution-time revalidation, SQL mutation, data-directory replacement, rollback, and round-trip equivalence remain open. Next slice: execution-time message/file correspondence revalidation contract; SQL restore transaction wiring remains blocked by the absent approved integration connection and isolated-create opt-in.

The offline SEARCH/SORT benchmark slice is complete in code/test commit `a829d308f`, following pushed commit `d7d5cb6c4`. Legacy `IMAPSearchParser::ParseCommand` (`hmailserver/source/Server/IMAP/IMAPSearchParser.cpp:118-195`) parses SEARCH/SORT input, `IMAPSortParser::Parse` (`IMAPSortParser.cpp:24-52`) preserves sort/reverse order, and `IMAPSort::Sort` (`IMAPSort.cpp:108-232,265-326`) sorts the selected field and reverses without an explicit UID tie-breaker. The current SQL plan (`hmailserver/source/Server.Net10/src/HMailServer.Search.SqlServer/SqlServerImapSortPlanner.cs:23-126`) emits requested criteria plus `m.messageuid ASC`; the new offline runner measures that deterministic contract and explicitly does not claim SQL FTS, live IMAP, legacy performance equivalence, or release acceptance. Focused benchmark coverage passes `2/2`; the 100k run produced `9,091` matches with p50 `7.125 ms`, p95 `7.720 ms`, and p99 `7.774 ms`. The full suite had `1484` passes, `2` unrelated scanner temporary-file cleanup failures, and `3` opt-in skips; excluding those scanner classes, `1479` tests passed with `3` skips. Next slice: read-only restore semantic validation for the remaining message/file graph. The approved disposable SQL transaction harness and backup/restore round-trip remain blocked because the required integration connection string and isolated-create opt-in are absent; restore mutation remains `E_NOTIMPL`, and SQL/data-directory rollback, SEC-18, broader COM/Admin parity, and soak/release gates remain open.

## Consolidated Security Review Triage (2026-06-28)

The 2026-06-13 review of commit `1909af082da83d608c2cffb98f6f33caaff4a65d` and the maintainer-supplied 2026-06-27 compiled inventory were merged and exact duplicates removed. The newer inventory adds no unique finding beyond the 21 records already tracked below. Status and severity are classified from the current source tree rather than copied from report labels without verification.

| ID | Severity | Finding | Current HEAD status / next action |
| --- | --- | --- | --- |
| SEC-01 | Critical | VBScript injection in `OnClientValidatePassword` | Disputed/not reproduced after 2026-06-28 revalidation: VBScript quote doubling keeps the reported `x" & ... & "` expression shape inside the string literal. Executable Windows Script Host coverage proves the payload reaches the handler as data; matching legacy regression coverage keeps this boundary monitored. |
| SEC-02 | High | JScript injection in `OnClientValidatePassword` | Fixed by `4dd984156`: backslashes are escaped before apostrophes and line separators are neutralized; regression coverage added. |
| SEC-03 | High | VBScript injection in `OnDeliveryFailed` | Disputed/not reproduced: the current legacy path removes quotes and the .NET runner emits a safely escaped literal. Keep under regression coverage. |
| SEC-04 | High | Custom antivirus command-template injection | Hardened by `0e3e90d61`: the attacker-influenced message file path is now inserted as a quoted/escaped Windows command-line argument, including legacy quoted `%FILE%` templates. Remaining migration debt: replace the server-admin command template with structured executable/argument configuration if the custom scanner feature is carried forward. |
| SEC-05 | High | Rule `ScriptFunction` code injection and COM policy bypass | Fixed by `4dd984156`: runtime identifiers are restricted and COM creation/edit/save requires server-admin authorization. |
| SEC-06 | High | Empty administrator password grants unauthenticated COM ServerAdmin | Fixed by `4dd984156`: empty administrator hashes now fail closed in legacy and .NET 10; constructor-time anonymous authentication was removed. |
| SEC-07 | High | Unauthenticated SMTP `ETRN` releases queued mail | Fixed by `4dd984156`: legacy `ETRN` now requires an authenticated SMTP session. .NET 10 does not implement `ETRN`. |
| SEC-08 | High | ClamAV INSTREAM framing corruption and silent bypass | Fixed earlier by `d8942bc12`; current legacy and .NET clients send raw four-byte big-endian chunk lengths. |
| SEC-09 | Medium | JScript injection in `OnDeliveryFailed` | Fixed by `4dd984156` through the shared JScript literal escaper and regression coverage. |
| SEC-10 | Medium | JScript injection in `OnExternalAccountDownload` via POP3 UID | Fixed by `4dd984156` through the shared JScript literal escaper and regression coverage. |
| SEC-11 | Medium | COM rules/forward authorization-policy bypass | Open P1: audit object ownership and authorization at every COM mutation boundary and add policy tests. Authenticated existing-row `RuleCriteria.HeaderField` setter/save parity is complete in `c8d69c9b8`; `RuleCriteria.MatchValue` setter/save parity is complete in `d95ce9c69`; `RuleCriteria.UsePredefined` setter/save parity is complete in `a4ff728c0`; `RuleCriteria.PredefinedField` setter/save parity is complete in `fabc7e03a`; `RuleCriteria.MatchType` setter/save parity is complete in `0d9e43b14`; the owner-scoped RuleCriteria save contract is complete in `edf97aeaa`; `RuleCriteria.RuleID` setter/save parity is complete in `66e72f39c`; authenticated existing-row `RuleAction.RuleID` ownership/save parity is complete in `9680640a5`; RuleAction parent-snapshot visibility within an owning collection is complete in `dc2fe2118`; per-Rules-generation repeated `Rule.Actions` adapter visibility is complete in `493848279`; per-account repeated `Account.Rules` adapter visibility is complete in `bb4142b99`; per-account repeated `Account.Messages` adapter visibility is complete in `0c2ee1226`; Account.Messages SQL projection parity is complete in `debc93dac`; authenticated per-account `Account.IMAPFolders` visibility and broader criteria/action/rule mutation remain open. |
| SEC-12 | Medium | COM `ValidatePassword` bypasses auto-ban/rate limiting | Open P2 with the authorization fence closed in `aab2e307e`, the non-COM caller-aware boundary implemented in `acd52d43a`, injected protocol propagation covered in `10111a50f`, shared protocol registration smoke coverage in `0da18ad53`, hosted-service descriptor coverage in `ccaa3f7ed`, production host composition resolution in `f1718658a`, real loopback listener propagation in `ac3798455`, the bootstrap barrier in `a6e150134`, and listener startup coordination in `64424782b`: legacy `InterfaceAccount::ValidatePassword` (DISPID 22) validates an attached account directly through `PasswordValidator::ValidatePassword`, while detached objects return `E_ACCESSDENIED`; protocol `AccountLogon::Logon` carries the client-aware `hm_logon_failures`/`hm_securityranges` boundary that COM lacks. .NET 10 `ClientAwareAuthenticationService` carries explicit client IP plus `Imap`/`Pop3`/`Smtp` caller kind through `IImapAccountAuthenticator` and `IAutoBanLogonFailureRecorder`, preserving failure/disconnect behavior. `AddCallerAwareProtocolServices`, `AddProductionHostedServices`, and `Host.Build()` are called by production `HMailServer.Service.Program`; `ServerReadinessSignal` now separates bootstrap from final readiness, and `ServerStartupCoordinator` waits for every enabled listener `Started` task before `ComLocalServerHostedService` reaches the existing class-resumption path. `SqlServerAutoBanLogonFailureRecorder` now protects the per-IP count transition with SQL Server `UPDLOCK, HOLDLOCK`; the guarded concurrent fixture in `d2fe76e14` proves the intended three-failure transition and single deny range when run against an approved disposable local instance. Focused readiness/listener/composition coverage remains `24/24`; focused auth/SQL coverage passes `9` with two explicit inconclusives; full Net10 passes `1225` with three opt-in tests skipped. Direct `Account.ValidatePassword` remains `E_ACCESSDENIED` for detached objects and authenticated SQL-backed snapshots remain explicit `E_NOTIMPL`. Account IID `{E5EDC050-0899-4A3B-BF4C-420212FC3895}`, CLSID `{369BE902-9F27-4722-A29F-3059E4D7021D}`, ProgID `hMailServer.Account.1`, and vtable/DISPID shape remain unchanged. Legacy `ServiceMain` resumes COM only after `InitializeApplication` and `StartServers()`; the .NET barrier covers bootstrap checks and enabled listener bind completion, but not delivery/external-fetch worker readiness or a real successful host/COM lifecycle. Next slice: audit authenticated `Settings -> SecurityRanges` ownership and legacy `InterfaceSecurityRanges::Add`/`InterfaceSecurityRange::Save` insert parity; keep `SetDefault`, deletion, live policy reconfiguration, SMTP trust, and the COM password method out of scope. |
| SEC-13 | Medium | Predictable WebAdmin CSRF fallback | Fixed by `4dd984156`: use `random_bytes`, require a strong OpenSSL fallback, and compare tokens with `hash_equals`. |
| SEC-14 | Medium | WebAdmin state changes over GET / token exposure in URLs | Partially hardened: AV/SpamAssassin AJAX test actions, external-account add/edit/deletion/download-now, performance settings/cache-clear, backup settings/start, auto-ban settings, greylisting, logging, scripting, diagnostics `performTests`, status `control`, TLS `save`, POP3 `save`, IMAP `save`, SMTP `save`, SMTP AntiVirus `save`, SMTP AntiSpam `save`, Mirror `save`, the DomainAliases add/delete handler, the TCP/IP port handler, the SSL certificate, SURBL-server, DNS-blacklist, whitelist-address, distribution-list, distribution-list-recipient, alias, incoming-relay, server-message, route-address, route, blocked-attachment, security-range, account, rule, and domain handlers now use POST bodies or POST-only handlers so CSRF tokens and action parameters are not placed in mutation URLs. The external-account edit path also clears a retained password when its authority tuple changes and the submitted password is blank. The historical `background_iphome_save.php` endpoint is orphaned because `hm_iphomes` was dropped during the 5.0 upgrade and no current C++/IDL/.NET/form surface exists. Remaining P1: audit live less-visible legacy mutations; the authenticated per-account `Account.Messages` visibility and SQL projection slices are complete in `0c2ee1226` and `debc93dac`; the next COM/Admin slice is authenticated per-account `Account.IMAPFolders` cached snapshot and shared folder-adapter visibility. |
| SEC-15 | Medium | Antivirus test endpoint SSRF | Hardened by `7e98a4178` and `f586332cf`: the WebAdmin virus-test endpoint keeps server-admin + CSRF checks, requires POST-only target inputs, resolves the hostname once to an IPv4 literal verified by legacy `Utilities::IsLocalHost`, validates a strict decimal `1..65535` port, and rejects invalid targets before COM with HTTP 400/body `0`. Valid local scanner deployments remain supported; ClamWin/custom scanner process paths and normal SMTP/external-fetch scanning remain unchanged. |
| SEC-16 | Medium | SpamAssassin test endpoint SSRF | Hardened by `7e98a4178` and `f586332cf`: the WebAdmin SpamAssassin test endpoint keeps server-admin + CSRF checks, requires POST-only target inputs, resolves the hostname once to an IPv4 literal verified by legacy `Utilities::IsLocalHost`, validates a strict decimal `1..65535` port, and rejects invalid targets before COM with HTTP 400/body `0`. Valid local scanner deployments remain supported; normal SpamAssassin message scanning and SMTP behavior remain unchanged. |
| SEC-17 | Medium | WebAdmin session fixation | Fixed by `4dd984156`: successful login regenerates the session ID and rotates the CSRF token. |
| SEC-18 | Medium | Plaintext mailbox password in PHP session | Open P1: replace cleartext session storage with a safer server-side reauthentication/session design. Legacy C++ has no broker or preflight equivalent; its existing Application registration remains unchanged. Canonical evidence-boundary hardening is complete in code/test commit `6d40f1b6d`, after final-validation commit `16e8b431f`: collector and attester accept only the installed Application AppID `{5EDEC473-39E0-43F6-A234-1947071721C8}`, report paths must remain under `artifacts/sec18-staging`, existing/reparse-point paths are rejected, and exclusive `CreateNew` output is used. The attester emits 19 checks across 14 source hashes. Focused collector/attester/registry-binary/graph tests pass and full Net10 passes `1198` with one opt-in native-registry test skipped. The prior `staging-inventory-20260726-live-bound.json` and the new canonical-output-gate rerun are not approval evidence: the stale caller token correctly yields `Incomplete`/exit `2` with `TimestampFresh=false`; no fresh authorized probe has been run in this slice. The post-host-read timestamp is not a trusted final-state seal and operator-supplied correlation equality is not a collector-issued challenge. Bounded evidence remains YELLOW and permanent broker registration/implementation remains RED. `ReadyForBrokerRegistration=false`: permanent broker-only AppID authorization/method enforcement is not approved. Evidence: `artifacts/sec18-staging/staging-inventory-20260726-canonical-output-gate.json`, focused fixtures, and prior graph/cleanup snapshots. Next slice: rerun the isolated authorized PHP/FastCGI and non-pool matrix with one shared invocation ID; then implement collector-issued challenge/final-state sealing, externally pin/sign the full manifest, and obtain fresh independent review. Permanent registration, DCOM ACL changes, PHP cutover, and existing Application identity changes remain blocked. Legacy identity evidence is anchored to `InterfaceApplication.rgs`, `hMailServer.rgs`, `hMailServer.idl`, `hMailServer.cpp`, and `hMailServer.rc`; external AD accounts fail closed until a current verifier source is separately reviewed. |
| SEC-19 | Medium | IMAP `RENAME` omits destination-parent Create ACL | Fixed by `862c620a0`: legacy public-folder `RENAME` now requires source Delete plus destination-parent Create permission, with focused deny/allow regression coverage. .NET 10 `RENAME` remains unimplemented. |
| SEC-20 | Historical high | External-fetch COM SSRF | Partially hardened: .NET 10 external fetch now resolves once, evaluates the complete answer set (including mapped IPv6, private, CGNAT, link-local, ULA, reserved, transition, and metadata ranges), pins the first resolver-order numeric endpoint, preserves the original TLS hostname, logs credential-free policy decisions, and supports audit-only rollout plus explicit CIDR allow-listing for private/loopback destinations. Enforcement defaults off to preserve configured local POP3 compatibility. The .NET external-fetch client now applies a configurable 900-second default operation deadline, preserves caller cancellation, enforces the legacy 250,000-byte CRLF control-line budget, and bounds best-effort QUIT cleanup to five seconds. The registered legacy `InterfaceFetchAccount` constructor now leaves direct activations unattached and access-denied while preserving parent-owned collection attachment. Remaining P1: live DNS/TLS integration coverage and adaptive high-load timeout behavior; the .NET rewrite direct-activation boundary is exhaustively denied and .NET COM fetch mutation/download remains `E_NOTIMPL`. |
| SEC-21 | Historical medium | Static Blowfish key / reversible stored passwords | Open migration debt: introduce versioned secret encryption and migration while retaining legacy decryption only for backward reads. |

## Current Status

Authoritative update (2026-08-01, `afd0de0da`): backup-side encrypted fetch-account `Password` and nested `FetchAccountUIDs`/`UID` serialization are complete. Legacy `FetchAccount::XMLStore` (`hmailserver/source/Server/Common/BO/FetchAccount.cpp:55-79`) writes Blowfish ciphertext immediately after `Username`; `FetchAccountUIDs::Refresh` (`hmailserver/source/Server/Common/BO/FetchAccountUIDs.cpp:29-59`) filters by `uidfaid` without an `ORDER BY`, and `FetchAccountUID::XMLStore` (`hmailserver/source/Server/Common/BO/FetchAccountUID.cpp:41-58`) emits `UID` then `Date`. The .NET path uses a dedicated backup-only SQL projection, preserves raw ciphertext and reader order, omits empty UID containers, and leaves ordinary fetch administration, COM password behavior, external-fetch runtime, restore, and mutation unchanged. Focused backup/fetch/COM/security coverage passes `59/59`; full Net10 passes `1353` with `3` opt-in tests skipped. The next bounded slice is backup-side `Rules` child serialization; keep rule mutation/execution, Folders, message/data-directory payloads, restore, destructive SQL, event dispatch, and SEC-18 work fenced. Older current-next paragraphs below are historical context.

Authoritative update (2026-07-30, `ae97a70eb`): backup-side non-secret `FetchAccounts` scalar child serialization is complete. Legacy `Account::XMLStore` (`hmailserver/source/Server/Common/BO/Account.cpp:280-331`) invokes `FetchAccounts::XMLStore` after account attributes; `FetchAccounts::Refresh` (`hmailserver/source/Server/Common/BO/FetchAccounts.cpp:36-42`) scopes rows by account and orders by `faid ASC`; `FetchAccount::XMLStore` (`hmailserver/source/Server/Common/BO/FetchAccount.cpp:55-79`) emits `Name`, `ServerAddress`, `ServerType`, `Port`, `Username`, `Minutes`, `DaysToKeep`, `Active`, `MIMERecipientHeaders`, `ProcessMIMERecipients`, `ProcessMIMEDate`, `UseAntiSpam`, `UseAntiVirus`, `EnableRouteRecipients`, and `ConnectionSecurity` in that order. The .NET path emits those escaped attributes only for non-empty owning snapshots through the existing secret-free `IFetchAccountAdministrationStore`, with no COM, authentication, or external-fetch runtime changes. Focused backup/fetch/COM coverage passes `49/49`; full Net10 passes `1349` with `3` opt-in tests skipped. The next bounded slice is encrypted fetch `Password` plus `FetchAccountUIDs`/`UID` serialization. Keep Rules, Folders, message/data-directory payloads, restore, destructive SQL, event dispatch, and SEC-18 work fenced. Older current-next paragraphs below are historical context.

Authoritative latest update (2026-07-30): authenticated `BackupManager` archive/XML creation, raw settings-property parity, backup-side `DomainAliases`, backup-side non-secret scalar `Accounts`, backup-side normal domain `Aliases`, backup-side `DistributionLists` child serialization, and backup-side account `Password`/`PasswordEncryption` serialization are complete in `a1f1d92f4`, `59ac1b7c6`, `f15e857a8`, `ac611987c`, `3e7535d76`, `5d4981240`, and `fd30ceb33`. Legacy `PropertySet::Refresh`/`XMLStore` (`hmailserver/source/Server/Common/Application/PropertySet.cpp:31-181`), `Configuration::XMLStore` (`hmailserver/source/Server/Common/Application/Configuration.cpp:687-713`), `XMLite` escaping (`hmailserver/source/Server/Common/Util/XMLite.cpp:27-74`), `BackupExecuter::StartBackup` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:57-190`), `Domain::XMLStore` (`hmailserver/source/Server/Common/BO/Domain.cpp:104-149`), `Accounts::Refresh` (`hmailserver/source/Server/Common/BO/Accounts.cpp:34-56`), `Account::XMLStore` (`hmailserver/source/Server/Common/BO/Account.cpp:280-331`), `PersistentAccount::ReadObject` (`hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:146-191`), `Aliases::Refresh` (`hmailserver/source/Server/Common/BO/Aliases.cpp:26-32`), `Alias::XMLStore` (`hmailserver/source/Server/Common/BO/Alias.cpp:28-37`), `Time::GetCurrentDateTime` (`hmailserver/source/Server/Common/Util/Time.cpp:25-34`), `Collection<T,P>::XMLStore` (`hmailserver/source/Server/Common/BO/Collection.h:61-82`), `DistributionLists::Refresh` (`hmailserver/source/Server/Common/BO/DistributionLists.cpp:40-47`), `DistributionList::XMLStore` (`hmailserver/source/Server/Common/BO/DistributionList.cpp:31-45`), `DistributionListRecipients::Refresh` (`hmailserver/source/Server/Common/BO/DistributionListRecipients.cpp:27-34`), `DistributionListRecipient::XMLStore` (`hmailserver/source/Server/Common/BO/DistributionListRecipient.cpp:30-38`), and `DomainAlias::XMLStore` (`hmailserver/source/Server/Common/BO/DomainAlias.cpp:26-33`) remain the references. The .NET path carries one successful plan evidence object into a shell-free 7z writer, keeps ordinary account administration projections non-secret, reads `accountpassword`/`accountpwencryption` only through `IBackupAccountAdministrationStore`, preserves `LongValue`/`StringValue` and ordinal property ordering, excludes `smtprelayerpassword`, and emits per-domain `DomainAliases`, scalar `Accounts`, normal `Aliases`, `DistributionLists`, and account credentials in legacy child/attribute order while omitting empty containers. `FetchAccounts`, `Rules`, and `Folders` remain explicitly fenced; `BOMessages` still rejects before file creation. Focused backup/account credential coverage passes `44/44`; full Net10 passes `1346` with `3` opt-in tests skipped. Collection-wide `FetchAccounts.Delete`/`DeleteByDBID`, Add/Save/password/setter mutation, POP3 behavior, SEC-18 broker registration, and PHP session cutover remain fenced. The next production-gate slice is backup-side `FetchAccounts` child serialization; keep its encrypted fetch password, UIDs, nested Rules/Folders, message/data-directory payloads, restore execution, destructive SQL, and event dispatch out of scope. Older current-next paragraphs below are historical context.

Current SEC-14 reconciliation (2026-07-30): the distribution-list recipient, alias, route, SecurityRanges, account, rule, and domain handlers are no longer in the mixed-GET remainder; `9d6a8dda2`, `1dc35f169`, `8d684e638`, `97e3096c3`, `95a7e4284`, `6736e161e`, and `3d25cb0a7` cover their POST-only/POST-body CSRF boundaries. The historical `background_iphome_save.php` endpoint remains fenced as an orphan. The Account.Messages and Account.IMAPFolders visibility slices and authenticated `FetchAccount.Delete()` parity are complete in `0c2ee1226`, `debc93dac`, `893a5c768`, and `cccc3e64c`; authenticated `BackupManager` archive/XML, non-secret raw settings-property coverage, backup-side `DomainAliases`, backup-side non-secret scalar `Accounts`, backup-side normal domain `Aliases`, backup-side `DistributionLists`, and backup-side account credential child serialization are now complete in `a1f1d92f4`, `59ac1b7c6`, `f15e857a8`, `ac611987c`, `3e7535d76`, `5d4981240`, and `fd30ceb33`. The next production-gate slice is backup-side `FetchAccounts` child serialization.

## SEC-11 Backup Archive/XML Creation (2026-07-30)

- Legacy parity was confirmed in `InterfaceBackupManager::StartBackup` (`hmailserver/source/Server/COM/InterfaceBackupManager.cpp:26-40`), `BackupManager::StartBackup` (`hmailserver/source/Server/Common/Application/BackupManager.cpp:37-71`), `BackupTask::DoWork` (`hmailserver/source/Server/Common/Application/BackupTask.cpp:26-40`), `BackupExecuter::StartBackup` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:57-190`), `Configuration::XMLStore` (`hmailserver/source/Server/Common/Configuration/Configuration.cpp:687-713`), and `Compression::AddFile` (`hmailserver/source/Server/Common/Util/Compression.cpp:27-45`). The installed `IInterfaceBackupManager` identity and vtable/DISPID shape remain unchanged.
 - Code/test commit `a1f1d92f4` carries one successful read-only `BackupStartPlanEvidence` object into a shell-free 7z writer. Code/test commit `59ac1b7c6` adds a backup-only raw `hm_settings` snapshot, ordinal property ordering, legacy `LongValue`/`StringValue` output, `Domains`-before-`Properties` ordering, seeded/unknown row coverage, and explicit relayer-credential exclusion. Code/test commit `f15e857a8` adds backup-side `DomainAliases`, code/test commit `ac611987c` adds backup-side non-secret scalar `Accounts` after `DomainAliases`, code/test commit `3e7535d76` adds normal domain `Aliases` after `Accounts`, and code/test commit `5d4981240` adds backup-side `DistributionLists` after `Aliases`, all omitting empty containers. Account credentials, `FetchAccounts`, `Rules`, and `Folders` remain fenced; `BOMessages` rejects before any file creation.
- Focused backup/distribution-list coverage passes `34/34`; full Net10 passes `1344` with `3` opt-in tests skipped. The historical next bounded slice was backup-side account `Password`/`PasswordEncryption` serialization, now complete in `fd30ceb33`; the current next slice is backup-side `FetchAccounts` child serialization. Keep nested account children, message/data-directory payloads, restore execution, destructive SQL, event dispatch, COM registration, and SEC-18 broker work out of scope.

## SEC-11 Backup DistributionLists XML Serialization (2026-07-30)

- Legacy `Domain::XMLStore` (`hmailserver/source/Server/Common/BO/Domain.cpp:104-149`) writes `DistributionLists` after normal `Aliases`. `DistributionLists::Refresh` (`hmailserver/source/Server/Common/BO/DistributionLists.cpp:40-47`) orders lists by address; `DistributionList::XMLStore` (`hmailserver/source/Server/Common/BO/DistributionList.cpp:31-45`) emits `Name`, `Active`, `RequiresAuth`, `RequiresAuthAddress`, and `ListMode` in that order. `DistributionListRecipients::Refresh` (`hmailserver/source/Server/Common/BO/DistributionListRecipients.cpp:27-34`) orders recipients by address, and `DistributionListRecipient::XMLStore` (`hmailserver/source/Server/Common/BO/DistributionListRecipient.cpp:30-38`) emits the optional inner `DistributionList` container with `Recipient Name`. `Collection<T,P>::XMLStore` (`hmailserver/source/Server/Common/BO/Collection.h:61-82`) omits empty containers.
- Code/test commit `5d4981240` wires the existing `IDistributionListAdministrationStore` and `IDistributionListRecipientAdministrationStore` into the backup payload provider, loads one scoped list snapshot per selected domain and one recipient snapshot per selected list, and emits escaped `DistributionLists` after `Aliases` with no `ID` or `DomainID` attributes. Existing COM identities, authenticated Settings/Domain read boundaries, direct activation denial, SMTP list-policy behavior, mutation status, restore behavior, and SQL schema remain unchanged.
- Focused backup/distribution-list coverage passes `34/34`; full Net10 passes `1344` with `3` opt-in tests skipped. The historical next bounded backup slice was account `Password`/`PasswordEncryption` serialization, now complete in `fd30ceb33`; `FetchAccounts`, nested account children, message/data-directory payloads, restore, destructive SQL, and event dispatch remain explicit gaps. The current next slice is `FetchAccounts` child serialization.

## SEC-11 Backup Account Password/PasswordEncryption XML Serialization (2026-07-30)

- Legacy `Account::XMLStore` (`hmailserver/source/Server/Common/BO/Account.cpp:280-331`) emits `Password` and integer `PasswordEncryption` immediately after `Active`; `PersistentAccount::ReadObject` (`hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:146-191`) loads `accountpassword` and `accountpwencryption`, and the schema defines both columns in `hmailserver/source/DBScripts/CreateTablesMSSQL.sql:168-195`.
- Code/test commit `fd30ceb33` adds `AccountBackupAdministrationSnapshot` and `IBackupAccountAdministrationStore`, a dedicated SQL projection, host wiring, and backup XML emission at the legacy attribute position. Ordinary `IAccountAdministrationStore` queries remain secret-free; attached `Account.Password`, `ValidatePassword`, mutation, COM identities, direct activation denial, SMTP/POP3 behavior, restore behavior, and nested account children remain unchanged.
- Focused backup/account credential coverage passes `44/44`; full Net10 passes `1346` with `3` opt-in tests skipped. The next bounded backup slice is `FetchAccounts` child serialization; its encrypted fetch password, UID children, Rules, Folders, message/data-directory payloads, restore, destructive SQL, and event dispatch remain fenced.

### SEC-14 Route Handler Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceRoutes::LoadSettings`, `Add`, and `DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceRoutes.cpp:12-29,75-105`), `InterfaceRoute` setters, `Save`, and `Delete` (`hmailserver/source/Server/COM/InterfaceRoute.cpp:67-243,284-582`), and `PersistentRoute::SaveObject`/`DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentRoute.cpp:31-70`). The installed `IInterfaceRoute` and `IInterfaceRoutes` contracts, DISPIDs, and collection order remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1378-1426,1533-1550`; route coclasses remain at `hmailserver/source/Server/hMailServer/hMailServer.idl:3052-3094`.
- Code/test commit `8d684e638` hardens `hmailserver/source/WebAdmin/background_route_save.php`: the existing server-admin denial remains first, the handler requires `hmailRequirePostCsrfToken()`, and `action`, `routeid`, and all route fields are read from POST only. Existing `Settings -> Routes` ItemByDBID/Add/DeleteByDBID lookup, field assignments, conditional `SetRelayerAuthPassword`, Save, redirects, and `hm_route.php`/`hm_routes.php` forms remain unchanged. `WebAdminRoutePostOnlySourceTests` plus route COM/address and SQL-store coverage passes `19/19`; full Net10 passes `1280` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP validation was not run.
- The .NET `Routes` adapter and SQL administration store remain intentionally read-only for collection/item mutation; direct route activation remains `E_ACCESSDENIED`, while the authenticated Settings read boundary and installed COM identity/vtable/DISPID shape remain unchanged. SMTP routing, live reconfiguration, service/database/Data-directory state, and SEC-18 staging state did not change. Next slice: `hmailserver/source/WebAdmin/background_securityrange_save.php` POST-only/CSRF hardening.

### SEC-14 SecurityRanges Handler Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceSecurityRanges::LoadSettings`, `DeleteByDBID`, and `Add` (`hmailserver/source/Server/COM/InterfaceSecurityRanges.cpp:13-21,60-74,158-185`), `InterfaceSecurityRange::Save`, `put_LowerIP`, `put_UpperIP`, and the remaining field setters (`hmailserver/source/Server/COM/InterfaceSecurityRange.cpp:36-841`), and `PersistentSecurityRange::SaveObject`/`Validate` (`hmailserver/source/Server/Common/Persistence/PersistentSecurityRange.cpp:52-117,268-289`). Legacy Save validates and persists through `hm_securityranges`; the handler field mapping remains the reference. Installed `IInterfaceSecurityRange`/`IInterfaceSecurityRanges` IIDs, vtable order, and DISPIDs remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1232-1320`, with `Settings.SecurityRanges` at DISPID 18 (`:540`).
- Code/test commit `97e3096c3` hardens `hmailserver/source/WebAdmin/background_securityrange_save.php`: the existing server-admin denial remains first, the handler requires `hmailRequirePostCsrfToken()`, and `action`, `securityrangeid`, and all remaining range fields are read from POST only. Existing `Settings -> SecurityRanges` ItemByDBID/Add/DeleteByDBID lookup, field assignments, Save, redirects, delete flow, and both forms remain unchanged. `WebAdminSecurityRangePostOnlySourceTests` plus SecurityRanges COM and SQL-store coverage passes `30/30`; full Net10 passes `1281` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP validation was not run.
- The .NET `SecurityRanges` adapter and SQL administration store retain the existing authenticated server-administrator boundary, installed COM identity/vtable/DISPID shape, and current mutation implementation; this slice changes no COM, SQL, IP-policy, auto-ban, SMTP trust, live-reconfiguration, service/database/Data-directory, or SEC-18 behavior. Next slice: `hmailserver/source/WebAdmin/background_account_save.php` POST-only/CSRF hardening.

### SEC-14 Account Handler Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceAccounts::Add`/`DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceAccounts.cpp:42-74,202-231`), `InterfaceAccount::Save`, `put_Password`, and the account field setters (`hmailserver/source/Server/COM/InterfaceAccount.cpp:74-1048`), and `PersistentAccount::SaveObject`/`DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentAccount.cpp:55-329`). Installed `IInterfaceAccounts`/`IInterfaceAccount` IIDs, vtable order, and DISPIDs remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:794-895`; account coclasses remain at `hmailserver/source/Server/hMailServer/hMailServer.idl:2904-2919`.
- Code/test commit `95a7e4284` hardens `hmailserver/source/WebAdmin/background_account_save.php`: the handler requires `hmailRequirePostCsrfToken()` before reading `domainid`, `accountid`, `action`, or account fields, and all 26 inputs use `hmailGetPostVar()`. Existing user self-edit and session-password update behavior, domain-admin ownership, server-admin restrictions, `Settings -> Domains -> Domain -> Accounts` lookup, Add/Edit/Delete, account field mappings, Save, redirects, and both forms remain unchanged. `WebAdminAccountPostOnlySourceTests` plus account COM/store coverage passes `19/19`; full Net10 passes `1282` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP validation was not run.
- The .NET `Accounts` adapter and SQL administration store retain the existing authenticated domain-owned read boundary, direct activation denial, installed COM identity/vtable/DISPID shape, and current mutation status; this slice changes no password storage, SMTP delivery, account-rule runtime, service/database/Data-directory, or SEC-18 behavior. Rule handler hardening is recorded in `6736e161e`; the next COM/Admin slice is authenticated existing-row `RuleCriteria.MatchType` setter parity through the owning `RuleCriteria.Save()` path.

### SEC-14 Rule Handler Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceRules::Add`, `DeleteByDBID`, and `Refresh` (`hmailserver/source/Server/COM/InterfaceRules.cpp:19-143`), `InterfaceRule` field setters, `Save`, and `Delete` (`hmailserver/source/Server/COM/InterfaceRule.cpp:66-306`), `InterfaceRuleCriterias::Add`/`InterfaceRuleCriteria::Save` (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:91-122`, `hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:13-258`), `InterfaceRuleActions::Add`/`InterfaceRuleAction::Save` (`hmailserver/source/Server/COM/InterfaceRuleActions.cpp:93-124`, `hmailserver/source/Server/COM/InterfaceRuleAction.cpp:30-587`), and `PersistentRule*::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRule.cpp:73-192`, `PersistentRuleCriteria.cpp:58-116`, `PersistentRuleAction.cpp:65-141`). Installed rule interfaces and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1758-1900,3173-3215`.
- Code/test commit `6736e161e` hardens `hmailserver/source/WebAdmin/background_rule_save.php`: `GetHasRuleAccess` remains the ownership boundary, the handler requires `hmailRequirePostCsrfToken()`, and all scope, rule, criteria, and action inputs use `hmailGetPostVar()`. Existing global/account lookup, Add/Edit/Delete, criteria/action branches, non-server-admin action restrictions, field mapping, Save, redirects, and POST+CSRF forms remain unchanged. `WebAdminRulePostOnlySourceTests` plus rule COM/store coverage passes `70/70`; full Net10 passes `1283` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP validation was not run.
- The .NET rule adapters retain their current read-only/E_NOTIMPL mutation boundaries and direct activation denial; `RuleCriteria.HeaderField`, `RuleCriteria.MatchValue`, `RuleCriteria.UsePredefined`, and `RuleCriteria.PredefinedField` now stage through their authorized owning Save callbacks, while the other criteria setters remain E_NOTIMPL. No COM identity, authenticated Settings boundary, SMTP rule behavior, service/database/Data-directory state, or SEC-18 staging state changed. Next slice: authenticated existing-row `RuleCriteria.MatchType` setter parity through the owning `RuleCriteria.Save()` path.

### SEC-14 Domain Handler Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceDomains::Add`, `get_ItemByDBID`, `DeleteByDBID`, and authentication (`hmailserver/source/Server/COM/InterfaceDomains.cpp:44-64,99-219,252-269`), `InterfaceDomain` setters, `Save`, and `Delete` (`hmailserver/source/Server/COM/InterfaceDomain.cpp:56-353,480-1431`), and `PersistentDomain::DeleteObject`/`SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentDomain.cpp:46-234`). Installed `IInterfaceDomain`/`IInterfaceDomains` contracts and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:707-753,1512-1525,2900-2902,3084-3086`.
- Code/test commit `3d25cb0a7` hardens `hmailserver/source/WebAdmin/background_domain_save.php`: the existing domain-admin ownership and server-admin delete checks remain, the handler requires `hmailRequirePostCsrfToken()`, and all 30 scope/domain/DKIM/signature inputs use `hmailGetPostVar()`. Existing domain lookup/mutation/field mappings, Save, redirects, and POST+CSRF forms remain unchanged. `WebAdminDomainPostOnlySourceTests` plus domain/domain-alias COM/store coverage passes `16/16`; full Net10 passes `1284` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP validation was not run.
- The .NET `Domains`/`Domain` adapters and SQL administration stores retain their current authenticated access boundaries, read-only COM mutation status, direct activation denial, installed identities, SMTP/domain behavior, service/database/Data-directory state, and SEC-18 staging state. The next COM/Admin slice is authenticated existing-row `RuleCriteria.MatchType` setter parity through the existing owning `RuleCriteria.Save()` path.

### SEC-11 RuleCriteria HeaderField Setter Parity (2026-07-29)

- Legacy parity was confirmed in `InterfaceRuleCriteria::put_HeaderField` and `Save` (`hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:30-48,122-152`), the owning lookup/parent attachment path (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:19-38` and `hmailserver/source/Server/COM/COMCollection.h:11-38`), and `PersistentRuleCriteria::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp:65-100`). The legacy setter stages the raw BSTR with no validation or normalization; detached objects return `E_ACCESSDENIED`; Save persists `criteriaheadername` for the attached criterion.
- Installed `IInterfaceRuleCriteria`/`IInterfaceRuleCriterias` IIDs, vtable order, DISPIDs, and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1792-1837,2844-2849,3185-3200`. The .NET contracts retain the same identities, BSTR marshaling, and direct activation boundary.
- Code/test commit `c8d69c9b8` makes `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/RuleCriterias.cs` use a mutable item-local snapshot for `HeaderField`, passes the staged snapshot through the owning save delegate, and leaves other criteria setters, Add, Delete, Refresh, rule execution, and SMTP behavior unchanged. `RuleCriteriasComContractTests` and related SQL/integration coverage pass `31/31`; full Net10 passes `1284` with `3` opt-in tests skipped. PHP CLI is unavailable and no live SQL integration ran because the approved connection variable was unset.
- The next bounded COM/Admin slice is authenticated existing-row `RuleCriteria.MatchType` setter parity through the same owning `RuleCriteria.Save()` path.

- Updated 2026-07-26: SEC-12 authorization fencing is committed in `aab2e307e`. `AccountsComContractTests` proves detached `Account.ValidatePassword` returns `E_ACCESSDENIED` and authenticated snapshot accounts return `E_NOTIMPL`; the subsequent caller-aware boundary is recorded below in `acd52d43a`. No COM password lookup or production state changed; SEC-18 remains separately blocked on fresh evidence/challenge sealing.
- Updated 2026-07-26: SEC-12 caller-aware authentication boundary committed in `acd52d43a` after the authorization fence `aab2e307e`; injected IMAP/POP3/SMTP session propagation and failure/disconnect coverage is now in `10111a50f`. `ClientAwareAuthenticationService` is registered in the service host and used by all three protocols; it carries parsed client IP and caller kind into the existing authenticator/auto-ban path and preserves invalid-IP, cancellation, recorder-failure, response, and disconnect behavior. Focused service tests pass `5/5`, focused protocol tests pass `71/71`, and full Net10 passes `1209` with one opt-in native-registry test skipped. The remaining evidence gaps are a no-database DI construction smoke test, real loopback listener propagation, isolated SQL threshold/disconnect integration, and a trusted COM caller boundary. `Account.ValidatePassword` remains fenced; no COM identity or production state changed. Next slice: add the no-database service-host DI construction smoke test without enabling the COM method.
- Updated 2026-07-26: SEC-12 caller-aware authentication boundary committed in `acd52d43a` after the authorization fence `aab2e307e`; injected IMAP/POP3/SMTP session propagation and failure/disconnect coverage is in `10111a50f`, and shared protocol DI registration smoke coverage is in `0da18ad53`. `AddCallerAwareProtocolServices` is used by production `HMailServer.Service.Program`; the no-database test resolves `ClientAwareAuthenticationService`, all three sessions, and all three listeners with test doubles, without SQL or data-directory setup. Focused caller-aware/protocol tests pass `77/77`, the Windows service build passes with 0 warnings and 0 errors, and full Net10 passes `1210` with one opt-in native-registry test skipped. Security review is GREEN; reality review is YELLOW for this bounded seam and RED for production because the real service host, hosted-service descriptors, listener I/O, and SQL threshold integration remain unproven. `Account.ValidatePassword` remains fenced; no COM identity or production state changed. Next slice: add actual no-database `HMailServer.Service` host/hosted-service registration coverage without enabling the COM method.
- Updated 2026-07-26: SEC-12 hosted-service descriptor registration coverage is committed in `ccaa3f7ed` after protocol DI smoke `0da18ad53`. `AddProductionHostedServices` is used by production `HMailServer.Service.Program`; `ProductionHostedServiceRegistrationTests` verifies ordered `IHostedService` implementation descriptors for external fetch enabled and disabled, without building or starting a host. Focused registration/caller-aware/protocol tests pass `78/78`, the Windows service build passes with 0 warnings and 0 errors, and full Net10 passes `1211` with one opt-in native-registry test skipped. Security review is YELLOW because the existing legacy-vs-.NET COM readiness ordering remains unaddressed; reality review is GREEN for this descriptor slice and RED for production because `Host.Build()`, hosted-service instance resolution, SQL/data-directory isolation, COM readiness, and listener I/O remain unproven. `Account.ValidatePassword` remains fenced; no COM identity or production state changed. Next slice: add no-database production-composition coverage that builds the host and resolves hosted-service instances without starting them.
- Updated 2026-07-26: SEC-12 production host composition resolution is committed in `f1718658a`, and real loopback listener caller propagation is committed in `ac3798455`. `ProductionHostCompositionTests.HostBuild_ResolvesEveryRegisteredHostedServiceWithoutStartingOrUsingDatabase` resolves the production hosted-service graph without starting it; `ImapTcpListenerTests.RunAsync_PropagatesLoopbackAddressToImapAuthenticationBoundary`, `Pop3TcpListenerTests.RunAsync_PropagatesLoopbackAddressToPop3AuthenticationBoundary`, and `SmtpTcpListenerTests.RunAsync_PropagatesLoopbackAddressToSmtpAuthenticationBoundary` use actual loopback sockets and assert the endpoint address and protocol caller kind at the shared authentication boundary. Focused listener tests pass `13/13`; full Net10 passes `1215` with one opt-in native-registry test skipped. No production code, COM identity, service, SQL/data directory, or SMTP trust behavior changed. Security review found no actionable boundary regression; reality review remains RED for production because COM readiness ordering, SQL threshold/disconnect integration, broader COM/Admin parity, migration/backup, and performance/soak gates remain open. Next slice: review the COM readiness barrier against legacy `ResumeObjects`, without enabling `Account.ValidatePassword` or changing COM registration.
- Updated 2026-07-27: SEC-12 readiness barrier commit `a6e150134` adds `ServerReadinessSignal`, publishes `ServerBootstrapper` success/failure/cancellation, registers the bootstrapper before `ComLocalServerHostedService`, and blocks COM startup until the bootstrap checks complete. `ServerReadinessSignalTests` and `ProductionHostCompositionTests.ComLocalServerHostedService_WaitsForReadinessBeforeStartingCom` cover signal lifecycle and cancellation without starting COM, opening SQL, binding sockets, or creating the data directory; the existing registration/composition tests preserve the production graph. Focused readiness/listener coverage passes `19/19`; full Net10 passes `1219` with one opt-in native-registry test skipped. No COM identity, direct activation boundary, protocol behavior, SMTP trust, SQL schema, service, or SEC-18 state changed. Reality review remains RED because the signal does not yet prove all required listener/worker startup completion or real startup/COM lifecycle success; broader COM/Admin parity, migration/backup, performance/soak, and SEC-18 gates also remain open. Next slice: align the readiness signal with a production startup coordinator covering the remaining required server services, without enabling `Account.ValidatePassword` or changing COM registration.
- Updated 2026-07-27: SEC-12 listener startup coordinator is committed in `64424782b` after the bootstrap barrier `a6e150134`. `ServerStartupCoordinator` separates bootstrap completion from final readiness, waits for every enabled IMAP/POP3/SMTP listener `Started` task, and propagates listener bind failures or cancellation before the existing `ComLocalServerHostedService` start/resume path. `ProductionHostCompositionTests` and `ProductionHostedServiceRegistrationTests` preserve the production order; `ServerStartupCoordinatorTests` cover bootstrap gating, disabled-listener readiness, listener failure, and cancellation; listener wrappers fail the readiness signal closed on startup faults. Focused readiness/listener/composition coverage passes `24/24`; full Net10 passes `1224` with one opt-in native-registry test skipped. No COM identity, direct activation boundary, protocol command behavior, SMTP trust, SQL schema, service state, data directory, or SEC-18 state changed. Reality review remains RED for production because delivery/external-fetch worker readiness and a real successful host/COM lifecycle are not proven; the trusted caller-aware COM mutation boundary, broader COM/Admin parity, migration/backup, performance/soak, and SEC-18 gates remain open. Next slice: isolated SQL threshold/disconnect integration without enabling `Account.ValidatePassword` or changing COM registration.
- Updated 2026-07-27: SEC-12 SQL threshold/disconnect test evidence is committed in `dba32694a`. `ClientAwareAuthenticationServiceTests.RecorderCancellationIsPropagated` covers cancellation propagation; `SqlServerAutoBanLogonFailureRecorderIntegrationTests` exercises legacy threshold counts, per-IP failure clearing, priority-100 expiring range creation, the no-range disconnect branch, and the real client-aware service result against a GUID-named disposable database. The fixture requires a local SQL/LocalDB data source plus `HMAILSERVER_NET10_SQLSERVER_INTEGRATION_ALLOW_ISOLATED_CREATE=1`, so it cannot target a shared or production server; this host ran no SQL integration because the approved connection was unset. Focused auth/SQL coverage passes `9` with one explicit inconclusive; full Net10 passes `1225` with two opt-in tests skipped. Security review found no SQL injection, COM identity, or authorization regression in the test-only slice. Reality remains RED for production: the opt-in SQL path lacks a captured disposable-instance execution, the production recorder has a concurrent per-IP threshold race, and non-cancellation recorder failures are swallowed by the client-aware service. Next slice: make threshold enforcement atomic per IP without enabling `Account.ValidatePassword` or changing COM registration.
- Updated 2026-07-27: SEC-12 atomic per-IP threshold enforcement is committed in `d2fe76e14` after the guarded integration fixture `dba32694a`. `SqlServerAutoBanLogonFailureRecorder.CountFailuresSql` now uses `UPDLOCK, HOLDLOCK` inside the existing transaction, matching the installed `idx_hm_logon_failures_ipaddress` index; `RecordFailureAsync_SerializesConcurrentFailuresPerIp` asserts three concurrent failures produce counts `1`, `2`, `3`, exactly one disconnect/range transition, no residual failure rows, and one priority-100 expiring deny range. The fixture remains local-data-source-only and requires `HMAILSERVER_NET10_SQLSERVER_INTEGRATION_ALLOW_ISOLATED_CREATE=1`; this host ran no SQL integration because the approved connection was unset. Focused auth/SQL coverage passes `9` with two explicit inconclusives; full Net10 passes `1225` with three opt-in tests skipped. No COM identity, direct activation boundary, SMTP trust behavior, production database, or data directory changed. The SQL concurrency result remains unexecuted evidence until an approved disposable instance is configured; non-cancellation recorder failures remain swallowed by the client-aware service. Next slice: audit authenticated `Settings -> SecurityRanges` ownership and legacy `InterfaceSecurityRanges::Add`/`InterfaceSecurityRange::Save` insert parity without enabling `SetDefault`, deletion, live policy reconfiguration, or the COM password method.

- Updated 2026-07-26: SEC-18 canonical evidence-boundary hardening is committed in `6d40f1b6d` after final-validation `16e8b431f`. The collector and attester enforce the installed Application AppID `{5EDEC473-39E0-43F6-A234-1947071721C8}`, constrain output beneath `artifacts/sec18-staging`, reject existing/reparse-point paths, and use exclusive creation; the attester emits 19 checks across 14 source hashes. Focused collector/attester/registry-binary/graph tests and full Net10 (`1198` passed, one opt-in skip) remain green. The stale caller artifact and canonical-output-gate rerun both fail closed with exit `2`; a fresh authorized probe is required before live evidence can be re-attested. Remaining review requirements are a collector-issued challenge, final-state sealing, external manifest pin/sign, and fresh independent review. Bounded evidence remains YELLOW and permanent registration remains RED. Next slice: rerun the isolated authorized PHP/FastCGI and non-pool matrix with one shared invocation ID, then close challenge/final-state sealing. Permanent registration and production changes remain blocked.

- Updated 2026-07-26: SEC-18 final-validation hardening completed in code/test commit `16e8b431f`. Focused collector/attester/registry-binary/graph tests pass; full Net10 passes `1198` with one opt-in native-registry test skipped. The existing live artifact is intentionally stale under the new contract and the final-timestamp-gated rerun exits `2`; no production registration, DCOM ACL, PHP cutover, service, database, data directory, firewall, or protocol change occurred. Next slice: rerun the isolated authorized PHP/FastCGI and non-pool matrix with a shared invocation ID, then close challenge/path hardening and externally seal the evidence bundle with fresh independent review; permanent broker registration remains RED.
- Updated 2026-07-26: SEC-18 bounded non-pool recheck ran as the authorized medium-integrity desktop user without changing ACLs or creating a temporary user. Historical evidence still records desktop COM activation denied at `0x80070005` before interface/method entry with zero invocation-counter delta; the temporary probe is now absent from both registry views, service/process list, and probe path. The fresh non-elevated collector output `artifacts/sec18-staging/staging-inventory-20260726-nonpool-current.json` is `Incomplete` because it cannot read IIS mappings and has no fresh server caller-token evidence; the current attestation `SEC18-denial-evidence-attestation-20260726-nonpool-current.json` exits `2` with `14/19` checks passed. `hMailServer` remains `Stopped`/`Disabled` with no process, the installed Application graph remains unchanged by the retained pre/post evidence, and no database/data directory was accessed. `hmail_security_reviewer` and `hmail_reality_checker` both returned RED. Next slice remains an elevated isolated authorized/non-pool matrix with one collector-issued challenge/correlation and fresh final-state sealing; permanent broker registration, DCOM ACL changes, installed Application activation, and PHP session cutover remain blocked.
- Updated 2026-07-22: SEC-18 semantic broker-AppID key-DACL policy, evidence-only API hardening, native owner/DACL readback, and the bounded non-production caller-identity matrix are complete, but the rerun report `artifacts/sec18-staging/SEC18-nonpool-denial-rerun-20260722.json` remains RED for permanent registration. It records the authorized pool caller-token path, activation-stage `0x80070005`, captured desktop activation denial, cleanup, and zero post-cleanup differences in the 44-snapshot Application graph. Both independent reviews require a true unauthorized-process test, stronger evidence binding, reviewed broker-only AppID ACL/method enforcement, and stronger cleanup verification; the collector remains fail-closed with `ReadyForBrokerRegistration=false`. SEC-11 `RuleAction.To`, `RuleAction.IMAPFolder`, `RuleAction.HeaderName`, `RuleAction.Value`, `RuleAction.RouteID`, and `RuleAction.AbortSpamFlagged` existing-row setter parity plus account-rule Forward/CreateCopy generated-message flag propagation, SPF/DKIM/DMARC pre-rule classification, and WebAdmin ClamAV/SpamAssassin scanner-test target hardening are complete in `e628dc487`, `0ec2ff11e`, `0f0f576b6`, `2ce09ccdb`, `c7e2c408`, `e14040d63`, `3909d2441`, `2b944058c`, and `f586332cf`. The `BackupManager.StartBackup` preflight/plan, authenticated operation-state/dispatch, service-owned maintenance queue/status/failure callback boundary, read-only queued `BackupStartPlan` acquisition, archive/XML creation, non-secret raw settings-property coverage, backup-side `DomainAliases`, backup-side non-secret scalar `Accounts`, and backup-side normal domain `Aliases` child serialization are complete in `8681b1d23`, `cf15929a0`, `832b9c933`, `d4360fd3e`, `a1f1d92f4`, `59ac1b7c6`, `f15e857a8`, `ac611987c`, and `3e7535d76`; the next candidate is backup-side `DistributionLists` child serialization. Permanent registration, DCOM ACL writes, isolated COM activation, and PHP cutover remain blocked.
- Done: SEC-11 existing-row `RuleAction.FromAddress` setter parity (`4ed42c2cf`). Legacy `InterfaceRuleAction::put_FromAddress` only denies detached objects, otherwise stages the raw BSTR without an administrator guard or normalization; the .NET authorized item now stages the exact value through its owning collection and existing parameterized `actionfromaddress` update. Direct activation remains `E_ACCESSDENIED`, DISPID 7 and COM identity are unchanged, and Add/new-item Save, other setters, SMTP behavior, and broader mutation remain out of scope. Focused RuleActions/Rules/store tests pass 27/27; full Net10 passes 1156/1156 with one opt-in native registry test skipped. No SQL or live database integration was added.
- Done: SEC-11 existing-row `RuleAction.IMAPFolder` setter parity (`0ec2ff11e`). Legacy `InterfaceRuleAction::put_IMAPFolder` stores the incoming Unicode BSTR as Modified UTF-7 without validation or administrator checks; the getter decodes the persisted representation. The authorized .NET item preserves that encode/decode boundary and uses the existing owning `Save()` path; direct activation remains `E_ACCESSDENIED`, no-save/read-only facades remain `E_NOTIMPL`, and folder lookup, ACL, creation, data-directory movement, SMTP trust, and live reconfiguration remain unchanged. Focused RuleActions/Rules/store tests pass 49/49; full Net10 passes 1163/1164 with one opt-in native registry test skipped. No live SQL integration was run.
- Done: SEC-11 existing-row `RuleAction.HeaderName` setter parity (`0f0f576b6`). Legacy `InterfaceRuleAction::put_HeaderName` directly stores the raw BSTR and returns `S_OK` for attached items without administrator or validation checks; `PersistentRuleAction` writes `actionheader`. The authorized .NET item stages exact raw values through the existing owning `Save()` path; direct activation remains `E_ACCESSDENIED`, no-save/read-only facades remain `E_NOTIMPL`, and the existing SMTP `SetHeaderValue` runtime path is unchanged. Focused RuleActions/Rules/store tests pass 51/51; full Net10 passes 1165/1166 with one opt-in native registry test skipped. No live SQL integration was run.

- Done: .NET 10 solution skeleton, local build/test wrappers, prerequisite checks.
- Done for staging, additive contract, caller-source, preflight, and registry-readback gates: SEC-18 evidence proves direct PHP activation, exact worker/caller SID matching, wrong-SID method denial, and non-elevated desktop activation denial; the temporary probe was fully rolled back. Commit `06820aa7d` freezes the fresh four-member broker contract without adding it to the legacy manifest or COM host. Commit `24df969ac` adds the policy guard, commit `4b109682d` adds the Windows `CoImpersonateClient`/`OpenThreadToken` source and in-memory preflight evaluator, and commit `dfef2a136` adds read-only 32/64 AppID snapshots. The current slice extends that readback to the complete installed Application CLSID/ProgID/CurVer/LocalServer32/TypeLib/Interface graph, compares key paths/presence/value names/kinds/native bytes, preserves the observed six-key Registry32 Application CLSID-subtree absence, rejects unexpected direct descendants, and validates canonical fixed values plus installation-specific executable/HELPDIR path relationships. Focused graph/preflight tests pass 20/20; the full Net10 suite passes 1122/1122. Live graph evidence is captured and tracked in `artifacts/sec18-staging/SEC18-application-graph-live-evidence-20260718.json` and `.md`, and both independent reviews remain RED. The broker remains unregistered and PHP cutover remains blocked pending deterministic collector attestation, key security-descriptor capture, native-reader coverage, and isolated COM integration.
- Done: legacy C++ phase-0 fixes for ClamAV INSTREAM framing, synchronous timeout cancellation, SpamAssassin partial/invalid responses, and MSBuild 17 discovery.
- Done: SQL Server Full-Text Search migration, search document queue, backfill processor, MIME text extraction, IMAP SEARCH planner/parser/executor, IMAP session loop, SQL-backed LOGIN/SELECT slice, legacy password verification, and bounded TCP listener.
- Done: IMAP nested private folder path resolution, public folder root mapping, and ACL-aware public folder SELECT/EXAMINE read/write selection.
- Done: IMAP FETCH/UID FETCH metadata and RFC822/body literal slice backed by SQL Server and the existing data directory.
- Done: IMAP LIST/LSUB folder discovery and STATUS counters backed by SQL Server folders/messages and public-folder ACL lookup/read checks.
- Done: IMAP STORE/UID STORE flag mutation and EXPUNGE cleanup of deleted messages, search docs, metadata, and message files.
- Done: IMAP COPY/UID COPY and MOVE/UID MOVE with destination UID allocation, message file copy, search reindex queueing, and source cleanup for MOVE.
- Done: IMAP APPEND synchronizing literal handling, destination UID allocation, message file write, and search indexing queue.
- Done: IMAP FETCH ENVELOPE and BODYSTRUCTURE MIME response formatting with raw message reads only for MIME-dependent FETCH items.
- Done: IMAP SORT/UID SORT parser, executor, session dispatch, SQL Server metadata ordering, and FTS candidate filtering.
- Done: IMAP SEARCH/SORT sent-date criteria (`SENTSINCE`, `SENTBEFORE`, `SENTON`) stay SQL-backed through message metadata date filters.
- Done: IMAP SEARCH/SORT sequence-set criteria stay SQL-backed through mailbox `ROW_NUMBER()` predicates.
- Done: IMAP IDLE/DONE session flow, capability advertising, idle event formatting, and mailbox status polling notifier.
- Done: IMAP ACL commands (`GETACL`, `SETACL`, `DELETEACL`, `LISTRIGHTS`, `MYRIGHTS`) backed by the existing public-folder `hm_acl` model.
- Done: IMAP QUOTA commands (`GETQUOTA`, `GETQUOTAROOT`, `SETQUOTA`) backed by live mailbox usage and hMailServer account/domain limits.
- Done: Shared IMAP sequence-set parser used by SEARCH, FETCH, STORE, COPY, and MOVE.
- Done: IMAP recent flag lifecycle: SELECT captures and clears DB `\Recent`, EXAMINE preserves it, SEARCH/SORT use session recent UID snapshots, and APPEND/COPY/MOVE update selected-mailbox snapshots.
- Done: IMAP SASL PLAIN with initial response and continuation flow, plus TLS-required authentication policy hooks.
- Done: IMAP `OnClientLogon(HMAILSERVER_CLIENT)` event hook runs after successful and failed authentication attempts and exposes endpoint/session/TLS metadata.
- Done: shared IMAP/SMTP `OnClientValidatePassword(HMAILSERVER_ACCOUNT, password)` event hook runs before built-in password verification with legacy accept/reject/continue decisions and expanded scalar account facade fields.
- Done: SMTP TCP listener/session skeleton with bounded connection handling and `EHLO`/`HELO`/`NOOP`/`RSET`/`QUIT` responses.
- Done: SMTP `MAIL`/`RCPT`/`DATA` receive staging with dot-terminator handling, dot-stuffing, size limits, and `ISmtpMessageReceiver` storage boundary.
- Done: SQL-backed SMTP receive store persists inbound DATA to the hMailServer data directory, inserts locked `hm_messages` queue rows, writes `hm_messagerecipients`, and unlocks atomically.
- Done: SMTP recipient/domain validation for active local domains, domain aliases, plus-addressing, active accounts, aliases, distribution lists, postmaster catch-all, and local account id resolution before queueing.
- Done: SMTP `AUTH PLAIN`/`AUTH LOGIN` using the SQL-backed account authenticator, plus authenticated relay allowance for non-local recipients.
- Done: SMTP route-aware recipient validation for `hm_routes`/`hm_routeaddresses`, including wildcard route domains and route delivery metadata on resolved recipients.
- Done: atomic delivery queue lease store skeleton.
- Done: delivery queue message loading, local/route/remote target classification, and dispatcher-based delivery processor handoff.
- Done: SMTP local mailbox delivery writer copies leased queue messages into account Inbox folders, allocates UIDs, inserts delivered `hm_messages`, and queues search indexing.
- Done: SMTP remote delivery sender for route and remote-domain batches with EHLO/HELO, optional/required STARTTLS hooks, route AUTH LOGIN, dot-stuffed DATA streaming, and dispatcher integration.
- Done: SMTP remote DNS/MX resolution using system DNS servers, MX preference ordering, TTL/negative caching, domain fallback, and per-domain/route concurrency limiting.
- Done: SMTP delivery retry/backoff classification, recipient-level queue cleanup after successful batches, permanent-failure bounce submission, and retry-limit bounce handling.
- Done: SMTP server-side STARTTLS capability, stream upgrade, session reset after TLS negotiation, TLS-required AUTH policy, and PFX certificate loading.
- Done: SMTP global rule queue-acceptance hook backed by legacy `hm_rules` / criteria / actions tables, with criteria matching plus `Delete`, `SetHeaderValue`, and `StopRuleProcessing`.
- Done: SMTP account-level local-delivery rule hook applies `Delete`, `SetHeaderValue`, and `StopRuleProcessing` to each per-account message copy before DB insert/index queueing.
- Done: SMTP `MoveToIMAPFolder` rule action for local delivery resolves destinations through the IMAP mailbox store, moves the per-account/public-folder file when needed, and allocates UID in the rule-selected folder.
- Done: SMTP `Forward` and `CreateCopy` rule actions submit generated messages through the atomic queue writer, increment `X-hMailServer-LoopCount`, honor the rule loop limit, and preserve generated-message recipients.
- Done: SMTP `SendUsingRoute` and `BindToAddress` global rule actions persist delivery metadata, force remote recipients through the selected route, and bind remote SMTP sockets to the rule-selected local IP address.
- Done: SMTP `Reply` rule action generates auto-replied queue messages, skips auto-submitted sources, increments rule loop count, and works for global and account-level rules.
- Done: SMTP `ScriptFunction` rule action plumbing now calls a pluggable executor boundary that can mutate message bytes, drop a message, or reject processing.
- Done: Windows-only process-isolated SMTP rule script executor runs `EventHandlers.vbs`/`.js` through `cscript.exe`, timeboxes execution, round-trips message file mutations, exposes file-backed scalar, recipient, and attachment facades, and fails closed when the script runner does not return status.
- Done: SMTP/IMAP/POP3 `OnClientConnect(HMAILSERVER_CLIENT)` event hooks run before protocol greeting or implicit TLS setup and can close the connection on legacy `Result.Value = 1`.
- Done: SMTP `OnHELO(HMAILSERVER_CLIENT)` event hook runs before HELO/EHLO success responses and maps legacy `Result.Value`/`Result.Message` rejection responses.
- Done: SMTP `OnClientLogon(HMAILSERVER_CLIENT)` event hook runs after AUTH attempts and exposes attempted username plus authenticated state.
- Done: SMTP `OnRecipientUnknown(HMAILSERVER_CLIENT, HMAILSERVER_MESSAGE)` event hook runs for unknown RCPT targets.
- Done: SMTP `OnSMTPData(HMAILSERVER_CLIENT, HMAILSERVER_MESSAGE)` event hook runs after DATA is read and before receiver/queue processing, can mutate/drop/reject messages, and maps legacy rejection responses.
- Done: SMTP `OnAcceptMessage(HMAILSERVER_CLIENT, HMAILSERVER_MESSAGE)` event hook runs before global rule processing, can mutate/drop/reject messages, exposes basic client context, and maps legacy `Result.Value`/`Result.Message` rejection responses.
- Done: SMTP `OnTooManyInvalidCommands(HMAILSERVER_CLIENT, HMAILSERVER_MESSAGE)` event hook runs when the configured invalid-command disconnect threshold is exceeded.
- Done: SMTP delivery queue `OnDeliveryStart(HMAILSERVER_MESSAGE)`, `OnDeliverMessage(HMAILSERVER_MESSAGE)`, and `OnDeliveryFailed(HMAILSERVER_MESSAGE, recipient, error)` event hooks run with queue-file mutation persistence where applicable.
- Done: POP3 session command engine for `USER`/`PASS`, `STAT`, `LIST`, `UIDL`, `RETR`, `DELE`, `RSET`, `NOOP`, and `QUIT` over a streaming mailbox-store boundary.
- Done: SQL Server POP3 mailbox store opens the legacy account Inbox, lists by `messageuid`, streams message files from the data directory, and deletes message/search/metadata rows plus files on committed `DELE`.
- Done: POP3 disabled-by-default TCP listener and Windows service wiring with bounded concurrent connections and plain stream factory boundary.
- Done: POP3 `CAPA` and `TOP` command parity; `TOP` streams headers plus requested body lines with dot-stuffing.
- Done: POP3 process-local mailbox lock manager prevents concurrent sessions from opening the same account mailbox and releases locks on session end.
- Done: SQL auto-ban recorder mirrors legacy failed-logon settings, records `hm_logon_failures`, clears expired failures, creates deny `hm_securityranges` rows when the threshold is reached, and is wired into IMAP/SMTP/POP3 failed authentication paths.
- Done: POP3 implicit TLS listener wiring uses `SslStream` and configured PFX certificates before the session greeting.
- Done: POP3 `OnClientLogon(HMAILSERVER_CLIENT)` event hook runs after successful and failed authentication attempts and exposes endpoint/session/TLS metadata.
- Done: SQL Server external fetch account store leases due legacy `hm_fetchaccounts`, defers inactive account/domain rows, decrypts legacy Blowfish passwords, and tracks `hm_fetchaccounts_uids`.
- Done: external fetch MIME/Received recipient resolution uses the SMTP recipient validator and applies legacy local/route recipient filtering.
- Done: modern TLS option factory and spam/virus protocol helpers.
- Done: ClamAV antivirus pipeline wiring for SMTP queue acceptance and external POP3 fetch account `UseAntiVirus` scans.
- Done: async/timeboxed SpamAssassin client and SMTP receiver pipeline wiring, preserving original messages on invalid/partial spamd responses and honoring external fetch account `UseAntiSpam`.
- Done: optional spam policy pipeline adds legacy `X-hMailServer-Spam`, `X-hMailServer-Reason-*`, and subject-prefix mutations after successful spam scans.
- Done: spam policy mark threshold sets the legacy `eMFSpam` queue flag (`128`) while preserving the default `\Recent` flag.
- Done: spam policy delete threshold rejects matching SMTP messages with `554` before antivirus scanning and queue persistence.
- Done: optional MIME-aware attachment blocking replaces matching SMTP attachments with legacy-style text attachments before antivirus scanning and queue persistence.
- Done: optional DNSBL checks reject listed SMTP client IPs before scripts/rules/spam/AV/queue while failing open on DNS errors and skipping authenticated clients by default.
- Done: optional reverse DNS/PTR checks reject missing or non-forward-confirmed client hostnames before scripts/rules/spam/AV/queue while failing open on transient DNS errors.
- Done: optional sender-domain MX checks reject unauthenticated envelope sender domains without MX records before scripts/rules/spam/AV/queue while failing open on transient DNS errors.
- Done: optional SQL-backed greylisting checks legacy `hm_greylisting_triplets` before scripts/rules/spam/AV/queue, honors white-address wildcard entries, and fails open on SQL errors.
- Done: optional URL/SURBL checks extract bounded MIME text/html URL hosts, honor `EnableSpamScan`, reject listed hosts before antivirus/queue persistence, and fail open on DNS errors.
- Done: external POP3 fetch treats permanent SMTP receiver rejections as non-accepted messages with normal UID/remote-delete retention instead of failing the whole account batch.
- Done: external POP3 fetch hosted worker resets stale `falocked` fetch-account rows on startup, matching legacy `PersistentFetchAccount::UnlockAll()` recovery.
- Done: script message facade exposes legacy `Flag(eMessageFlag)` bitmask access over `State` for VBScript/JScript handlers.
- Done: JScript message facade exposes the legacy `Filename` alias alongside the existing `FileName` path.
- Done: JScript attachment facade exposes the legacy `Filename` alias alongside `FileName`.
- Done: global `EventLog.Write(value)` script facade appends legacy-shaped Unicode event-log rows for VBScript/JScript rule, error, and password-validation handlers.
- Done: JScript `OnClientValidatePassword` receives the legacy writable `Result.Parameter` scalar initialized to zero, matching the other event runners and native `Result` constructor.
- Done: VBScript `OnClientValidatePassword` explicitly seeds writable `Result.Parameter` to numeric zero instead of leaving the public class field as `Empty`.
- Done: password-validation `HMAILSERVER_ACCOUNT.Password` exposes the stored legacy SQL account value in VBScript/JScript while the attempted plaintext remains the separate handler argument.
- Done: the .NET COM assembly preserves the legacy dual `IInterfaceMessageIndexing` IID and DISPIDs 1-5, including `VARIANT_BOOL` marshaling for `Enabled`, with reflection contract tests covering both the legacy interface and additive `IInterfaceMessageIndexing2`.
- Done: the COM-visible `MessageIndexing` class preserves the legacy CLSID and versioned ProgID, keeps `IInterfaceMessageIndexing` as its default interface, implements `IInterfaceMessageIndexing2`, and delegates all v1/v2 operations through a required process-hosted runtime boundary.
- Done: a SQL Server message-indexing administration store and service-configured runtime implement delivered/indexed counts, persisted legacy `Enabled`, FTS/queue status, `Clear`, `Index`, and `Rebuild`; direct COM class activation remains access-denied and the backfill processor skips leasing while indexing is disabled.
- Done: the Windows service hosts COM local-server class factories on a dedicated MTA using suspended registration/resume and same-thread revoke; registry-free process activation proves that direct `MessageIndexing` CLSID objects remain access-denied and disappear after shutdown.
- Done: preserve the legacy dual `Application` and `Account` IIDs, full 20/61-member vtable order, CLSIDs, versioned ProgIDs, default interfaces, server-admin account result, and detached-account access denial; registry-free process activation authenticates through the legacy root interface.
- Done: preserve server-administrator credential semantics for case-insensitive `Administrator`, MD5/salted-SHA256 hashes, and wrong credentials while deliberately hardening the unsafe legacy empty-hash boundary to fail closed.
- Done: scripted `HMAILSERVER_MESSAGE.RefreshContent` reloads file-backed headers and body after direct script-side message file rewrites.
- Done: script message `FileName`/`Filename` facade keeps `Load`, `Save`, and `Copy` tied to the original backing file path.
- Done: script message `To`/`CC` direct assignment no longer rewrites saved recipient headers, preserving legacy read-only property shape.
- Done: script attachment `FileName`/`Filename` and `Size` direct assignment cannot mutate attachment collection metadata, preserving the legacy read-only property shape.
- Done: script message `ID`, `UID`, `State`, `DeliveryAttempt`, and `InternalDate` preserve legacy read-only queue metadata, including message IDs above the 32-bit range; `State` and `Flag(eMessageFlag)` use distinct legacy fields.
- Done: script message `Size` preserves the legacy read-only floor-KiB calculation and refreshes from the backing file after `Save`.
- Done: script recipient item `Address`, `OriginalAddress`, and `IsLocalUser` preserve legacy read-only metadata without blocking message-level `AddRecipient`/`ClearRecipients` mutations.
- Done: script `Recipients` collection no longer publishes the non-legacy `Add`, `Clear`, or `ToHeaderValue` names; supported collection reads remain `Count`/`Item` and mutations remain message-level.
- Done: script message `AddRecipient` uses the legacy quoted display-name MIME shape, including empty names, and comma joining in VBScript/JScript.
- Done: script message `ClearRecipients` clears the envelope recipient collection plus `To`, `Cc`, and `Bcc` MIME headers in VBScript/JScript, matching the legacy message contract.
- Done: script message `Save` generates a current local legacy-format MIME `Date` when the message has no date in VBScript/JScript.
- Done: non-empty script message `Body` and `HTMLBody` assignments gain the legacy trailing `CRLF` before persistence in VBScript/JScript while empty values stay empty.
- Done: script `Headers` collection no longer publishes runner-only `Refresh` or `Commit`; legacy `Count`/`Item`/`ItemByName` reads and header item mutations remain supported.
- Done: script `Recipients.Item`, `Headers.Item`, and `Headers.ItemByName` raise legacy-style bad-index errors for invalid lookups in VBScript/JScript instead of returning `Nothing`/`null`.
- Done: script `Attachments` collection no longer publishes runner-only `Load` or `DeleteAt`; legacy `Count`/`Item`/`Clear`/`Add` plus item `SaveAs`/`Delete` remain supported.
- Done: script `Attachments.Add` raises the legacy `Failed to attach file.` error for a missing source file in VBScript/JScript instead of silently returning.
- Done: script `Attachments.Item` raises a legacy-style bad-index error for an out-of-range index in VBScript/JScript instead of returning `Nothing`/`null`.
- Done: captured script attachment items retain stable object identity across earlier collection deletions, so `Delete` removes the originally selected MIME part in VBScript/JScript.
- Done: script message `HasBodyType` matches clean MIME content types case-insensitively across the root and two nested part levels instead of matching arbitrary message text, including quoted boundary values containing semicolons.
- Done: external POP3 fetch tolerates duplicate persisted `hm_fetchaccounts_uids.uidvalue` rows when building the known-UID lookup for a batch.
- Done: external POP3 fetch coalesces duplicate remote UIDL sequence-number entries so a malformed listing cannot download the same remote slot twice.
- Done: external POP3 fetch processes UIDL entries in ascending sequence order and retains the last UID for duplicate sequence numbers, matching the legacy `std::map` behavior.
- Done: external POP3 fetch prepends `X-hMailServer-ExternalAccount: <account name>` to newly downloaded messages before script, MIME, antivirus, and SMTP receiver processing.
- Done: external POP3 fetch preserves negative `OnExternalAccountDownload` `Result.Parameter` values and treats every negative retention as legacy immediate remote deletion for new and known UIDs.
- Done: external POP3 fetch evaluates positive known-UID retention with the full elapsed timestamp span, preserving the legacy inclusive day boundary instead of truncating to calendar dates.
- Done: external POP3 fetch accepts an empty, normally terminated RETR payload after prepending the legacy account header and continues normal script, receiver, UID, and retention processing.
- Done: external POP3 fetch uses only the first field for each configured MIME recipient header name, matching legacy `GetRawFieldValue` without changing all-Received-header recipient extraction.
- Done: external POP3 fetch validates `Received ... for` recipients with the legacy 254-character email regex before route/local resolution, rejecting malformed addresses before fallback selection.
- Done: external POP3 fetch matches the legacy case-sensitive `Received` token lookup, ignoring uppercase `FOR ` variants and using the account fallback recipient.
- Done: external POP3 fetch validates the first parsed `From` mailbox with the legacy 254-character email contract and uses an empty envelope sender for invalid or over-limit values.
- Done: external POP3 fetch deduplicates resolved recipients by final address only, preserving the first alias when multiple original addresses resolve to one mailbox.
- Done: external POP3 fetch recovers valid configured-header recipients beside malformed addresses by parsing legacy quote/escape-aware comma compounds independently after whole-list parsing fails.
- Done: external POP3 fetch treats whitespace-only configured MIME recipient-header names as non-empty for the legacy processing gate, preserving `Received ... for` recipient scans.
- Done: external POP3 fetch probes CAPA before STLS so optional STARTTLS falls back only when STLS is not advertised, required STARTTLS fails before credentials, and an advertised-but-rejected STLS fails both modes before authentication.
- Done: external POP3 fetch treats a rejected CAPA response as unavailable STLS, continuing optional STARTTLS over plaintext while failing required STARTTLS before credentials.
- Done: external POP3 fetch rejects a failed server greeting before sending any command or credentials in plain and STARTTLS modes.
- Done: external POP3 fetch fails a rejected USER command before sending PASS in plain and optional-STARTTLS plaintext fallback paths.
- Done: external POP3 fetch fails a rejected PASS command before sending UIDL or any later command in plain and optional-STARTTLS plaintext fallback paths.
- Done: external POP3 fetch handles rejected UIDL with legacy QUIT cleanup and no RETR, DELE, or other message-processing commands in plain and optional-STARTTLS plaintext fallback paths.
- Done: external POP3 fetch treats truncated UIDL listings as fatal, failed-releases the account, and avoids receiver, UID, RETR, or DELE side effects.
- Done: external POP3 fetch handles empty UIDL listings without RETR/DELE and removes stale known UID rows missing from the remote server.
- Done: external POP3 fetch skips malformed UIDL listing rows while preserving valid rows from the same response.
- Done: external POP3 fetch handles rejected RETR with legacy QUIT cleanup, failed account-lease release, and no receiver, UID, or remote-deletion side effects.
- Done: external POP3 fetch treats truncated RETR bodies as fatal, failed-releases the account, and avoids receiver, UID, or remote-delete side effects.
- Done: external POP3 fetch treats DELE server rejection as legacy best-effort, continuing UID cleanup and QUIT while preserving fatal socket, I/O, and cancellation failures.
- Done: external POP3 fetch preserves known UID state and failed-releases the account when DELE transport fails before any server response.
- Done: external POP3 fetch treats rejected QUIT responses and disconnects before QUIT response as best-effort cleanup during session disposal.

## Production Parity Backlog

1. IMAP authentication and mailbox selection.
   - Done: SQL account lookup against `hm_accounts` + active `hm_domains`.
   - Done: Legacy password verification for plaintext, Blowfish, MD5, and salted SHA256.
   - Done: LOGIN, SELECT, EXAMINE, authenticated/selected session states.
   - Done: Nested private folder selection with hierarchy delimiter.
   - Done: Public folder root mapping through `imappublicfoldername` / `#Public`.
   - Done: Public folder ACL inheritance for read and read/write selection.
   - Done: UIDVALIDITY, UIDNEXT, RECENT/UNSEEN counters.
   - Done: SASL PLAIN authentication with SASL-IR and TLS-required auth policy.
   - Done: `OnClientLogon(HMAILSERVER_CLIENT)` script hook after successful and failed IMAP authentication attempts.
   - Done: shared `OnClientValidatePassword(HMAILSERVER_ACCOUNT, password)` script hook with expanded scalar account facade.
   - Done: password-validation VBScript/JScript `Result` facades expose writable `Value`, `Message`, and numeric zero-initialized `Parameter` scalars, including an explicit VB seed rather than `Empty`.
   - Done: password-validation account facades expose the legacy stored `Password` scalar without conflating it with the attempted plaintext argument.
   - Remaining: Deeper account facade collections/methods, Active Directory auth, and master user.

2. IMAP command parity beyond SEARCH.
   - Done: FETCH/UID FETCH for `FLAGS`, `UID`, `RFC822.SIZE`, `INTERNALDATE`, `ENVELOPE`, `BODYSTRUCTURE`, `BODY[]`, `BODY.PEEK[]`, and `RFC822`.
   - Done: LIST/LSUB folder discovery and STATUS `MESSAGES`, `RECENT`, `UIDNEXT`, `UIDVALIDITY`, `UNSEEN`.
   - Done: STORE/UID STORE for `FLAGS`, `+FLAGS`, `-FLAGS`, silent variants, and EXPUNGE for `\Deleted` messages.
   - Done: COPY/UID COPY and MOVE/UID MOVE for selected source mailboxes and writable destination folders.
   - Done: APPEND for synchronizing literals with optional flags and internal date.
   - Done: SORT/UID SORT using SQL-backed metadata ordering and FTS-filtered candidate sets.
   - Done: `SENTSINCE`, `SENTBEFORE`, and `SENTON` search criteria using metadata-backed sent-date predicates.
   - Done: message sequence-set search criteria using SQL mailbox sequence predicates.
   - Done: IDLE/DONE with EXISTS/RECENT/EXPUNGE/FETCH FLAGS event formatting and a SQL status polling notifier.
   - Done: ACL command set on top of public-folder ACL rights (`lrswipkxtea`) and SQL account/group/Anyone principals.
   - Done: QUOTA command set using `accountmaxsize`, domain per-account limits, and live `hm_messages.messagesize` usage.
   - Done: Sequence set parser shared across SEARCH/FETCH/STORE/COPY/MOVE.
   - Done: Correct recent flag lifecycle and notifications based on selected-mailbox recent UID snapshots.

3. SMTP production pipeline.
   - Done: disabled-by-default TCP listener, bounded connection handling, greeting, `EHLO`/`HELO`, `NOOP`, `RSET`, and `QUIT`.
   - Done: `MAIL`/`RCPT`/`DATA` transaction staging, declared/actual message-size checks, and protocol-to-storage receive boundary.
   - Done: durable SQL/data-directory receive store for delivery queue staging.
   - Done: active local domain/account/alias/distribution-list validation, domain aliases, plus-addressing, postmaster fallback, and `recipientlocalaccountid` population.
   - Done: `AUTH PLAIN`/`AUTH LOGIN` with initial/challenge responses and authenticated relay allowance for non-local recipients.
   - Done: route-aware recipient validation against `hm_routes`/`hm_routeaddresses` with route classification metadata.
   - Done: delivery queue lease/load/target-resolution processor skeleton for local account, configured route, and remote-domain batches.
   - Done: local mailbox writer for leased local-account batches with Inbox UID allocation and search indexing queue.
   - Done: remote SMTP sender for configured route and remote-domain batches, including route authentication and protocol-level DATA delivery.
   - Done: DNS/MX resolution cache and per-domain/route concurrency gate for remote delivery.
   - Done: retry/backoff classification with 4xx transient / 5xx permanent SMTP semantics, retry-limit bounce handling, and recipient-level queue cleanup to avoid duplicate delivery.
   - Done: server-side STARTTLS with `SslStream`, capability advertising, post-upgrade session reset, TLS-required AUTH policy, and service-level PFX certificate loading.
   - Done: global SMTP rules before queue acceptance for common criteria plus `Delete`, `SetHeaderValue`, and `StopRuleProcessing`.
   - Done: account-level/local-delivery rules for per-account message copies with `Delete`, `SetHeaderValue`, and `StopRuleProcessing`.
   - Done: `MoveToIMAPFolder` for local delivery using rule-selected existing private/public IMAP folders.
   - Done: `Forward` and `CreateCopy` queue submissions with generated-message loop-count protection.
   - Done: `SendUsingRoute` and `BindToAddress` delivery metadata for global rules, including forced route target resolution and local socket bind for remote SMTP.
   - Done: `Reply` generated response action with Auto-Submitted and rule loop protection.
   - Done: `ScriptFunction` executor boundary inside the rule processor.
   - Done: Windows-only process-isolated VBScript/JScript host for SMTP rule functions with file-backed scalar message facade (`FileName`/`Filename`, `DropMessage`, `RejectReason`, IDs/state placeholders, `Flag(eMessageFlag)`, size, delivery attempt, charset, body type checks, common envelope fields, body fields, header value access, message header collection, and `Save`), envelope recipient collection facade, attachment collection facade with `FileName`/`Filename`, size, save, delete, clear, and add support, plus global `EventLog.Write(value)` event-log output.
   - Done: `HMAILSERVER_MESSAGE.RefreshContent` reloads file-backed headers and body after scripts mutate the message file directly.
   - Done: message `FileName`/`Filename` script aliases keep backing file operations on the original runner path, including read-only VBScript `Filename` parity.
   - Done: message `To`/`CC` script properties preserve legacy read-only direct-assignment behavior while leaving `AddRecipient`, `ClearRecipients`, `Recipients`, and `HeaderValue` mutation paths intact.
   - Done: attachment `FileName`/`Filename` and `Size` script properties preserve legacy read-only metadata while leaving `Add`, `Clear`, `Delete`, and `SaveAs` behavior intact.
   - Done: message `ID`, `UID`, `State`, `DeliveryAttempt`, and `InternalDate` script properties preserve canonical read-only queue metadata in VBScript/JScript while retaining 64-bit message IDs; delivery events seed `State = 1` independently from queue-backed `Flag(eMessageFlag)` values.
   - Done: message `Size` script property uses legacy integer `bytes / 1024` semantics, remains read-only, and re-measures the backing file after `Save` in VBScript/JScript.
   - Done: recipient item `Address`, `OriginalAddress`, and `IsLocalUser` script properties remain read-only in VBScript/JScript while message-level recipient mutation paths stay intact.
   - Done: recipient collection script facade exposes the legacy supported `Count`/`Item` surface without the rewrite-only `Add`, `Clear`, or `ToHeaderValue` names; `Message.AddRecipient`/`ClearRecipients` remain supported.
   - Done: message-level `AddRecipient` writes the legacy quoted display-name form for named and unnamed recipients and joins consecutive `To` entries with a comma in both VBScript and JScript.
   - Done: message-level `ClearRecipients` removes `To`, `Cc`, and `Bcc` MIME headers while clearing envelope recipients in both VBScript and JScript.
   - Done: message-level `Save` supplies a current local legacy-format MIME `Date` before writing a message whose date is empty in both VBScript and JScript.
   - Done: message `Body` and `HTMLBody` setters preserve empty values and append the legacy trailing `CRLF` to non-empty values before the message is written in both VBScript and JScript.
   - Done: message header collection script facade exposes the legacy supported `Count`/`Item`/`ItemByName` surface without runner-only `Refresh`/`Commit`; header `Name`/`Value`/`Delete` changes still persist on `Save`.
   - Done: recipient `Item` and message header `Item`/`ItemByName` reject out-of-range indexes or missing names with script errors in both VBScript and JScript, matching the legacy `DISP_E_BADINDEX` contract.
   - Done: attachment collection script facade exposes the legacy supported `Count`/`Item`/`Clear`/`Add` surface without runner-only `Load`/`DeleteAt`; attachment `SaveAs`/`Delete` behavior remains intact.
   - Done: attachment collection `Add` rejects a missing source file with the legacy `Failed to attach file.` error in both VBScript and JScript.
   - Done: attachment collection `Item` rejects an out-of-range index with a script error in both VBScript and JScript, matching the legacy `DISP_E_BADINDEX` contract.
   - Done: attachment item `Delete` resolves a stable runner-internal identity at call time, preventing prior deletions from shifting a captured item onto another MIME part in VBScript/JScript.
   - Done: message `HasBodyType` script method follows legacy clean MIME content-type matching across the root and two nested part levels, with case-insensitive checks and quoted boundary parsing instead of raw message substring matching.
   - Done: account-rule `HMAILSERVER_MESSAGE.Copy(folderId)` captures call-time message content, preserves repeated copy requests, validates same-account destination folders, allocates distinct UIDs, writes delivered message files/rows, and queues each copy for search indexing.
   - Done: shared `OnClientConnect` protocol event hook before SMTP/IMAP/POP3 greeting or implicit TLS setup with connection-close handling for legacy `Result.Value = 1`.
   - Done: `OnHELO` protocol event hook before HELO/EHLO success responses with `HMAILSERVER_CLIENT` and legacy `Result.Value`/`Result.Message` rejection handling.
   - Done: `OnClientLogon` protocol event hook after successful and failed SMTP AUTH attempts with attempted username and authenticated state.
   - Done: `HMAILSERVER_CLIENT` exposes legacy `Authenticated` and `EncryptedConnection` aliases alongside the existing authenticated/TLS state fields.
   - Done: `OnRecipientUnknown` protocol event hook for unknown RCPT validation failures.
   - Done: `OnSMTPData` protocol event hook after DATA is read and before receiver/queue processing with message mutation/drop/reject handling.
   - Done: `OnAcceptMessage` protocol event hook before global rule processing with `HMAILSERVER_CLIENT`, `HMAILSERVER_MESSAGE`, and legacy `Result.Value`/`Result.Message` rejection handling.
   - Done: `OnTooManyInvalidCommands` protocol event hook plus disconnect-invalid-clients/maximum-incorrect-commands session policy.
   - Done: `OnError(iSeverity, iError, sSource, sDescription)` script hook for .NET warning/error/critical logging with legacy severity mapping, `EventId` propagation, exception details, fail-open execution, and recursive-log suppression.
   - Done: delivery queue `OnDeliveryStart` and `OnDeliverMessage` script hooks before target resolution with queue-file mutation persistence and legacy `Result.Value = 1` drop handling.
   - Done: delivery queue `OnDeliveryFailed` script hook for final failed recipients with legacy recipient/error arguments.
   - Done: delivery queue status observer boundary with best-effort events for lease, load-missing, target success/defer/final failure, bounce, completion, release, and processing-failure transitions.
   - Done: optional SQL Server delivery status sink and additive `hm_delivery_queue_status` migration for durable queue transition history.
   - Done: configurable delivery bounce subject/body templates with queue metadata tokens, failed-recipient formatting, header sanitization, and bounded failure descriptions.
   - Done: delivery status retention cleanup worker with configurable retention window, interval, and batch size.
- Done: delivery status metrics query surface for event-kind counts over a requested time window.
- Done: delivery event script message metadata seeds real queue ID, UID, state, delivery attempt, and internal date for VBScript/JScript handlers.
- Done: delivery event failures emit a distinct `DeliveryEventFailed` queue status before deferral, preserving script/error text for SQL metrics and diagnostics.
- Done: delivery bounce templates expose richer queue/recipient tokens including message UID/account/folder/state, delivery attempt, failed recipient count/address list/first recipient, and rule route/bind metadata.
- Done: external POP3 fetch `OnExternalAccountDownload(HMAILSERVER_FETCHACCOUNT, HMAILSERVER_MESSAGE/Nothing, uid)` script hook boundary with legacy fetch-account fields including `NextDownloadTime`/`IsLocked`, nullable message argument handling, and `Result.Value`/`Result.Parameter` delete-retention mapping.
- Remaining: full legacy script object model plus `OnBackupCompleted`/`OnBackupFailed` once the .NET backup engine exists; legacy protocol and delivery event hooks are connected.

4. POP3 and external fetch.
   - Done: POP3 session command engine with shared account authentication, session-held deletes committed on `QUIT`, `RSET` undo, `LIST`/`UIDL` visibility checks, and streaming `RETR` dot-stuffing.
   - Done: SQL Server/data-directory POP3 mailbox store for the authenticated account's root `Inbox`, using legacy `messageuid` as UIDL and removing search queue/document plus metadata artifacts on delete.
   - Done: POP3 TCP listener and service configuration via `HMAILSERVER_POP3_ENABLED`, bind address, port, backlog, and max connection settings.
   - Done: `CAPA` advertises current POP3 capabilities and `TOP` returns headers plus requested body lines without loading the full message.
   - Done: process-local mailbox lock parity for one POP3 session per account mailbox.
   - Done: implicit TLS listener stream factory and service certificate configuration for POP3.
   - Done: `OnClientLogon(HMAILSERVER_CLIENT)` script hook after successful and failed POP3 authentication attempts.
   - Done: SQL Server external fetch account lease/UID store for existing `hm_fetchaccounts` and `hm_fetchaccounts_uids`.
   - Done: external fetch processor core that leases accounts, uses a bounded POP3 session abstraction, removes stale UID rows, runs `OnExternalAccountDownload` for new and already-known remote UIDs, queues accepted messages through the SMTP receiver path, and applies legacy remote delete retention decisions.
   - Done: external POP3 network session factory and hosted worker scheduling with plain, implicit TLS, and STLS modes, plus UIDL/RETR/DELE/QUIT loopback protocol coverage.
   - Done: external fetch received-time parity from valid `Received` header dates, falling back to `Date` and then current UTC when MIME dates are missing or outside legacy bounds.
   - Done: external fetch MIME recipient headers and `Received ... for <recipient>` values resolve through the SQL SMTP recipient validator, then preserve legacy local-account filtering unless route recipients are enabled.
   - Done: external fetch applies UID tracking and remote-delete retention for permanent SMTP receiver rejections such as spam delete-threshold `554` responses.
   - Done: external fetch hosted worker clears stale account locks once on startup before polling due accounts.
   - Done: external fetch skips duplicate new UIDL values within the same POP3 listing so a malformed server response cannot queue the same remote message twice.
   - Done: external fetch skips duplicate already-known UIDL values within the same POP3 listing so script/retention cleanup runs once per remote UID.
   - Done: external fetch tolerates duplicate persisted known-UID rows so corrupted or legacy-created duplicates do not fail the whole account batch.
   - Done: external fetch coalesces duplicate remote sequence numbers within the same UIDL listing before download/retention processing.
   - Done: UIDL coalescing follows the legacy ordered-map contract by sorting sequence numbers ascending and letting the final row replace an earlier UID for the same sequence.
   - Done: newly downloaded messages receive the legacy `X-hMailServer-ExternalAccount` account-name header before script and receiver processing.
   - Done: negative script `DeleteAfterDays` retention values take the legacy immediate remote-delete path for both new and already-known UIDs instead of being clamped to never-delete.
   - Done: positive known-UID retention compares fractional elapsed days against the inclusive legacy boundary rather than comparing midnight-truncated calendar dates.
   - Done: zero-byte RETR payloads are not failed before the legacy external-account header is prepended; the resulting header-only message follows normal processing.
   - Done: duplicate fields for a configured MIME recipient header name contribute only the first field's addresses, while all `Received` fields remain scanned separately.
   - Done: malformed `Received ... for` values cannot become route recipients because extraction now applies the legacy `StringParser::IsValidEmailAddress` contract.
   - Done: `Received` recipient extraction uses the legacy case-sensitive lowercase `for ` token lookup, so uppercase `FOR ` falls through to the account recipient.
   - Done: the first parsed `From` mailbox passes the legacy 254-character email validator before becoming the external-fetch envelope sender; rejected values produce an empty sender.
   - Done: resolved recipients use legacy case-insensitive final-address deduplication, so distinct aliases targeting one mailbox produce one delivery recipient with the first alias metadata.
   - Done: malformed configured-header addresses no longer discard valid neighbors; failed whole-list parsing recovers with legacy quote/escape-aware comma compounds and structured per-address parsing.
   - Done: whitespace-only configured MIME recipient-header names still enter legacy recipient processing and scan all `Received` fields even though no configured field-name token remains.
   - Done: external fetch probes CAPA before STLS, preserving legacy optional STARTTLS plaintext fallback, required STARTTLS pre-auth failure when STLS is not advertised, and pre-auth failure in both modes when an advertised STLS command is rejected.
   - Done: external fetch preserves legacy CAPA-rejection behavior by continuing optional STARTTLS over plaintext and failing required STARTTLS before authentication.
   - Done: external fetch preserves legacy greeting-rejection behavior without sending any client command or credentials in plain and STARTTLS modes.
   - Done: external fetch preserves legacy USER-rejection behavior without sending PASS in plain and optional-STARTTLS plaintext fallback paths.
   - Done: external fetch preserves legacy PASS-rejection behavior without sending UIDL or later commands in plain and optional-STARTTLS plaintext fallback paths.
   - Done: external fetch preserves legacy UIDL-rejection cleanup by sending QUIT without RETR, DELE, or other message-processing commands in plain and optional-STARTTLS plaintext fallback paths.
   - Done: external fetch keeps truncated UIDL multiline listings fatal without RETR, DELE, receiver, or UID side effects.
   - Done: external fetch completes empty UIDL listings, skips message commands, and deletes stale known UID rows no longer returned by the server.
   - Done: external fetch skips malformed UIDL rows and keeps valid rows in the same listing available for processing.
   - Done: external fetch preserves rejected-RETR cleanup by sending only QUIT, completing the failed account lease through the existing SQL completion path, and avoiding receiver, UID, or remote-deletion side effects.
   - Done: external fetch keeps truncated RETR multiline bodies fatal without receiver, UID, or remote-delete side effects.
   - Done: external fetch preserves legacy best-effort DELE semantics by advancing UID cleanup after any server response while keeping transport and cancellation failures fatal.
   - Done: external fetch keeps DELE transport failures fatal before any server response, preserving known UID state and completing the failed account lease through the existing SQL completion path.
   - Done: external fetch failure cleanup now uses the existing `CompleteAsync`/`CompleteSql` operation, so resolution, connection, POP3, receiver, and retention failures clear `falocked` and schedule `fanexttry` from `faminutes` like legacy `ExternalFetchTask::DoWork`; successful completion behavior and the separate `ReleaseAsync` lock-only operation remain unchanged. Focused `ExternalFetchProcessorTests` coverage includes session-factory connection failure.
- Done: external fetch keeps QUIT cleanup best-effort during session disposal when the server rejects QUIT or closes before a QUIT response.
- Done: external fetch resolves the configured server once through an injected address resolver, evaluates all answers through an egress policy, pins the first approved numeric endpoint, retains the original hostname for TLS, denies metadata and special-use destinations when enforcement is enabled, and permits private/ULA/loopback destinations only through explicit CIDR configuration. Audit-only mode is the default so legacy local POP3 compatibility is preserved; credential-free policy decisions are sent to the service logger; no proxy, redirect, SMTP trust, live reconfiguration, or COM mutation behavior changed.
- Done: close the bounded SEC-20 POP3 client timeout/line-budget gap. `TcpExternalFetchSessionFactory` now applies configurable 900-second default operation deadlines across resolution, TCP connect, implicit/STARTTLS, command and line operations, maps only internal expiry to a transport timeout while preserving caller cancellation, requires CRLF within the legacy 250,000-byte control-line window, leaves UIDL/RETR binary data logically unbounded, and caps best-effort QUIT cleanup at five seconds. Focused filter is 79/79 and full Net10 is 1076/1076; adaptive 14,000-session timeout and live DNS/TLS evidence remain open.
- Done: close the SEC-20 registered legacy `InterfaceFetchAccount` constructor ownership risk. Direct activation now leaves the ATL item unattached so existing member guards return access-denied before `hm_fetchaccounts` persistence or external-fetch scheduling; authorized `InterfaceFetchAccounts::Add`, `get_Item`, and `get_ItemByDBID` attachment paths remain unchanged. The focused legacy regression source builds, the changed C++ translation unit compiles with `/utf-8`, the .NET COM/manifest filter is 12/12, and full Net10 is 1076/1076; the installed legacy service regression remains unavailable because the service is stopped/disabled and the NUnit console package is absent.
- Remaining: additional external fetch edge-case parity.

5. Security and anti-abuse modernization.
   - Done: SQL failed-logon auto-ban recorder preserves legacy settings/table compatibility, deny-range creation semantics, and IMAP/SMTP/POP3 threshold-triggered disconnect wiring.
   - Done: async/timeboxed ClamAV INSTREAM client with raw network-order chunk framing, bounded streaming, clean/infected/error result parsing, and fake-daemon protocol tests.
   - Done: ClamAV scanner adapter wired into SMTP receive processing and external POP3 fetch, including fail-closed scan errors, infected-message rejection/skipping, and account-level antivirus enablement.
   - Done: async/timeboxed SpamAssassin `PROCESS SPAMC/1.2` client with bounded response headers/body, original-message preservation on invalid/partial responses, score parsing, SMTP receive wiring, and external fetch `UseAntiSpam` propagation.
   - Done: optional hMailServer spam header/subject policy for `X-hMailServer-Spam`, `X-hMailServer-Reason-*`, score header replacement, and subject prefixing.
   - Done: hMailServer spam mark threshold maps scan scores to the legacy spam message flag before queue persistence.
   - Done: hMailServer spam delete/reject threshold maps scan scores to SMTP `554` rejection before antivirus/queue persistence.
   - Done: optional attachment blocking policy replaces matching MIME attachments with text notice attachments before antivirus/queue persistence.
   - Done: optional DNSBL checker with bounded DNS queries, fail-open lookup errors, IPv4/IPv6 query formatting, and SMTP receiver rejection wiring.
   - Done: optional reverse DNS/PTR checker with bounded lookup, authenticated-client bypass, forward-confirmed hostname mode, SMTP receiver rejection wiring, and fail-open transient DNS handling.
   - Done: optional sender-domain MX checker with bounded lookup, authenticated-client bypass, null reverse-path/domain-literal skip, SMTP receiver rejection wiring, and fail-open transient DNS handling.
   - Done: optional SQL-backed greylisting checker with authenticated-client bypass, legacy triplet/white-address table compatibility, SMTP receiver temporary rejection wiring, and fail-open SQL error handling.
   - Done: optional SURBL checker with bounded MIME URL host extraction, parent-domain candidate limits, fail-open lookup errors, and SMTP receiver rejection wiring.
   - Done: WebAdmin AV/SpamAssassin AJAX test actions now use POST bodies and POST-only background handlers, retaining existing server-admin and CSRF checks while keeping CSRF tokens/test parameters out of URLs.
   - Done: harden the bounded WebAdmin ClamAV/SpamAssassin test-target egress path. `hmailResolveLocalScannerTarget` resolves a hostname once to an IPv4 literal, verifies that literal through legacy `Utilities::IsLocalHost`, and both AJAX handlers pass only that resolved literal to `TestClamAVScanner`/`TestSpamAssassinConnection`. Remote, unresolved, IPv6-only, and array-valued targets return HTTP 400 with the existing failed-test body `0`; this prevents the legacy scanner clients from resolving a checked hostname again. Existing local scanner tests, POST/server-admin/CSRF boundaries, direct COM calls, saved scanner settings, SMTP scanner behavior, and external-fetch behavior remain unchanged. Broader egress policy remains a separate P1 item.
   - Done: harden the bounded WebAdmin account-delete path. `hm_accounts.php` now submits `background_account_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation. Account selection, authorization, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin domain-delete path. `hm_domains.php` now submits `background_domain_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST or non-server-admin requests before COM mutation. Domain selection, edit behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin alias-delete path. `hm_aliases.php` now submits `background_alias_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing user and domain-scope authorization checks. Alias selection, edit behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin distribution-list-delete path. `hm_distributionlists.php` now submits `background_distributionlist_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing user and domain-scope authorization checks. Distribution-list selection, edit behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin distribution-list-recipient-delete path. `hm_distributionlist_recipients.php` now submits `background_distributionlist_recipient_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before recipient lookup and COM mutation while preserving the existing user and domain-scope authorization checks. Distribution-list/recipient selection, edit behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin domain-alias-delete path. The Names tab in `hm_domain.php` now submits `background_domain_name_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check. Domain/alias selection, other domain editing and saving, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin route-delete path. `hm_routes.php` now submits `background_route_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check and terminal redirect. Route selection, edit/save behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin route-address-delete path. `hm_route_addresses.php` now submits `background_route_address_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check, parent-route selection, and terminal redirect. Route-address selection, edit/save behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin security-range-delete path. `hm_securityranges.php` now submits `background_securityrange_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check and legacy delete/save control flow. Security-range selection, IP policy, auto-ban behavior, live reconfiguration, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin incoming-relay-delete path. `hm_incomingrelays.php` now submits `background_incomingrelay_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check and legacy delete/save control flow. Incoming-relay selection, SMTP trust behavior, live reconfiguration, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin SSL-certificate-delete path. `hm_sslcertificates.php` now submits `background_sslcertificate_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check and legacy delete/save control flow. Certificate selection, certificate/private-key file behavior, TCP/IP port configuration, TLS policy, live reconfiguration, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin TCP/IP-port-delete path. `hm_tcpipports.php` now submits `background_tcpipport_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check and terminal redirect. Port selection, protocol/listener configuration, certificate association, live binding/restart behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin DNS-blacklist-delete path. `hm_dnsblacklists.php` now submits `background_dnsblacklist_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check and legacy delete/save control flow. Blacklist selection, anti-spam DNS-blacklist behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin SURBL-server-delete path. `hm_surblservers.php` now submits `background_surblserver_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check and legacy delete/save control flow. SURBL-server selection, anti-spam SURBL behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin whitelist-address-delete path. `hm_whitelistaddresses.php` now submits `background_whitelistaddress_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving the existing server-administrator check and terminal redirect. Whitelist-address selection, anti-spam whitelist behavior, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin external-account-delete path. `hm_account_externalaccounts.php` now submits `background_account_externalaccount_save` deletion data and the CSRF token in a POST body, and the delete branch rejects non-POST requests before COM mutation while preserving existing account/domain administrator ownership checks, selected account scope, and terminal redirect. External-fetch configuration, the existing download-now action, non-delete navigation, and other mutation/delete paths remain unchanged.
   - Done: harden the bounded WebAdmin external-account-download-now path. `hm_account_externalaccounts.php` now submits `background_account_externalaccount_save` download-now data and the CSRF token in a POST body, and the download-now branch rejects non-POST requests before selected-item lookup and `DownloadNow()` while preserving existing account/domain administrator ownership checks, selected account scope, and terminal redirect. External-fetch configuration, delete behavior, non-delete navigation, and other mutation paths remain unchanged.
   - Done: harden the bounded WebAdmin external-account add/edit save path. `background_account_externalaccount_save.php` now rejects every non-POST request before scope ID reads or domain/account/fetch-account lookup, uses POST-only accessors for all action/scope/field values, and requires the CSRF token in POST. The existing add/edit form submits the same CSRF token and fields in a POST body. Existing field mapping, account/domain ownership checks, password-retention behavior, save redirect, delete, and download-now behavior remain unchanged. PHP runtime lint was unavailable on this host; deterministic source-surface checks passed.
   - Done: harden the bounded WebAdmin external-account credential-retarget path (`0a667b9fc`). On edit, `background_account_externalaccount_save.php` captures the old `ServerAddress`/`Port`/`Username`/`ConnectionSecurity` tuple before setters, compares strings type-safely and numeric fields as integers, and clears the stored password when the tuple changes with a blank or omitted submitted password. Explicit password replacement takes precedence; unchanged edits and new adds retain their existing behavior. The legacy reference behavior is `InterfaceFetchAccount::get/put_*`, `InterfaceFetchAccount::Save`, `PersistentFetchAccount::ReadObject`, and `PersistentFetchAccount::SaveObject`; the .NET 10 `FetchAccount` mutation surface remains `E_NOTIMPL`. Manual Web form scenarios plus deterministic source-surface checks passed; PHP runtime lint was unavailable.
   - Done: harden the bounded WebAdmin global-rule-delete path. `hm_rules.php` now submits `background_rule_save` deletion data and the CSRF token in a POST body, and the shared rule handler rejects non-POST `savetype=rule`/`domainid=0` deletion attempts after `GetHasRuleAccess` but before global-rule lookup and mutation. The `domainid=0`/`accountid=0` global scope, selected-rule delete/redirect behavior, rule execution, account-rule deletion, criteria/action deletion, non-delete navigation, and other mutation paths remain unchanged.
   - Done: harden the bounded WebAdmin account-rule-delete path. `hm_account.php` uses detached per-rule POST forms after the existing account-edit form, so its visible rule controls submit `background_rule_save` deletion data and the CSRF token without nesting or changing the account form. The shared rule handler rejects non-POST `savetype=rule` requests after `GetHasRuleAccess` but before rule lookup and mutation. Selected domain/account/rule scope, account-rule delete redirect, global-rule deletion, account-edit submission, rule execution, criteria/action deletion, non-delete navigation, and other mutation paths remain unchanged.
   - Done: harden the bounded WebAdmin rule-criteria-delete path. `hm_rule.php` uses detached per-criteria POST forms after the existing rule-edit form, so its visible criteria controls submit `background_rule_save` deletion data and the CSRF token without nesting or changing the rule form. The shared rule handler rejects non-POST `savetype=criteria` requests after `GetHasRuleAccess` but before criteria lookup and mutation. Selected domain/account/rule/criteria scope, rule-criteria delete redirect, global/account-rule deletion, rule-edit submission, rule execution, action deletion, non-delete navigation, and other mutation paths remain unchanged.
   - Done: harden the bounded WebAdmin rule-action-delete path. `hm_rule.php` uses detached per-action POST forms after the existing rule-edit form, so its visible action controls submit `background_rule_save` deletion data and the CSRF token without nesting or changing the rule form. The shared rule handler rejects non-POST `savetype=action` requests after `GetHasRuleAccess` but before action lookup and mutation. Selected domain/account/rule/action scope, rule-action delete redirect, global/account-rule deletion, rule-edit submission, rule execution, criteria deletion, non-delete navigation, and other mutation paths remain unchanged.
   - Done: harden the bounded WebAdmin blocked-attachment-delete path. `hm_smtp_antivirus.php` uses detached per-attachment POST forms after the existing antivirus settings form, so its visible delete controls submit `background_blocked_attachment_save` deletion data and the CSRF token without nesting or changing that settings form. The handler rejects non-POST `action=delete` requests after the existing server-administrator boundary but before settings/collection lookup and mutation. Selected blocked-attachment scope, delete redirect, antivirus settings save, scanner tests, blocked-attachment add/edit, non-delete navigation, and other mutation paths remain unchanged.
   - Done: SEC-18 read-only WebAdmin session-reauthentication design audit. `WEBADMIN_SESSION_REAUTH_DESIGN.md` verifies that every request creates a fresh COM application whose authentication state is instance-local, so PHP cannot simply discard `session_password`. It records an additive service-local opaque-token broker with token/session binding, current-principal refresh, credential-version invalidation, current-user password rotation, direct-activation restrictions, test strategy, and rollout/rollback plan.
   - Done: SEC-18 legacy broker foundation. `Server/COM/WebAdminSessionBroker` is an unregistered native, process-local store with 32-byte random tokens and HMAC-only token/session/credential records, 20-minute idle/8-hour absolute expiry, binding-checked revoke, injected principal/credential hooks, and process-restart invalidation. `COMAuthentication::AttachAuthenticatedPrincipal` is internal only. The `ClassTester` coverage verifies lifecycle, binding mismatch, expiry, revocation, principal/credential denial, restart invalidation, and installed Application IID/CLSID/`Authenticate` signature/DISPID 17. No PHP, public COM, token persistence, password persistence, protocol, or live WebAdmin behavior changed.
   - Done: SEC-18 legacy authoritative-source hooks. `LegacyWebAdminSessionBrokerFactory` binds the native broker to fresh `PersistentAccount::ReadObject`, active `PersistentDomain::ReadObject`, and `IniFileSettings::GetAdministratorPassword` reads. The focused `ClassTester` coverage denies disabled/deleted accounts, domain/admin-level changes, and administrator/account verifier changes. External AD accounts fail closed because this bounded source cannot obtain a current persisted verifier. No broker registration, `InterfaceApplication` factory, PHP, public COM member, token/password persistence, protocol, or live WebAdmin behavior changed.
   - Done: SEC-18 legacy internal-application factory. `LegacyWebAdminApplicationFactory` accepts only an authenticated broker result, creates a fresh existing `InterfaceApplication`, and attaches its private shared `COMAuthentication` before publishing an AddRef-owned `IInterfaceApplication`. The focused `ClassTester` coverage denies null/anonymous authentication, accepts a broker-authenticated administrator, and confirms direct activation remains unauthenticated while the installed Application IID/CLSID/`Authenticate` signature/DISPID 17 remain unchanged. No broker registration, PHP, public COM member, token/password persistence, protocol, or live WebAdmin behavior changed.
   - Done: SEC-18 legacy credential admission. `LegacyWebAdminCredentialAdmission` creates a fresh local `COMAuthentication` for production username/password validation and delegates only a successful legacy principal to the unchanged broker token path. Its native test seam covers accepted, null, throwing, empty-PHP-session, and credential-version-denied outcomes while clearing failed raw tokens. The helper and `SessionRecord` retain no password. Legacy account/domain caches remain part of admission; later token verifier checks use fresh persistence/configuration data. No broker registration, PHP, public COM member, token/password persistence, protocol, or live WebAdmin behavior changed.
   - Done: SEC-18 legacy session-request composition. `LegacyWebAdminSessionRequest::CreateApplication` opens an existing broker's raw token plus PHP-session binding and publishes only a successful authenticated result through `LegacyWebAdminApplicationFactory`. Its `ClassTester` coverage accepts a valid broker-authenticated Application and denies null output, missing/unknown token, wrong session binding, expired token, and revoked token while clearing every caller-provided output pointer. No broker registration, PHP, public COM member, token/password persistence, protocol, or live WebAdmin behavior changed.
   - Done: SEC-18 legacy broker-lifetime owner. `LegacyWebAdminSessionService` creates and retains one broker through `LegacyWebAdminSessionBrokerFactory` per owning native service instance, without exposing the broker itself; it delegates only to the established credential-admission and authenticated-request helpers. Its `ClassTester` coverage admits and opens through the same owner, denies the token through a separate owner as the process-restart model, and fails closed for an injected null broker. It is not wired into service startup, broker registration, PHP, public COM, token/password persistence, protocol, or live WebAdmin behavior.
   - Done: SEC-18 broker-bridge identity and caller-access audit. PHP creates the installed `hMailServer.Application` per request, while the installer only copies PHPWebAdmin and records no IIS worker SID or DCOM ACL. The installed Application shares AppID `{5EDEC473-39E0-43F6-A234-1947071721C8}` with all existing COM registration and the server has no caller-token/SID verification path. A future bridge therefore requires a separate type library, IID/CLSID/ProgID/AppID, broker-only launch/access descriptors, and per-method impersonated caller-SID validation; registration is intentionally blocked pending non-production deployment evidence.
    - Done: SEC-18 non-production deployment inventory. The available test installation has `C:\hMailServer57-Test\PHPWebAdmin`, but its hMailServer service is disabled/stopped and the host has no IIS application-pool configuration, PHP/IIS worker, or authorized caller-token probe. Its installed Application AppID has no per-AppID `LaunchPermission`/`AccessPermission` and falls back to the machine `DefaultLaunchPermission`, which includes `BA`, `IU`, and `SY`. This does not meet the broker caller boundary and intentionally did not start COM or alter deployment state; a dedicated IIS staging host remains required.
    - Done: SEC-18 repeatable dedicated-IIS staging-inventory collector. `build/get-webadmin-broker-staging-inventory.ps1` reads the WebAdmin path, IIS site/application-pool mappings and worker SID, both existing Application AppID registry views plus default DCOM descriptors, and externally produced local caller-token evidence. It emits JSON, can fail incomplete evidence with exit `2`, never starts COM or changes deployment state, and always leaves broker registration subject to security review. Its local focused checks verify the no-IIS result stays incomplete and fail closed.
    - Done: preserve registry `REG_BINARY` DCOM descriptors through `Get-RegistryValueEvidence` so `Convert-SecurityDescriptorToEvidence` receives a single `System.Byte[]` and can decode effective machine DCOM SDDL. Focused read-only `build/test-webadmin-broker-staging-inventory-regbinary.ps1` and parser validation pass; the 2026-07-15 dedicated-IIS run fixed the doubled-backslash virtual-account lookup and proved the pool/PHP process SID, x64 compatibility, temporary AppID readback, and desktop `E_ACCESSDENIED` denial, but the real PHP path and same-token child still fail activation with `0x80070005` before the probe method. The collector remains `Incomplete`/exit `2` without valid server-generated caller-token evidence; both security and reality reviews are RED. Temporary probe registration/endpoints were removed; IIS site/pool/PHP remain isolated and available. No existing hMailServer Application registration, service, database, data directory, or firewall object changed.
    - Done: SPF evaluation and policy foundation with a bounded resolver abstraction, deterministic record parser, RFC result model (`None`, `Neutral`, `Pass`, `Fail`, `SoftFail`, `TempError`, `PermError`), macro expansion, include/redirect/mechanism evaluation, DNS term/void/recursion/MX/PTR limits, system-DNS TXT/A/AAAA/MX/PTR resolver, disabled-by-default SMTP anti-spam policy wiring, and disabled-by-default SPF `Pass` greylisting bypass parity. The policy preserves the safe legacy subset: only `Fail` marks the message with the legacy spam flag, only `Pass` can bypass greylisting when explicitly enabled, and all other results fail open without bypass, reject, or tempfail behavior.
   - Done: DKIM evaluation-only foundation with the legacy result model (`Neutral`, `Pass`, `TempFail`, `PermFail`), deterministic `DKIM-Signature` tag parser, required-field validation, default/simple/relaxed canonicalization selection, signed-header list parsing, `b=` signature-value blanking, simple/relaxed body/header canonicalization tests, `bh=` body-hash verification over canonicalized body data including optional `l=` body-length handling and SHA1/SHA256 hash selection, injected-public-key header cryptographic verification for `rsa-sha1`/`rsa-sha256`, selector TXT public-key lookup behind an injected resolver, message-level orchestration that extracts canonical header/body inputs, evaluates up to the legacy first five `DKIM-Signature` fields, skips unusable signatures as `Neutral`, and returns `Pass` when any signature verifies, plus a disabled-by-default SMTP DKIM policy boundary that consumes the verifier result without changing default SMTP behavior. The policy mirrors the safe legacy spam-test subset: `PermFail` can add the configured DKIM failure spam score/flag when explicitly enabled, `Pass` is preserved as a pass signal, and `Neutral`/`TempFail` fail open without marking spam or rejecting. This does not perform SMTP rejection, signing, or Administrator setting wiring.
   - Done: DMARC evaluation-only foundation with an injected TXT resolver, deterministic record parser for `p`/`sp`/`aspf`/`adkim`/`pct`, RFC-shaped result model (`None`, `Pass`, `Fail`, `TempError`, `PermError`), exact-domain lookup with optional organizational-domain fallback, strict/relaxed SPF and DKIM alignment checks over caller-provided authentication results, subdomain policy selection, temporary DNS failure mapping, and malformed/duplicate record handling.
   - Done: disabled-by-default SMTP DMARC policy/input boundary over the existing evaluator. `HMAILSERVER_DMARC_ENABLED=false` preserves default SMTP behavior; when explicitly enabled, the policy extracts the RFC5322.From domain, consumes SPF and DKIM pass-domain results as data, fails open on malformed input/DNS/runtime errors, and can optionally map DMARC policy failures to the existing spam-flag path without direct SMTP reject/quarantine. DKIM policy results now expose passing signing domains for DMARC alignment.
   - Done: injected offline organizational-domain resolver backed by Nager.PublicSuffix 3.8.0 and a local PSL file. The service uses `HMAILSERVER_DMARC_PUBLIC_SUFFIX_LIST`/`AntiSpam:Dmarc:PublicSuffixListPath`, or `public_suffix_list.dat` beside the executable when present; rules are loaded once lazily, wildcard/exception semantics are regression-covered, and missing/invalid/unreadable rule data fails open to exact-domain DMARC evaluation. A valid list enables parent-record fallback, `sp=` selection, and relaxed sibling alignment without online lookup in the SMTP path.
   - Done: pinned official Public Suffix List snapshot `2026-06-24_06-18-09_UTC` at upstream commit `18ecca5d54471f21918798da451dd8d03a18f3c7` with SHA-256 `8208f0c918c6cb3ab77b484635fc8683c94cbfff818be81950908e881a5f8be2`. Service build/publish package the snapshot and deterministic metadata, an offline build gate verifies headers/hash/length, and an explicit maintainer-only refresh command requires both expected commit and hash. Runtime and SMTP hot paths never download the list.
   - Remaining: DKIM signing/setter/Admin mutation wiring and DMARC enforcement/Admin policy wiring; later Administrator/COM setting parity for SPF/greylisting knobs.
   - Implicit TLS stream factories and listener ports; STARTTLS uses OS default TLS policy and online certificate revocation checks.

6. COM/API compatibility.
   - Preserve existing GUID/ProgID/DISPID/type library contracts.
   - Implement legacy Administrator-visible objects and collections.
   - Done: preserve the legacy dual `IInterfaceMessageIndexing` IID, DISPIDs 1-5, member types, mutability, and `VARIANT_BOOL` marshaling in the .NET COM contract assembly.
   - Done: preserve the `MessageIndexing` CLSID and versioned `hMailServer.MessageIndexing.1` ProgID in a COM-visible class with the legacy default interface and complete v1/v2 runtime delegation.
   - Done: configure the SQL-backed message-indexing runtime in the service, preserve direct-activation access denial, persist legacy `Enabled`, expose counts/status, perform queue-driven clear/index/rebuild, and gate backfill leases while disabled.
   - Done: add the service-process COM local-server class-factory lifecycle and register the direct `MessageIndexing` class object without mutating the registry; process-level activation/revoke coverage preserves the legacy access boundary.
   - Done: preserve complete legacy `Application` and `Account` COM vtable/identity contracts and the server-admin authentication result; unimplemented database-backed account members fail explicitly rather than publishing substitute behavior.
   - Done: preserve the complete legacy `Settings` vtable/identity contract, load the administrator hash from the configured/default legacy `hMailServer.ini`, register the real `Application`, `Settings`, and `MessageIndexing` class factories, return the authorized `Application -> Settings -> MessageIndexing` adapter, keep other Settings members explicit `E_NOTIMPL`, and preserve direct-child access denial across the COM boundary.
   - Done: expose authenticated read-only `Settings.HostName`, `WelcomeSMTP`, `WelcomePOP3`, and `WelcomeIMAP` from existing `hm_settings.settingstring` rows through a bounded administration snapshot/store, preserving installed vtable/DISPIDs and direct-activation access denial while leaving setters, listener reconfiguration, service state, secret settings, and broader Settings mutation out of scope.
   - Done: extend the same bounded Settings snapshot/store with read-only `MaxSMTPConnections`, `MaxPOP3Connections`, `MaxIMAPConnections`, and `MaxDeliveryThreads` from their legacy `hm_settings.settinginteger` rows, preserving the installed COM contract while leaving setters and live listener/delivery-worker reconfiguration out of scope.
   - Done: extend the bounded Settings snapshot/store with read-only `ServiceSMTP`, `ServicePOP3`, and `ServiceIMAP` from the legacy `protocolsmtp`, `protocolpop3`, and `protocolimap` rows, preserving `VARIANT_BOOL` metadata while leaving setters and live listener enable/disable out of scope.
   - Done: expose read-only `Settings.SMTPNoOfTries` and `Settings.SMTPMinutesBetweenTry` from legacy `smtpnoofretries` and `smtpminutesbetweenretries` rows, explicitly excluding the obsolete `smtpnooftries` row while leaving setters and retry scheduling changes out of scope.
   - Done: expose read-only `Settings.MaxMessageSize`, `MaxSMTPRecipientsInBatch`, `DisconnectInvalidClients`, and `MaxNumberOfInvalidCommands` from their existing SMTP guardrail rows, preserving `VARIANT_BOOL` metadata while leaving setters and live SMTP session/listener policy changes out of scope.
   - Done: expose read-only `Settings.IMAPSortEnabled`, `IMAPQuotaEnabled`, `IMAPIdleEnabled`, and `IMAPACLEnabled` from their existing IMAP capability rows, preserving DISPIDs and `VARIANT_BOOL` metadata while leaving setters and live IMAP capability/session changes out of scope.
   - Done: expose read-only `Settings.IMAPSASLPlainEnabled` and `IMAPSASLInitialResponseEnabled` from their existing IMAP SASL rows, preserving DISPIDs 101/102 and `VARIANT_BOOL` metadata while leaving setters and live IMAP authentication/capability changes out of scope.
   - Done: expose read-only `Settings.IMAPPublicFolderName` and `IMAPHierarchyDelimiter` from their existing IMAP naming rows, preserving DISPIDs 74/87 and `BSTR` metadata while leaving setters, folder/rule rename-rewrite, and live IMAP namespace changes out of scope.
   - Done: expose read-only `Settings.AllowSMTPAuthPlain`, `DenyMailFromNull`, `AllowIncorrectLineEndings`, and `AddDeliveredToHeader` from their existing SMTP policy rows, preserving the legacy `DenyMailFromNull` inversion, DISPIDs 8/11/61/73, and `VARIANT_BOOL` metadata while leaving setters and live SMTP authentication/session/delivery changes out of scope.
   - Done: expose read-only `Settings.MirrorEMailAddress`, `DefaultDomain`, and `SMTPDeliveryBindToIP` from their existing SMTP routing string rows, preserving DISPIDs 7/50/51 and `BSTR` metadata while leaving setters, relayer credentials, and live SMTP routing/bind changes out of scope.
   - Done: expose read-only `Settings.RuleLoopLimit`, `WorkerThreadPriority`, `TCPIPThreads`, `MaxAsynchronousThreads`, and `MaxNumberOfMXHosts` from their existing numeric runtime rows, including the legacy `MaxNumberOfAsynchronousTasks` storage name, preserving DISPIDs 48/57/60/88/90 and integer metadata while leaving setters, live rule-loop/thread/work-queue tuning, service reconfiguration, and SMTP MX scheduling out of scope.
   - Done: expose read-only `Settings.VerifyRemoteSslCertificate` and `SslCipherList` from their existing SSL/TLS scalar rows, preserving DISPIDs 93/94 and `VARIANT_BOOL`/`BSTR` metadata while leaving setters, live TLS reload, cipher validation, certificate policy, and TLS version flags out of scope.
   - Done: expose read-only `Settings.IPv6PreferredEnabled` from the existing `IPv6Preferred` row, preserving DISPID 104 and `VARIANT_BOOL` metadata while leaving setters and live network preference changes out of scope.
   - Done: expose read-only `Settings.AutoBanOnLogonFailure`, `MaxInvalidLogonAttempts`, `MaxInvalidLogonAttemptsWithin`, and `AutoBanMinutes` from existing auto-ban rows, preserving DISPIDs 82/83/84/85 and `VARIANT_BOOL`/integer metadata while leaving setters, `ClearLogonFailureList`, live auto-ban policy, security-range mutation behavior, and broader Settings/Admin mutation out of scope.
   - Done: expose authenticated `Settings.ClearLogonFailureList()` through a narrow SQL administration store, preserve parameterless DISPID 86, direct-activation denial, and the server-administrator Settings boundary, and mirror legacy MSSQL `ClearOldFailures(-1)` with `failuretime < DATEADD(minute, 1, GETDATE())` without touching security ranges, auto-ban settings/policy, or failed-logon recording.
   - Done: expose read-only non-secret SMTP relayer getters (`Settings.SMTPRelayer`, `SMTPRelayerRequiresAuthentication`, `SMTPRelayerUsername`, `SMTPRelayerPort`, and `SMTPRelayerConnectionSecurity`) from existing relayer rows, preserving DISPIDs 22/34/35/37/91 and marshaling while omitting `smtprelayerpassword`, leaving `SetSMTPRelayerPassword`, setters, credential handling, and live SMTP delivery routing out of scope.
   - Done: expose authenticated read-only `Settings.SMTPRelayerUseSSL` from the already-loaded relayer connection-security value, preserving legacy true-only-for-direct-TLS semantics, DISPID 71, and `VARIANT_BOOL` metadata while leaving the setter, SQL projection, credential handling, and live relay routing unchanged.
   - Done: expose read-only `Settings.SMTPConnectionSecurity` from existing `SmtpDeliveryConnectionSecurity`, preserving DISPID 92 and `ComConnectionSecurity` enum values while leaving the setter, WebAdmin/Admin mutation, live SMTP delivery routing, delivery-worker reconfiguration, and TLS policy changes out of scope.
   - Done: expose read-only TLS version and option Settings flags from existing `SslVersions`/`TlsOptions` bitmasks (`TlsVersion10Enabled`, `TlsVersion11Enabled`, `TlsVersion12Enabled`, `TlsVersion13Enabled`, `TlsOptionPreferServerCiphersEnabled`, and `TlsOptionPrioritizeChaChaEnabled`), preserving DISPIDs 96/97/98/103/105/106 and `VARIANT_BOOL` metadata while leaving setters, live TLS reload/listener context rebuild, cipher validation, OS TLS policy changes, and broader Settings/Admin mutation out of scope.
   - Done: expose read-only `Settings.IMAPMasterUser` from the existing `ImapMasterUser` string row, preserving DISPID 100 and `BSTR` metadata while leaving the setter, IMAP authentication/master-user behavior, live IMAP configuration reload, secret handling, and broader Settings/Admin mutation out of scope.
   - Done: expose getter-only `Settings.PublicFolderDiskName` as the legacy constant `#Public`, preserving DISPID 79 and `BSTR` metadata while leaving filesystem access, public-folder creation/rename behavior, data-directory reconfiguration, and broader Settings/Admin mutation out of scope.
   - Done: expose read-only `Settings.UserInterfaceLanguage` from the configured/default legacy `hMailServer.ini` `[Settings] UseLanguage` value, preserving the `English` default, DISPID 42, and `BSTR` metadata while leaving the setter, INI persistence, Administrator UI resource reload, and broader Settings/Admin mutation out of scope.
   - Done: expose read-only `Settings.RewriteEnvelopeFromWhenForwarding` from the configured/default legacy `hMailServer.ini` value, preserving the disabled default, DISPID 107, and `VARIANT_BOOL` metadata while leaving the setter, INI persistence, SMTP/rule forwarding behavior, DKIM signing interaction, and broader Settings/Admin mutation out of scope.
   - Done: expose read-only `Settings.CrashSimulationMode` from the process-local runtime snapshot with legacy default `0`, preserving DISPID 99 and integer metadata while leaving the setter, SMTP crash/fault injection, exception-handler test behavior, and broader Settings/Admin mutation out of scope.
   - Done: preserve the legacy `Logging` COM vtable/enum/class identity, register and host its class, and expose authenticated `Settings -> Logging` read-only persisted logging flags, device, format, and AWStats state from existing `hm_settings` rows while keeping direct activation access-denied and leaving setters, live logging, logger reconfiguration, and broader Settings/Admin mutation explicit `E_NOTIMPL`.
   - Done: expose authenticated read-only `Logging.Directory` from the configured/default legacy `hMailServer.ini` `[Directories] LogFolder` value through the existing directory store, preserving DISPID 14, `BSTR` metadata, class identity, and direct-activation access denial without adding file reads, logger reconfiguration, or mutation behavior.
   - Done: expose authenticated read-only `Logging.CurrentEventLog`, `CurrentErrorLog`, `CurrentAwstatsLog`, and `CurrentDefaultLog` using the configured directory plus legacy filename and local-date shapes, preserving DISPIDs 18-21 and `BSTR` metadata while performing no file reads/creation and leaving live logging and logger reconfiguration out of scope.
   - Done: preserve obsolete authenticated `Logging.MaskPasswordsInLog` behavior as a deterministic `false` getter plus no-op setter, matching the legacy no-op intent while keeping direct activation access-denied and avoiding persistence or logger reconfiguration.
   - Done: expose authenticated `Logging.EnableLiveLogging`, `LiveLoggingEnabled`, and `LiveLog` through a shared thread-safe process-local buffer, preserving the installed vtable/DISPIDs, direct-activation access denial, legacy enable/disable clearing, destructive reads, and the 1,000,000-character overflow auto-disable boundary while adding no file reads, persisted setting changes, managed logger-provider reconfiguration, cross-process sharing, or broader mutation support.
   - Done: register a service-side managed `ILoggerProvider` that skips formatting while the existing process-local live buffer is disabled and appends deterministic CRLF-terminated level/category/message/exception records while enabled, reusing destructive COM reads and overflow auto-disable without file logging, persistence changes, COM filter/provider reconfiguration, protocol transcript reconstruction, or cross-process state.
   - Done: replace the `Scripting` placeholder with the complete legacy IID/vtable/class identity and hosted registration, expose authenticated read-only `Enabled`/`Language` from existing `usescriptserver`/`scriptlanguage` rows plus INI-backed `Directory`, preserve direct-activation access denial, and leave setters, reload, syntax compilation, execution, and runtime reconfiguration explicit `E_NOTIMPL`.
   - Done: expose authenticated read-only `Scripting.CurrentScriptFile` from the already-loaded event directory and script language using legacy case-sensitive `EventHandlers.vbs`/`.js`/empty-extension mapping, preserving DISPID 6 and `BSTR` metadata without file access or runtime reconfiguration.
   - Done: expose authenticated `Scripting.CheckSyntax` for only the configured `CurrentScriptFile` through a shell-free, time-bounded Windows Script Host runtime, preserving DISPID 4, `BSTR`, direct-activation denial, legacy missing/empty/valid-file empty-string success, and file-scoped error text while leaving `Reload`, settings persistence, arbitrary paths, and event-executor reconfiguration out of scope.
   - Done: expose authenticated `Scripting.Reload` through a narrow runtime that checks only the configured current file, logs non-empty syntax errors and contained load exceptions with legacy event IDs 5016/5017, and relies on the managed executor's existing per-invocation file load so subsequent events observe updated content without cached script-engine state; settings persistence, arbitrary paths, and COM-thread event execution remain out of scope.
   - Done: replace the `BackupSettings` placeholder with the complete legacy IID/vtable/class identity and hosted registration, expose authenticated read-only `Destination` plus settings/domains/messages/compression bits from existing `backupdestination`/`backupoptions` rows and deterministic `LogFile` from the configured log directory, preserve direct-activation denial, and leave setters, backup/restore execution, filesystem writes, and runtime reconfiguration explicit gaps.
   - Done: replace the `AntiVirus` placeholder with the complete legacy IID/vtable/class identity and hosted registration, expose authenticated `Settings -> AntiVirus` read-only non-secret scalar getters from existing `hm_settings` rows (`avclamwinenable`, `avclamwinexec`, `avclamwindb`, `avaction`, notification flags, custom scanner path/return value, max scan size, attachment-blocking flag, and ClamAV host/port/enabled), preserve legacy invalid-action fallback to delete-email, and leave setters, broad network/process policy, live scanner reconfiguration, and mutations explicit `E_NOTIMPL`.
   - Done: expose authenticated `AntiVirus.TestClamAVScanner(host, port)` through a narrow runtime boundary over the existing time-bounded ClamAV INSTREAM client, preserve installed DISPID 18, direct-activation denial, legacy success flag plus result-text shape, and clean/EICAR test checks without changing antivirus settings, SMTP/external-fetch virus scanning behavior, file/process scanner tests, or broader network/egress policy.
   - Done: expose authenticated `AntiVirus.TestClamWinScanner(executable, database)` through a narrow process/file runtime boundary, preserve installed DISPID 17, direct-activation denial, legacy success flag plus result-text shape, clean/EICAR test checks, ClamWin-style `--database`, filename, and `--tempdir` arguments, and test-file cleanup without changing antivirus settings, SMTP/external-fetch virus scanning behavior, custom scanner command-template execution, live scanner reconfiguration, or broader process policy.
   - Done: expose authenticated `AntiVirus.TestCustomerScanner(command, returnCode)` through a narrow process/file runtime boundary, preserve installed DISPID 16, direct-activation denial, legacy success flag plus result-text shape, clean/EICAR test checks, `%FILE%` substitution, and SEC-04 quote/escaping of the generated test-file path while using shell-free structured process arguments. Antivirus settings, SMTP/external-fetch virus scanning behavior, live scanner reconfiguration, and broader custom-scanner configuration/policy remain unchanged.
   - Done: replace the `BlockedAttachments`/`BlockedAttachment` placeholders with complete legacy IID/vtable/class identities and hosted registration, expose authenticated `Settings -> AntiVirus -> BlockedAttachments` count/index/DBID lookup plus read-only `ID`, `Wildcard`, and `Description` from existing `hm_blocked_attachments` rows ordered by wildcard, and leave add/delete/save/refresh, SMTP attachment-policy behavior changes, process/network/file access, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `BlockedAttachments.Refresh` read-only collection reload through the configured blocked-attachment administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current blocked-attachment rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete-by-DBID/save/delete/setters, scanner test/runtime execution, attachment-policy runtime changes, SQL writes, and broader mutation remain out of scope.
   - Done: replace the `AntiSpam` placeholder with the complete legacy IID/vtable/class identity and hosted registration, expose authenticated `Settings -> AntiSpam` read-only scalar getters from existing `hm_settings` rows for greylisting, HELO/PTR/SPF/MX scores, spam header/subject/threshold policy, SpamAssassin scalar configuration, DKIM verification scalar configuration, and anti-spam max size, preserve obsolete `TarpitDelay`/`TarpitCount` zero getters, and leave setters, DNS/network/file access beyond the bounded operational probes, SMTP policy behavior changes, live reconfiguration, and mutations explicit `E_NOTIMPL`.
   - Done: close the bounded SEC-11 reauthentication parity gap for `Settings -> AntiSpam` operational methods. After failed `Application.Authenticate`, a retained `Settings` facade returns a denied `AntiSpam` child whose `ClearGreyListingTriplets` and `TestSpamAssassinConnection` calls return `E_ACCESSDENIED` without rereading settings, while an already attached child retains legacy usability and invokes each configured runtime exactly once. Direct activation remains denied; anti-spam setters, SMTP policy, scanner egress, and live reconfiguration remain unchanged.
   - Done: expose authenticated `AntiSpam.DKIMVerify(file)` through an injected runtime over the existing message verifier, preserve the installed vtable/DISPID, four-value DKIM enum, direct-activation denial, caller-supplied file argument, legacy 50 MiB oversize-to-`Neutral` behavior, Latin-1 message bytes, bounded system DNS lookup, and exact `Neutral`/`Pass`/`TempFail`/`PermFail` mapping without adding signing, private-key access, SMTP policy changes, settings mutation, or arbitrary command execution.
   - Done: close the bounded SEC-11 reauthentication parity gap for `AntiSpam.DKIMVerify`. Legacy `InterfaceSettings::get_AntiSpam`, `InterfaceAntiSpam::LoadSettings`, and `InterfaceAntiSpam::DKIMVerify` behavior is covered after failed `Application.Authenticate`: a newly obtained child returns `E_ACCESSDENIED` before file/runtime access, while a retained child remains usable and invokes the configured verifier once with the caller-supplied file. Direct activation, `DKIM::Verify` runtime behavior, signing/setters, SMTP policy, and network scope remain unchanged.
   - Done: expose authenticated `AntiSpam.ClearGreyListingTriplets()` through a narrow SQL administration store, preserve the installed parameterless DISPID 29 method and direct-activation denial, execute only the legacy all-row `DELETE FROM hm_greylisting_triplets`, and leave whitelist/settings rows, greylisting policy, SMTP behavior, cleanup scheduling, and all other AntiSpam mutations unchanged.
   - Done: expose authenticated `AntiSpam.TestSpamAssassinConnection(host, port)` through a narrow runtime boundary over the existing time-bounded SpamAssassin client, preserve installed DISPID 36, direct-activation denial, legacy success flag plus result-text shape, and the GTUBE test payload without changing anti-spam settings, SMTP spam policy, SpamAssassin message scanning behavior, or egress policy.
   - Done: preserve and host the legacy `DNSBlackLists`/`DNSBlackList` COM vtable/class identities, expose authenticated `Settings -> AntiSpam -> DNSBlackLists` count/index/DBID/DNS-host lookup plus read-only item `Active`, `ID`, `DNSHost`, `RejectMessage`, `ExpectedResult`, and `Score` from existing `hm_dnsbl` rows ordered by `sblid`, preserve direct-activation denial and legacy missing-item results, and leave add/delete/save/refresh, DNS lookup/test execution, SMTP policy changes, live reconfiguration, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `DNSBlackLists.Refresh` read-only collection reload through the configured DNS blacklist administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID/DNS-host lookup, and item getters observe current DNS blacklist rows while preserving missing DNS-host lookup results. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete-by-DBID/save/delete/setters, DNS lookup execution, SMTP policy/runtime changes, SQL writes, and broader mutation remain out of scope.
   - Done: preserve and host the legacy `SURBLServers`/`SURBLServer` COM vtable/class identities, expose authenticated `Settings -> AntiSpam -> SURBLServers` count/index/DBID/DNS-host lookup plus read-only item `Active`, `ID`, `DNSHost`, `RejectMessage`, and `Score` from existing `hm_surblservers` rows ordered by `surblid`, preserve direct-activation denial and legacy missing-item results, and leave add/delete/save/refresh, DNS lookup/test execution, SMTP policy changes, live reconfiguration, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `SURBLServers.Refresh` read-only collection reload through the configured SURBL server administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID/DNS-host lookup, and item getters observe current SURBL server rows while preserving missing DNS-host lookup results. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete-by-DBID/save/delete/setters, DNS lookup execution, SMTP policy/runtime changes, SQL writes, and broader mutation remain out of scope.
   - Done: close the bounded SEC-11 reauthentication parity gap for retained `Settings -> AntiSpam -> SURBLServers`. Legacy `InterfaceAntiSpam::get_SURBLServers`, `InterfaceSURBLServers::LoadSettings`, and collection access order are covered after failed `Application.Authenticate`: a retained `AntiSpam` facade returns a denied new SURBL collection without rereading the store, while a collection captured before reauthentication remains usable without a fresh principal check. Direct activation, COM identity, DNS lookup, SMTP policy, setters, save/delete, and live reconfiguration remain unchanged.
   - Done: preserve and host the legacy `GreyListingWhiteAddresses`/`GreyListingWhiteAddress` COM vtable/class identities, expose authenticated `Settings -> AntiSpam -> GreyListingWhiteAddresses` count/index/DBID/name lookup plus read-only item `ID`, `IPAddress`, and `Description` from existing `hm_greylisting_whiteaddresses` rows ordered by stored IP, preserve legacy SQL-LIKE-to-wildcard and bigint-to-COM-ID projections, direct-activation denial, and `DISP_E_BADINDEX`, and leave add/delete/save/refresh, greylisting policy changes, live reconfiguration, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `GreyListingWhiteAddresses.Refresh` read-only collection reload through the configured greylisting white-address administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID/name lookup, and item getters observe current greylisting whitelist rows while preserving stored SQL-LIKE name lookup and wildcard conversion. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete-by-DBID/save/delete/setters, greylisting policy/runtime changes, SQL writes, and broader mutation remain out of scope.
   - Done: close the bounded SEC-11 reauthentication parity gap for retained `Settings -> AntiSpam -> GreyListingWhiteAddresses`. Legacy `InterfaceAntiSpam::get_GreyListingWhiteAddresses`, `InterfaceGreyListingWhiteAddresses` collection access, and `InterfaceGreyListingWhiteAddress` item lifetime are covered after failed `Application.Authenticate`: a newly obtained AntiSpam child denies before store access, a retained AntiSpam creates a new collection with the expected read, and pre-reauthentication collection/item getters remain usable without rereads. Item save/delete, collection mutation, refresh failure semantics, greylisting policy, and live reconfiguration remain unchanged.
    - Done: preserve and host the legacy `WhiteListAddresses`/`WhiteListAddress` COM vtable/class identities, expose authenticated `Settings -> AntiSpam -> WhiteListAddresses` count/index/DBID lookup plus read-only item `ID`, lower/upper IP, email, and description from existing `hm_whitelist` rows in lower-IP numeric order, preserve IPv4/IPv6 two-column conversion, bigint-to-COM-ID projection, direct-activation denial, and `DISP_E_BADINDEX`, and leave add/delete/save/refresh/clear, SMTP whitelist policy changes, live reconfiguration, and mutations explicit `E_NOTIMPL`.
    - Done: implement authenticated legacy `WhiteListAddresses.Refresh` read-only collection reload through the configured whitelist address administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current whitelist rows while preserving IPv4/IPv6 two-column conversion, lower-IP numeric ordering, and bigint-to-COM-ID projection. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete-by-DBID/save/delete/Clear/setters, SMTP whitelist policy/runtime changes, SQL writes, and broader mutation remain out of scope.
    - Done: close the bounded SEC-11 reauthentication parity gap for retained `Settings -> AntiSpam -> WhiteListAddresses`. Legacy `InterfaceAntiSpam::get_WhiteListAddresses`, `InterfaceWhiteListAddresses`, and `InterfaceWhiteListAddress` lifetime/access ordering are covered after failed `Application.Authenticate`: a newly obtained AntiSpam child denies before whitelist store access, a retained AntiSpam creates a new collection with the expected store read, and pre-reauthentication collection/item getters remain usable without rereads. Item save/delete, collection mutation, refresh behavior, SMTP whitelist policy, and live reconfiguration remain unchanged. Focused WhiteList/AntiSpam/SQL coverage is 23/23; full Net10 coverage is 1066/1066.
   - Done: replace the `BackupManager` placeholder with the complete legacy IID/vtable/class identity and hosted registration, expose authenticated `Application -> BackupManager`, preserve direct-activation denial, and keep `StartBackup`, `LoadBackup`, archive parsing, filesystem access, restore behavior, and event dispatch explicit `E_NOTIMPL`.
   - Done: preserve and host the complete legacy `Backup` IID/vtable/class identity, implement authenticated `BackupManager.LoadBackup` by streaming only the named `hMailServerBackup.xml` entry from legacy 7z archives through the packaged shell-free `7za.exe` boundary, parse Mode bits 1/2/4 with DTD disabled, expose contains flags and process-local restore selections, and leave `StartBackup`, `StartRestore`, database/data-directory writes, restore execution, and backup events out of scope.
   - Done: add a read-only rollback archive integrity preflight before `install-net10-service.ps1 -ReplaceExisting` can register COM or mutate the Windows service. The gate requires an explicit `-BackupArchive` for replacement of a stopped service pointing to another executable, uses the packaged shell-free `7za.exe` full archive test plus bounded metadata streaming, rejects timeout/nonzero archive-test exit, malformed or DTD XML, missing metadata, incomplete option masks, and non-`7z` `DataFiles`, and leaves extraction, SQL, production data, backup execution, restore, and events out of scope. Focused PowerShell 5.1/7 tests and the full 1066-test Net10 suite pass.
   - Done: harden that rollback preflight with a bounded shell-free 7z technical listing. The gate now requires the compressed legacy `DataBackup` directory entry, accepts an empty copied data directory, rejects file-shaped/missing payloads and absolute or parent-traversal archive paths, and keeps installer ordering, COM identity, authentication, SQL, extraction, restore, and production data untouched. Focused PowerShell 7 and Windows PowerShell 5.1 tests pass; the full Net10 suite remains 1066/1066.
   - Done: replace the `GlobalObjects` placeholder and add the complete legacy `DeliveryQueue` IID/vtable/class identities plus hosted registration, expose authenticated `Application -> GlobalObjects -> DeliveryQueue`, preserve direct-activation denial and 64-bit message-ID method signatures, and leave queue mutations, worker wake-up, language collections, and broader runtime behavior explicit `E_NOTIMPL`.
   - Done: replace the remaining `GlobalObjects.Languages` placeholder with complete legacy `Languages`/`Language` IID/vtable/class identities plus hosted registration, enumerate local `.ini` files from the executable-directory `Languages` folder through `[GUILanguages] ValidLanguages`, preserve lower-case names, deterministic order, `DISP_E_BADINDEX`, direct-activation denial, and expose read-only `Name`, `IsDownloaded`, and translated `String` with English fallback while keeping `Download`, network access, file writes, and live reload out of scope.
   - Done: implement authenticated `DeliveryQueue.ResetDeliveryTime` through a narrow SQL Server administration store that changes only matching type-1/type-3 `hm_messages` rows to `messagetype = 1` with an immediately eligible UTC next-try timestamp, preserves 64-bit IDs and retry/lease/recipient/content state, and leaves clear/remove/start behavior pending.
   - Done: make durable outbound delivery operational under one service-owned hosted worker that runs the existing lease-aware processor, immediately drains full batches, waits on a coalescing signal with the legacy one-minute idle poll fallback, recovers from batch failures, honors shutdown cancellation, and exposes authenticated `DeliveryQueue.StartDelivery` as a non-mutating wake operation while preserving direct-activation denial and leaving clear/remove pending.
   - Done: route production SMTP primary and rule-generated queue writes through a signaling writer decorator that wakes the hosted delivery worker only after the durable SQL/file writer completes, emits no signal for failed or canceled writes, and treats post-commit signal failure as best-effort without changing SMTP acceptance, queue SQL, file cleanup, or lease/retry behavior.
   - Done: close the source queue-file lifecycle after successful lease-owned processor completion by deleting through the path-contained content-store boundary only after database completion succeeds for normal delivery, event drops, and no-target completion, while preserving files on defer/release/load failure/cancellation/lost lease and treating delete I/O failure as best-effort.
   - Done: implement authenticated `DeliveryQueue.Remove` through a narrow SQL Server administration operation that row-locks one type-1/type-3 queue message, skips current owner leases and delivered rows, atomically deletes recipients plus the message, performs contained best-effort file cleanup after commit, preserves 64-bit IDs/direct-activation denial, and treats missing/ineligible IDs as silent no-ops.
   - Done: implement authenticated `DeliveryQueue.Clear` through a process-local single-flight coordinator that returns promptly from COM, coalesces repeated calls, drains bounded SQL batches with active-owner-lease/delivered-row exclusion, atomically deletes recipients/messages, performs contained best-effort file cleanup, reports completion/failure, and honors shutdown cancellation.
   - Done: generate the authoritative legacy type library during service build/publish and add guarded service install/uninstall wiring for the hosted classes' AppID, CLSIDs, LocalServer32 paths, versioned/version-independent ProgIDs, CurVer aliases, and 64-bit type-library registration; manifest tests do not mutate the machine registry.
   - Done: add explicit opt-in SQL Server integration coverage that creates/drops an isolated database and exercises the authenticated `Application -> Settings -> MessageIndexing` status, enable, queue, clear, and index path against the real store.
   - Done: preserve the full legacy `Domains` and `Domain` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Application -> Domains` count/index/name/id lookup from existing `hm_domains` SQL data, keep direct child activation access-denied, and leave mutations plus nested collections explicit `E_NOTIMPL`.
   - Done: expose authenticated read-only `Domains.Names` from the already-loaded domain snapshots using the legacy tab-separated `id\tname\tactive\r\n` string shape, while keeping collection mutation and broader domain mutation out of scope.
   - Done: implement authenticated legacy `Domains.Refresh` read-only collection reload through the configured domain administration store, atomically replacing only the authorized COM collection's process-local snapshot so `Count`, `Names`, index/name/DBID lookup, and item getters observe current rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters and broader mutation remain out of scope.
   - Done: preserve the legacy `Accounts` collection COM vtable/identity contract, register the hosted `Accounts`/`Account` identities, expose authenticated `Domain -> Accounts` count/index/address/id lookup from existing `hm_accounts` SQL data, keep direct activation access-denied, and leave account mutations plus deeper child collections explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `Accounts.Refresh` read-only collection reload through the configured account administration store for the selected parent domain, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/address/DBID lookup, and item getters observe current rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters, password/AD/auth behavior, and broader mutation remain out of scope.
   - Done: preserve the legacy `Aliases` and `Alias` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Domain -> Aliases` count/index/name/id lookup from existing `hm_aliases` SQL data, keep direct activation access-denied, and leave alias mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `Aliases.Refresh` read-only collection reload through the configured alias administration store for the selected parent domain, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/name/DBID lookup, and item getters observe current alias rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters, domain-alias refresh, and broader mutation remain out of scope.
   - Done: preserve the legacy `DistributionLists` and `DistributionList` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Domain -> DistributionLists` count/index/address/id lookup and read-only list scalars from existing `hm_distributionlists` SQL data, keep direct activation access-denied, and leave list mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `DistributionLists.Refresh` read-only collection reload through the configured distribution-list administration store for the selected parent domain, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/address/DBID lookup, and item getters observe current distribution-list rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters, recipient refresh, and broader mutation remain out of scope.
   - Done: preserve the legacy `DistributionListRecipients` and `DistributionListRecipient` COM vtable/identity contracts, register their hosted class identities, expose authenticated `DistributionList -> Recipients` count/index/id lookup and read-only recipient address from existing `hm_distributionlistsrecipients` SQL data, keep direct activation access-denied, and leave recipient mutations explicit `E_NOTIMPL`.
   - Done: preserve the legacy `DomainAliases` and `DomainAlias` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Domain -> DomainAliases` count/index/id lookup and read-only alias/domain-id scalars from existing `hm_domain_aliases` SQL data, keep direct activation access-denied, and leave domain-alias mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `DomainAliases.Refresh` read-only collection reload through the configured domain-alias administration store for the selected parent domain, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current domain-alias rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters, alias refresh, and broader mutation remain out of scope.
   - Done: extend the authenticated SQL-backed `Domain` adapter with selected read-only core detail scalars from `hm_domains` (`Postmaster`, max message size, plus-addressing fields, domain/account/list quota and limitation flags), while keeping scalar mutations plus behavior-heavy/computed fields explicit `E_NOTIMPL`.
   - Done: extend the authenticated SQL-backed `Domain` adapter with the existing read-only greylisting anti-spam flag from `hm_domains.domainantispamoptions` as `AntiSpamEnableGreylisting`, while keeping the setter, SMTP policy behavior, and runtime greylisting changes out of scope.
   - Done: extend the authenticated SQL-backed `Domain` adapter with existing read-only signature configuration from `hm_domains` (`SignatureEnabled`, signature method, plain-text/HTML signature text, add-to-replies, and add-to-local-mail) while keeping setters, message mutation, SMTP signature application, and migrations out of scope.
   - Done: extend the authenticated SQL-backed `Domain` adapter with existing read-only DKIM configuration from `hm_domains` (`DKIMSignEnabled`, selector, private-key file path, header/body canonicalization, signing algorithm, and alias signing) while keeping setters, signing, private-key file-content access, SMTP policy changes, and migrations out of scope.
   - Done: extend the authenticated SQL-backed `Domain` adapter with the existing read-only Active Directory domain name from `hm_domains.domainaddomain` as `ADDomainName`, while keeping the setter, AD synchronization, and authentication behavior out of scope.
   - Done: extend the authenticated SQL-backed `Domain` adapter with the read-only legacy `AllocatedSize` aggregate from the selected domain's `hm_accounts.accountmaxsize` values, while keeping `Domain.Size`, quota enforcement, account mutation, filesystem/mailbox scans, and broader domain mutation behavior out of scope.
   - Done: extend the authenticated SQL-backed `Domain` adapter with the read-only legacy MSSQL `Size` aggregate from `hm_messages.messagesize`, preserving the legacy `messageaccountid IN (SELECT accountdomainid ...)` SQL shape and MB truncation while keeping quota enforcement, account mutation, filesystem/mailbox scans, and broader domain mutation behavior out of scope.
   - Done: extend the authenticated SQL-backed `Account` adapter with selected read-only core detail scalars from `hm_accounts` (`MaxSize`, `PersonFirstName`, `PersonLastName`), while keeping password/security-sensitive fields, behavior-heavy fields, child collections, and scalar mutations explicit `E_NOTIMPL`.
   - Done: extend the authenticated SQL-backed `Account` adapter with selected read-only delivery/detail scalars from `hm_accounts` (vacation/autoreply, forwarding, and signature fields), while keeping behavior execution, password/security-sensitive fields, child collections, and scalar mutations explicit `E_NOTIMPL`.
   - Done: extend the authenticated SQL-backed `Account` adapter with the read-only legacy `Size` aggregate from the selected account's `hm_messages.messagesize` values, returning the legacy 3-decimal MB float while keeping quota enforcement, account mutation, filesystem/mailbox scans, and broader account behavior out of scope.
   - Done: extend the authenticated SQL-backed `Account` adapter with the read-only legacy `QuotaUsed` computed scalar from the selected account's `hm_messages.messagesize` byte total and `accountmaxsize` MB limit, matching legacy integer percentage truncation while keeping quota enforcement, account mutation, filesystem/mailbox scans, and broader account behavior out of scope.
   - Done: extend the authenticated SQL-backed `Account` adapter with the read-only legacy `LastLogonTime` scalar from `hm_accounts.accountlastlogontime`, preserving the installed COM contract and returning the configured SQL value while keeping login-time updates, authentication behavior, account mutation, and broader account behavior out of scope.
   - Done: extend the authenticated SQL-backed `Account` adapter with non-secret read-only Active Directory scalars from `hm_accounts` (`IsAD`, `ADDomain`, and `ADUsername`), while keeping setters, Active Directory authentication, password/security-sensitive fields, account mutation, and broader account behavior out of scope.
   - Done: preserve the legacy `FetchAccounts` and `FetchAccount` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Account -> FetchAccounts` count/index/id lookup and read-only non-secret fetch-account scalars from existing `hm_fetchaccounts` SQL data, keep direct activation access-denied, and leave password access, download execution, and mutations explicit `E_NOTIMPL`; focused contract/store/integration coverage now locks legacy enum values, the direct-TLS-only `UseSSL` alias, non-secret SQL projection order, read-only mutation boundaries, and account-scoped DBID isolation.
   - Done: implement authenticated legacy `FetchAccounts.Refresh` read-only collection reload through the configured fetch-account administration store for the selected parent account, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current non-secret rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; delete/download/password/save/setters, external POP3 execution, and broader mutation remain out of scope.
   - Done: close the bounded .NET direct-activation evidence gap for `FetchAccount`/`FetchAccounts`. The installed CLSID/IID/ProgID/DISPID/vtable shape is unchanged, while registered-class-style `Activator.CreateInstance` item getter/setter, `Save`, `Delete`, and `DownloadNow`, plus every directly constructed collection lookup, `Refresh`, `Delete`, `DeleteByDBID`, and `Add`, returns `E_ACCESSDENIED` before snapshot or store access. Authenticated `Account -> FetchAccounts` DBID lookup remains valid before and after refresh. The legacy references are `InterfaceFetchAccount::InterfaceFetchAccount`, `InterfaceFetchAccounts::get_ItemByDBID`, `get_Item`, and `Add`: the C++ constructor creates a detached `HM::FetchAccount`, while collection paths attach items to an owning `HM::FetchAccounts`; the .NET rewrite intentionally preserves its direct-activation denial boundary. No legacy C++ registration, authenticated collection access, password/download behavior, or fetch-account mutation was changed.
   - Done: preserve the legacy `Rules` and `Rule` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Account -> Rules` count/index/id lookup and read-only ID/account/name/active/use-AND scalars from existing `hm_rules` SQL data, keep direct activation access-denied, and leave execution and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy account-scoped `Rules.Refresh` read-only collection reload through the configured rule administration store for the selected parent account, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; criterias/actions refresh, rule execution behavior, add/delete/save/setters, and broader mutation remain out of scope.
   - Done: reuse the preserved `Rules`/`Rule` adapters and rule administration store for authenticated `Application -> Rules`, selecting global `hm_rules` rows with `ruleaccountid = 0` while preserving the server-administrator boundary, account/global isolation, read-only scalars, and explicit execution/mutation gaps.
   - Done: implement authenticated legacy global `Application -> Rules.Refresh` read-only collection reload through the configured rule administration store for `ruleaccountid = 0`, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; account-rule behavior, criterias/actions refresh, rule execution behavior, add/delete/save/setters, and broader mutation remain out of scope.
   - Done: preserve the legacy `RuleCriterias` and `RuleCriteria` COM vtable/identity contracts plus rule-field/match-type enum GUIDs and values, register their hosted class identities, expose authenticated `Rule -> Criterias` count/index/DBID lookup and criterion scalars from existing `hm_rule_criterias` SQL data filtered to the selected rule and ordered by `criteriaid`, keep direct activation access-denied, and leave Add, setters, rule execution behavior changes, and broader mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `RuleCriterias.Refresh` read-only collection reload through the configured rule criteria administration store for the selected parent rule, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current criterion rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; Add, Save/setters, action refresh, rule execution behavior, and broader mutation remain out of scope.
   - Done: implement SEC-11 existing-row `RuleCriterias.DeleteByDBID` parity (`2da6a368c`). The authorized collection checks membership in the selected parent rule's snapshot before invoking a parameterized `DELETE` with both `criteriaruleid = @RuleId` and `criteriaid = @CriteriaId`; success removes only that snapshot item, foreign/unknown/repeated IDs no-op, and contained store failure maps to COM `E_FAIL` while retaining the snapshot. Direct activation remains `E_ACCESSDENIED` with zero store work, retained attached collections preserve legacy no-recheck behavior after failed reauthentication, and Add, Save/setters, RuleActions, rule execution, SMTP/delivery behavior, and broader mutation remain out of scope. Focused COM/store tests are 12/12 and full Net10 is 1080/1080; no live SQL Server deletion integration was run.
   - Done: implement authenticated `RuleCriteria.Delete()` ownership parity (`f537bd8aa`). Legacy `InterfaceRuleCriteria::Delete` delegates to its attached parent `RuleCriterias` collection, which owns rule-scoped persistence and snapshot removal. Authorized .NET item facades now retain the owning collection delete path; successful deletion removes only that parent snapshot item, repeated/stale item deletion no-ops, contained store failure maps to `E_FAIL` with the item retained, and direct activation remains `E_ACCESSDENIED`. Add, Save, setters, rule execution, SMTP/delivery behavior, and broader mutation remain out of scope. Focused RuleCriterias/Rules/store tests pass 20/20; no live SQL Server deletion integration was run.
   - Done: implement authenticated legacy `RuleCriterias.Delete(index)` parity (`a209e2cc1`). `InterfaceRuleCriterias::Delete` in `hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp` delegates to the owning `Collection::DeleteItem` in `hmailserver/source/Server/Common/BO/Collection.h`, which invokes `PersistentRuleCriteria::DeleteObject` in `hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp`; negative and out-of-range indices silently no-op while valid indices return `S_OK` after deletion. The authorized .NET collection uses the selected rule snapshot and scoped store path, removes only after store success, maps contained store failure to `E_FAIL` while retaining the snapshot, and preserves direct activation denial and the installed COM contract. Add, Save/setters, RuleCriteria item behavior, rule execution, SMTP/delivery behavior, and broader mutation remain out of scope. Focused RuleCriterias/Rules/store tests pass 22/22; full Net10 with native integration passes 1141/1141; no live SQL Server deletion integration was run.
   - Done: implement authenticated existing-row `RuleCriteria.Save()` parity (`e1c74c9a4`). Legacy `InterfaceRuleCriteria::Save` (`hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:30`) persists only when attached to an owning rule through `PersistentRuleCriteria::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp:65`), updating `criteriaruleid`, `criteriausepredefined`, `criteriapredefinedfield`, `criteriaheadername`, `criteriamatchtype`, and `criteriamatchvalue` for the existing `criteriaid`. The authorized .NET item keeps the installed COM identity and direct-activation denial, uses the owning rule snapshot and a parameterized rule-plus-criteria SQL update, maps contained store failure to `E_FAIL` without losing the readable item for retry, and preserves the authenticated Settings/rule ownership boundary. Setter staging, Add/new-item Save, and broader rule mutation remain out of scope. Focused COM/store tests pass 25/25; full Net10 with native integration passes 1144/1144; no live SQL Server integration was run.
   - Done: preserve the legacy `RuleActions` and `RuleAction` COM vtable/identity contracts plus action-type enum GUID/values, register their hosted class identities, expose authenticated `Rule -> Actions` count/index/DBID lookup and all persisted read-only action scalars from existing `hm_rule_actions` SQL data filtered to the selected rule and ordered by `actionsortorder`, keep direct activation access-denied, and leave add/delete/`RuleID` and remaining setters/reordering, rule execution behavior changes, new-item persistence, and broader mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `RuleActions.Refresh` read-only collection reload through the configured rule action administration store for the selected parent rule, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current action rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/Save/`RuleID` and remaining setters, reordering, rule execution behavior, and broader mutation remain out of scope.
   - Done: implement `RuleActions.DeleteByDBID` existing-row parity (`449d0a692`). Legacy `InterfaceRuleActions::DeleteByDBID` at `Server/COM/InterfaceRuleActions.cpp:125` delegates to `Collection::DeleteItemByDBID` at `Server/Common/BO/Collection.h:181`, which persists through `PersistentRuleAction::DeleteObject` at `Server/Common/Persistence/PersistentRuleAction.cpp:121` and silently no-ops for an unknown ID. The authorized .NET collection now requires selected-rule snapshot membership, executes a parameterized `actionruleid` plus `actionid` delete, removes only after the store call succeeds, maps contained store failure to `E_FAIL`, and preserves direct activation denial and the installed `RuleActions`/`RuleAction` COM contract. Add, index-based Delete, item Delete/Save/setters, reordering, rule execution, SMTP/delivery behavior, and broader mutation remain out of scope. Focused COM/store tests pass 16/16; no live SQL Server deletion integration was run.
   - Done: implement `RuleActions.Delete(index)` parity (`181ffa958`). Legacy `InterfaceRuleActions::Delete` (`Server/COM/InterfaceRuleActions.cpp:142`) delegates to the owning `Collection::DeleteItem` (`Server/Common/BO/Collection.h:218`) and `PersistentRuleAction::DeleteObject` (`Server/Common/Persistence/PersistentRuleAction.cpp:121`); valid zero-based indices return `S_OK`, while negative/out-of-range indices silently no-op. The authorized .NET collection reuses the rule/action-scoped delete delegate, removes only the selected snapshot item after store success, maps contained failure to `E_FAIL`, and preserves direct activation denial. RuleAction item Delete, Add, Save, setters, reordering, rule execution, SMTP/delivery behavior, and broader mutation remain out of scope. Focused RuleActions/Rules/store tests pass 18/18; no live SQL Server deletion integration was run.
    - Done: implement authenticated `RuleAction.Delete()` ownership parity (`18267d2f3`). Legacy `InterfaceRuleAction::Delete` (`Server/COM/InterfaceRuleAction.cpp`) delegates to its attached parent `RuleActions` collection, which owns the rule-scoped persistence and snapshot removal. Authorized .NET item facades now retain their owning collection delete path; successful deletion removes only that parent snapshot item, repeated/stale item deletion no-ops, contained store failure maps to `E_FAIL` with the item retained, and direct activation remains `E_ACCESSDENIED`. Add, Save, setters, MoveUp, MoveDown, rule execution, SMTP/delivery behavior, and broader mutation remain out of scope. Focused RuleActions/Rules/store tests pass 20/20; no live SQL Server deletion integration was run.
   - Done: implement authenticated existing-row `RuleAction.Save()` parity (`5e24a9bda`). Legacy `InterfaceRuleAction::Save` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:30`) preserves direct null-object access denial, the server-administrator guard for script actions, parent-rule attachment, and persistence through `PersistentRuleAction::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleAction.cpp:72`). The legacy update writes the existing action's rule/type/IMAP folder/subject/from/to/body/filename/sort/script/header/value/route/abort-spam fields; the authorized .NET facade reissues only its owning rule snapshot through a parameterized `actionruleid` plus `actionid` update, maps contained store failure to `E_FAIL`, and retains the readable item for retry. Installed `RuleActions`/`RuleAction` identity, direct activation denial, and authenticated administrator boundary remain unchanged. Add/new-item Save, `RuleID` and remaining setters, MoveUp, MoveDown, reordering, rule execution, SMTP/delivery behavior, and broader mutation remain out of scope. Focused COM/store tests pass 23/23; full Net10 with native integration passes 1147/1147; no live SQL Server integration was run.
   - Done: implement authenticated existing-row `RuleAction.Type` setter parity (`8b608f396`). Legacy `InterfaceRuleAction::put_Type` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:155`) returns `E_ACCESSDENIED` for detached objects and for `eRARunScriptFunction` when `GetIsServerAdmin()` is false (`:162`); otherwise it stages the enum and returns `S_OK`. The authorized .NET item facade stages `Type` only when backed by the owning collection's Save path, rejects the script-action transition through the administrator predicate, and persists the staged value through existing-row `Save()`. Direct activation, installed COM identity, Add/new-item persistence, `RuleID` and remaining setters, ordering, rule execution, SMTP/delivery behavior, and broader mutation remain unchanged. Focused COM/store tests pass 25/25; full Net10 with native integration passes 1149/1149; no live SQL Server integration was run.
   - Done: implement authenticated existing-row `RuleAction.ScriptFunction` setter parity (`47f33df20`). Legacy `InterfaceRuleAction::put_ScriptFunction` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:415`) returns `E_ACCESSDENIED` for detached objects and when `GetIsServerAdmin()` is false (`:419`), otherwise stages the raw `SetScriptFunction(newVal)` value and returns `S_OK` without validation or normalization; existing `InterfaceRuleAction::Save` and `PersistentRuleAction::SaveObject` persist it through the `actionscriptfunction` column. The authorized .NET item facade applies the existing administrator predicate, stages the exact string only through the owning collection's Save path, and reuses the existing parameterized full-row update. Direct activation, installed COM identity, Add/new-item persistence, `RuleID` and remaining setters, ordering, rule execution, SMTP/delivery behavior, and broader mutation remain unchanged. Focused COM/store tests pass 27/27; full Net10 reached 1149/1151 with only unrelated ClamWin/CustomScanner cleanup failures (`UnauthorizedAccessException` removing generated `.eml` files); no live SQL Server integration was run.
   - Done: preserve the legacy `IMAPFolders` and `IMAPFolder` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Account -> IMAPFolders` top-level count/index/name/id lookup plus parent-scoped `IMAPFolder -> SubFolders` child collections ordered by folder ID from existing `hm_imapfolders` SQL data, expose read-only ID/parent/name/subscribed/current-UID/creation-time scalars, preserve legacy modified UTF-7 folder-name conversion, keep direct activation access-denied, leave folder mutations explicit `E_NOTIMPL`, and preserve the legacy public-folder-only error for private account-folder permission access.
   - Done: preserve the legacy `Messages` and `Message` COM vtable/identity contracts plus `eMessageFlag` enum values, register their hosted class identities, expose authenticated `Account -> Messages` and `IMAPFolder -> Messages` count/index/DBID lookup from existing delivered `hm_messages` rows scoped to the selected account or folder, expose read-only safe metadata getters (`ID`, `Filename`, `FromAddress`, `State`, `Size`, `DeliveryAttempt`, `InternalDate`, `UID`, and flag reads), and add controlled file-backed read-only MIME getters (`Subject`, `From`, `Date`, `To`, `CC`, `Body`, `HTMLBody`, `HeaderValue`, `HasBodyType`, `Charset`) plus read-only `Headers`, `Recipients`, and `Attachments` child collections. Direct activation remains access-denied; `Save`, `Delete`, `Clear`, `RefreshContent`, `Copy`, `Add`, scalar/header/recipient/attachment mutations, flag mutation, delivery/rescan side effects, message-file writes, and broader message mutation remain explicit `E_NOTIMPL`.
   - Done: reuse the preserved `IMAPFolders`/`IMAPFolder` adapters and root-folder SQL store for authenticated `Settings -> PublicFolders`, selecting only `folderaccountid = 0` public roots while preserving the existing access-denied, read-only scalar, and explicit nested-behavior boundaries.
   - Done: preserve the legacy `IMAPFolderPermissions` and `IMAPFolderPermission` COM vtable/identity contracts plus ACL enum GUIDs/values, register their hosted class identities, expose authenticated public-folder `IMAPFolder -> Permissions` count/index/DBID/legacy-name lookup and read-only ACL scalars/permission flags from existing `hm_acl` SQL data filtered to the selected folder and ordered by `aclid`, expose read-only `Account`/`Group` child objects for existing user/group principals while preserving `DISP_E_BADINDEX` for missing/zero principal IDs, preserve the legacy public-folder-only error for account folders, keep direct activation access-denied, and leave add/delete/refresh/save, setters, account/group mutations, ACL policy/runtime changes, and SQL mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy public `IMAPFolderPermissions.Refresh` read-only collection reload through the configured IMAP folder administration store for the selected public folder, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID/name lookup, item getters, and read-only `Account`/`Group` child access observe current ACL rows. Direct activation remains access-denied; private account-folder permission access keeps the existing public-folder-only COM error; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete-by-DBID/save/delete/setters, ACL policy/runtime changes, SQL writes, and broader mutation remain out of scope.
   - Done: preserve the legacy `Cache` COM vtable/identity contract, register its hosted class identity, expose authenticated `Settings -> Cache` read-only `Enabled` and domain/account/alias/distribution-list TTL scalars from existing `hm_settings` rows, keep direct activation access-denied, and leave hit-rate/current-size/max-size runtime stats, `Clear`, setters, persistence, and live cache reconfiguration explicit `E_NOTIMPL`.
   - Done: expose authenticated legacy `Settings -> Cache.Clear` through a narrow process-local runtime boundary that clears host-provided .NET 10 cache state when configured and otherwise no-ops because the rewrite does not currently populate shared admin cache state. Direct activation remains access-denied; contained runtime failure maps to COM `E_FAIL`; hit-rate/current-size/max-size stats, cache setters, persistence, SQL writes, and live cache reconfiguration remain out of scope.
   - Done: expose authenticated legacy `Settings -> Cache` hit-rate/current-size/max-size getters through the same process-local cache runtime boundary, returning host-provided statistics when configured and deterministic zero values otherwise because the rewrite does not currently populate shared admin cache state. Direct activation remains access-denied; contained runtime failure maps to COM `E_FAIL`; `Cache.Clear`, cache setters, persistence, SQL writes, and live cache reconfiguration remain unchanged.
   - Done: close the bounded SEC-11 reauthentication parity gap for `Settings -> Cache`. With a configured runtime, after failed `Application.Authenticate`, a retained `Settings` facade cannot obtain `Cache`; attached `Cache.Clear` and the four cache hit-rate getters deny before runtime access. Attached cache configuration, current-size, and maximum-size getters intentionally retain the legacy no-recheck behavior. Direct activation denial, IID/vtable/DISPID shape, persistence, and live cache reconfiguration remain unchanged.
   - Done: preserve the legacy `Routes` and `Route` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Settings -> Routes` count/index/domain-name/id lookup and read-only non-secret route target/retry/auth-username/local-domain/connection-security scalars from existing `hm_routes` SQL data, preserve the obsolete recipient-local alias and legacy `UseSSL` mapping, keep direct activation access-denied, omit the password column, and leave credentials and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `Routes.Refresh` read-only collection reload through the configured route administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/name/DBID lookup, and item getters observe current route rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters, route-address refresh, live routing behavior, credential handling, and broader mutation remain out of scope.
   - Done: preserve the legacy `RouteAddresses` and `RouteAddress` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Route -> Addresses` count/index/DBID lookup and read-only item `ID`, `Address`, and `RouteID` from existing `hm_routeaddresses` SQL data filtered to the selected route without adding a sort order, keep direct activation access-denied, and leave add/delete/delete-by-address/save, routing behavior changes, live reconfiguration, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `RouteAddresses.DeleteByDBID(id)` through the configured route-address administration store, deleting only the matching `hm_routeaddresses` row for the selected route and atomically removing that row from the authorized collection's process-local snapshot. Direct activation remains access-denied; contained delete failure maps to COM `E_FAIL` and retains the prior snapshot; `Add`, `DeleteByAddress`, item `Delete`, `Save`/setters, route matching behavior, live reconfiguration, and broader route mutation remain out of scope.
   - Done: implement authenticated legacy `RouteAddress.Delete()` for items returned from authorized `Route -> Addresses`, reusing the selected-route-scoped delete-by-ID administration operation and removing only that row from the owning collection's process-local snapshot. Direct activation remains access-denied; item facades without a configured delete operation remain `E_NOTIMPL`; contained delete failure maps to COM `E_FAIL` through the collection path and retains the prior snapshot; `Add`, `DeleteByAddress`, `Save`/setters, route matching behavior, live reconfiguration, and broader route mutation remain out of scope.
   - Done: implement authenticated legacy `RouteAddresses.DeleteByAddress(address)` for authorized `Route -> Addresses`, using the legacy case-insensitive first-match collection lookup and reusing the selected-route-scoped delete-by-ID administration operation. Missing addresses no-op; contained delete failure maps to COM `E_FAIL` and retains the prior snapshot; direct activation remains access-denied; `Add`, `Save`/setters, route matching behavior, live reconfiguration, and broader route mutation remain out of scope.
   - Done: close one bounded SEC-11 ownership boundary for existing `RouteAddresses` deletes. `DeleteByDBID` now requires the target ID to be present in the owning route-address snapshot before invoking the already route-scoped SQL store, matching legacy `Collection::DeleteItemByDBID`; cross-route/unknown IDs and repeated deletes through stale item facades no-op before persistence. `DeleteByAddress` and `RouteAddress.Delete` continue through the same parent-owned path; direct activation remains access-denied, no new mutator was opened, and routing/live-reconfiguration behavior remains unchanged.
   - Done: close the bounded SEC-11 reauthentication parity gap for `Settings -> Routes -> Addresses`. With a configured runtime, after failed `Application.Authenticate`, a retained `Settings` facade cannot obtain a new route adapter or read the route store, and an attached `RouteAddress.Delete` denies before persistence. Attached `Routes` count/lookups/`Refresh`, `Route.Addresses`, and route-address collection `DeleteByDBID`/`DeleteByAddress` intentionally retain legacy no-recheck behavior. IID/vtable/DISPID shape, direct activation denial, route matching, credentials, live routing, and other Admin collections remain unchanged.
   - Done: preserve the legacy `IncomingRelays` and `IncomingRelay` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Settings -> IncomingRelays` count/index/name/id lookup and read-only ID/name/lower-IP/upper-IP scalars from existing `hm_incoming_relays` SQL data ordered by relay name, convert legacy two-column IP storage to COM strings, keep direct activation access-denied, and leave SMTP trust behavior plus mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `IncomingRelays.Refresh` read-only collection reload through the configured incoming-relay administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/name/DBID lookup, and item getters observe current incoming-relay rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters, SMTP trust/policy behavior, live reconfiguration, and broader mutation remain out of scope.
   - Done: implement authenticated legacy `IncomingRelays.DeleteByDBID(id)` through the configured incoming-relay administration store, deleting only the matching `hm_incoming_relays` row and atomically removing that row from the authorized collection's process-local snapshot. Direct activation remains access-denied; contained delete failure maps to COM `E_FAIL` and retains the prior snapshot; `Add`, item `Delete`, `Save`/setters, SMTP trust behavior, live reconfiguration, and broader relay mutation remain out of scope.
   - Done: implement authenticated legacy `IncomingRelay.Delete()` for items returned by authorized `Settings -> IncomingRelays`, reusing the configured delete-by-ID path so only the matching `hm_incoming_relays` row is deleted and only that row is removed from the owning collection's process-local snapshot. Direct activation remains access-denied; item delete without a configured collection delete delegate remains `E_NOTIMPL`; contained delete failure maps to COM `E_FAIL` and retains the prior snapshot; `Add`, collection index `Delete`, `Save`/setters, SMTP trust behavior, live reconfiguration, and broader relay mutation remain out of scope.
   - Done: implement authenticated legacy `IncomingRelays.Delete(index)` for authorized `Settings -> IncomingRelays`, validating the collection index with the existing `DISP_E_BADINDEX` boundary, reusing the configured delete-by-ID path for the indexed relay, and atomically removing only that row from the authorized collection's process-local snapshot. Direct activation remains access-denied; valid indexed delete without a configured delete delegate remains `E_NOTIMPL`; contained delete failure maps to COM `E_FAIL` and retains the prior snapshot; `Add`, `Save`/setters, SMTP trust behavior, live reconfiguration, and broader relay mutation remain out of scope.
   - Done: implement authenticated existing-row legacy `IncomingRelay` scalar setters and `Save()` for items returned by authorized `Settings -> IncomingRelays`, staging `Name`, `LowerIP`, and `UpperIP` on the item facade until save, updating only the matching `hm_incoming_relays` row through the configured incoming-relay administration store, converting IPv4/IPv6 strings to the legacy two-column SQL storage, and atomically replacing only that row in the owning collection snapshot. Direct activation remains access-denied; setter/save without a configured save delegate remains `E_NOTIMPL`; contained save failure maps to COM `E_FAIL` and retains the prior collection snapshot; `Add`, SMTP trust behavior, live reconfiguration, and broader relay mutation remain out of scope.
   - Done: implement authenticated legacy `IncomingRelays.Add()` plus new-item insert-on-`IncomingRelay.Save`, returning an owning-collection-scoped unsaved item facade, staging `Name`, `LowerIP`, and `UpperIP`, inserting one `hm_incoming_relays` row through a parameterized `INSERT ... OUTPUT INSERTED.relayid`, assigning the generated ID, and appending only that new relay to the authorized collection snapshot. Direct activation remains access-denied; `Add` without an insert operation remains `E_NOTIMPL`; contained insert failure maps to COM `E_FAIL` and leaves the item unsaved with the prior collection snapshot; SMTP trust behavior, live reconfiguration, and broader relay mutation remain out of scope.
   - Done: implement residual legacy invalid-IP setter/save parity for authenticated `IncomingRelay` items returned from `Settings -> IncomingRelays`, including unsaved items from `Add()`. `LowerIP` and `UpperIP` setters preserve the legacy `S_OK` timing by normalizing malformed no-colon values to `0.0.0.0` and malformed colon-containing values to `::` before the configured save/insert delegate runs, so save writes the legacy zero-address fallback and the owning snapshot updates only after store success. Installed IID/vtable/DISPID shape and direct activation access-denied boundaries remain unchanged; SMTP trust behavior, live reconfiguration, and broader relay mutation remain out of scope.
   - Done: match legacy collection-membership containment for existing `IncomingRelays.DeleteByDBID` and stale `IncomingRelay.Delete` facades. The configured delete store is invoked only when the target ID is present in the owning collection snapshot; unknown IDs and repeated deletes through item facades removed from that snapshot no-op before persistence. Authenticated Settings boundary, direct activation denial, SQL shape, collection-index `Delete`, `Add`/`Save`, SMTP trust, live reconfiguration, and broader relay mutation remain unchanged.
   - Done: close the bounded SEC-11 reauthentication parity gap for `Settings -> IncomingRelays`. With a configured runtime, direct activation of either child class still denies every collection/item mutation and performs no store work. After a failed `Application.Authenticate`, a retained `Settings` facade cannot obtain a relay adapter and attached `IncomingRelay.Save`/`Delete`, including an item returned by `Add`, deny before persistence. Attached collection `Delete`/`DeleteByDBID`/`Refresh`/`Add` and staged item setters intentionally retain the legacy no-recheck behavior; IID/vtable/DISPID shape, direct activation, SMTP trust, live reconfiguration, and broader relay mutation remain unchanged.
   - Done: preserve the legacy `TCPIPPorts` and `TCPIPPort` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Settings -> TCPIPPorts` count/index/id lookup and read-only ID/protocol/port/address/SSL-certificate/connection-security scalars from existing `hm_tcpipports` SQL data ordered by address and port, convert legacy two-column IP storage to COM strings, keep direct activation access-denied, and leave listener reconfiguration, `SetDefault`, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `TCPIPPorts.Refresh` read-only collection reload through the configured TCP/IP port administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current TCP/IP port rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/SetDefault/save/setters, listener reconfiguration, certificate loading/validation, live binding changes, and broader mutation remain out of scope.
    - Done: preserve the legacy `SecurityRanges` and `SecurityRange` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Settings -> SecurityRanges` count/index/name/id lookup and read-only IP range, priority, expiry, and option-bit scalars from existing `hm_securityranges` SQL data ordered by expiry, priority, and name, convert legacy two-column IP storage to COM strings, keep direct activation access-denied, and leave IP policy enforcement, auto-ban runtime behavior, `SetDefault`, and existing-row mutations explicit `E_NOTIMPL`.
    - Done: implement authenticated legacy `InterfaceSecurityRanges::Add` plus new-item insert-on-`InterfaceSecurityRange::Save`, returning an owning-collection-scoped unsaved facade with legacy zero/default state, staging all persisted range scalars, retaining the prior IP on invalid setter input, validating/truncating/falling back as legacy persistence requires, inserting one parameterized `hm_securityranges` row through `OUTPUT INSERTED.rangeid`, assigning the generated ID, and appending only that new snapshot. Installed IID/vtable/DISPID/class identities and direct activation denial remain unchanged; existing-row setters/save, `Delete`, `DeleteByDBID`, index deletion, `SetDefault`, IP policy/auto-ban behavior, live reconfiguration, and SMTP trust remain out of scope.
   - Done: implement authenticated legacy `SecurityRanges.Refresh` read-only collection reload through the configured security-range administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/name/DBID lookup, and item getters observe current security-range rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/SetDefault/save/setters, IP policy/auto-ban behavior, live reconfiguration, and broader mutation remain out of scope.
   - Done: preserve the legacy `SSLCertificates` and `SSLCertificate` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Settings -> SSLCertificates` count/index/id lookup and read-only ID/name/certificate-file/private-key-file scalars from existing `hm_sslcertificates` SQL data ordered by certificate name, keep direct activation access-denied, and leave certificate loading/validation, TCP/IP port reconfiguration, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `SSLCertificates.Refresh` read-only collection reload through the configured SSL certificate administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, and item getters observe current SSL certificate rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters, certificate/private-key file reads, certificate validation/loading, TCP/IP port reconfiguration, and broader mutation remain out of scope.
   - Done: implement authenticated legacy `SSLCertificates.Clear` through the configured SSL certificate administration store, deleting only existing `hm_sslcertificates` rows and atomically emptying the authorized collection's process-local snapshot. Direct activation remains access-denied; contained clear failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete-by-DBID/save/setters, certificate/private-key file reads, certificate validation/loading, TCP/IP port reconfiguration, and broader TLS runtime changes remain out of scope.
   - Done: implement authenticated legacy `SSLCertificates.DeleteByDBID(id)` through the configured SSL certificate administration store, deleting only the matching `hm_sslcertificates` row and atomically removing that row from the authorized collection's process-local snapshot. Direct activation remains access-denied; contained delete failure maps to COM `E_FAIL` and retains the prior snapshot; add/save/setters, item `Delete`, certificate/private-key file reads, certificate validation/loading, TCP/IP port reconfiguration, and broader TLS runtime changes remain out of scope.
   - Done: implement authenticated legacy `SSLCertificate.Delete()` for items returned from authorized `Settings -> SSLCertificates`, reusing the same delete-by-ID administration operation and removing only that row from the owning collection's process-local snapshot. Direct activation remains access-denied; item facades without a configured delete operation remain `E_NOTIMPL`; contained delete failure maps to COM `E_FAIL` through the collection path and retains the prior snapshot; add/save/setters, certificate/private-key file reads, certificate validation/loading, TCP/IP port reconfiguration, and broader TLS runtime changes remain out of scope.
   - Done: implement authenticated existing-row legacy `SSLCertificate` scalar setters plus `Save()` for items returned from authorized `Settings -> SSLCertificates`, staging name/certificate-file/private-key-file changes on the item facade and persisting only the matching `hm_sslcertificates` row through a parameterized update. Direct activation remains access-denied; item facades without a configured save operation keep setters/`Save` as `E_NOTIMPL`; contained save failure maps to COM `E_FAIL` and retains the owning collection snapshot; `SSLCertificates.Add`, new-row inserts, certificate/private-key file reads, certificate validation/loading, TCP/IP port reconfiguration, live TLS reload, and broader TLS runtime changes remain out of scope.
   - Done: implement authenticated legacy `SSLCertificates.Add` plus new-item insert-on-`SSLCertificate.Save`, returning an owning-collection-scoped unsaved item facade, staging scalar values, inserting one `hm_sslcertificates` row through a parameterized `INSERT ... OUTPUT INSERTED.sslcertificateid`, assigning the generated ID, and appending the item to the authorized collection snapshot. Direct activation remains access-denied; Add without an insert operation remains `E_NOTIMPL`; contained insert failure maps to COM `E_FAIL` and leaves the item unsaved with the prior collection snapshot; certificate/private-key file reads, certificate validation/loading, TCP/IP port reconfiguration, live TLS reload, and broader TLS runtime changes remain out of scope.
   - Done: audit the bounded SEC-11 authorization boundary for the existing `Settings -> SSLCertificates` mutation surface. With the runtime store configured, direct activation of `SSLCertificates`/`SSLCertificate` still returns `E_ACCESSDENIED` for `DeleteByDBID`, `Add`, `Refresh`, `Clear`, all item setters, `Save`, and `Delete`, performs no store reads or mutations, and cannot obtain the Settings child. Mutation delegates remain available only through the authenticated Settings-bound adapter; no production-code gap or new mutator was introduced.
   - Done: match legacy collection-membership containment for existing `SSLCertificates.DeleteByDBID` and stale `SSLCertificate.Delete` facades. The configured delete store is invoked only when the target ID is present in the owning collection snapshot; unknown IDs and repeated deletes through an item removed from that snapshot no-op before persistence. The authenticated Settings boundary, direct activation denial, SQL shape, `Clear`/`Add`/`Save`, certificate/private-key file scope, TCP/IP listeners, and live TLS behavior remain unchanged.
   - Done: close the bounded SEC-11 reauthentication parity gap for `Settings -> SSLCertificates`. With a configured runtime, direct activation of either child class still denies every collection/item mutation and performs no store work. After a failed `Application.Authenticate`, a retained `Settings` facade cannot obtain a certificate adapter and attached `SSLCertificate.Save`/`Delete`, including an item returned by `Add`, deny before persistence. Attached collection `DeleteByDBID`/`Add`/`Refresh`/`Clear` and staged item setters intentionally retain the legacy no-recheck behavior; IID/vtable/DISPID shape, direct activation, certificate loading/validation, TCP/IP listeners, live TLS reload, and broader TLS mutation remain unchanged.
   - Done: preserve the legacy `Groups` and `Group` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Settings -> Groups` count/index/name/id lookup and read-only ID/name scalars from existing `hm_groups` SQL data ordered by group name, keep direct activation access-denied, and leave group members, ACL behavior integration, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `Groups.Refresh` read-only collection reload through the configured group administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/name/DBID lookup, and item getters observe current group rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete/save/setters, group-member refresh, ACL behavior integration, membership recalculation, and broader mutation remain out of scope.
   - Done: preserve the legacy `GroupMembers` and `GroupMember` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Group -> Members` count/index/id lookup and read-only ID/group-ID/account-ID scalars from existing `hm_group_members` SQL data filtered by group and ordered by member ID, expose the read-only `Account` child object through the existing non-secret account-by-ID projection while preserving `DISP_E_BADINDEX` for missing accounts, keep direct activation access-denied, and leave ACL runtime behavior, member/account mutations, membership recalculation, and SQL writes out of scope.
   - Done: implement authenticated legacy `GroupMembers.Refresh` read-only collection reload through the configured group-member administration store for the selected parent group, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/DBID lookup, item getters, and read-only `Account` child access observe current group-member rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; add/delete-by-DBID/save/delete/setters, account child mutation, ACL behavior integration, membership recalculation, SQL writes, and broader mutation remain out of scope.
   - Done: preserve the legacy `ServerMessages` and `ServerMessage` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Settings -> ServerMessages` count/index/name/id lookup and read-only ID/name/text scalars from existing `hm_servermessages` SQL data ordered by message name, keep direct activation access-denied, and leave delivery-template execution, `Refresh`, `Save`, and mutations explicit `E_NOTIMPL`.
   - Done: implement authenticated legacy `ServerMessages.Refresh` read-only collection reload through the configured server-message administration store, atomically replacing only that authorized COM collection's process-local snapshot so `Count`, index/name/DBID lookup, and item getters observe current server-message rows. Direct activation remains access-denied; contained reload failure maps to COM `E_FAIL` and retains the prior snapshot; delivery-template execution, save/setters, SQL writes, and broader mutation remain out of scope.
   - Done: implement authenticated existing-row legacy `ServerMessage` scalar setters plus `Save()` for items returned from authorized `Settings -> ServerMessages`, staging name/text changes on the item facade and persisting only the matching `hm_servermessages` row through a parameterized update. Direct activation remains access-denied; item facades without a configured save operation keep setters/`Save` as `E_NOTIMPL`; contained save failure maps to COM `E_FAIL` and retains the owning collection snapshot; template execution, delivery behavior, inserts/deletes, live reload, and broader Settings/Admin mutation remain out of scope.
   - Done: close the bounded SEC-11 retained-Settings parity gap for `Settings -> ServerMessages`. With a configured runtime, direct child activation remains access-denied. After a failed `Application.Authenticate`, a retained `Settings` facade cannot obtain a new server-message adapter or read the store, while already attached `ServerMessages.Refresh` and `ServerMessage` scalar setters/`Save` intentionally retain their legacy no-recheck behavior. IID/vtable/DISPID shape, delivery-template execution, inserts/deletes, live reload, and broader Settings/Admin mutation remain unchanged.
   - Done: preserve the legacy `Directories` COM vtable/identity contract, register its hosted class identity, expose authenticated `Settings -> Directories` read-only directory scalars from the configured/default legacy `hMailServer.ini` with legacy normalization, keep direct activation access-denied, and leave directory mutation/persistence explicit `E_NOTIMPL`.
   - Done: preserve the legacy `Database` COM vtable/enum/identity contract, register its hosted class identity, expose `Application -> Database` read-only required/current version, requires-upgrade, exists, connected, type, server, and database-name scalars from SQL/runtime plus configured/default legacy `hMailServer.ini`, preserve legacy per-member authentication for admin-only configuration fields, keep direct activation access-denied, and leave SQL execution, transactions, database creation/default-selection, script execution, message-file utility, and prerequisite operations explicit `E_NOTIMPL`.
   - Done: expose authenticated `Database.UtilGetFileNameByMessageID` through a parameterized single-column SQL lookup that preserves the 64-bit message ID, per-call server-admin check, stored filename string, missing-row empty string, and direct-activation denial without joins, message-file reads, path normalization, content access, arbitrary SQL, or mutation support.
   - Done: preserve the legacy `Status` COM vtable/identity contract, register its hosted class identity, expose authenticated `Application -> Status` delivery-queue text plus start-time, processed-message, spam, virus, session-count, and thread-ID values from SQL/runtime snapshot boundaries, keep direct activation access-denied, and wire SMTP/POP3/IMAP session counts plus delivery/spam/virus counters without widening administrative mutation scope.
   - Done: preserve the legacy `Diagnostics`, `DiagnosticResults`, and `DiagnosticResult` COM vtable/identity contracts, register their hosted class identities, expose authenticated `Application -> Diagnostics` child access, keep `LocalDomainName` and `TestDomainName` as process-local COM state, return read-only diagnostic result collections through a deterministic runtime boundary, keep direct activation access-denied, and leave actual DNS/SMTP/network/filesystem diagnostics plus broader operational health checks out of scope.
   - Done: expose bounded read-only `Application` core scalars from a runtime/configuration boundary: unauthenticated legacy `Version`, authenticated `ServerState` and `InitializationFile`, and legacy `VersionArchitecture` (`x86`/`x64`), while keeping `Start`, `Stop`, `Connect`, `Reinitialize`, and `SubmitEMail` explicit `E_NOTIMPL`.
   - Done: preserve the legacy `Utilities` COM identity/vtable contract and hosted class registration, expose the non-mutating `MD5`, legacy salted `SHA256`, `GenerateGUID`, email/domain/IP validators, `IsStrongPassword`, and `CriteriaMatch` helpers without authentication as legacy does, and keep import/test-suite operations explicit `E_NOTIMPL`, with legacy server-admin checks retained for side-effecting members.
   - Done: expose unauthenticated `Utilities.BlowfishEncrypt`/`BlowfishDecrypt` through an injected service runtime over the existing tested compatibility cipher, preserving DISPIDs 5/6, BSTR contracts, the legacy static key/table, Latin-1 conversion, lower-case hex, block padding, empty strings, and valid-ciphertext round trips without adding storage, database writes, key rotation, migration, or closing SEC-21.
   - Done: expose unauthenticated `Utilities.IsLocalHost` through an injected service runtime shared by `Application -> Utilities` and the hosted Utilities class, preserving DISPID 12 and `VARIANT_BOOL`, using a literal IPv4 address or only the first resolved IPv4 address, comparing it with current local IPv4 interface addresses, returning `false` for unresolved/non-IPv4 input, and leaving runtime-less public construction `E_NOTIMPL` without database/configuration writes, security-range/firewall policy, listener changes, persistent caching, reverse DNS, or IPv6 parity.
   - Done: expose unauthenticated `Utilities.GetMailServer` through an injected service runtime shared by `Application -> Utilities` and the hosted Utilities class, preserving DISPID 1/BSTR, final-`@` domain extraction, bounded MX/CNAME recursion, null-MX, implicit A/AAAA fallback when no MX exists, IPv4-first system availability behavior, partial target success, first-seen IP de-duplication, and comma-separated/empty-string results without altering SMTP delivery routing, adding persistent DNS caching, database/configuration writes, reverse DNS, policy, or mutations.
   - Done: expose authenticated `Utilities.RetrieveMessageID` through a service-injected read-only resolver, preserving DISPID 17 and the 64-bit `hyper` return contract, applying the server-admin check before runtime access, deriving legacy partial filenames from configured data-directory paths, querying the partial value before the exact supplied filename with parameterized single-table `hm_messages.messagefilename` lookups, returning `0` for no row, and retaining direct-activation denial without message-file reads/existence checks, path canonicalization beyond legacy extraction, joins, content access, or SQL/file mutations.
   - Done: expose authenticated `Utilities.PerformMaintenance(UpdateImapFolderUid)` through a service-injected SQL store, preserving DISPID 19 and maintenance enum GUID/value `1`, reading `messagefolderid` plus `MAX(messageuid)` without joins, failing on non-positive legacy folder/UID rows, and advancing only matching lower `hm_imapfolders.foldercurrentuid` values through parameterized updates. Unknown operation, store false/exception, missing runtime, and direct activation retain explicit COM failure boundaries; message UIDs/files, higher folder UIDs, scheduling, imports, and unrelated tables remain untouched.
   - Done: expose authenticated `Utilities.MakeDependent` through a service-injected Windows SCM runtime, preserving DISPID 7, the server-admin check, local target service name `hMailServer`, replacement dependency order `RPCSS` then caller-supplied service, double-NUL multi-string shape, caller-visible void success after native return, runtime-less `E_NOTIMPL`, and direct-activation denial. The runtime closes SCM/service/lock handles and does not start/stop/install/delete services, alter other service configuration, touch registry/database/files, or append unknown existing dependencies.
- Done: expose authenticated `Utilities.EmailAllAccounts` through a service-injected recipient store and the existing signaling queue writer, preserving DISPID 9 and `VARIANT_BOOL`, account-address ordering, active-account plus active address-derived-domain filtering, legacy case-insensitive `*`/`?` wildcard matching, one legacy-shaped plain-text MIME message, local recipient IDs, an empty envelope sender, and delivery wake-up only after the durable write. The operation returns `false` on contained store/write failure, preserves direct-activation denial and runtime-less `E_NOTIMPL`, and uses an explicit request flag to retain legacy zero-recipient queue creation without changing the default SMTP empty-recipient guard; SMTP validation, rules, spam/virus checks, remote expansion, per-recipient messages, imports, and unrelated mutations remain out of scope.
- Done: expose authenticated `Utilities.ImportMessageFromFile` through a service-injected runtime and SQL store, preserving DISPID 8 and `VARIANT_BOOL`, accepting only existing files under the configured data directory, applying legacy partial-filename-first then exact-path message lookup with `true` on already-partial persisted rows, normalizing exact-path rows and misplaced in-tree files to legacy GUID/bucket filenames before persistence, importing `accountId > 0` files into the account Inbox with MIME-derived sender/size/internal-date metadata and allocated UIDs, importing `accountId == 0` files into the queue with only local To/CC recipients and delivery wake-up only after the durable write, and returning `false` on contained failures. Direct activation remains access-denied; the IMAP-folder overload, external-path imports, rules/spam/virus processing, and broader mutations remain out of scope.
- Done: expose authenticated `Utilities.ImportMessageFromFileToIMAPFolder` through the same service-injected runtime and SQL store, preserving DISPID 13 and `VARIANT_BOOL`, reusing the existing-file-under-data-directory validation, partial-first/exact lookup, GUID/bucket normalization, and `accountId == 0` queue behavior from `ImportMessageFromFile`, preserving existing-only empty-folder/InBox fallback, cleaning legacy `%YEAR%`/`%MONTH%`/`%DAY%` tokens plus one leading hierarchy delimiter, resolving legacy modified UTF-7 path segments, transactionally creating missing named private/public paths as subscribed folders, requiring inherited `Insert` ACL permission for existing public destinations, preserving the legacy create bypass for missing public paths, allocating UIDs against the destination folder account while retaining the source message account, and returning `false` on contained failures. Direct activation remains access-denied; external paths, rules/spam/virus processing, unrelated ACL mutations, and broader mutations remain out of scope.
- Done: preserve the legacy `Links` COM identity/vtable contract and hosted class registration, expose authenticated `Application -> Links` DBID lookup for `Domain`, `Account`, `Alias`, and `DistributionList` through the existing read-only administration stores/adapters, preserve `DISP_E_BADINDEX` and direct-activation access-denied boundaries, and add no SQL or mutation behavior.
   - Remaining: implement the database-backed Administrator object model in bounded slices.

7. Migration, operations, and observability.
   - Done: replacement install requires a read-only rollback archive integrity and `DataBackup` payload preflight before COM registration or service changes; full in-place upgrade runner, backup creation, restore execution, semantic restore validation, and rollback-from-backup documentation remain.
   - Search backfill progress metrics, health endpoints/logging, orphan cleanup.
   - Done: guarded Windows Service install/uninstall scripts with owned COM registration and explicit legacy-service replacement opt-in.
   - Remaining: production configuration packaging and upgrade/rollback integration.

8. Performance and soak validation.
   - SQL Server FTS integration tests.
   - 100k message mailbox SEARCH/SORT acceptance.
   - 1k concurrent IMAP connections, SMTP latency, delivery queue throughput, memory/handle leak soak tests.

   - Done: implement authenticated existing-row `RuleAction.Subject` setter parity (`fd6d58f3a`). Legacy `InterfaceRuleAction::put_Subject` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:207-237`) returns `E_ACCESSDENIED` for detached objects, otherwise stages the raw BSTR through `SetSubject(newVal)` and returns `S_OK` without administrator checks or normalization; existing `InterfaceRuleAction::Save` and `PersistentRuleAction::SaveObject` persist it through `actionsubject`. The authorized .NET item facade stages the exact string only through the owning collection's Save path and reuses the existing parameterized full-row update. Direct activation, installed COM identity, Add/new-item persistence, `RuleID` and remaining setters, ordering, rule execution, SMTP/delivery behavior, and broader mutation remain unchanged. Focused COM/store tests pass 28/28; full Net10 reached 1150/1152 with only unrelated ClamWin/CustomScanner cleanup failures (`UnauthorizedAccessException` removing generated `.eml` files); no live SQL Server integration was run.

   - Done: implement authenticated existing-row `RuleAction.Body` setter parity (`fee2fec0a`). Legacy `InterfaceRuleAction::put_Body` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:239-269`) returns `E_ACCESSDENIED` for detached objects, otherwise stages the raw BSTR through `SetBody(newVal)` and returns `S_OK` without administrator checks, trimming, validation, or newline normalization; existing `InterfaceRuleAction::Save` and `PersistentRuleAction::SaveObject` persist it through `actionbody`. The authorized .NET item facade stages the exact string only through the owning collection's Save path and reuses the existing parameterized full-row update. Direct activation, installed COM identity, Add/new-item persistence, `RuleID` and remaining setters, ordering, rule execution, SMTP/delivery behavior, and broader mutation remain unchanged. Focused COM/store tests pass 30/30; full Net10 reached 1152/1154 with only unrelated ClamWin/CustomScanner cleanup failures (`UnauthorizedAccessException` removing generated `.eml` files); no live SQL Server integration was run.

   - Done: implement authenticated existing-row `RuleAction.FromName` setter parity (`e7ee6b669`). Legacy `InterfaceRuleAction::put_FromName` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:271-301`) returns `E_ACCESSDENIED` for detached objects, otherwise stages the raw BSTR through `SetFromName(newVal)` and returns `S_OK` without administrator checks or normalization; existing `InterfaceRuleAction::Save` and `PersistentRuleAction::SaveObject` persist it through `actionfromname`. The authorized .NET item facade stages the exact string only through the owning collection's Save path and reuses the existing parameterized full-row update. Direct activation, installed COM identity, Add/new-item persistence, `RuleID` and remaining setters, ordering, rule execution, SMTP/delivery behavior, and broader mutation remain unchanged. Focused COM/store tests pass 32/32; full Net10 passes 1156/1156; no live SQL Server integration was run.

   - Done: implement authenticated existing-row `RuleAction.Filename` setter parity (`fe9763b6f`). Legacy `InterfaceRuleAction::put_Filename` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:351-365`) stores the raw BSTR on an attached object and returns `S_OK`; `PersistentRuleAction` reads/writes `actionfilename` (`hmailserver/source/Server/Common/Persistence/PersistentRuleAction.cpp:31-109`). `RuleApplier::ApplyAction_Reply` (`hmailserver/source/Server/SMTP/RuleApplier.cpp:429-482`) does not use this field for file I/O, path resolution, or network access. The authorized .NET item facade stages the exact value through the owning collection's Save path, preserves detached `E_ACCESSDENIED`, no-save-delegate `E_NOTIMPL`, and installed DISPID 8. Focused COM/store tests pass 29/29; full Net10 passes 1158/1159 with one opt-in native registry test skipped; no live SQL Server integration was run.

   - Done: implement authenticated existing-row `RuleAction.To` setter parity (`e628dc487`). Legacy `InterfaceRuleAction::put_To` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:367-397`) stores the raw BSTR on an attached object and returns `S_OK`; `RuleAction::GetTo/SetTo` are raw accessors and `PersistentRuleAction` reads/writes `actionto` (`hmailserver/source/Server/Common/Persistence/PersistentRuleAction.cpp:53,102`). `RuleApplier::ApplyAction_Forward` (`hmailserver/source/Server/SMTP/RuleApplier.cpp:243-307`) passes the field to `RecipientParser::CreateMessageRecipientList`; the .NET runtime already has the equivalent `action.To` forwarding path, so this slice changes no delivery or egress behavior. The authorized .NET item facade stages the exact value through the owning collection's Save path, preserves detached `E_ACCESSDENIED`, no-save-delegate `E_NOTIMPL`, and installed DISPID 9. Focused COM/store tests pass 31/31; full Net10 passes 1160/1161 with one opt-in native registry test skipped; no live SQL Server integration was run.

   - Done: implement authenticated existing-row `RuleAction.IMAPFolder` setter parity (`0ec2ff11e`). Legacy `InterfaceRuleAction::put_IMAPFolder` encodes Unicode to Modified UTF-7 and returns `S_OK` without lookup, ACL, creation, or validation; the getter decodes it. The authorized .NET item preserves the COM boundary and existing owning Save path, while runtime folder resolution and movement remain unchanged. Focused COM/store tests pass 49/49; full Net10 passes 1163/1164 with one opt-in native registry test skipped; no live SQL Server integration was run.

   - Done: implement authenticated existing-row `RuleAction.HeaderName` setter parity (`0f0f576b6`). Legacy `InterfaceRuleAction::put_HeaderName` directly stages the raw BSTR and returns `S_OK` without administrator or validation checks; `PersistentRuleAction` persists `actionheader`. The authorized .NET item stages the exact raw value through the existing owning Save path, preserving detached access denial, read-only `E_NOTIMPL`, installed DISPID 15, and the existing SMTP header runtime path. Focused COM/store tests pass 51/51; full Net10 passes 1165/1166 with one opt-in native registry test skipped; no live SQL Server integration was run.

   - Done: implement authenticated existing-row `RuleAction.Value` setter parity (`2ce09ccdb`). Legacy `InterfaceRuleAction::put_Value` directly stages the raw BSTR and returns `S_OK` without administrator or validation checks; `PersistentRuleAction` persists `actionvalue`. The authorized .NET item stages the exact raw value through the existing owning Save path, preserving detached access denial, read-only `E_NOTIMPL`, installed DISPID 16, and the existing `SetHeaderValue`/`BindToAddress` runtime paths. Focused COM/store tests pass 53/53; full Net10 passes 1167/1168 with one opt-in native registry test skipped; no live SQL Server integration was run.

   - Done: implement authenticated existing-row `RuleAction.RouteID` setter parity (`c7e2c4080`). Legacy `InterfaceRuleAction::put_RouteID` directly stages the raw LONG and returns `S_OK` without route-existence, range, ownership, normalization, or field-specific administrator checks; `PersistentRuleAction` persists `actionrouteid` unchanged. The authorized .NET item stages the raw integer through the existing owning Save path, preserving detached access denial, read-only `E_NOTIMPL`, installed DISPID 18, and existing route selection/delivery runtime paths. Focused COM/store/SMTP tests pass 55/55; full Net10 passes 1169/1170 with one opt-in native registry test skipped; no live SQL Server integration was run. Legacy route-resolution and delivery differences remain a separate runtime-risk gap.

   - Done: implement authenticated existing-row `RuleAction.AbortSpamFlagged` setter parity (`e14040d63`). Legacy `InterfaceRuleAction::put_AbortSpamFlagged` stores `newVal == VARIANT_TRUE` and returns `S_OK` for attached objects without an action-specific administrator or validation guard; `PersistentRuleAction` persists `actionabortspamflagged` on insert/update. The authorized .NET item stages the exact bool through the existing owning Save path, preserving detached access denial, read-only `E_NOTIMPL`, installed DISPID 19, and the existing SMTP rule model/load path. Focused COM/store/SMTP tests pass 58/58; full Net10 passes 1172/1173 with one opt-in native registry test skipped; no live SQL Server integration was run. Legacy Forward/Reply spam-abort consumption remains a separate runtime parity gap.

   - Done: implement account-rule `AbortSpamFlagged` Forward/Reply runtime parity (`3909d2441`). Legacy `RuleApplier::ApplyAction_Forward`/`ApplyAction_Reply` reads the original message spam flag after the existing loop/recipient prechecks and silently skips only the flagged action. The .NET account-rule path now carries `hm_messages.messageflags` spam state through `SmtpReceiveRequest`, applies the same action-local checks, and leaves later actions running. Focused processor/local-delivery/receiver tests pass 51/51; full Net10 passes 1180/1181 with one opt-in native registry test skipped. SPF/DKIM/DMARC classification coverage remained a separate gap.

   - Done: implement inbound SMTP spam-state ordering before global rule processing. Legacy `SMTPConnection::DoPreAcceptSpamProtection_`/`DoPreAcceptMessageModifications_` establish the message spam flag before the queued message reaches `SMTPDeliverer::RunGlobalRules_`; the .NET receiver now runs its existing spam scan/policy before global rules and carries the resulting flag and message bytes through `SmtpReceiveRequest`. Later DKIM/DMARC, antivirus, trust, and delivery stages remain unchanged. Focused receiver/rule tests pass 52/52; full Net10 passes 1181/1182 with one opt-in native registry test skipped. SPF/DKIM/DMARC classification coverage remained a separate gap.

   - Done: implement generated-message flag parity for global `Forward`/`Reply`. Legacy `PersistentMessage::CopyToQueue`/`RuleApplier::ApplyAction_Forward` copies the source flags, while `ApplyAction_Reply` constructs a fresh `Message` and receives only the new-message `Recent` flag. The .NET `SmtpRuleGeneratedMessage.SpamFlagged` field carries Forward state through the global and account generated queue writers; Reply remains clean. Focused processor/receiver tests pass 53/53; full Net10 passes 1182/1183 with one opt-in native registry test skipped. SPF/DKIM/DMARC classification ordering remained a separate gap.

   - Done: implement `CreateCopy` generated-message spam-flag parity. Legacy `RuleApplier::ApplyAction_Copy` calls `PersistentMessage::CopyToQueue`, whose `CreateCopy_` copies source flags before the queued message is saved. The .NET CreateCopy path now carries `SmtpReceiveRequest.OriginalMessageSpamFlagged` into `SmtpRuleGeneratedMessage.SpamFlagged`; focused processor/receiver tests pass 53/53 and full Net10 passes 1182/1183 with one opt-in native registry test skipped. SPF/DKIM/DMARC classification ordering and coverage remain separate gaps.
   - Done: implement SPF/DKIM/DMARC pre-rule classification ordering. Legacy SPF runs in `SMTPConnection::DoSpamProtection_(SPPreTransmission)`, DKIM runs in the post-transmission `SpamTestRunner` path, and `DoPreAcceptMessageModifications_` establishes the message spam flag before `SMTPDeliverer::RunGlobalRules_`. The .NET receiver now evaluates the existing SPF/DKIM/DMARC policy results before the rule processor, carries the combined spam bit through `SmtpReceiveRequest`, and preserves the existing no-default-reject behavior. Focused receiver tests pass 54/54; full Net10 passes 1183/1184 with one opt-in native registry test skipped. The next security gate is a shared WebAdmin scanner-target egress policy.
   - Done: implement shared WebAdmin ClamAV/SpamAssassin scanner-test target hardening (`f586332cf`, following `7e98a4178`). Legacy `Utilities::IsLocalHost` accepts the first IPv4 result and the legacy `VirusScannerTester::TestClamAVConnect`/`SpamAssassinTestConnect::TestConnect` methods receive the caller's port; the WebAdmin path now reads target inputs only from POST, passes the once-resolved local IPv4 literal to COM, validates a strict decimal `1..65535` port, and rejects invalid input through one HTTP 400/body `0` path before COM. Valid local scanner behavior, direct COM identity, scanner settings, SMTP scanning, and external fetch remain unchanged. PHP lint/runtime port checks pass; focused .NET contract tests pass 38/38 and full Net10 passes 1183/1184 with one opt-in native registry test skipped. The next production-gate candidate is a bounded `BackupManager.StartBackup` preflight/plan parity slice.

   - Done: implement the non-destructive `BackupManager.StartBackup` preflight/plan slice (`8681b1d23`). Legacy `InterfaceBackupManager::StartBackup` (`hmailserver/source/Server/COM/InterfaceBackupManager.cpp`) authorizes the child and calls `BackupManager::StartBackup` (`hmailserver/source/Server/Common/Application/BackupManager.cpp`), which queues `BackupTask(true)`; `BackupTask::DoWork` then invokes `BackupExecuter::StartBackup` (`hmailserver/source/Server/Common/Application/BackupTask.cpp`). The plan mirrors `BackupExecuter::LoadSettings_` one-trailing-backslash normalization and `BackupExecuter::StartBackup` precondition order: message placement is checked before normalized destination accessibility, including when `BackupMessagesDBOnly` is enabled. It reports whether the legacy domain/message combination would copy the data directory, performs no filesystem/SQL/data-directory/archive/queue/event work, preserves the installed `IInterfaceBackupManager` IID/vtable/DISPIDs, and leaves COM `StartBackup` pending. Focused backup/settings/store tests pass 24/24; full Net10 passes 1187/1188 with one opt-in native registry test skipped.

   - Done: implement authenticated `BackupManager.StartBackup` operation-state and maintenance-queue dispatch parity (`cf15929a0`). Legacy `BackupManager::StartBackup` (`hmailserver/source/Server/Common/Application/BackupManager.cpp`) rejects a second operation while `is_running_`, sets the state before creating/queuing `BackupTask(true)`, resets it when the maintenance queue is unavailable, and releases it through `OnThreadStopped`; `InterfaceBackupManager::StartBackup` (`hmailserver/source/Server/COM/InterfaceBackupManager.cpp`) keeps the COM call asynchronous and returns `S_OK` after dispatch. The internal `BackupOperationCoordinator` and `BackupManagerRuntimeHost` preserve the installed `IInterfaceBackupManager` IID/vtable/DISPID shape and direct activation boundary, invoke the authenticated runtime only when configured, suppress duplicate enqueue attempts, reset on unavailable queue, and expose thread-stop release without archive, SQL, data-directory, status, or event execution. Focused backup/settings/store tests pass 36/36; full Net10 passes 1190/1191 with one opt-in native registry test skipped.
   - Done: implement the service-owned backup maintenance queue and status/failure callback boundary (`832b9c933`). Legacy `BackupManager::StartBackup` and `BackupTask::DoWork` remain the references for asynchronous `BackupTask(true)` dispatch and `OnThreadStopped` release. The .NET `BackupTaskQueue` is registered by `HMailServer.Service`, consumed by `BackupTaskHostedService`, and connected to the authenticated `BackupManager` runtime without changing the installed `IInterfaceBackupManager` identity or direct activation boundary. One queued request reports the legacy start/loading/failure/completion callbacks and releases operation state; its execution delegate remains an explicit `E_NOTIMPL` boundary with no archive, SQL, data-directory, restore, or event work. Focused backup manager/task-queue tests pass 15/15; the Windows service build passes with 0 warnings/0 errors; full Net10 passes 1193/1194 with one opt-in native registry test skipped.
   - Done: wire read-only `BackupStartPlan` acquisition into the queued task preflight (`d4360fd3e`). Legacy `BackupExecuter::LoadSettings_` and `BackupExecuter::StartBackup` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp`) load the configured destination/options, honor `IniFileSettings::GetBackupMessagesDBOnly`, check `PersistentMessage::GetAllMessageFilesAreInDataFolder` before destination existence, and report the first failure through `BackupManager::OnBackupFailed`; the .NET `BackupStartPlanRuntime` obtains the current `hm_settings` snapshot, uses a parameterized read-only `hm_messages` filename predicate, probes the normalized destination, and evaluates the existing plan inside the service-owned queued request. `IInterfaceBackupManager` identity and direct activation remain unchanged; archive/XML creation, SQL/data-directory writes, restore, and event execution remain out of scope. Focused backup/preflight/store tests pass 24/24; full Net10 passes 1198/1199 with one opt-in native registry test skipped.

## SEC-11 Account.IMAPFolders Cached Shared Visibility (2026-07-29)

- Legacy parity was confirmed in `hmailserver/source/Server/COM/InterfaceAccount.cpp:817-840` (`InterfaceAccount::get_IMAPFolders`), `hmailserver/source/Server/IMAP/IMAPFolderContainer.cpp:38-159` (`GetFoldersForAccount`, `UncacheAccount`, `Clear`, `UpdateCurrentUID`), `hmailserver/source/Server/Common/BO/IMAPFolders.cpp:21-147,149-214,303-366`, `hmailserver/source/Server/COM/InterfaceIMAPFolders.cpp:31-214`, `hmailserver/source/Server/COM/InterfaceIMAPFolder.cpp:47-301`, and `hmailserver/source/Server/Common/Persistence/PersistentIMAPFolder.cpp:193-246`. The process-wide folder container is keyed by account ID: first access loads all `hm_imapfolders` rows in `folderid ASC` order, and fresh Account/child COM wrappers share the cached folder graph until explicit invalidation.
- The .NET path now adds an account-ID keyed lazy `ImapFolderAdministrationState` in `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/IMAPFolders.cs`, carries it through `AccountComClass.cs` and `Accounts.cs`, and filters fresh root/child facades over that snapshot. `GetFoldersForAccountAsync` and the ordered all-folder SQL projection are in `IImapFolderAdministrationStore.cs` and `SqlServerImapFolderAdministrationStore.cs`.
- Code/test commit `893a5c768` changes only the Account/Accounts/IMAPFolders path, the IMAP folder store contract/SQL query, and focused COM/SQL test doubles and regressions. `ImapFoldersComContractTests`, `AccountsComContractTests`, `SqlServerImapFolderAdministrationStoreTests`, and `SettingsComContractTests` pass `50/50`; full Net10 passes `1311` with `3` opt-in tests skipped. No live COM or SQL integration ran.
- Protocol cache invalidation, `CurrentUID` synchronization, folder mutation, ACL mutation, SMTP/POP3 behavior, installed COM identity, and SEC-18 state remain unchanged. The next bounded slice is parent-account-scoped `FetchAccount.DownloadNow()` retry-now parity: legacy `InterfaceFetchAccount::DownloadNow` (`hmailserver/source/Server/COM/InterfaceFetchAccount.cpp:500-520`) calls `PersistentFetchAccount::SetRetryNow` (`hmailserver/source/Server/Common/Persistence/PersistentFetchAccount.cpp:203-210`) and wakes the external-fetch manager; current `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/FetchAccounts.cs:353` is `E_NOTIMPL`. Keep broader FetchAccount mutation and external-fetch runtime out of scope.

## SEC-11 FetchAccount DownloadNow Retry-Now Persistence (2026-07-30)

- Legacy parity was confirmed in `InterfaceFetchAccount::DownloadNow` (`hmailserver/source/Server/COM/InterfaceFetchAccount.cpp:500-520`), `PersistentFetchAccount::SetRetryNow` (`hmailserver/source/Server/Common/Persistence/PersistentFetchAccount.cpp:203-210`), and `SQLStatement::GetCurrentTimestamp` (`hmailserver/source/Server/Common/SQL/SQLStatement.cpp:435-449`). The attached COM item updates `hm_fetchaccounts.fanexttry` and returns `S_OK`; detached activation returns `E_ACCESSDENIED`. MSSQL uses `GETDATE()`. The installed `IInterfaceFetchAccount.DownloadNow` DISPID 14 remains anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1626-1656`.
- Code/test commits `3d234ccde` and `e05379132` add parent-scoped `SetRetryNowAsync`, a dedicated coalescing `IExternalFetchWakeSignal`, signal-after-successful-persistence wiring, idle worker wake-up, and MSSQL `GETDATE()` parity for lease/defer/complete `fanexttry` scheduling. Installed COM identity and DISPID 14 remain unchanged; direct activation remains denied and store failures map to `E_FAIL`. `FetchAccountsComContractTests`, `ExternalFetchWakeSignalTests`, `ExternalFetchHostedServiceTests`, `SqlServerExternalFetchAccountStoreTests`, `ProductionHostCompositionTests`, and `SettingsComContractTests` pass `42/42`; full Net10 passes `1320` with `3` opt-in tests skipped. No live COM or SQL integration ran.
 - Legacy `ExternalFetchManager::SetCheckNow` (`hmailserver/source/Server/ExternalFetcher/ExternalFetchManager.cpp:144-147`) is represented by the dedicated external-fetch signal; its delivery signal remains a separate instance. The subsequent attached existing-row `FetchAccount.Delete()` parity slice is complete in `cccc3e64c`; the bounded `BackupManager` archive/XML, non-secret raw settings-property, backup-side `DomainAliases`, backup-side non-secret scalar `Accounts`, backup-side normal domain `Aliases`, backup-side `DistributionLists`, and backup-side account credential child-serialization slices are complete in `a1f1d92f4`, `59ac1b7c6`, `f15e857a8`, `ac611987c`, `3e7535d76`, `5d4981240`, and `fd30ceb33`; the next production-gate slice is backup-side `FetchAccounts` child serialization.

## SEC-11 FetchAccount Delete Parity (2026-07-30)

- Legacy parity was confirmed in `InterfaceFetchAccount::Delete` (`hmailserver/source/Server/COM/InterfaceFetchAccount.cpp:590-611`), `InterfaceAccount::get_FetchAccounts` (`hmailserver/source/Server/COM/InterfaceAccount.cpp:694-720`), `FetchAccounts::Refresh` (`hmailserver/source/Server/Common/BO/FetchAccounts.cpp:35-43`), `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`), and `PersistentFetchAccount::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentFetchAccount.cpp:117-133`). An attached item requires domain-admin authentication, normal deletion is routed through the owning collection, and persistence deletes the selected `hm_fetchaccounts` row followed by its `hm_fetchaccounts_uids` rows without a transaction or affected-row check in the legacy implementation. The installed `IInterfaceFetchAccount.Delete` DISPID 17 and interface/class identities remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1620-1686` and existing registration resources.
- Code/test commit `cccc3e64c` adds an owner-scoped `DeleteFetchAccountAsync(accountId, fetchAccountId, cancellationToken)` contract and SQL implementation, parent-row affected-row protection before UID cleanup, selected-child-only snapshot removal after successful persistence, and synchronous `FetchAccount.Delete()` HRESULT mapping. Direct activation remains `E_ACCESSDENIED`; collection-wide `FetchAccounts.Delete`/`DeleteByDBID` remains fenced; Add/Save/password/setter mutation, POP3 behavior, SMTP trust, live reconfiguration, and SEC-18 state are unchanged. Focused FetchAccounts/SQL coverage passes `19/19`; full Net10 passes `1324` with `3` opt-in tests skipped.
 - The bounded `BackupManager` archive/XML, non-secret raw settings-property, backup-side `DomainAliases`, backup-side non-secret scalar `Accounts`, backup-side normal domain `Aliases`, backup-side `DistributionLists`, and backup-side account credential child-serialization slices are complete in `a1f1d92f4`, `59ac1b7c6`, `f15e857a8`, `ac611987c`, `3e7535d76`, `5d4981240`, and `fd30ceb33`. The next production-gate slice is backup-side `FetchAccounts` child serialization. Restore execution, message/data-directory work, destructive SQL, event dispatch, and PHP session cutover remain out of scope.

## Current Next Slice

Authoritative continuation (2026-08-01, `dad4bf79a`, following `63b555fe8`): legacy `Domains::XMLLoad`/`Domain::XMLLoad`/`Account::XMLLoadSubItems` (`hmailserver/source/Server/Common/BO/Domain.cpp`, `Account.cpp`, and `Collection.h`) require the selected domain graph to be rooted at `Backup/Domains`, with named `Domain` and `Account` nodes under `Domain/Accounts`. `BackupRestoreIntegrityRuntime` now validates exactly that graph read-only, rejects missing names and misplaced nodes, and preserves archive/DTD/path/reparse and metadata checks; the dry-run planner carries invalid evidence forward without mutation. Focused restore integrity/planner coverage passes `22/22`; full Net10 passes `1398` with `3` opt-in tests skipped. The next bounded slice is domain child-container placement/scalar validation for `DomainAliases`, `Aliases`, and `DistributionLists`; SQL writes, data-directory replacement, restore execution, rollback mutation, and COM activation remain fenced. The backup ScriptServer event adapter remains an open production gap. Older paragraphs below are historical context.

Authoritative continuation (2026-08-01, `63b555fe8`, following `cd3adfcb2`): legacy `Backup::BackupOptions` (`hmailserver/source/Server/Common/Application/Backup.h:14-20`) and `BackupExecuter::StartBackup` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:57-190`) require `DataFiles` whenever `BOMessages` is selected, require `Raw` versus `7z` to match the compression bit, and only permit physical `DataBackup` payloads for domain+message non-DB-only backups. `BackupRestoreIntegrityRuntime` now validates those relationships read-only, rejects DB-only compressed payloads containing `DataBackup`, accepts DB-only raw metadata without a sibling directory, and preserves DTD/path/containment checks. Focused restore integrity/planner coverage passes `19/19`; full Net10 passes `1395` with `3` opt-in tests skipped. The next bounded slice is semantic `Domains -> Domain -> Accounts -> Account` graph validation; SQL writes, data-directory replacement, restore execution, rollback mutation, and COM activation remain fenced. The backup ScriptServer event adapter remains an open production gap. Older paragraphs below are historical context.

Authoritative continuation (2026-08-01, `cd3adfcb2`, following `d210c5611`): legacy `BackupManager::OnBackupCompleted`/`OnBackupFailed` (`hmailserver/source/Server/Common/Application/BackupManager.cpp:160-193`) synchronously fire only `EventOnBackupCompleted=1004` and `EventOnBackupFailed=1005` after status/log/error handling. The .NET path now has an injectable internal `IBackupEventDispatcher`, invokes it after the existing status callbacks, and covers completion ordering, failure reason propagation, duplicate-start, queue-unavailable, and unauthenticated/direct-activation denial without changing the COM/IDL surface. The default dispatcher is intentionally a no-op because the general ScriptServer runtime is not yet implemented; this slice is a bounded seam, not a production event-parity claim. Focused backup manager/task coverage passes `18/18`; full Net10 passes `1392` with `3` opt-in tests skipped. The next bounded slice is the real ScriptServer-backed adapter for these two backup events; restore execution, SQL writes, data-directory replacement, and COM activation remain fenced. Older paragraphs below are historical context.

Authoritative continuation (2026-08-01, `d210c5611`, following `c0f1402f1`): legacy `Backup::BackupOptions` (`hmailserver/source/Server/Common/Application/Backup.h:14-20`) and `BackupExecuter::StartBackup` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:57-190`) were confirmed for the independent settings/domains/messages/compression matrix. The focused archive tests now cover settings+domains+messages in DB-only raw, DB-only compressed, non-DB-only raw, and non-DB-only compressed modes, asserting legacy `DataFiles` format/attributes, `Domains` before `Properties`, temporary XML cleanup, root-file omission, nested-file retention, source preservation, and raw/compressed `DataBackup` retention/removal. `FileUtilities::CopyDirectory`/`DeleteFilesInDirectory` (`hmailserver/source/Server/Common/Util/FileUtilities.cpp:370-442`) and `Compression::AddFile`/`AddDirectory` (`hmailserver/source/Server/Common/Util/Compression.cpp:28-45`) remain the behavior references. Focused `BackupArchiveRuntimeTests` passes `42/42`; full Net10 passes `1389` with `3` opt-in tests skipped. The next bounded slice is durable backup completion/failure event dispatch after archive validation; restore execution, SQL writes, data-directory replacement, and COM activation remain fenced. Older paragraphs below are historical context.

Authoritative continuation (2026-08-01, `a33a8032e`): the zero-mutation restore dry-run planner is complete. Legacy `Backup::BackupOptions` (`hmailserver/source/Server/Common/Application/Backup.h:14-20`) stores independent settings/domains/messages/compression bits; `BackupExecuter::StartRestore` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:274-333`) orders domain cleanup, public-folder cleanup, physical data restore, domain XML/SQL load, settings load, and reinitialization, while messages-only has no restore effect because data restore is nested under domains. The .NET path adds internal `BackupRestoreDryRunPlanner.Plan` and `BackupRestoreDryRunPlan`, reports those destructive phases without executing them, explicitly omits physical steps for DB-only mode, and keeps `WouldMutate=false`. The integrity validator now accepts DB-only compressed metadata without `DataBackup` only when the message bit and explicit DB-only policy are present. COM identities and `StartRestore` remain unchanged. Focused restore-integrity/planner coverage passes `16/16`; full Net10 passes `1384` with `3` opt-in tests skipped. The next bounded slice is semantic restore XML/graph validation over the report; SQL writes, data-directory replacement, restore execution, and COM activation remain fenced. Older paragraphs below are superseded context.

Authoritative continuation (2026-08-01, `f7f90c84a`): compressed non-DB-only message `DataBackup` staging is complete for `BODomains|BOMessages|BOCompression`. Legacy `BackupExecuter::BackupDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:196-217`) recursively copies the configured data directory and removes only files directly under the staging root; `BackupExecuter::StartBackup` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:119-130,165-184`) archives `hMailServerBackup.xml` first, recursively adds `DataBackup`, and deletes the staging directory after compression. `FileUtilities::CopyDirectory` and `DeleteFilesInDirectory` (`hmailserver/source/Server/Common/Util/FileUtilities.cpp:370-440`) define the copy/root-file cleanup behavior, while `Compression::AddDirectory` (`hmailserver/source/Server/Common/Util/Compression.cpp:28-35`) defines recursive 7z input. The .NET path wires the configured data directory through `Program.cs`, stages into an isolated destination `DataBackup`, preserves nested message files, removes the staging directory after success/failure cleanup, and fails closed for raw mode, missing source data, pre-existing staging, and source-nested staging paths. Focused backup/archive coverage passes `49/49`; full Net10 passes `1367` with `3` opt-in tests skipped. The next bounded slice is raw non-DB-only `BODomains|BOMessages` staging, where the external `DataBackup` directory remains beside the archive; restore, destructive SQL, and data-directory replacement remain fenced. The older paragraphs below are superseded context.

Authoritative continuation (2026-08-01, `1e5e4a75b`): DB-only message archive execution and legacy `DataFiles` metadata are complete. Legacy `BackupExecuter::StartBackup` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:57-147,172-184`) reads `BackupMessagesDBOnly`, skips `DataBackup` staging and message-file compression in DB-only mode, but still appends `BackupInformation/DataFiles` for `BOMessages`; `BOCompression` selects `Format="7z"` plus `Size`, while the pre-archive `FileUtilities::FileSize` (`hmailserver/source/Server/Common/Util/FileUtilities.cpp:318-327`) returns `0` for the not-yet-created archive. Without compression the legacy attributes are `Format="Raw"` and `FolderName="DataBackup"`. The .NET path adds `BackupStartPlan.BackupCompressionFlag`, emits those attributes in `SevenZipBackupArchiveRuntime.WriteDataFiles`, allows the existing 7z writer only when `BackupMessagesDBOnly` is true, and keeps non-DB-only physical message staging rejected before file creation. Focused backup/archive/start-plan coverage passes `40/40`; full Net10 passes `1365` with `3` opt-in tests skipped. The next bounded slice is compressed `DataBackup` staging for non-DB-only `BODomains|BOMessages|BOCompression`; raw staging, restore, destructive SQL, and data-directory replacement remain fenced. The older paragraphs below are superseded context.

Authoritative continuation (2026-08-01, `384e67788`): backup-side private-account nested subfolder metadata is complete. Legacy `IMAPFolders::Refresh` (`hmailserver/source/Server/Common/BO/IMAPFolders.cpp:42-145`) loads all account rows in `folderid ASC` order and rebuilds the `folderparentid` hierarchy; `Collection<T,P>::XMLStore` (`hmailserver/source/Server/Common/BO/Collection.h:61-82`) emits direct items and omits empty collections; and `IMAPFolder::GetSubFolders`/`XMLStore` (`hmailserver/source/Server/Common/BO/IMAPFolder.cpp:61-68,123-145`) emits scalar attributes before recursive nested `Folders` containers. The .NET path now loads each selected account once through `GetFoldersForAccountAsync`, groups snapshots by `ParentId`, and uses `WriteFolder` for depth-first root/child/grandchild XML while preserving scalar order and XMLite-compatible escaping. Messages, data files, ACLs, public folders, and restore remain unchanged. Focused backup/folder/host coverage passes `45/45`; full Net10 passes `1361` with `3` opt-in tests skipped. The next bounded slice is DB-only folder message metadata serialization; keep data-directory copying, `DataFiles`, ACLs/public folders, restore, destructive SQL, event dispatch, and SEC-18 work fenced. The older paragraphs below are superseded context.

Authoritative continuation (2026-08-01, `db73812c7`): backup-side private-account root folder metadata/scalar serialization is complete. Legacy `Account::GetFolders`/`Account::XMLStore` (`hmailserver/source/Server/Common/BO/Account.cpp:131-140,280-331`) gates folder output on `BOMessages` and places `Folders` after `Rules`; `IMAPFolders::Refresh` (`hmailserver/source/Server/Common/BO/IMAPFolders.cpp:42-145`) preserves `folderid ASC` order and root scope; `IMAPFolder::XMLStore` (`hmailserver/source/Server/Common/BO/IMAPFolder.cpp:123-145`) emits `Name`, `Subscribed`, `CreateTime`, and `CurrentUID` in that order; `Collection<T,P>::XMLStore` (`hmailserver/source/Server/Common/BO/Collection.h:61-82`) omits empty collections; and `Time::GetTimeStampFromDateTime` (`hmailserver/source/Server/Common/Util/Time.cpp:62-72`) defines the legacy timestamp shape. The .NET path adds optional `BackupArchiveXmlPayload.Folders`, selected-account root loading through `IImapFolderAdministrationStore.GetRootFoldersAsync`, and `SevenZipBackupArchiveRuntime.WriteFolders` with XMLite-compatible escaping, exact attribute order, empty-container omission, and the `BackupMessagesFlag` gate. Focused backup/folder/host coverage passes `45/45`; full Net10 passes `1361` with `3` opt-in tests skipped. The actual archive still rejects message payload creation before writing, so this is metadata-only parity coverage. The next bounded slice is backup-side nested subfolder hierarchy metadata; keep messages, data-directory copying, ACLs, public folders, restore, destructive SQL, event dispatch, and SEC-18 work fenced. The older paragraphs below are superseded context.

Authoritative continuation (2026-08-01, `bd37be125`): backup-side account `Rules` serialization is complete, including read-only `RuleCriterias`/`Criteria` and `RuleActions`/`Action` children. Legacy `Account::XMLStore` emits `FetchAccounts` then `Rules`; `Rules::Refresh` scopes by account and orders by `rulesortorder ASC`; `Collection<T,P>::XMLStore` omits empty collections; and `Rule::XMLStore` emits the four rule attributes before criteria/actions. The .NET path uses a backup-only rule query without the COM `ruleid` tie-breaker, existing read-only criteria/action stores, exact legacy attribute/container names and order, XMLite-compatible entity escaping, and selected account/rule scoping. Focused backup/rule/host coverage is `115/115`; full Net10 is `1358` with `3` opt-in skips. COM/admin mutation, SMTP rule execution, folders, message/data-directory payloads, restore, destructive SQL, event dispatch, and SEC-18 remain fenced. The next bounded slice is backup-side folder metadata/scalar serialization, with nested messages, data-directory copying, subfolders, ACLs, and restore still out of scope. The older paragraphs below are superseded context.

Authoritative continuation (2026-08-01, `afd0de0da`): backup-side encrypted fetch `Password` plus `FetchAccountUIDs`/`UID` serialization is complete; focused coverage is `59/59` and full Net10 is `1353` with `3` opt-in skips. The next bounded slice is backup-side `Rules` child serialization. Keep rule mutation/execution, Folders, message/data-directory payloads, restore, destructive SQL, event dispatch, and SEC-18 work fenced. The older paragraphs below are superseded context.

Authoritative continuation (2026-07-30, `ae97a70eb`): backup-side non-secret `FetchAccounts` scalar child serialization is complete; focused backup/fetch/COM coverage is `49/49` and full Net10 is `1349` with `3` opt-in skips. The next bounded slice is encrypted fetch `Password` plus `FetchAccountUIDs`/`UID` serialization. Keep Rules, Folders, message/data-directory payloads, restore, destructive SQL, event dispatch, and SEC-18 work fenced. The older paragraphs below are superseded context.

Authoritative current next slice (2026-07-30): authenticated `BackupManager` archive/XML creation, non-secret raw settings-property parity, backup-side `DomainAliases`, backup-side non-secret scalar `Accounts`, backup-side normal domain `Aliases`, backup-side `DistributionLists` child serialization, and backup-side account `Password`/`PasswordEncryption` serialization are complete in `a1f1d92f4`, `59ac1b7c6`, `f15e857a8`, `ac611987c`, `3e7535d76`, `5d4981240`, and `fd30ceb33`; focused backup/account credential coverage passes `44/44` and full Net10 passes `1346` with `3` opt-in skips. The next production-gate slice is backup-side `FetchAccounts` child serialization. Keep its encrypted fetch password, UIDs, nested Rules/Folders, message/data-directory payloads, restore execution, destructive SQL, event dispatch, SEC-18 broker registration, and PHP session cutover fenced. The older paragraphs below are superseded context.

Authoritative current next slice (2026-07-29): authenticated existing-row `RuleCriteria.HeaderField`, `RuleCriteria.MatchValue`, and `RuleCriteria.UsePredefined` setter/save parity are complete in `c8d69c9b8`, `d95ce9c69`, and `a4ff728c0`; focused criteria/SQL/integration coverage passes `35/35` and full Net10 passes `1288` with `3` opt-in skips. The next smallest safe production-gate slice is authenticated existing-row `RuleCriteria.PredefinedField` setter parity through the owning `RuleCriteria.Save()` path. The immediately following superseded paragraph is retained only as historical context.

Authoritative current next slice (2026-07-29): authenticated existing-row `RuleCriteria.HeaderField` and `RuleCriteria.MatchValue` setter/save parity are complete in `c8d69c9b8` and `d95ce9c69`; focused criteria/SQL/integration coverage passes `33/33` and full Net10 passes `1286` with `3` opt-in skips. The next smallest safe production-gate slice is authenticated existing-row `RuleCriteria.UsePredefined` setter parity through the owning `RuleCriteria.Save()` path. The immediately following superseded paragraph is retained only as historical context.

IncomingRelays WebAdmin hardening is complete in code/test commit `fc2aa90f6`, blocked-attachment WebAdmin hardening is complete in `bfee58cab`, route-address WebAdmin hardening is complete in `2394e026`, distribution-list recipient WebAdmin hardening is complete in `9d6a8dda2`, alias WebAdmin hardening is complete in `1dc35f169`, rule hardening is complete in `6736e161e`, domain hardening is complete in `3d25cb0a7`, and authenticated existing-row `RuleCriteria.HeaderField` setter parity is complete in `c8d69c9b8`. Legacy route-address, recipient, alias, rule, domain, and criteria collection ownership, staged child behavior, persistence, installed COM identities, and the existing domain/user-admin boundaries remain documented below. `RuleCriteria.HeaderField` stages raw input through the owning Save delegate while direct activation remains denied; focused criteria/SQL/integration coverage passes `31/31`; full Net10 passes `1284` with `3` opt-in skips; PHP CLI remains unavailable. The next smallest safe production-gate slice is authenticated existing-row `RuleCriteria.MatchValue` setter parity through the owning `RuleCriteria.Save()` path. Keep broader criteria mutation, background hardening, backup archive/XML execution, SEC-18 broker registration, SMTP rule behavior, and PHP session cutover out of scope.

### SEC-11 RuleCriteria MatchValue Setter Parity (2026-07-29)

- Legacy parity was confirmed in `InterfaceRuleCriteria::put_MatchValue` and `Save` (`hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:30-48,154-184`), the owning lookup/parent attachment path (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:19-38`), and `PersistentRuleCriteria::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp:65-100`). The setter stages the raw BSTR without validation, trimming, or normalization; detached objects return `E_ACCESSDENIED`; Save persists `criteriamatchvalue` for the attached criterion.
- Installed `IInterfaceRuleCriteria`/`IInterfaceRuleCriterias` identities, vtable order, DISPIDs, and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1792-1837,2844-2849,3185-3200`. The .NET contracts, BSTR marshaling, authenticated access boundary, and direct activation denial remain unchanged.
- Code/test commit `d95ce9c69` changes only `RuleCriteria.MatchValue` to use the existing mutable snapshot/owning Save delegate pattern in `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/RuleCriterias.cs`. Focused criteria/SQL/integration coverage passes `33/33`; full Net10 passes `1286` with `3` opt-in tests skipped. PHP CLI is unavailable and no live SQL integration ran because the approved connection variable was unset.
- The next bounded COM/Admin slice is an ownership/save-containment audit for authenticated existing-row `RuleAction.RuleID`; keep the setter fenced until its parent ownership and affected-row behavior are proven.

### SEC-11 RuleCriteria UsePredefined Setter Parity (2026-07-29)

- Legacy parity was confirmed in `InterfaceRuleCriteria::put_UsePredefined` and `Save` (`hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:30-48,105-120`), the owning lookup/parent attachment path (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:19-38`), and `PersistentRuleCriteria::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp:65-100`). The attached setter stores exactly `newVal == VARIANT_TRUE` and returns `S_OK`; detached objects return `E_ACCESSDENIED`; Save persists `criteriausepredefined` for the attached criterion.
- Installed `IInterfaceRuleCriteria`/`IInterfaceRuleCriterias` identities, vtable order, DISPIDs, and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1792-1837,2844-2849,3185-3200`. The .NET contracts, `VARIANT_BOOL` marshaling, authenticated access boundary, and direct activation denial remain unchanged.
- Code/test commit `a4ff728c0` changes only the authorized `RuleCriteria.UsePredefined` setter to use the existing mutable snapshot/owning Save delegate pattern in `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/RuleCriterias.cs`. Focused criteria/SQL/integration coverage passes `35/35`; full Net10 passes `1288` with `3` opt-in tests skipped. PHP CLI is unavailable and no live SQL integration ran because the approved connection variable was unset.
- The next bounded COM/Admin slice is an ownership/save-containment audit for authenticated existing-row `RuleAction.RuleID`; keep the setter fenced until its parent ownership and affected-row behavior are proven.

### SEC-11 RuleCriteria PredefinedField Setter Parity (2026-07-29)

- Legacy parity was confirmed in `InterfaceRuleCriteria::put_PredefinedField` and `Save` (`hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:30-48,186-216`), the owning lookup/parent attachment path (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:19-38`), and `PersistentRuleCriteria::ReadObject`/`SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp:46-52,65-100`). The attached setter casts and stores the raw enum value without validation or normalization and returns `S_OK`; detached objects return `E_ACCESSDENIED`; Save persists `criteriapredefinedfield` for the attached criterion.
- Installed `IInterfaceRuleCriteria`/`IInterfaceRuleCriterias` identities, vtable order, DISPIDs, enum GUID/values, and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:205-238,1792-1837,3185-3200`. The .NET contracts, enum mapping, authenticated access boundary, and direct activation denial remain unchanged.
- Code/test commit `fabc7e03a` changes only the authorized `RuleCriteria.PredefinedField` setter to stage `(int)value` through the existing mutable snapshot/owning Save delegate pattern in `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/RuleCriterias.cs`. Focused criteria/SQL/integration coverage passes `37/37`; full Net10 passes `1290` with `3` opt-in tests skipped. PHP CLI is unavailable and no live SQL integration ran because the approved connection variable was unset.
- The next bounded COM/Admin slice is an ownership/save-containment audit for authenticated existing-row `RuleAction.RuleID`; keep the setter fenced until its parent ownership and affected-row behavior are proven.

### SEC-11 RuleCriteria MatchType Setter Parity (2026-07-29)

- Legacy parity was confirmed in `InterfaceRuleCriteria::get_MatchType`, `put_MatchType`, and `Save` (`hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:30-55,218-248`), the owning lookup/parent attachment path (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:19-38` and `hmailserver/source/Server/COM/COMCollection.h:11-38`), and `PersistentRuleCriteria::ReadObject`/`SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp:46-52,64-100`). The attached setter raw-casts and stores any enum integer without validation or normalization and returns `S_OK`; existing-row Save persists `criteriamatchtype`; detached objects remain access-denied.
- Installed `IInterfaceRuleCriteria`/`IInterfaceRuleCriterias` identities, vtable order, DISPIDs, enum GUID/values, and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:240-272,1792-1837,3185-3200`. The .NET contracts, enum mapping, authenticated Settings boundary, owning collection, and direct activation denial remain unchanged.
- Code/test commit `0d9e43b14` changes only the authorized `RuleCriteria.MatchType` setter to stage `(int)value` through the existing mutable snapshot/owning Save delegate pattern in `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/RuleCriterias.cs`. Focused criteria/SQL/integration coverage passes `39/39`; full Net10 passes `1292` with `3` opt-in tests skipped. PHP CLI is unavailable and no live SQL integration ran because the approved connection variable was unset.
- Next slice: an ownership/save-containment audit for authenticated existing-row `RuleAction.RuleID`; keep the setter fenced until its parent ownership and affected-row behavior are proven.

### SEC-11 RuleCriteria Owner-Scoped Save Contract (2026-07-29)

- Legacy ownership behavior was confirmed in `InterfaceRuleCriteria::put_RuleID`/`Save` (`hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:30-87`), parent attachment (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:14-114` and `hmailserver/source/Server/COM/COMCollection.h:11-32`), and `PersistentRuleCriteria::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp:64-100`). Legacy accepts raw attached `RuleID` values and writes the mutable destination by `criteriaid`; detached access remains denied.
- The .NET save contract now carries the immutable owning rule ID separately from the snapshot destination in `IRuleCriteriaAdministrationStore`, passes the captured parent ID from `RuleCriteriaAdministrationRuntimeHost`, scopes SQL with `criteriaruleid = @OwningRuleId AND criteriaid = @CriteriaId`, and retains `SET criteriaruleid = @RuleId`. Non-single-row updates now fail deterministically. COM identity, authenticated Settings access, direct activation denial, and the RuleID staging boundary remain unchanged.
- Code/test commit `edf97aeaa` changes `IRuleCriteriaAdministrationStore`, the RuleCriteria runtime save closure, the SQL administration store, and focused COM/SQL contract tests. Focused criteria/SQL/integration coverage passes `40/40`; full Net10 passes `1293` with `3` opt-in tests skipped. No live SQL integration ran because the approved connection variable was unset.
- Next slice: an ownership/save-containment audit for authenticated existing-row `RuleAction.RuleID`; keep the setter fenced until its parent ownership and affected-row behavior are proven.

### SEC-11 RuleCriteria RuleID Setter Parity (2026-07-29)

- Legacy parity was confirmed in `InterfaceRuleCriteria::get_RuleID`, `put_RuleID`, and `Save` (`hmailserver/source/Server/COM/InterfaceRuleCriteria.cpp:30-87`), the owning lookup/parent attachment path (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:14-114` and `hmailserver/source/Server/COM/COMCollection.h:11-32`), and `PersistentRuleCriteria::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleCriteria.cpp:64-100`). The attached setter stores any raw `LONG`, including foreign, zero, negative, and nonexistent target rule IDs, and returns `S_OK`; Save writes the destination by criterion ID while the original parent collection remains the owner.
- Installed `IInterfaceRuleCriteria`/`IInterfaceRuleCriterias` identities, vtable order, DISPIDs, and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1792-1837,3185-3200`. The .NET contracts, authenticated Settings boundary, direct activation denial, and owner-scoped SQL save path remain unchanged.
- Code/test commit `66e72f39c` changes only the authorized `RuleCriteria.RuleID` setter to stage raw values through the existing mutable snapshot/owner-scoped Save delegate pattern in `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/RuleCriterias.cs`. Focused criteria/SQL/integration coverage passes `43/43`; full Net10 passes `1296` with `3` opt-in tests skipped. No live SQL integration ran because the approved connection variable was unset.
- Next slice: an ownership/save-containment audit for authenticated existing-row `RuleAction.RuleID`; keep RuleAction setter mutation, parent snapshot propagation, broader action/rule mutation, SMTP behavior, backup archive/XML execution, SEC-18 broker registration, DCOM ACL changes, and PHP session cutover out of scope.

### SEC-11 RuleAction RuleID Ownership/Save Parity (2026-07-29)

- Legacy parity was confirmed in `InterfaceRuleAction::put_RuleID`, `Save`, and `get_RuleID` (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:30-72,106-136`), parent lookup/attachment (`hmailserver/source/Server/COM/InterfaceRuleActions.cpp:22-116` and `hmailserver/source/Server/COM/COMCollection.h:6-38`), and `PersistentRuleAction::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRuleAction.cpp:77-116`). Attached setters accept raw `LONG` values, including foreign, zero, negative, and nonexistent rule IDs, and return `S_OK`; detached access remains `E_ACCESSDENIED`. Save persists the mutable destination by action ID, while the original parent collection remains the owner and shared legacy items expose the mutation before refresh.
- Installed `IInterfaceRuleAction`/`IInterfaceRuleActions` identities, vtable order, DISPIDs, and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1839-1900,3201-3215`; the .NET contracts and direct activation/read-only facade boundaries remain unchanged.
- Code/test commit `9680640a5` changes only the authorized `RuleAction.RuleID` setter, the RuleAction administration store owner parameter, the SQL owner-plus-action scope, and focused contract/store tests in `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/RuleActions.cs`, `hmailserver/source/Server.Net10/src/HMailServer.Core/Abstractions/IRuleActionAdministrationStore.cs`, `hmailserver/source/Server.Net10/src/HMailServer.Storage.SqlServer/SqlServerRuleActionAdministrationStore.cs`, and the corresponding tests. Focused coverage passes `47/47`; full Net10 passes `1300` with `3` opt-in tests skipped. No live SQL integration ran because the approved connection variable was unset.
- The next bounded COM/Admin slice is an authenticated repeated-`Account.Messages` adapter visibility audit. Do not broaden to message mutation, SMTP execution, backup/XML behavior, COM identity, or SEC-18 work.

### SEC-11 Rule.Actions Per-Rules-Generation Adapter Visibility (2026-07-29)

- Legacy parity was confirmed in `HM::Rule::GetActions` (`hmailserver/source/Server/Common/BO/Rule.cpp:49-59`, `Rule.h:45-46`), which lazily caches one `RuleActions` collection, and `InterfaceRule::get_Actions` (`hmailserver/source/Server/COM/InterfaceRule.cpp:195-213`), which creates a fresh COM collection wrapper over that cached collection. `InterfaceRules::get_Item`/`get_ItemByDBID` (`hmailserver/source/Server/COM/InterfaceRules.cpp:19-72`) create fresh rule wrappers over shared `HM::Rule` objects; `Rules::Refresh` rebuilds the parent objects while existing wrappers retain their prior cached rule/action state.
- The .NET adapters now keep private action-state generations per authenticated `Rules` facade. Fresh `Rule` and `RuleActions` wrappers share per-rule state; `Rules.Refresh()` publishes a new generation and leaves existing wrappers on the old snapshot. Authenticated `Settings` access, direct activation denial, installed COM identities, and owner-scoped Save/delete/Refresh behavior remain unchanged.
- Code/test commit `493848279` changes only `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/Rules.cs`, `RuleActions.cs`, and focused `RuleActionsComContractTests.cs`. Focused Rules/RuleActions/SQL coverage passes `58/58`; full Net10 passes `1305` with `3` opt-in tests skipped. No live SQL integration ran and PHP CLI remains unavailable. SMTP rule execution, COM registration/ACLs, service/database/Data-directory state, and SEC-18 staging state did not change.
- Next slice: authenticated repeated-`Account.Messages` adapter visibility audit; keep message mutation and broader rule/action mutation out of scope.

### SEC-11 Account.Rules Per-Account-State Visibility (2026-07-29)

- Legacy parity was confirmed in `InterfaceAccount::get_Rules` (`hmailserver/source/Server/COM/InterfaceAccount.cpp:790-815`), `Account::GetRules` (`hmailserver/source/Server/Common/BO/Account.cpp:119-128`), and the cached `Account::rules_` member (`hmailserver/source/Server/Common/BO/Account.h:162`). Legacy lazily loads one `HM::Rules(id_)`, refreshes it once, and returns fresh COM `InterfaceRules` wrappers over that cached object. `InterfaceRules::Refresh` (`hmailserver/source/Server/COM/InterfaceRules.cpp:143-152`) refreshes the shared object; `InterfaceAccounts` lookups (`hmailserver/source/Server/COM/InterfaceAccounts.cpp:98-190`) share the underlying account until the owning collection refreshes.
- The .NET adapters now create one lazy `RuleAdministrationState` per attached account entry, including Administrator account ID `0`; repeated `Account.Rules` calls return fresh `Rules` facades over that state, one store load is shared, and `Rules.Refresh()` is visible through all facades. `Accounts.Refresh()` publishes new account entries and leaves prior Account/Rules wrappers on their old state. Existing authenticated/direct-activation boundaries and installed Account/Rules COM identities remain unchanged.
- Code/test commit `bb4142b99` changes `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/AccountComClass.cs`, `Accounts.cs`, `Rules.cs`, and focused `AccountsComContractTests.cs`/`RulesComContractTests.cs`. Focused Rules/Accounts/SQL coverage passes `38/38`; full Net10 passes `1307` with `3` opt-in tests skipped. No live SQL integration ran and PHP CLI remains unavailable; SMTP, service/database/Data-directory, COM registration/ACL, and SEC-18 staging state did not change.
- Next slice: authenticated repeated-`Account.Messages` adapter visibility audit; keep message mutation and broader account/admin mutation out of scope.

### SEC-11 Account.Messages Per-Account-State Visibility (2026-07-29)

- Legacy parity was confirmed in `HM::Account::GetMessages` (`hmailserver/source/Server/Common/BO/Account.cpp:107-116`), cached `Account::messages_` (`hmailserver/source/Server/Common/BO/Account.h:161`), `InterfaceAccount::get_Messages` (`hmailserver/source/Server/COM/InterfaceAccount.cpp:420-445`), `HM::Messages::Refresh` (`hmailserver/source/Server/Common/BO/Messages.cpp:144-209`), and `Accounts::Refresh`/collection lookup lifetime (`hmailserver/source/Server/Common/BO/Accounts.cpp:24-55`, `hmailserver/source/Server/Common/BO/Collection.h:138-177,232-277`). Legacy creates one cached message collection per loaded account and fresh COM wrappers over it; Accounts.Refresh publishes new account objects while existing wrappers retain their old children.
- The .NET adapter now creates one lazy `AccountMessageAdministrationState` per attached account entry. Repeated `Account.Messages` calls return fresh `Messages` facades over one cached store snapshot; Accounts.Refresh publishes new account/message state while old Account wrappers retain their prior snapshot. Direct activation still returns `E_ACCESSDENIED`, authenticated Settings access is unchanged, and installed Account/Messages IID, vtable, DISPID, CLSID, and ProgID identity remains unchanged.
- Code/test commit `0c2ee1226` changes only `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/AccountComClass.cs`, `Accounts.cs`, `Messages.cs`, and focused `AccountsComContractTests.cs`/`MessagesComContractTests.cs`. Focused Messages/Accounts/Application/SQL coverage passes `48/48`; full Net10 passes `1308` with `3` opt-in tests skipped. No live COM integration ran, PHP CLI remains unavailable, and no SMTP, service/database/Data-directory, COM registration/ACL, or SEC-18 staging state changed.
### SEC-11 Account.Messages SQL Projection Parity (2026-07-29)

- Legacy parity was confirmed in `HM::Account::GetMessages` (`hmailserver/source/Server/Common/BO/Account.cpp:107-116`), `HM::Messages::Refresh` (`hmailserver/source/Server/Common/BO/Messages.cpp:143-209`), `Messages::AddToCollection` (`hmailserver/source/Server/Common/BO/Messages.cpp:211-241`), and `InterfaceAccount::get_Messages` (`hmailserver/source/Server/COM/InterfaceAccount.cpp:420-445`). Account-level `Messages(accountId, -1)` uses `messageaccountid = @MESSAGEACCOUNTID`, no message-type or folder predicate, and `ORDER BY messageuid ASC`; queue and folder branches remain distinct.
- The .NET `SqlServerMessageAdministrationStore.GetAccountMessagesSql` query now matches that account-level predicate/order. `GetFolderMessagesSql`, the message snapshot shape, COM identities, and IMAP/POP3/SMTP stores remain unchanged.
- Code/test commit `debc93dac` changes only `hmailserver/source/Server.Net10/src/HMailServer.Storage.SqlServer/SqlServerMessageAdministrationStore.cs` and `SqlServerMessageAdministrationStoreTests.cs`. Focused SQL/Message/Account coverage passes `38/38`; full Net10 passes `1308` with `3` opt-in tests skipped. No live SQL integration ran.
- Next slice: authenticated per-account `Account.IMAPFolders` cached snapshot and shared folder-adapter visibility. Keep folder Add/Delete/Save/setters, ACL mutation, live protocol/cache synchronization, `CurrentUID` protocol semantics, SQL schema changes, COM identity, and SEC-18 work out of scope.

### SEC-11 RuleAction Parent-Snapshot Visibility (2026-07-29)

- Legacy parity was confirmed in cached parent access (`hmailserver/source/Server/Common/BO/Rule.cpp:49`, `Rule.h:33`), child lookup/attachment (`hmailserver/source/Server/COM/InterfaceRuleActions.cpp:22-49` and `hmailserver/source/Server/COM/COMCollection.h:11-22`), mutable action setters (`hmailserver/source/Server/COM/InterfaceRuleAction.cpp:122-563`), and collection refresh/delete behavior (`hmailserver/source/Server/COM/InterfaceRuleActions.cpp:125-159`, `hmailserver/source/Server/Common/BO/RuleActions.cpp:25-67`). Existing child wrappers and the owning vector observe the same mutable object immediately, including after Save failure; Refresh replaces the collection and new Add items remain distinct until successful Save.
- The .NET adapter now uses a private shared `RuleActionAdministrationEntry` per loaded row. Index and DBID lookups share that entry, setters update it immediately, Save reads it, Refresh replaces entries, and Delete removes entries only after store success. Installed `IInterfaceRuleAction`/`IInterfaceRuleActions` identities and direct activation/read-only boundaries remain unchanged.
- Code/test commit `dc2fe2118` changes only `hmailserver/source/Server.Net10/src/HMailServer.ComInterop/RuleActions.cs` and focused `RuleActionsComContractTests`. Coverage passes `48/48`; full Net10 passes `1301` with `3` opt-in tests skipped. No live SQL integration ran because the approved connection variable was unset.
- The next bounded COM/Admin slice is an authenticated repeated-`Rule.Actions` adapter visibility audit. Do not broaden to Add/new-item Save, other action setters, Delete, Refresh, reordering, SMTP rule execution, backup/XML behavior, COM identity, or SEC-18 work.

## Historical Next Slice Record (superseded)

The current smallest safe production-gate slice is a bounded SEC-14 audit of `hmailserver/source/WebAdmin/background_surblserver_save.php` for POST-only handling, POST-body field reads, and preservation of the authenticated `Settings -> AntiSpam -> SURBLServers -> SURBLServer` field mapping. SSL certificate mutation hardening is complete in `4ed9d2f26`: `background_sslcertificate_save.php` now requires `hmailRequirePostCsrfToken()` before POST-only reads of `action`, `id`, `Name`, `CertificateFile`, and `PrivateKeyFile`, while preserving the server-admin guard, `Settings -> SSLCertificates`, Add/Edit/DeleteByDBID, field assignments, Save, redirects, and installed COM identity. Focused SSL WebAdmin/COM/store coverage passes `18/18`; full Net10 passes `1267` with `3` opt-in tests skipped. PHP CLI lint remains unavailable. Keep SSL runtime listener reconfiguration, certificate-file policy, broader background handler hardening, SMTP trust, backup archive/XML execution, and SEC-18 broker registration out of scope.

The next smallest safe production-gate slice is a bounded SEC-14 audit of `hmailserver/source/WebAdmin/background_sslcertificate_save.php` for POST-only handling, POST-body field reads, and preservation of the authenticated `Settings -> SSLCertificates -> SSLCertificate` field mapping. The TCP/IP port slice is complete in `272d56b5c`: `background_tcpipport_save.php` now requires `hmailRequirePostCsrfToken()` before POST-only reads of all seven mutation fields and preserves its `Settings -> TCPIPPorts` mapping, Add/Edit/DeleteByDBID, field assignments, Save, Stop/Start, and redirects. Legacy TCPIP references are `hmailserver/source/Server/COM/InterfaceSettings.cpp:1187-1203`, `hmailserver/source/Server/COM/InterfaceTCPIPPorts.cpp:101-168`, `hmailserver/source/Server/COM/InterfaceTCPIPPort.cpp:33-293`, `hmailserver/source/Server/Common/Persistence/PersistentTCPIPPort.cpp:53-101`, `hmailserver/source/Server/Common/TCPIP/IOService.cpp:66-145`, and installed contracts at `hmailserver/source/Server/hMailServer/hMailServer.idl:616,2391-2432`. Focused TCPIP/WebAdmin/store/COM coverage passes `10/10`; full Net10 passes `1266` with `3` opt-in tests skipped. PHP CLI lint remains unavailable. Preserve the existing server-admin and COM authorization checks, TCPIP field mapping, installed COM identities, direct activation boundaries, authenticated Settings boundary, Stop/Start behavior, service/database/data-directory state, and SEC-18 isolation. Keep IPHome restoration, SSL runtime behavior, broader background handler hardening, SMTP trust, `Account.ValidatePassword`, backup archive/XML execution, and SEC-18 broker registration out of scope.

- Updated 2026-07-27: authenticated owning-collection `SecurityRange.Delete()` parity is committed in `641599b5c`. Legacy `InterfaceSecurityRange::Delete` (`hmailserver/source/Server/COM/InterfaceSecurityRange.cpp:759-780`) checks the object and server-admin state, then calls `parent_collection_->DeleteItemByDBID(object_->GetID())`; `InterfaceSecurityRanges` supplies that parent for existing lookup results and unsaved `Add()` items through `AttachParent` (`hmailserver/source/Server/COM/COMCollection.h:11-38`), so stale/repeated and unsaved ID-zero item deletes return success without persistence. The .NET item facade now rechecks the administrator callback and calls the owning `SecurityRanges.DeleteByDBID` delegate, preserving membership containment, atomic snapshot removal after store success, and `E_FAIL` failure retention. Installed IID/vtable/DISPID/class identities, direct activation denial, SMTP trust, live policy behavior, and collection index `Delete` remain unchanged. Focused SecurityRanges/store coverage passes `18/18`; full Net10 passes `1236` with `3` opt-in tests skipped. No live SQL integration ran because no approved isolated connection was configured. Next slice: authenticated `Settings -> SecurityRanges.Delete(index)` parity; keep `SetDefault`, existing-row setters/Save, live policy reconfiguration, SMTP trust, and `Account.ValidatePassword` out of scope.

- Updated 2026-07-27: authenticated `Settings -> SecurityRanges.DeleteByDBID` membership-scoped delete parity is committed in `864d2e6d6`. Legacy `InterfaceSecurityRanges::DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceSecurityRanges.cpp:59-74`) delegates to `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`); matching IDs invoke `PersistentSecurityRange::DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentSecurityRange.cpp:33-49`) with `delete from hm_securityranges where rangeid = @RANGEID`, while missing IDs return success without persistence. The .NET path keeps the authenticated `Settings.SecurityRanges` boundary, adds a parameterized SQL delete, removes only the matching authorized snapshot after successful store completion, maps contained exceptions to `E_FAIL`, and preserves direct activation denial and installed IID/vtable/DISPID/class identities. Focused SecurityRanges/store coverage passes `13/13`; full Net10 passes `1231` with `3` opt-in tests skipped. No live SQL integration ran because no approved isolated connection was configured. Next slice: owning-collection `SecurityRange.Delete()` parity; keep index deletion, `SetDefault`, existing-row setters/Save, live policy reconfiguration, SMTP trust, and `Account.ValidatePassword` out of scope.

## SEC-11 SecurityRange Index Delete Parity (2026-07-27)

- Code/test commit `77e16bd4d` implements authenticated `Settings -> SecurityRanges.Delete(index)` parity without changing the installed `IInterfaceSecurityRanges`/`IInterfaceSecurityRange` identity, vtable, DISPID, CLSID, ProgID, type-library, or direct-activation boundaries. The collection selects one zero-based owning snapshot entry, invokes the configured security-range administration store, removes exactly the selected entry only after successful persistence, maps contained store exceptions to COM `E_FAIL`, and silently no-ops for negative, out-of-range, and empty snapshots.
- Legacy references: `hmailserver/source/Server/COM/InterfaceSettings.cpp:440-463` (`get_SecurityRanges` authentication), `hmailserver/source/Server/COM/InterfaceSecurityRanges.cpp:42-57` (`Delete`), `hmailserver/source/Server/Common/BO/Collection.h:217-230` (`DeleteItem`), `hmailserver/source/Server/Common/BO/SecurityRanges.cpp:25-37` (expiry/priority/name ordering), `hmailserver/source/Server/Common/Persistence/PersistentSecurityRange.cpp:33-49` (`DeleteObject`), `hmailserver/source/Server/hMailServer/hMailServer.idl:528-540` (`Settings.SecurityRanges`), and `:1309-1320` (`IInterfaceSecurityRanges.Delete`/`DeleteByDBID`). Legacy negative/out-of-range `LONG` indexes convert to an unsigned collection index and no-op; a valid delete ignores the persistence Boolean result and returns `S_OK`.
- Focused SecurityRanges/store coverage passes `20/20`; full Net10 passes `1238` with `3` opt-in tests skipped. No live SQL integration ran because the approved isolated connection was unset. No production service, database, data directory, COM registration/ACL, SMTP trust behavior, or SEC-18 staging state changed.
- Residual risks remain: existing-row SecurityRange setters/Save, `SetDefault`, live IP policy/auto-ban reconfiguration, and SQL integration evidence. Next slice: audit and implement authenticated existing-row `SecurityRange.Save()` update parity; keep `SetDefault`, live reconfiguration, SMTP trust, and `Account.ValidatePassword` out of scope.

## SEC-11 SecurityRange Existing Save Parity (2026-07-27)

- Code/test commit `02e445a5c` implements authenticated existing-row `SecurityRange.Save()` update parity. Items returned by authorized index, DBID, and name lookups now receive the owning save delegate; setters stage the full persisted snapshot, Save rechecks server-admin authorization, validates empty/duplicate names and mixed or reversed IP ranges, updates all mutable `hm_securityranges` columns through a parameterized `UPDATE ... WHERE rangeid = @id`, preserves the ID and current collection index, and replaces only that owner snapshot after store success. Store exceptions map to COM `E_FAIL` and retain the previous owner snapshot; legacy validation failures use `0x800403E9`.
- Legacy references: `hmailserver/source/Server/COM/InterfaceSettings.cpp:440-463` (`get_SecurityRanges`), `hmailserver/source/Server/COM/InterfaceSecurityRanges.cpp:12-21,92-185` (authenticated load and attached lookups), `hmailserver/source/Server/COM/InterfaceSecurityRange.cpp:36-61` (`Save`), `hmailserver/source/Server/COM/COMCollection.h:6-38` (existing-item parent attachment), `hmailserver/source/Server/Common/Persistence/PersistentSecurityRange.cpp:60-117,268-289` (`SaveObject`/`Validate`), `hmailserver/source/Server/Common/Persistence/PreSaveLimitationsCheck.cpp:407-423` (duplicate names), and `hmailserver/source/Server/Common/BO/SecurityRanges.cpp:25-38` (ordering). The installed SecurityRanges/SecurityRange IID, vtable, DISPID, CLSID, ProgID, type-library, direct activation, SMTP trust, and live policy boundaries remain unchanged.
- Focused SecurityRanges/store coverage passes `27/27`; full Net10 passes `1245` with `3` opt-in tests skipped. No live SQL integration ran because the approved isolated connection was unset. No production service, database, data directory, COM registration/ACL, SMTP trust behavior, or SEC-18 staging state changed.
- Residual risks remain: exact live SQL row-count/error-info evidence, `SetDefault`, live IP policy/auto-ban reconfiguration, and the documented snapshot-model difference where staged changes on one retained item facade are not visible through a second lookup until Save succeeds. Next slice: audit and implement authenticated `Settings -> SecurityRanges.SetDefault()` parity; keep live reconfiguration, SMTP trust, and `Account.ValidatePassword` out of scope.

## SEC-11 SecurityRange SetDefault Parity (2026-07-27)

- Code/test commit `56a668256` implements authenticated `Settings -> SecurityRanges.SetDefault()` parity without changing the installed `IInterfaceSecurityRanges`/`IInterfaceSecurityRange` IID, vtable, DISPID, CLSID, ProgID, type-library, direct-activation, or authenticated Settings boundaries. The collection keeps the legacy no-second-admin-recheck behavior after the authenticated collection has been obtained, refreshes the current snapshot, deletes each refreshed row through the existing parameterized store path, inserts `My computer` with `127.0.0.1` bounds, priority `30`, and options `71627`, inserts `Internet` with full IPv4 bounds, priority `10`, and options `96203`, then performs the final refresh that publishes the ordered snapshot. Store exceptions map to COM `E_FAIL` and the last published collection snapshot remains readable.
- Legacy references: `hmailserver/source/Server/COM/InterfaceSecurityRanges.cpp:219-237` (`SetDefault` authorization/COM wrapper), `hmailserver/source/Server/Common/BO/SecurityRanges.cpp:25-70` (refresh, delete-all, exact defaults, final refresh), `hmailserver/source/Server/Common/BO/Collection.h:203-215` (`DeleteAll`), `hmailserver/source/Server/Common/Persistence/PersistentSecurityRange.cpp:33-49,60-117` (delete/insert persistence), `hmailserver/source/Server/Common/SQL/IPAddressSQLHelper.cpp:74-84` (IPv4 split-column null behavior), `hmailserver/source/Server/hMailServer/hMailServer.idl:528-540,1309-1320` (Settings and SetDefault contracts), and `hmailserver/source/DBScripts/CreateTablesMSSQL.sql:372-388,673-675` (schema/default seed corroboration).
- Focused SecurityRanges/store coverage passes `29/29`; full Net10 passes `1247` with `3` opt-in tests skipped. No live SQL integration ran because the approved isolated connection was unset. No production service, database, data directory, COM registration/ACL, SMTP trust behavior, live IP policy, or SEC-18 staging state changed.
- Residual risks remain: legacy boolean-failure continuation and generic `0x800403E9` error behavior are not reproduced for contained .NET store exceptions, exact live SQL row-count/error-info evidence is still missing, and IP policy/auto-ban runtime reconfiguration remains fenced. Next slice: audit `hmailserver/source/WebAdmin/hm_logging.php` `save` action under SEC-14 for POST-only handling and CSRF-token placement; keep SEC-18 broker registration, PHP session cutover, live reconfiguration, SMTP trust, and `Account.ValidatePassword` out of scope.

## SEC-14 WebAdmin Performance Mutation Hardening (2026-07-27)

- Code/test commit `7c7ca1049` hardens `hmailserver/source/WebAdmin/hm_performance.php` without changing the legacy `MessageIndexing` COM method, settings persistence, indexing runtime, SMTP trust behavior, or any COM identity. The page now reads `action` and every performance save field from the POST body, and both `save` and `ClearMessageIndexingCache` call `hmailRequirePostCsrfToken()` before mutation. Normal GET page rendering remains available; query-only actions and fields no longer select a mutating branch.
- Legacy references: `hmailserver/source/Server/COM/InterfaceMessageIndexing.cpp:102-114` (`InterfaceMessageIndexing::Clear`, server-admin check plus `PersistentMessageMetaData::Clear`) and `hmailserver/source/Server/hMailServer/hMailServer.h:25684-25813` (`IInterfaceMessageIndexing` identity/vtable). WebAdmin references: `hmailserver/source/WebAdmin/index.php:47-50` (normal-page POST CSRF dispatch), `hmailserver/source/WebAdmin/include/functions.php:411-439` (`hmailRequirePost`/`hmailRequirePostCsrfToken`), and `hmailserver/source/WebAdmin/hm_performance.php:11-34` (bounded mutation branches).
- Focused source-surface coverage is `WebAdminPerformancePostOnlySourceTests.PerformanceMutationsUsePostBodyAndRequirePostCsrf`; the WebAdmin manual checklist covers GET clear, query-only action/field/token, valid POST, and server-admin behavior. The focused test passes `1/1`; full Net10 passes `1248` with `3` opt-in tests skipped. PHP CLI lint was unavailable because PHP is not installed on this host.
- Residual risk remains in other less-visible WebAdmin pages. Next slice: audit `hmailserver/source/WebAdmin/hm_backup.php` `save` and `startbackup` actions for POST-only handling and CSRF-token placement; keep backup archive/XML execution, restore, service/DB/data-directory changes, SMTP trust, live reconfiguration, `Account.ValidatePassword`, and SEC-18 broker registration out of scope.

## SEC-14 WebAdmin Backup Mutation Hardening (2026-07-27)

- Code/test commit `7338030e6` hardens `hmailserver/source/WebAdmin/hm_backup.php` without changing backup archive/XML behavior, asynchronous operation state, service maintenance-queue dispatch, or any COM identity. The page now reads `action` and all backup settings fields from the POST body, and both `save` and `startbackup` call `hmailRequirePostCsrfToken()` before mutation. Normal GET page rendering remains available; query-only actions and fields no longer select a mutating branch.
- Legacy references: `hmailserver/source/Server/COM/InterfaceBackupManager.cpp:27-40` (`InterfaceBackupManager::StartBackup`), `hmailserver/source/Server/Common/Application/BackupManager.cpp:38-71` (`BackupManager::StartBackup`), `hmailserver/source/Server/Common/Application/BackupTask.cpp:27-40` (`BackupTask::DoWork`), and `hmailserver/source/Server/Common/Application/BackupExecuter.cpp:47-78` (`LoadSettings_`/`StartBackup` preconditions). WebAdmin references: `hmailserver/source/WebAdmin/index.php:47-50`, `hmailserver/source/WebAdmin/include/functions.php:411-439`, and `hmailserver/source/WebAdmin/hm_backup.php:11-25`.
- Focused source-surface coverage is `WebAdminBackupPostOnlySourceTests.BackupMutationsUsePostBodyAndRequirePostCsrf`; the WebAdmin manual checklist covers GET start, query-only action/field/token, valid POST, and server-admin/asynchronous behavior. The focused test passes `1/1`; full Net10 passes `1249` with `3` opt-in tests skipped. PHP CLI lint was unavailable because PHP is not installed on this host.
- Residual risk remains in other less-visible WebAdmin pages. Next slice: audit `hmailserver/source/WebAdmin/hm_autoban.php` `save` action for POST-only handling and CSRF-token placement; keep backup archive/XML execution, restore, service/DB/data-directory changes, SMTP trust, live reconfiguration, `Account.ValidatePassword`, and SEC-18 broker registration out of scope.

## SEC-14 WebAdmin Greylisting Mutation Hardening (2026-07-27)

- Code/test commit `28a830f5f` hardens `hmailserver/source/WebAdmin/hm_greylisting.php` without changing anti-spam policy, SMTP behavior, persisted setting names, or any COM identity. The page now reads `action` and all greylisting fields from the POST body, and `save` calls `hmailRequirePostCsrfToken()` before mutation. The legacy day-to-hour conversion for initial/final deletion remains unchanged.
- Legacy references: `hmailserver/source/Server/COM/InterfaceAntiSpam.cpp:265-385,1121-1184` (greylisting and bypass getters/setters) and `hmailserver/source/Server/hMailServer/hMailServer.h:19556-19744` (`IInterfaceAntiSpam` contract). WebAdmin references: `hmailserver/source/WebAdmin/index.php:47-50`, `hmailserver/source/WebAdmin/include/functions.php:411-439`, and `hmailserver/source/WebAdmin/hm_greylisting.php:8-24`.
- Focused source-surface coverage is `WebAdminGreyListingPostOnlySourceTests.GreyListingSettingsSaveUsesPostBodyAndRequiresPostCsrf`; the WebAdmin manual checklist covers GET save, query-only action/field/token, valid POST, and server-admin/anti-spam behavior. The focused test passes `1/1`; full Net10 passes `1251` with `3` opt-in tests skipped. PHP CLI lint was unavailable because PHP is not installed on this host.
- Residual risk remains in other less-visible WebAdmin pages. Next slice: audit `hmailserver/source/WebAdmin/hm_logging.php` `save` action for POST-only handling and CSRF-token placement; keep diagnostics/network egress, live reconfiguration, SMTP trust, `Account.ValidatePassword`, and SEC-18 broker registration out of scope.

## SEC-14 WebAdmin Logging Mutation Hardening (2026-07-27)

- Code/test commit `363a9cfb8` hardens `hmailserver/source/WebAdmin/hm_logging.php`: `action` and all nine logging fields now come from the POST body, and `save` requires `hmailRequirePostCsrfToken()` before mutation. Normal `GET page=logging` rendering remains unchanged; the existing server-admin gate, COM logging property mapping, and logging configuration behavior remain unchanged.
- Legacy references: `hmailserver/source/Server/COM/InterfaceSettings.cpp:360-383` (`InterfaceSettings::get_Logging` authentication and child construction), `hmailserver/source/Server/COM/InterfaceLogging.cpp:29-47,64-85,103-124,142-165,183-201,219-240,258-281,459-486,578-601` (enabled, SMTP, POP3, IMAP, AWStats, TCP/IP, application, debug, and keep-open setters), `hmailserver/source/Server/Common/Application/Configuration.cpp:210-420` (persisted logging-bit/setting updates), and `hmailserver/source/Server/hMailServer/hMailServer.idl:539,1104-1148` (installed Settings/IInterfaceLogging contract). WebAdmin references: `hmailserver/source/WebAdmin/index.php:47-50`, `hmailserver/source/WebAdmin/include/functions.php:411-439`, and `hmailserver/source/WebAdmin/hm_logging.php:11-24`.
- Focused `WebAdminLoggingPostOnlySourceTests.LoggingSettingsSaveUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `4/4`; full Net10 passes `1252` with `3` opt-in skips. The manual checklist covers GET save, query-only action/field/token, valid POST, and server-admin/logging behavior. PHP CLI lint was unavailable because PHP is not installed on this host.
- No COM identity, direct-activation boundary, authenticated Settings boundary, logging runtime, production service, database, data directory, or SEC-18 staging state changed. Residual SEC-14 risk remains in other less-visible WebAdmin mutations. Next slice: audit `hmailserver/source/WebAdmin/hm_scripts.php` state-changing actions (`save`, `checksyntax`, and `reloadscripts`) for POST-only/CSRF handling.

## SEC-14 WebAdmin Scripts Mutation Hardening (2026-07-27)

- Code/test commit `8894239af` hardens `hmailserver/source/WebAdmin/hm_scripts.php`: `action`, `scriptingenabled`, and `scriptinglanguage` now come from the POST body, and `save`, `checksyntax`, and `reloadscripts` each require `hmailRequirePostCsrfToken()` before mutation or script execution. Normal `GET page=scripts` rendering remains unchanged; the existing server-admin gate, scripting COM property mapping, validation of `VBScript`/ `JScript`, and `Reload`/`CheckSyntax` behavior remain unchanged.
- Legacy references: `hmailserver/source/Server/COM/InterfaceSettings.cpp:1060-1081` (`InterfaceSettings::get_Scripting` authentication and child construction), `hmailserver/source/Server/COM/InterfaceScripting.cpp:29-57,61-91,95-127` (enabled/language setters, `Reload`, and `CheckSyntax`), `hmailserver/source/Server/Common/Application/Configuration.cpp:372-412` (script settings persistence), and `hmailserver/source/Server/hMailServer/hMailServer.idl:575,1697-1710` (installed Settings/IInterfaceScripting contract). WebAdmin references: `hmailserver/source/WebAdmin/index.php:47-50`, `hmailserver/source/WebAdmin/include/functions.php:411-439`, and `hmailserver/source/WebAdmin/hm_scripts.php:11-35`.
- Focused `WebAdminScriptsPostOnlySourceTests.ScriptActionsUsePostBodyAndRequirePostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `5/5`; full Net10 passes `1253` with `3` opt-in skips. The manual checklist covers GET save/checksyntax/reload, query-only action/field/token, valid POST, and server-admin scripting behavior. PHP CLI lint was unavailable because PHP is not installed on this host.
- No COM identity, direct-activation boundary, authenticated Settings boundary, scripting runtime, production service, database, data directory, or SEC-18 staging state changed. Residual SEC-14 risk remains in other less-visible WebAdmin mutations. Next slice: audit `hmailserver/source/WebAdmin/hm_diagnostics.php` `performTests` for POST-only/CSRF handling and diagnostics/network-egress safety.

## SEC-14 WebAdmin Diagnostics Mutation Hardening (2026-07-27)

- The parity audit confirmed the legacy authenticated path in `hmailserver/source/Server/COM/InterfaceApplication.cpp:481-501`: `InterfaceApplication::get_Diagnostics` denies non-server-admin callers, creates `InterfaceDiagnostics`, and attaches the current authentication object. `InterfaceDiagnostics::PerformTests` and `put_LocalDomainName` retain the server-admin check in `hmailserver/source/Server/COM/InterfaceDiagnostics.cpp:10-35,43-72`; `HM::Diagnostic::PerformTests` preserves the existing information, IPv6, optional outbound-port, backup-directory, optional MX/self-connect, data-directory, IP-range, and error-log test composition in `hmailserver/source/Server/Common/Diagnostics/Diagnostic.cpp:59-98`. The installed `IInterfaceApplication`/`IInterfaceDiagnostics` contract remains `hmailserver/source/Server/hMailServer/hMailServer.idl:1478-1490,2804-2812`.
- Code/test commit `5e49e73e1` hardens `hmailserver/source/WebAdmin/hm_diagnostics.php`: `action` and `LocalDomainName` use `hmailGetPostVar()`, and the `performTests` branch calls `hmailRequirePostCsrfToken()` before COM-backed diagnostics. Normal `GET page=diagnostics` rendering, the existing diagnostic test set, diagnostics/network behavior, authenticated Settings access, direct activation denial, and COM identity remain unchanged. Focused `WebAdminDiagnosticsPostOnlySourceTests.DiagnosticsPerformTestsUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `6/6`; full Net10 passes `1254` with `3` opt-in skips. PHP CLI is unavailable, so PHP lint was not run. Manual checklist cases cover GET action, query-only action/input/token, and valid POST behavior.
- No service Start/Stop, live IP policy reconfiguration, SMTP trust, production service, database, data directory, COM registration/ACL, or SEC-18 staging state changed. Residual SEC-14 risk remains in other less-visible WebAdmin mutations. Next bounded slice: audit `hmailserver/source/WebAdmin/hm_status.php` `control` for POST-only handling while preserving the legacy Start/Stop boundary; keep service mutation execution out of source-only tests.

## SEC-14 WebAdmin Status Control Mutation Hardening (2026-07-27)

- The parity audit confirmed the legacy Application control path in `hmailserver/source/Server/COM/InterfaceApplication.cpp:54-89`: `InterfaceApplication::Start` and `Stop` each require the attached caller to be a server administrator, call the existing `HM::Application::Instance()->StartServers()`/`StopServers()` boundary, and preserve the legacy start-failure COM error. The installed `IInterfaceApplication` contract keeps `Start` DISPID 1, `Stop` DISPID 2, and `ServerState` DISPID 5 at `hmailserver/source/Server/hMailServer/hMailServer.idl:1478-1485`.
- Code/test commit `ba2261292` hardens `hmailserver/source/WebAdmin/hm_status.php`: `action` and `controlaction` use `hmailGetPostVar()`, and the `control` branch calls `hmailRequirePostCsrfToken()` before the existing COM-backed Start/Stop calls. Normal `GET page=status` rendering, status reads, control-button mapping, service-state behavior, authenticated Settings access, direct activation denial, and COM identity remain unchanged. Focused `WebAdminStatusPostOnlySourceTests.StatusControlUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `7/7`; full Net10 passes `1255` with `3` opt-in skips. PHP CLI is unavailable, so PHP lint was not run. Manual checklist cases cover GET control, query-only action/input/token, and valid POST behavior.
- No service Start/Stop execution, live TLS/IP policy reconfiguration, SMTP trust, production service, database, data directory, COM registration/ACL, or SEC-18 staging state changed. Residual SEC-14 risk remains in other less-visible WebAdmin mutations. Next bounded slice: audit `hmailserver/source/WebAdmin/hm_ssltls.php` `save` for POST-only handling while preserving the legacy Settings TLS field mapping; keep TLS runtime mutation out of source-only tests.

## SEC-14 WebAdmin SSL/TLS Settings Mutation Hardening (2026-07-27)

- The parity audit confirmed the authenticated Settings path in `hmailserver/source/Server/COM/InterfaceApplication.cpp:110-120`: `InterfaceApplication::get_Settings` denies non-server-admin callers before returning the Settings facade. The existing `InterfaceSettings` setters preserve the TLS field mapping and configuration semantics in `hmailserver/source/Server/COM/InterfaceSettings.cpp:2244-2289,2292-2424,2426-2488`; the installed Settings DISPIDs are `VerifyRemoteSslCertificate` 93, `SslCipherList` 94, TLS 1.0/1.1/1.2 96-98, TLS 1.3 103, cipher-order 105, and ChaCha priority 106 in `hmailserver/source/Server/hMailServer/hMailServer.idl:656-667,681-691`.
- Code/test commit `1bd30eea` hardens `hmailserver/source/WebAdmin/hm_ssltls.php`: `action` and all TLS save fields use `hmailGetPostVar()`, and the `save` branch calls `hmailRequirePostCsrfToken()` before the existing COM-backed Settings property writes. Normal `GET page=ssltls` rendering, TLS field mapping, conditional ChaCha behavior, authenticated Settings access, direct activation denial, and COM identity remain unchanged. Focused `WebAdminSslTlsPostOnlySourceTests.SslTlsSaveUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `8/8`; full Net10 passes `1256` with `3` opt-in skips. PHP CLI is unavailable, so PHP lint was not run. Manual checklist cases cover GET save, query-only action/field/token, and valid POST behavior.
- No TLS runtime reconfiguration, service Start/Stop, live IP policy reconfiguration, SMTP trust, production service, database, data directory, COM registration/ACL, or SEC-18 staging state changed. Residual SEC-14 risk remains in other less-visible WebAdmin mutations. SMTP `save` is complete in `122847319`; focused source coverage is `11/11` combined and full Net10 is `1259` with `3` opt-in skips. Next bounded slice: audit `hmailserver/source/WebAdmin/hm_smtp_antispam.php` `save` for POST-only handling while preserving the legacy AntiSpam field mapping; keep SpamAssassin test behavior and broader AntiSpam mutations out of scope.

## SEC-14 WebAdmin POP3 Settings Mutation Hardening (2026-07-27)

- The parity audit confirmed the authenticated Settings path in `hmailserver/source/Server/COM/InterfaceApplication.cpp:110-120`: `InterfaceApplication::get_Settings` denies non-server-admin callers before returning Settings. `InterfaceSettings::put_MaxPOP3Connections` and `put_WelcomePOP3` preserve the two field writes in `hmailserver/source/Server/COM/InterfaceSettings.cpp:190-220,730-760`; installed DISPIDs are 6 and 24 at `hmailserver/source/Server/hMailServer/hMailServer.idl:528-532,549-550`.
- Code/test commit `c28d23d79` hardens `hmailserver/source/WebAdmin/hm_pop3.php`: `action`, `maxpop3connections`, and `welcomepop3` use `hmailGetPostVar()`, and the `save` branch calls `hmailRequirePostCsrfToken()` before COM-backed Settings writes. Normal GET rendering, POP3 field mapping, authenticated Settings access, direct activation denial, and COM identity remain unchanged. Focused `WebAdminPop3PostOnlySourceTests.Pop3SaveUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `9/9`; full Net10 passes `1257` with `3` opt-in skips. PHP CLI unavailable, so PHP lint was not run. Manual checklist cases cover GET save, query-only action/field/token, and valid POST behavior.
- No POP3 runtime reconfiguration, service Start/Stop, live TLS/IP policy reconfiguration, SMTP trust, production service, database, data directory, COM registration/ACL, or SEC-18 staging state changed. Residual SEC-14 risk remains in other less-visible WebAdmin mutations. SMTP `save` is complete in `122847319`; next bounded slice: audit `hmailserver/source/WebAdmin/hm_smtp_antispam.php` `save` for POST-only handling while preserving the legacy AntiSpam field mapping; keep SpamAssassin test behavior and broader AntiSpam mutations out of scope.

## SEC-14 WebAdmin IMAP Settings Mutation Hardening (2026-07-27)

- The parity audit confirmed the authenticated Settings path in `hmailserver/source/Server/COM/InterfaceApplication.cpp:110-120`: `InterfaceApplication::get_Settings` denies non-server-admin callers before returning Settings. `InterfaceSettings::put_MaxIMAPConnections` and `put_WelcomeIMAP` preserve their configuration writes at `hmailserver/source/Server/COM/InterfaceSettings.cpp:156-164,764-779`; the IMAP extension setters are `put_IMAPSortEnabled`, `put_IMAPQuotaEnabled`, `put_IMAPIdleEnabled`, and `put_IMAPACLEnabled` at `:1384-1494`; `put_IMAPHierarchyDelimiter` preserves its legacy folder-conflict error at `:2171-2187`; and `put_IMAPMasterUser`, `put_IMAPSASLPlainEnabled`, and `put_IMAPSASLInitialResponseEnabled` are at `:2508-2570`. Installed Settings DISPIDs remain 25, 53-56, 75, 87, and 100-102 at `hmailserver/source/Server/hMailServer/hMailServer.idl:551-552,589-597,625-626,645-646,672-679`.
- Code/test commit `5e694f49c` hardens `hmailserver/source/WebAdmin/hm_imap.php`: `action` and all ten existing IMAP fields use `hmailGetPostVar()`, and the `save` branch calls `hmailRequirePostCsrfToken()` before the existing COM-backed Settings property writes. Normal GET rendering, IMAP field mapping, hierarchy-delimiter behavior, authenticated Settings access, direct activation denial, and COM identity remain unchanged. Focused `WebAdminImapPostOnlySourceTests.ImapSaveUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `10/10`; full Net10 passes `1258` with `3` opt-in skips. PHP CLI is unavailable, so PHP lint was not run. Manual checklist cases cover GET save, query-only action/field/token, valid POST behavior, and hierarchy-delimiter preservation.
- No IMAP runtime reconfiguration, service Start/Stop, live TLS/IP policy reconfiguration, SMTP trust, production service, database, data directory, COM registration/ACL, or SEC-18 staging state changed. Residual SEC-14 risk remains in other less-visible WebAdmin mutations. SMTP `save` is complete in `122847319`; next bounded slice: audit `hmailserver/source/WebAdmin/hm_smtp_antispam.php` `save` for POST-only handling while preserving the legacy AntiSpam field mapping; keep SpamAssassin test behavior and broader AntiSpam mutations out of scope.

## SEC-14 WebAdmin SMTP Settings Mutation Hardening (2026-07-28)

- The parity audit confirmed the authenticated WebAdmin boundary in `hmailserver/source/Server/COM/InterfaceApplication.cpp:110-123`: `InterfaceApplication::get_Settings` denies non-server-admin callers before returning Settings, ensures the database, attaches authentication, and loads the settings snapshot. `hmailRequirePost()` and `hmailRequirePostCsrfToken()` in `hmailserver/source/WebAdmin/include/functions.php:411-439` provide the existing POST and POST-body CSRF enforcement.
- Legacy SMTP setter behavior is preserved by `InterfaceSettings.cpp`: `put_MaxMessageSize` `:92-105`, `put_MaxSMTPConnections` `:124-137`, `put_AllowSMTPAuthPlain` `:263-281`, `put_DenyMailFromNull` `:304-319`, `put_AllowIncorrectLineEndings` `:343-358`, `put_SMTPNoOfTries` `:482-498`, `put_SMTPMinutesBetweenTry` `:519-535`, `put_SMTPRelayer` `:592-607`, `put_SMTPRelayerPort` `:627-642`, `put_HostName` `:662-677`, `put_WelcomeSMTP` `:696-710`, `put_SMTPRelayerRequiresAuthentication` `:913-928`, `put_SMTPRelayerUsername` `:948-962`, `SetSMTPRelayerPassword` `:998-1008`, `put_RuleLoopLimit` `:1256-1268`, `put_SMTPDeliveryBindToIP` `:1352-1364`, `put_MaxSMTPRecipientsInBatch` `:1644-1659`, `put_DisconnectInvalidClients` `:1678-1693`, `put_MaxNumberOfInvalidCommands` `:1712-1724+`, `put_SMTPRelayerConnectionSecurity` `:1766-1780`, `put_SMTPConnectionSecurity` `:1799-1813`, `put_AddDeliveredToHeader` `:1849-1862`, and `put_MaxNumberOfMXHosts` `:2206-2218`. Installed Settings DISPIDs remain in `hmailserver/source/Server/hMailServer/hMailServer.idl:528-571,576-587,604-622,650-655`.
- Code/test commit `122847319` hardens `hmailserver/source/WebAdmin/hm_smtp.php`: `action` and all existing SMTP save fields use `hmailGetPostVar()`, and the `save` branch calls `hmailRequirePostCsrfToken()` before the existing COM-backed Settings writes. The focused `WebAdminSmtpPostOnlySourceTests.SmtpSaveUsesPostBodyAndRequiresPostCsrf` source test passes `1/1`; combined focused WebAdmin source coverage is `11/11`; full Net10 passes `1259` with `3` opt-in skips. PHP CLI is unavailable, so PHP lint was not run. Manual checklist cases cover GET save, query-only action/field/token, valid POST behavior, `AllowMailFromNull` inversion, `SMTPConnectionSecurity` mapping, and non-empty relayer-password updates.
- Normal GET rendering, the server-administrator Settings boundary, direct activation denial, installed COM identity/vtable/DISPID shape, SMTP/anti-spam/auto-ban behavior, live reconfiguration, service/database/data-directory state, and SEC-18 staging state remain unchanged. SMTP AntiVirus `save` is complete in `68d6f0006`; next bounded slice: audit `hmailserver/source/WebAdmin/hm_smtp_antispam.php` `save` for POST-only handling while preserving the legacy AntiSpam field mapping; keep SpamAssassin test behavior and broader AntiSpam mutations out of scope.

## SEC-14 WebAdmin SMTP AntiVirus Settings Mutation Hardening (2026-07-28)

- The parity audit confirmed the authenticated path: `InterfaceApplication::get_Settings` in `hmailserver/source/Server/COM/InterfaceApplication.cpp:110-123` denies non-server-admin callers before returning Settings, and `InterfaceSettings::get_AntiVirus` in `hmailserver/source/Server/COM/InterfaceSettings.cpp:387-410` repeats the server-administrator boundary before creating the child facade. Existing POST/CSRF enforcement remains `hmailRequirePost()` and `hmailRequirePostCsrfToken()` in `hmailserver/source/WebAdmin/include/functions.php:411-439`.
- Legacy AntiVirus setter behavior is preserved by `hmailserver/source/Server/COM/InterfaceAntiVirus.cpp`: ClamWin setters are `put_ClamWinEnabled`, `put_ClamWinExecutable`, and `put_ClamWinDBFolder` at `:37-111`; custom-scanner setters are `put_CustomScannerEnabled`, `put_CustomScannerExecutable`, and `put_CustomScannerReturnValue` at `:138-213`; action/notification/max-size setters are `put_Action`, `put_NotifySender`, `put_NotifyReceiver`, and `put_MaximumMessageSize` at `:248-369`; attachment blocking is `put_EnableAttachmentBlocking` at `:400-407`; and ClamAV setters are `put_ClamAVEnabled`, `put_ClamAVHost`, and `put_ClamAVPort` at `:451-526`. The installed contract keeps `Settings.AntiVirus` at DISPID 30 and `IInterfaceAntiVirus` property DISPIDs 1-15 in `hmailserver/source/Server/hMailServer/hMailServer.idl:561,1329-1364`.
- Code/test commit `68d6f0006` hardens `hmailserver/source/WebAdmin/hm_smtp_antivirus.php`: `action` and all 14 existing AntiVirus save fields use `hmailGetPostVar()`, and the `save` branch calls `hmailRequirePostCsrfToken()` before the existing COM-backed AntiVirus writes. `WebAdminSmtpAntivirusPostOnlySourceTests.SmtpAntivirusSaveUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `12/12`; full Net10 passes `1260` with `3` opt-in skips. PHP CLI is unavailable, so PHP lint was not run. Manual checklist cases cover GET save, query-only action/field/token, valid POST behavior, scanner-test preservation, and blocked-attachment deletion preservation.
- Normal GET rendering, AntiVirus field defaults, scanner AJAX behavior, blocked-attachment delete handling, the server-administrator Settings boundary, direct activation denial, installed COM identity/vtable/DISPID shape, SMTP/anti-spam/auto-ban behavior, live reconfiguration, service/database/data-directory state, and SEC-18 staging state remain unchanged. Next bounded slice: audit `hmailserver/source/WebAdmin/hm_smtp_antispam.php` `save` for POST-only handling while preserving the legacy AntiSpam field mapping; keep SpamAssassin test behavior and broader AntiSpam mutations out of scope.

## SEC-14 WebAdmin SMTP AntiSpam Settings Mutation Hardening (2026-07-28)

- The parity audit confirmed the authenticated path: `InterfaceApplication::get_Settings` in `hmailserver/source/Server/COM/InterfaceApplication.cpp:110-123` denies non-server-admin callers before returning Settings. `InterfaceSettings::get_AntiSpam` in `hmailserver/source/Server/COM/InterfaceSettings.cpp:1138-1159` creates the child facade and `InterfaceAntiSpam::LoadSettings` at `:28-35` requires the server-admin state before loading the legacy configuration. Existing POST/CSRF enforcement remains `hmailRequirePost()` and `hmailRequirePostCsrfToken()` in `hmailserver/source/WebAdmin/include/functions.php:411-439`.
- Legacy AntiSpam setter behavior is preserved by `hmailserver/source/Server/COM/InterfaceAntiSpam.cpp`: HELO/PTR checks and scores are `:61-196`; spam thresholds are `:197-263`; header/prepend settings are `:393-515`; SPF/MX settings are `:571-705`; SpamAssassin settings are `:794-961`; maximum anti-spam message size is `:964-998`; and DKIM verification settings are `:1053-1119`. The installed contract keeps `Settings.AntiSpam` at DISPID 63 and `IInterfaceAntiSpam` properties at DISPIDs 1-38 in `hmailserver/source/Server/hMailServer/hMailServer.idl:608,2195-2273`.
- Code/test commit `8c751e65f` hardens `hmailserver/source/WebAdmin/hm_smtp_antispam.php`: `action` and all 22 existing AntiSpam save fields use `hmailGetPostVar()`, and the `save` branch calls `hmailRequirePostCsrfToken()` before the existing COM-backed AntiSpam writes. `WebAdminSmtpAntispamPostOnlySourceTests.SmtpAntispamSaveUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `13/13`; full Net10 passes `1261` with `3` opt-in skips. PHP CLI is unavailable, so PHP lint was not run. Manual checklist cases cover GET save, query-only action/field/token, valid POST behavior, AntiSpam field mapping, SpamAssassin test behavior, and SMTP spam-policy preservation.
- Normal GET rendering, AntiSpam field defaults, SpamAssassin AJAX behavior, the server-administrator Settings boundary, direct activation denial, installed COM identity/vtable/DISPID shape, SMTP/anti-spam/auto-ban behavior, live reconfiguration, service/database/data-directory state, and SEC-18 staging state remain unchanged. Next bounded slice: audit `hmailserver/source/WebAdmin/hm_mirror.php` `save` for POST-only handling while preserving the `MirrorEMailAddress` field mapping; keep SMTP delivery mirroring, live reconfiguration, SMTP trust, and broader Settings mutation out of scope.

## SEC-14 WebAdmin Mirror Settings Mutation Hardening (2026-07-28)

- The parity audit confirmed the authenticated path: legacy `InterfaceApplication::get_Settings` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:110-123`) denies non-server-admin callers before returning Settings. `InterfaceSettings::get_MirrorEMailAddress` and `put_MirrorEMailAddress` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:207-240`) preserve the existing `GetMirrorAddress`/`SetMirrorAddress` behavior; the installed Settings contract retains `MirrorEMailAddress` at DISPID 7 (`hmailserver/source/Server/hMailServer/hMailServer.idl:528-534`).
- Code/test commit `9740cbc62` hardens `hmailserver/source/WebAdmin/hm_mirror.php`: `action` and the existing `mirroremailaddress` field use `hmailGetPostVar()`, and the `save` branch calls `hmailRequirePostCsrfToken()` before the existing COM-backed Settings write. `WebAdminMirrorPostOnlySourceTests.MirrorSaveUsesPostBodyAndRequiresPostCsrf` passes `1/1`; combined focused WebAdmin source coverage is `14/14`; full Net10 passes `1262` with `3` opt-in skips. PHP CLI is unavailable, so PHP lint was not run. Manual checklist cases cover GET save, query-only action/field/token, valid POST behavior, `MirrorEMailAddress` mapping, and SMTP delivery-mirroring preservation.
- Normal GET rendering, the Mirror field mapping, the server-administrator Settings boundary, direct activation denial, installed COM identity/vtable/DISPID shape, SMTP delivery behavior, live reconfiguration, service/database/data-directory state, and SEC-18 staging state remain unchanged. Next bounded slice: audit `hmailserver/source/WebAdmin/background_servermessage_save.php` for POST-only handling while preserving the existing server-message text Save behavior; keep broader background handler hardening and unrelated Settings mutation out of scope.

## Current Authoritative Update (2026-07-28)

SEC-14 DomainAlias add/delete hardening is complete in code/test commit `6c4d879ee`. `background_domain_name_save.php` now requires POST plus POST-body CSRF, reads `domainid`, `aliasid`, `action`, and `aliasname` with `hmailGetPostVar()`, and preserves the legacy `DomainAliases.Add`, `DomainAlias.AliasName`, `Save`, `DeleteByDBID`, and redirect paths. Legacy references are `InterfaceDomain::get_DomainAliases` (`hmailserver/source/Server/COM/InterfaceDomain.cpp:413-438`), `InterfaceDomainAliases::Add`/`DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceDomainAliases.cpp:73-103,139-154`), and `InterfaceDomainAlias::Save`/`put_AliasName` (`hmailserver/source/Server/COM/InterfaceDomainAlias.cpp:71-128`). Installed IIDs, vtable/DISPID order, and CLSIDs remain those in `hmailserver/source/Server/hMailServer/hMailServer.idl:1717-1752,3156-3168`; focused DomainAlias/source/store coverage is `8/8`, full Net10 is `1264` passed with `3` opt-in skips, and PHP CLI is unavailable.

SEC-18 is currently **RED**, not GREEN/YELLOW: the fresh fail-closed collector `artifacts/sec18-staging/staging-inventory-20260728-nonpool-denial-failclosed.json` exits `2`, has no IIS mapping or caller-token evidence, and reports `ReadyForBrokerRegistration=false`. The temporary probe is absent, the current endpoint is `404`, and hMailServer is `Stopped`/`Disabled`; the sanitized approval-rerun report is `artifacts/sec18-staging/SEC18-nonpool-approval-rerun-20260728.md`. Both independent reviewers returned RED. A fresh elevated authorized/non-pool matrix with immutable counter/correlation evidence is required before broker registration or PHP session cutover.

## SEC-14 WebAdmin DomainAlias Mutation Hardening (2026-07-28)

- The parity audit confirmed the authenticated legacy path: `InterfaceDomain::get_DomainAliases` (`hmailserver/source/Server/COM/InterfaceDomain.cpp:413-438`) requires an attached domain object and domain-admin authentication before creating and refreshing the child collection. `InterfaceDomainAliases::Add` (`hmailserver/source/Server/COM/InterfaceDomainAliases.cpp:73-103`) creates an unsaved `HM::DomainAlias`, assigns the owning domain ID, and attaches the item to its parent; `InterfaceDomainAlias::put_AliasName` and `Save` (`hmailserver/source/Server/COM/InterfaceDomainAlias.cpp:71-128`) preserve the field setter and persistence path; `InterfaceDomainAliases::DeleteByDBID` (`.../InterfaceDomainAliases.cpp:139-154`) delegates deletion to the owning collection. The installed `IInterfaceDomainAliases`/`IInterfaceDomainAlias` IIDs, vtable order, DISPIDs, and DomainAliases/DomainAlias CLSIDs remain those in `hmailserver/source/Server/hMailServer/hMailServer.idl:1717-1752,3156-3168`.
- Code/test commit `6c4d879ee` hardens `hmailserver/source/WebAdmin/background_domain_name_save.php`: the existing server-admin guard remains first, the handler requires `hmailRequirePostCsrfToken()`, and `domainid`, `aliasid`, `action`, and `aliasname` are read from POST only. The existing DomainAliases `Add`/`AliasName`/`Save`, `DeleteByDBID`, and redirect mappings remain unchanged. `WebAdminDomainAliasPostOnlySourceTests` plus `DomainAliasesComContractTests` and `SqlServerDomainAliasAdministrationStoreTests` pass `8/8`; the full Net10 suite passes `1264` with `3` opt-in skips. PHP CLI is unavailable, so PHP runtime validation was not run. Manual Web form cases cover add and delete GET rejection, query-only mutation inputs, and valid server-admin POST behavior.
- No COM identity, direct activation boundary, authenticated domain-admin/server-admin boundary, SMTP behavior, live reconfiguration, service, database, Data directory, or SEC-18 staging state changed. The next bounded slice is `hmailserver/source/WebAdmin/background_tcpipport_save.php`; keep IPHome restoration, TCPIP COM mutation implementation, broader WebAdmin mutations, and SEC-18 broker registration out of scope.

## SEC-14 WebAdmin TCPIP Port Mutation Hardening (2026-07-28)

- The parity audit confirmed that the live multihoming surface is `Settings -> TCPIPPorts`, not IPHome. Legacy `InterfaceSettings::get_TCPIPPorts` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:1187-1203`) returns the authenticated child collection; `InterfaceTCPIPPorts::Add`/`DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceTCPIPPorts.cpp:101-168`) preserve collection mutation dispatch; `InterfaceTCPIPPort::Save` and setters (`hmailserver/source/Server/COM/InterfaceTCPIPPort.cpp:33-293`) preserve the item field mapping and server-admin Save boundary; `PersistentTCPIPPort::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentTCPIPPort.cpp:53-101`) persists `portprotocol`, `portnumber`, split address columns, `portconnectionsecurity`, and `portsslcertificateid`. Runtime bind construction remains `IOService::DoWork` (`hmailserver/source/Server/Common/TCPIP/IOService.cpp:66-145`). Installed IIDs, vtable/DISPID order, and CLSIDs remain in `hmailserver/source/Server/hMailServer/hMailServer.idl:616,2391-2432`.
- Code/test commit `272d56b5c` hardens `hmailserver/source/WebAdmin/background_tcpipport_save.php`: the existing server-admin guard remains first, the handler requires `hmailRequirePostCsrfToken()`, and `tcpipportid`, `protocol`, `portnumber`, `action`, `ConnectionSecurity`, `SSLCertificateID`, and `Address` are read from POST only. The existing `Settings -> TCPIPPorts` Add/Edit/DeleteByDBID, property assignments, Save, Stop/Start restart, ID assignment, and redirects remain unchanged. The existing edit and delete forms already carry POST CSRF fields, so no form change was needed. `WebAdminTcpIpPortPostOnlySourceTests`, `TcpIpPortsComContractTests`, and `SqlServerTcpIpPortAdministrationStoreTests` pass `10/10`; full Net10 passes `1266` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP validation was not run.
- The historical `background_iphome_save.php` handler is not recorded as a parity implementation: `hm_iphomes` was created by the 4.1 migration and dropped by `Upgrade4402to5000{MySQL,MSSQL}.sql`; no current legacy C++ class, IDL contract, .NET surface, or WebAdmin form exists. The next live SEC-14 mutation is `hmailserver/source/WebAdmin/background_sslcertificate_save.php`. No COM identity, direct activation boundary, authenticated Settings boundary, listener runtime beyond the existing legacy restart, service/database/Data-directory state, SMTP behavior, or SEC-18 staging state changed.

## SEC-14 WebAdmin SSL Certificate Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceApplication::get_Settings` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:110-128`), `InterfaceSettings::get_SSLCertificates` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:1212-1237`), `InterfaceSSLCertificates` (`hmailserver/source/Server/COM/InterfaceSSLCertificates.cpp:75-196`), `InterfaceSSLCertificate` (`hmailserver/source/Server/COM/InterfaceSSLCertificate.cpp:13-171`), and `PersistentSSLCertificate::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentSSLCertificate.cpp:25-86`). `Add()` returns an unsaved certificate scoped to `SSLCertificates`; `ItemByDBID` resolves existing rows; `Save()` persists `Name`, `CertificateFile`, and `PrivateKeyFile`; and `DeleteByDBID` preserves the existing collection mutation path. The installed SSL IIDs, vtable/DISPID order, CLSIDs, and ProgIDs remain in `hmailserver/source/Server/hMailServer/hMailServer.idl:620,2514-2553,2844-2853` and `InterfaceSSLCertificate(s).rgs`.
- Code/test commit `4ed9d2f26` hardens `hmailserver/source/WebAdmin/background_sslcertificate_save.php`: the existing server-admin authorization check remains first, the handler requires `hmailRequirePostCsrfToken()`, and `action`, `id`, `Name`, `CertificateFile`, and `PrivateKeyFile` are read from POST only. Existing `Settings -> SSLCertificates` Add/Edit/DeleteByDBID, field assignments, Save, and redirects remain unchanged. `hm_sslcertificate.php` and `hm_sslcertificates.php` already carry POST, CSRF, page, action, ID, and certificate fields. `WebAdminSslCertificatePostOnlySourceTests`, `SslCertificatesComContractTests`, and `SqlServerSslCertificateAdministrationStoreTests` pass `18/18`; full Net10 passes `1267` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP lint and WebAdmin request execution were not run.
- TLS listener certificate loading remains the legacy startup path in `IOService::DoWork` (`hmailserver/source/Server/Common/TCPIP/IOService.cpp:82-131`) and `SslContextInitializer::InitServer` (`hmailserver/source/Server/Common/TCPIP/SslContextInitializer.cpp:63-99`); no live reconfiguration, certificate path validation, referenced-port deletion policy, COM implementation, service/database/Data-directory state, SMTP behavior, or SEC-18 staging state changed. The next smallest safe WebAdmin mutation is `hmailserver/source/WebAdmin/background_surblserver_save.php`; keep broader background hardening and PHP session cutover out of scope.

## SEC-14 WebAdmin SURBL Server Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceApplication::get_Settings` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:110-134`), `InterfaceSettings::get_AntiSpam` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:1138-1159`), `InterfaceAntiSpam::get_SURBLServers` (`hmailserver/source/Server/COM/InterfaceAntiSpam.cpp:39-59`), `InterfaceSURBLServers` (`hmailserver/source/Server/COM/InterfaceSURBLServers.cpp:12-192`), `InterfaceSURBLServer` (`hmailserver/source/Server/COM/InterfaceSURBLServer.cpp:11-208`), and `PersistentSURBLServer` (`hmailserver/source/Server/Common/Persistence/PersistentSURBLServer.cpp:25-90`). Legacy `Add()` returns an unsaved item scoped to the owning collection; `Save()` persists `Active`, `DNSHost`, `RejectMessage`, and `Score` and adds the item after success. The installed SURBL IIDs, vtable/DISPID order, CLSIDs, and ProgIDs remain in `hmailserver/source/Server/hMailServer/hMailServer.idl:2144-2220,3314-3337` and the SURBL registry resources.
- Code/test commit `d8e785231` hardens `hmailserver/source/WebAdmin/background_surblserver_save.php`: the existing server-admin check remains first, the handler requires `hmailRequirePostCsrfToken()`, and `action`, `id`, `Active`, `DNSHost`, `RejectMessage`, and `Score` are read from POST only. Existing `Settings -> AntiSpam -> SURBLServers` Add/Edit/DeleteByDBID, property assignments, Save, and redirects remain unchanged. `hm_surblserver.php` and `hm_surblservers.php` already carry POST, CSRF, page, action, ID, and SURBL fields. `WebAdminSurblServerPostOnlySourceTests`, `SurblServersComContractTests`, and `SqlServerSurblServerAdministrationStoreTests` pass `9/9`; full Net10 passes `1268` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP lint and WebAdmin request execution were not run.
- The legacy SMTP/SURBL runtime remains separate in `SpamTestSURBL::RunTest` (`hmailserver/source/Server/Common/AntiSpam/SpamTestSURBL.cpp:28-79`) and `SMTPConnection` (`hmailserver/source/Server/SMTP/SMTPConnection.cpp:802-869`); no DNSBL behavior, SMTP/SURBL runtime, live reconfiguration, COM/store mutation implementation, service/database/Data-directory state, or SEC-18 staging state changed. The next smallest safe WebAdmin mutation is `hmailserver/source/WebAdmin/background_dnsblacklist_save.php`; keep broader background hardening and PHP session cutover out of scope.

## SEC-14 WebAdmin DNS Blacklist Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceApplication::get_Settings` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:110-134`), `InterfaceSettings::get_AntiSpam` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:1138-1159`), `InterfaceAntiSpam::get_DNSBlackLists` (`hmailserver/source/Server/COM/InterfaceAntiSpam.cpp:707-722`), `InterfaceDNSBlackLists` (`hmailserver/source/Server/COM/InterfaceDNSBlackLists.cpp:12-194`), `InterfaceDNSBlackList` (`hmailserver/source/Server/COM/InterfaceDNSBlackList.cpp:14-242`), and `PersistentDNSBlackList` (`hmailserver/source/Server/Common/Persistence/PersistentDNSBlacklist.cpp:25-89`). Legacy `Add()` returns an unsaved item with `ID == 0` scoped to the owning collection; `Save()` inserts `Active`, `DNSHost`, `ExpectedResult`, `RejectMessage`, and `Score`, assigns the generated identity, and adds the item after success. The installed DNSBL IIDs, DISPIDs/vtable order, CLSIDs, and ProgIDs remain in `hmailserver/source/Server/hMailServer/hMailServer.idl:1437-1469,2236,3057-3070` and the DNSBL registry resources.
- Code/test commit `92586a66d` hardens `hmailserver/source/WebAdmin/background_dnsblacklist_save.php`: the existing server-admin check remains first, the handler requires `hmailRequirePostCsrfToken()`, and all seven mutation fields are read from POST only. Existing `Settings -> AntiSpam -> DNSBlackLists` Add/Edit/DeleteByDBID, `Active`/`DNSHost`/`ExpectedResult`/`RejectMessage`/`Score` assignments, Save, and redirects remain unchanged. `WebAdminDnsBlackListPostOnlySourceTests`, `DnsBlackListsComContractTests`, and `SqlServerDnsBlackListAdministrationStoreTests` pass `9/9`; full Net10 passes `1269` with `3` opt-in skips. PHP is unavailable, so runtime PHP lint and WebAdmin request execution were not run.
- The .NET read-only DNSBL path remains separate in `DnsBlackLists.cs` and `SqlServerDnsBlackListAdministrationStore.cs`; COM writes remain `E_NOTIMPL`. No DNSBL/SMTP runtime, live reconfiguration, COM identity, service/database/Data-directory state, or SEC-18 staging state changed. The next smallest live WebAdmin mutation is `hmailserver/source/WebAdmin/background_whitelistaddress_save.php`; keep broader background hardening and PHP session cutover out of scope.

## SEC-14 WebAdmin Whitelist Address Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceApplication::get_Settings` (`hmailserver/source/Server/COM/InterfaceApplication.cpp:110-134`), `InterfaceSettings::get_AntiSpam` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:1138-1159`), `InterfaceAntiSpam::get_WhiteListAddresses` (`hmailserver/source/Server/COM/InterfaceAntiSpam.cpp:546-569`), `InterfaceWhiteListAddresses` (`hmailserver/source/Server/COM/InterfaceWhiteListAddresses.cpp:63-215`), `InterfaceWhiteListAddress` (`hmailserver/source/Server/COM/InterfaceWhiteListAddress.cpp:8-197`), and `PersistentWhiteListAddress` (`hmailserver/source/Server/Common/Persistence/PersistentWhiteListAddress.cpp:27-92`). Legacy `Add()` returns an unsaved item with `ID == 0` scoped to the owning collection; `Save()` inserts or updates the four persisted address fields and appends a new item after the generated `whiteid` is assigned. The installed whitelist IIDs, DISPIDs/vtable order, CLSIDs, and ProgIDs remain in `hmailserver/source/Server/hMailServer/hMailServer.idl:2195-2220,2443-2475,3404-3420` and the whitelist registry resources.
- Code/test commit `34d8fe83e` hardens `hmailserver/source/WebAdmin/background_whitelistaddress_save.php`: the existing server-admin check remains first, the handler requires `hmailRequirePostCsrfToken()`, and all six mutation reads use POST only. Existing lower/upper IP defaults (`0.0.0.0`/`255.255.255.255`), empty email default (`*`), `Settings -> AntiSpam -> WhiteListAddresses` Add/Edit/DeleteByDBID, field assignments, Save, and redirects remain unchanged. `WebAdminWhiteListAddressPostOnlySourceTests`, `WhiteListAddressesComContractTests`, and `SqlServerWhiteListAddressAdministrationStoreTests` pass `12/12`; full Net10 passes `1270` with `3` opt-in skips. PHP is unavailable, so runtime PHP lint and WebAdmin request execution were not run.
- The .NET read-only whitelist path remains separate in `WhiteListAddresses.cs` and `SqlServerWhiteListAddressAdministrationStore.cs`; COM writes remain `E_NOTIMPL`. No whitelist/DNSBL/SMTP runtime, live reconfiguration, COM identity, service/database/Data-directory state, or SEC-18 staging state changed. The next smallest live WebAdmin mutation is `hmailserver/source/WebAdmin/background_distributionlist_save.php`; keep broader background hardening and PHP session cutover out of scope.

## SEC-14 WebAdmin Distribution List Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceDomains::Refresh/get_ItemByDBID` (`hmailserver/source/Server/COM/InterfaceDomains.cpp:49-65`), `InterfaceDomain::get_DistributionLists` (`hmailserver/source/Server/COM/InterfaceDomain.cpp:447-468`), `InterfaceDistributionLists::Add/DeleteByDBID` (`hmailserver/source/Server/COM/InterfaceDistributionLists.cpp:38-84`), `InterfaceDistributionList` setters, `Save`, and `Delete` (`hmailserver/source/Server/COM/InterfaceDistributionList.cpp:81-277`), `PersistentDistributionList::SaveObject/DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentDistributionList.cpp:36-157`), and `Collection::DeleteItemByDBID` (`hmailserver/source/Server/Common/BO/Collection.h:181-200`). Legacy `Add()` creates an unsaved item with `ID == 0` scoped to the owning domain collection; `Save()` inserts or updates `distributionlistdomainid`, `distributionlistenabled`, `distributionlistaddress`, `distributionlistrequireauth`, `distributionlistrequireaddress`, and `distributionlistmode`, assigns a generated identity on insert, and adds a new item after success. The installed `DistributionLists`/`DistributionList` IIDs, DISPIDs/vtable order, CLSIDs, and ProgIDs remain in `hmailserver/source/Server/hMailServer/hMailServer.idl:1148-1192,2993-3007` and the distribution-list registry resources. Legacy SMTP list-policy consumption remains in `RecipientParser::UserCanSendToList_` (`hmailserver/source/Server/SMTP/RecipientParser.cpp:365-451`).
- Code/test commit `c7e5bc23a` hardens `hmailserver/source/WebAdmin/background_distributionlist_save.php`: the existing user-level denial remains first, the handler requires `hmailRequirePostCsrfToken()`, and all eight mutation reads use POST only. Existing same-domain domain-admin ownership, `IsAddAllowed`, `Domain -> DistributionLists -> DistributionList`, Add/Edit/Delete, five field assignments, defaults, Save/error handling, and redirects remain unchanged. `WebAdminDistributionListPostOnlySourceTests`, `DistributionListsComContractTests`, `DomainsComContractTests`, and `SqlServerDistributionListAdministrationStoreTests` pass `14/14`; full Net10 passes `1271` with `3` opt-in skips. The edit and delete forms already submit POST with CSRF-bearing fields; PHP CLI is unavailable, so runtime PHP lint and WebAdmin request execution were not run.
- The .NET distribution-list path remains intentionally read-only in `DistributionLists.cs`, `IDistributionListAdministrationStore`, and `SqlServerDistributionListAdministrationStore.cs`; collection Add/Delete and item setters/Save/Delete remain `E_NOTIMPL`. No COM identity, direct activation boundary, domain-admin/server-admin boundary, SMTP list-policy behavior, live reconfiguration, service/database/Data-directory state, or SEC-18 staging state changed. The next smallest live WebAdmin mutation is `hmailserver/source/WebAdmin/background_servermessage_save.php`; keep broader background hardening and PHP session cutover out of scope.

## SEC-14 WebAdmin Incoming Relay Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceSettings::get_IncomingRelays` (`hmailserver/source/Server/COM/InterfaceSettings.cpp:1944-1973`), `InterfaceIncomingRelays::Add` (`hmailserver/source/Server/COM/InterfaceIncomingRelays.cpp:150-177`), `InterfaceIncomingRelay::Save` and scalar setters (`hmailserver/source/Server/COM/InterfaceIncomingRelay.cpp:12-36,72-87,106-121,139-153`), and `PersistentIncomingRelay::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentIncomingRelay.cpp:53-87`). Legacy `Add()` returns an unsaved item scoped to the owning collection; `LowerIP`/`UpperIP` setters call `TryParse` but return `S_OK` even when parsing fails; and `Save()` inserts or updates the `hm_incoming_relays` row, assigns the generated ID on insert, and adds a new item after successful persistence. Installed `IncomingRelays`/`IncomingRelay` interfaces and classes remain in `hmailserver/source/Server/hMailServer/hmailServer.idl:2744-2777,3532-3544` with the existing .NET identities in `HMailServer.ComInterop/IncomingRelays.cs:8-71,309-314`.
- Code/test commit `fc2aa90f6` hardens `hmailserver/source/WebAdmin/background_incomingrelay_save.php`: the existing server-admin guard remains first, the handler requires `hmailRequirePostCsrfToken()`, and `action`, `relayid`, `relayname`, `relaylowerip`, and `relayupperip` are read from POST only. Existing `Settings -> IncomingRelays` ItemByDBID/Add/DeleteByDBID, field assignments, Save, redirects, and the already POST+CSRF-bearing edit/delete forms remain unchanged. `WebAdminIncomingRelayPostOnlySourceTests`, `IncomingRelaysComContractTests`, `SqlServerIncomingRelayAdministrationStoreTests`, and `SettingsComContractTests` pass `37/37`; full Net10 passes `1272` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP lint and WebAdmin request execution were not run.
 - The .NET incoming-relay path remains scoped to the existing authenticated Settings adapter and configured SQL administration store; direct activation remains `E_ACCESSDENIED`, SMTP trust and live reconfiguration remain unchanged, and no COM identity, service, database, data-directory, or SEC-18 staging state changed. The next smallest live WebAdmin mutation is `hmailserver/source/WebAdmin/background_route_save.php` POST-only/CSRF handling; keep alias COM mutation implementation, SMTP list policy, and broader background hardening out of scope.

## SEC-14 WebAdmin Blocked Attachment Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceAntiVirus::get_BlockedAttachments` (`hmailserver/source/Server/COM/InterfaceAntiVirus.cpp:375-398`), `InterfaceBlockedAttachments::Add` and lookup/delete methods (`hmailserver/source/Server/COM/InterfaceBlockedAttachments.cpp:75-151`), `InterfaceBlockedAttachment::Save` and setters (`hmailserver/source/Server/COM/InterfaceBlockedAttachment.cpp:14-103`), and `PersistentBlockedAttachment::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentBlockedAttachment.cpp:51-84`). Legacy Add returns a staged item attached to the owning collection; Save inserts `hm_blocked_attachments` for ID zero, assigns the generated ID, and adds the item to the parent after successful persistence.
- Code/test commit `bfee58cab` hardens `hmailserver/source/WebAdmin/background_blocked_attachment_save.php`: the existing server-admin guard remains first, the handler requires `hmailRequirePostCsrfToken()`, and `id`, `wildcard`, `description`, and `action` are read from POST only. Existing `Settings -> AntiVirus -> BlockedAttachments` lookups, Add/Edit/DeleteByDBID, field assignments, Save, redirects, and forms remain unchanged. Focused source/COM/store coverage passes `38/38`; full Net10 passes `1274` with `3` opt-in skips. PHP CLI is unavailable.
 - The .NET BlockedAttachments adapter and SQL administration store remain intentionally read-only: collection Add, item setters/Save/Delete, and direct child activation remain unimplemented or denied outside the authenticated Settings boundary. SMTP attachment-policy behavior, live reconfiguration, service/database/Data-directory state, and SEC-18 staging state did not change. Next slice: `background_route_save.php` POST-only/CSRF hardening.

## SEC-14 WebAdmin Route Address Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceRoute::get_Addresses` (`hmailserver/source/Server/COM/InterfaceRoute.cpp:301-319`), `InterfaceRouteAddresses::Add`, `DeleteByDBID`, and `get_ItemByDBID` (`hmailserver/source/Server/COM/InterfaceRouteAddresses.cpp:33-164`), `InterfaceRouteAddress::put_Address`, `put_RouteID`, `Save`, and `Delete` (`hmailserver/source/Server/COM/InterfaceRouteAddress.cpp:47-148`), and `PersistentRouteAddress::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentRouteAddress.cpp:35-67`). The selected route owns the child collection; Add stages a child with that route ID; Save writes `hm_routeaddresses`, assigns the generated ID, and adds the item to its parent after successful persistence. Installed route-address IIDs, DISPIDs, CLSIDs, and coclass identities remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1551-1578,3097-3110`.
- Code/test commit `2394e026f` hardens `hmailserver/source/WebAdmin/background_route_address_save.php`: the existing domain-admin boundary remains first, the handler requires `hmailRequirePostCsrfToken()`, and `routeid`, `routeaddressid`, `action`, and `routeaddress` are read from POST only. Existing `Settings -> Routes -> Route -> RouteAddress` lookup, Add/Edit/Delete, `Address`/`RouteID` assignments, Save, redirects, and the already POST+CSRF-bearing edit/delete forms remain unchanged. Focused route-address source/COM/store coverage passes `19/19`; full Net10 passes `1276` with `3` opt-in skips. PHP CLI is unavailable.
 - The .NET `RouteAddresses` adapter and SQL store remain intentionally read-only for Add, item setters, and Save; direct child activation remains `E_ACCESSDENIED`, while existing authorized read/delete ownership and SQL parameterization remain unchanged. SMTP routing, live reconfiguration, service/database/Data-directory state, and SEC-18 staging state did not change. Next slice: `background_route_save.php` POST-only/CSRF hardening.

## SEC-14 WebAdmin Distribution-List Recipient Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceDistributionList::get_Recipients` (`hmailserver/source/Server/COM/InterfaceDistributionList.cpp:279-306`), `InterfaceDistributionListRecipients::Add`, `DeleteByDBID`, and `get_ItemByDBID` (`hmailserver/source/Server/COM/InterfaceDistributionListRecipients.cpp:37-82,154-180`), `InterfaceDistributionListRecipient::put_RecipientAddress`, `Delete`, and `Save` (`hmailserver/source/Server/COM/InterfaceDistributionListRecipient.cpp:93-159`), and `PersistentDistributionListRecipient::SaveObject` (`hmailserver/source/Server/Common/Persistence/PersistentDistributionListRecipient.cpp:94-137`). Legacy `Add()` creates an unsaved recipient scoped to the owning distribution-list collection; `Save()` inserts or updates `hm_distributionlistsrecipients`, assigns the generated identity on insert, and adds a new item to the parent only after successful persistence. Installed recipient interfaces and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1199-1230,3010-3022`.
- Code/test commit `9d6a8dda2` hardens `hmailserver/source/WebAdmin/background_distributionlist_recipient_save.php`: the existing user-level denial remains first, the handler requires `hmailRequirePostCsrfToken()`, and `distributionlistid`, `recipientid`, `domainid`, `action`, and `recipientaddress` are read from POST only. Existing domain-admin ownership, `Settings -> Domains -> Domain -> DistributionLists -> DistributionList -> Recipients` lookup, Add/Edit/Delete, `RecipientAddress` assignment, Save, redirects, and POST+CSRF forms remain unchanged. `WebAdminDistributionListRecipientPostOnlySourceTests` plus recipient/distribution-list/domain/store contract coverage passes `20/20`; full Net10 passes `1278` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP lint and WebAdmin request execution were not run.
- The .NET `DistributionListRecipients` adapter and SQL administration store remain intentionally read-only for Add, item setters, Save, and Delete; direct child activation remains `E_ACCESSDENIED`, while authenticated Settings read access and SQL parameterization remain unchanged. SMTP list-policy behavior, live reconfiguration, service/database/Data-directory state, and SEC-18 staging state did not change. The next live WebAdmin mutation is `hmailserver/source/WebAdmin/background_route_save.php` POST-only/CSRF hardening.

## SEC-14 WebAdmin Alias Mutation Hardening (2026-07-28)

- Legacy parity was confirmed in `InterfaceDomainAliases::get_ItemByDBID` and `Add` (`hmailserver/source/Server/COM/InterfaceDomainAliases.cpp:19-42,73-105`), `InterfaceDomainAlias::put_AliasName`, `Save`, `put_DomainID`, and `Delete` (`hmailserver/source/Server/COM/InterfaceDomainAlias.cpp:88-160`), and `PersistentDomainAlias::SaveObject`/`DeleteObject` (`hmailserver/source/Server/Common/Persistence/PersistentDomainAlias.cpp:58-117`). Legacy `Add()` creates an unsaved alias scoped to the owning domain collection; `Save()` inserts or updates `hm_domain_aliases`, assigns the generated `daid` on insert, and adds the new item to its parent after successful persistence. `DomainID` is retained as a compatibility setter and does not change the owning domain. Installed `DomainAliases`/`DomainAlias` interfaces and coclasses remain anchored by `hmailserver/source/Server/hMailServer/hMailServer.idl:1717-1750,3155-3167`.
- Code/test commit `1dc35f169` hardens `hmailserver/source/WebAdmin/background_alias_save.php`: the existing user-level denial remains first, the handler requires `hmailRequirePostCsrfToken()`, and `domainid`, `aliasid`, `action`, `aliasname`, `aliasvalue`, and `aliasactive` are read from POST only. Existing same-domain domain-admin ownership, `Domain -> Aliases` lookup, `IsAddAllowed`, Add/Edit/Delete, `Name`/`Value`/`Active` assignments, Save, redirects, and POST+CSRF forms remain unchanged. `WebAdminAliasPostOnlySourceTests`, `DomainAliasesComContractTests`, `DomainsComContractTests`, and `SqlServerDomainAliasAdministrationStoreTests` pass `14/14`; full Net10 passes `1279` with `3` opt-in skips. PHP CLI is unavailable, so runtime PHP lint and WebAdmin request execution were not run.
- The .NET `DomainAliases` adapter and SQL administration store remain intentionally read-only for Add, item setters, Save, and Delete; direct child activation remains `E_ACCESSDENIED`, while the installed IID/vtable/DISPID/class identities and authenticated domain-owned read boundary remain unchanged. SMTP alias behavior, live reconfiguration, service/database/Data-directory state, and SEC-18 staging state did not change. The next live WebAdmin mutation is `hmailserver/source/WebAdmin/background_route_save.php` POST-only/CSRF hardening.

### Historical SEC-18 context

SEC-18 dedicated-IIS staging evidence is complete and the additive broker contract is frozen in `06820aa7d`: the actual PHP FastCGI `new COM(...)` path reached a fresh isolated out-of-process probe, the server captured the exact dedicated pool SID through `CoImpersonateClient`/`OpenThreadToken` at `SecurityIdentification`, wrong-SID method access returned `E_ACCESSDENIED`, and non-elevated desktop activation was denied before method entry. The elevated collector reports one dedicated mapping and matching worker/caller SID; both independent reviewers are GREEN for bounded non-production implementation. Commit `4b109682d` adds the Windows caller-token source, reverts before operation execution, and evaluates exact broker-only AppID evidence without writing registry state. Commit `dfef2a136` adds the read-only dual-view broker/legacy AppID snapshot source. The current slice extends readback to the full installed Application CLSID/ProgID/CurVer/LocalServer32/TypeLib/Interface graph, compares key paths/presence/value names/kinds/native bytes, and preserves the observed six-key Registry32 Application CLSID-subtree absence, rejects unexpected direct descendants, and validates canonical fixed values plus installation-specific executable/HELPDIR path relationships; focused graph/preflight tests pass 20/20 and the full Net10 suite passes 1122/1122. The broker remains unregistered: live graph evidence is now captured, but both independent security and reality reviews remain RED because key ACLs, collector attestation, and native-reader integration coverage are still missing. The next smallest safe production-gate slice is a deterministic checked-in collector/attestation with DirectSubkeyNames and canonical-validation metadata, followed by key security-descriptor capture and native-reader integration coverage; do not register the broker, write AppID ACLs, enable PHP session cutover, alter the installed `hMailServer.Application` IID/CLSID/ProgID/AppID/type library/permissions, activate hMailServer.Application, or touch production service/database/data/firewall/protocol state. SEC-11 existing-row `RuleCriterias.DeleteByDBID` is complete in `2da6a368c`; SEC-20 remains partially hardened with live DNS/TLS integration and adaptive high-load timeout gaps documented below.
- Updated 2026-07-27: SecurityRanges Add/new-item Save insert parity is committed in `5c7c1010e`. Legacy `InterfaceSecurityRanges::LoadSettings` (`hmailserver/source/Server/COM/InterfaceSecurityRanges.cpp:12-21`) requires server-admin ownership before loading; `InterfaceSecurityRanges::Add` (`.../InterfaceSecurityRanges.cpp:158-185`) creates an unsaved `HM::SecurityRange` facade scoped to its owning collection; `InterfaceSecurityRange::Save` (`hmailserver/source/Server/COM/InterfaceSecurityRange.cpp:36-61`) requires server-admin authentication, calls `PersistentSecurityRange::SaveObject`, assigns the identity, and adds the item to its parent. `SecurityRange::SecurityRange`/`SetLowerIPString`/`SetUpperIPString` (`hmailserver/source/Server/Common/BO/SecurityRange.cpp:17-53`) establish zero/default state and ignore invalid setter return values; `PersistentSecurityRange::SaveObject`/`Validate` (`hmailserver/source/Server/Common/Persistence/PersistentSecurityRange.cpp:60-117,268-289`) define validation, 100-character name truncation, fallback expiry time, split-IP persistence, and insert-on-ID-zero semantics. The .NET path now threads the authenticated administrator callback through `Settings.SecurityRanges`, stages the unsaved facade, inserts parameterized `hm_securityranges` data with `OUTPUT INSERTED.rangeid`, assigns the generated ID, and appends only the new snapshot; existing rows remain read-only and direct activation remains `E_ACCESSDENIED`. Focused SecurityRanges/store coverage passes `10/10`; full Net10 passes `1228` with `3` opt-in tests skipped. No live SQL integration ran because no approved isolated connection was configured. Next slice: authenticated `SecurityRanges.DeleteByDBID` membership-scoped delete parity; keep item `Delete`, index `Delete`, `SetDefault`, live policy reconfiguration, SMTP trust, and `Account.ValidatePassword` out of scope.
## Current Completed Slice (2026-08-08, RESTORE ROLLBACK ACCEPTANCE)

Test-only code/test commit `e93d0021e` adds the missing disposable LocalDB failure-path acceptance for the bound raw non-DB restore executor. The fixture now includes the production SQL delete-path tables used by `SqlServerAccountAdministrationStore.DeleteAccountSql` and `SqlServerDomainAdministrationStore.DeleteDomainByIdSql`; an injected alias insertion failure proves that the original Data directory is restored and generated account/domain rows are removed. The direct executor rethrows the original `InvalidOperationException` after successful compensation.

Legacy behavior is documented by `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`), `BackupManager::OnBackupFailed` (`BackupManager.cpp:177`), and sequential `Collection<T,P>::XMLLoad`/`DeleteAll` (`Collection.h:85`): legacy restore is destructive and non-compensating, while the bounded .NET slice deliberately adds compensation for disposable targets. The parity review also inspected `PersistentDomain::DeleteObject`, `PersistentAccount::DeleteObject`, and the current SQL delete symbols.

Focused LocalDB restore coverage is `3 passed, 0 failed, 0 skipped`; default full Net10 is `1908 passed, 0 failed, 18 skipped`; SQL-enabled full Net10 is `1919 passed, 5 failed, 2 skipped`, with the five failures confined to pre-existing `SqlServerMessageIndexingIntegrationTests` fixtures. No production code, service, database, Data directory, COM registration, DCOM ACL, or IIS state changed.

This slice does not prove later distribution-list/recipient rollback or queued COM `OnBackupFailed` delivery. The next independent slice is either the shared SQL/filesystem transaction or a durable restore journal with crash/recovery tests; service/COM queued acceptance remains environment-blocked, and normal-installation deletion/reinitialization ordering remains open.
## Current Completed Slice (2026-08-08, DISTRIBUTION-LIST ROLLBACK ACCEPTANCE)

Test-only code/test commit `387589ce1` adds disposable LocalDB failure-path acceptance after distribution-list creation. An injected first recipient insertion failure verifies the temporary Data directory is restored and generated distribution-list, alias, account, and domain rows are removed through the real SQL stores. Focused restore coverage is `4 passed, 0 failed, 0 skipped`; the default full suite remains `1908 passed, 0 failed, 18 skipped`, and the SQL-enabled full run is `1919 passed, 5 failed, 2 skipped` with five unrelated message-indexing fixture failures.

Legacy anchors inspected by `hmail_parity_explorer`: `BackupExecuter::StartRestore` (`BackupExecuter.cpp:230`), `Collection<T,P>::XMLLoad`/`DeleteAll` (`Collection.h:85`), `DistributionList::XMLLoad`/`XMLLoadSubItems` (`DistributionList.cpp:31`), `DistributionListRecipients::PreSaveObject`/`GetCollectionName`, and persistent distribution-list/list-recipient `SaveObject` methods. Legacy persists the list before recipients and leaves partial rows on failure; .NET restores in dependency order and compensates in reverse order. This test does not yet prove deletion of an already-inserted recipient.

No production code, service, SQL/Data directory, COM registration, DCOM ACL, or IIS state changed. Next smallest slice: allow one real recipient insert, fail the second, and assert real recipient deletion precedes list cleanup. Shared SQL/filesystem transactionality, queued COM acceptance, normal-installation ordering/reinitialization, and release readiness remain open.
## Current Completed Slice (2026-08-08, TRANSACTION-SCOPED RESTORE STORES FAIL CLOSED)

Code/test commit `342f95325` closes the transaction-scoped SQL store null-dereference gap. The DB-only restore transaction adapter in `SqlServerBackupRestoreMetadataTransactionFactory` supplies one shared connection and transaction to the five insert stores used by `MetadataBackupRestoreExecutor`; every other store member now rejects a transaction-scoped call with `InvalidOperationException` instead of dereferencing its intentionally absent standalone connection factory. Focused SQL-enabled coverage is `1 passed, 0 failed, 0 skipped` for all unsupported members and `11 passed, 0 failed, 0 skipped` for the restore round-trip group; default full Net10 is `1914 passed, 0 failed, 26 skipped`.

Legacy references: `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`), `Collection<T,P>::XMLLoad`/`DeleteAll` (`hmailserver/source/Server/Common/BO/Collection.h:85-220`), and persistence through each collection's `SaveObject`. Legacy exposes no transaction-scoped administration-store object; its `IInterfaceDatabase::BeginTransaction` only affects `ExecuteSQL` (`hmailserver/source/Server/COM/InterfaceDatabase.idl` and implementation). Current symbols are `IBackupRestoreMetadataTransaction`, `SqlServerBackupRestoreMetadataTransaction`, `SqlServerDomainAdministrationStore`, `SqlServerAccountAdministrationStore`, `SqlServerAliasAdministrationStore`, `SqlServerDistributionListAdministrationStore`, and `SqlServerDistributionListRecipientAdministrationStore`. No COM IID/vtable/DISPID, authentication boundary, SQL schema, SMTP trust, service, IIS, DCOM, or production state changed.

This is a fail-closed adapter hardening slice, not broader restore parity. Security/reality status remains RED: queued restore work needs execution-time authorization revalidation; archived account credentials need restore-specific encryption-type preservation; commit-failure/connection-loss ambiguity, normal-installation deletion/reinitialization, service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Next slice: revalidate authorization immediately before a queued restore opens its SQL transaction, with cancellation/negative coverage.
## Current Completed Slice (2026-08-08, QUEUED RESTORE AUTHORIZATION REVALIDATION)

Code/test commit `1e717bb1d` adds an execution-time `EnsureAuthorized()` at the start of `BackupManager.ExecuteRestoreAsync`, before the queued restore executor is invoked. A delayed queue test invalidates authentication after enqueue and proves the executor is not called and the COM error is `E_ACCESSDENIED`. Focused `BackupManagerComContractTests` coverage is `23 passed, 0 failed, 0 skipped`; default full Net10 is `1915 passed, 0 failed, 26 skipped`.

Legacy references: `COMAuthentication::Authenticate` (`hmailserver/source/Server/COM/COMAuthentication.cpp:30-68`) clears `account_`; `InterfaceBackupManager::StartBackup`/`LoadBackup` (`hmailserver/source/Server/COM/InterfaceBackupManager.cpp:26-69`) and `InterfaceBackup::StartRestore` (`hmailserver/source/Server/COM/InterfaceBackup.cpp:16-33`) do not recheck that state after object acquisition; `BackupManager::StartRestore` (`hmailserver/source/Server/Common/Application/BackupManager.cpp:74-98`) queues work; `BackupTask::DoWork` (`hmailserver/source/Server/Common/Application/BackupTask.cpp:27-40`) directly calls `BackupExecuter::StartRestore` (`BackupExecuter.cpp:230-336`). Current symbols are `BackupManager.ExecuteRestoreAsync`, `BackupTaskQueue`, `BackupTaskHostedService`, and `MetadataBackupRestoreExecutor.ExecuteAsync`. This is deliberate .NET security hardening over legacy behavior; installed COM identity, vtable/DISPID shape, direct activation, SMTP trust, SQL schema, and production state are unchanged.

The guard closes the queue-to-worker dispatch gap, but security review found two remaining boundaries: authentication may be invalidated during async archive/preflight before the final SQL/filesystem mutation admission, and pending queue items are not deterministically drained on service cancellation (`BackupTaskQueue.Dispose`, `BackupTaskHostedService.ExecuteAsync`). Account credential/encryption preservation, crash-safe SQL/NTFS recovery, full deletion/reinitialization, service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Security/reality status is RED. Next slice: final authorization admission immediately before restore mutation, with an in-flight invalidation test.
## Current Completed Slice (2026-08-08, DB-ONLY RESTORE FINAL AUTHORIZATION ADMISSION)

Code/test commit `2e9728452` threads an internal, non-COM authorization capability from `Backup` into the DB-only `RestoreMetadataAsync` path. It runs after existing-domain/duplicate validation and immediately before the SQL transaction factory can open a transaction or metadata inserts can begin. A gated `GetDomainsAsync` test invalidates authentication during read-only preflight and proves `BeginAsync` and all metadata inserts are skipped with `E_ACCESSDENIED`. Focused coverage is `8 passed, 0 failed, 0 skipped`; default full Net10 is `1916 passed, 0 failed, 26 skipped`.

Legacy anchors: `BackupExecuter::StartRestore` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-335`) parses, deletes, and restores without worker-time COM authorization; `Collection<T,P>::XMLLoad`/`DeleteAll` (`hmailserver/source/Server/Common/BO/Collection.h:85-135,203-215`) governs legacy metadata ordering. Current symbols are `Backup.EnsureAuthorizedForRestoreCommit`, `MetadataBackupRestoreExecutor.ExecuteDbOnlyMetadataRestoreAsync`, and `RestoreMetadataAsync`. Installed IID/CLSID/ProgID/DISPID/vtable/type-library identity, direct activation boundaries, SQL schema, SMTP trust, non-DB restore behavior, and production state are unchanged.

This closes deterministic invalidation during read-only DB preflight, but not the atomic race between a plain generation check and the subsequent `BeginAsync`; a shared authorization authority/lease with a linearization point is still required. Non-DB filesystem staging has no equivalent final admission. Pending queue shutdown cleanup, archived credential/encryption preservation, crash-safe SQL/NTFS recovery, full deletion/reinitialization, service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Security/reality status is RED. Next slice: atomic authorization admission lease at the SQL mutation boundary.
## Current Completed Slice (2026-08-08, ATOMIC DB-ONLY RESTORE AUTHORIZATION LEASE)

Code/test commit `aae5137a9` replaces the DB-only restore check-then-use gap with an internal per-`Application` authorization authority and disposable lease. `BeginAuthentication` and `CompleteAuthentication` serialize generation/state publication; `AcquireLeaseAsync` linearizes restore admission against invalidation and holds the gate through SQL transaction begin, metadata mutation, commit, and disposal. Focused `BackupRestoreExecutionTests` coverage is `9 passed, 0 failed, 0 skipped`; default full Net10 is `1917 passed, 0 failed, 26 skipped`.

Legacy anchors: `COMAuthentication::Authenticate` (`hmailserver/source/Server/COM/COMAuthentication.cpp:30-68`) resets shared state without a generation/lease; `InterfaceBackup::StartRestore` (`hmailserver/source/Server/COM/InterfaceBackup.cpp:16-33`) queues through `BackupManager`; `BackupTask::DoWork` (`hmailserver/source/Server/Common/Application/BackupTask.cpp:27-40`) performs restore without worker authorization. Current symbols are `ApplicationAuthorizationAuthority`, `Application.Authenticate`, `Backup.AcquireAuthorizationLeaseAsync`, `BackupManager.ExecuteRestoreAsync`, and DB-only `MetadataBackupRestoreExecutor.RestoreMetadataAsync`. The new authority is `[ComVisible(false)]`; IID/CLSID/ProgID/DISPID/vtable/type-library identity, direct activation, SQL schema, SMTP behavior, non-DB restore, and production state are unchanged.

The DB-only auth race is closed with linearization semantics: invalidation first denies lease acquisition; lease acquisition first allows the admitted restore to finish. Non-DB filesystem staging still lacks an equivalent lease, pending queue shutdown cleanup is incomplete, and archived credential/encryption preservation, crash-safe SQL/NTFS recovery, full deletion/reinitialization, service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Security/reality status remains RED. Next slice: apply the same lease before non-DB filesystem staging, without changing SMTP or COM identity.
## Current Completed Slice (2026-08-08, NON-DB RESTORE AUTHORIZATION LEASE)

Code/test commit `efd873fea` extends the existing per-Application authorization lease from DB-only restore to the non-DB `RestoreDomains|RestoreMessages` path. `MetadataBackupRestoreExecutor.ExecuteNonDbDataRestoreAsync` now acquires the lease immediately before `BackupRestoreDataDirectoryRuntime.RestoreAsync`, so authentication invalidation cannot interleave with Data-directory extraction/replacement, rollback, or the subsequent metadata commit. The installed COM identities, vtables, DISPIDs, authentication boundary, SMTP trust, and live reconfiguration are unchanged.

Legacy anchors are `BackupExecuter::StartRestore` and `BackupExecuter::RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`), which perform the non-DB Data-directory replacement without worker-time Administrator revalidation. The current symbols are `MetadataBackupRestoreExecutor.ExecuteNonDbDataRestoreAsync`, `BackupRestoreDataDirectoryRuntime.RestoreAsync`, and `Backup.AcquireAuthorizationLeaseAsync`; the lease is deliberate security hardening around the legacy mutation sequence. Focused `BackupRestoreExecutionTests` coverage is `11 passed, 0 failed, 0 skipped`; default full Net10 is `1919 passed, 0 failed, 26 skipped`.

Tests cover invalidation before lease admission with zero filesystem or SQL mutation and invalidation blocked through copy and metadata commit, followed by retained-object denial. This closes the non-DB authorization interleaving gap but does not provide crash-safe SQL/filesystem atomicity, automatic journal reconciliation, full normal-installation deletion/reinitialization ordering, or service/COM evidence. Next slice: deterministic shutdown drain/abort and idempotent cleanup for queued restore tasks.
## Current Completed Slice (2026-08-08, QUEUED RESTORE SHUTDOWN CLEANUP)

Code/test commit `68a75427c` closes the queued restore shutdown-cleanup gap. `BackupTaskQueue` now completes its channel and aborts every pending request exactly once; `BackupTaskHostedService.StopAsync` drains after the worker stops, and a request dequeued after cancellation is aborted before execution. Restore requests supply `Backup.CleanupArchiveBinding` as the abort callback, while `ThreadStopped` is guarded against duplicate notification so `BackupOperationCoordinator` is released deterministically. Installed COM IID/CLSID/ProgID/DISPID/vtable/type-library identities, direct activation boundaries, authenticated access, SMTP trust, and live reconfiguration are unchanged.

Legacy anchors are `WorkQueue::Stop` (`hmailserver/source/Server/Common/Threading/WorkQueue.cpp:128-181`), `BackupTask::DoWork` (`hmailserver/source/Server/Common/Application/BackupTask.cpp:27-41`), and maintenance queue removal in `Application::ExitInstance` (`hmailserver/source/Server/Common/Application/Application.cpp:211-234`). Legacy stops the IO context and joins workers, so pending tasks do not start; only a task that runs calls `BackupManager::OnThreadStopped`. Current symbols are `BackupTaskQueue.CompleteAndAbortPending`, `BackupTaskRequest.AbortPending`, `BackupTaskHostedService.StopAsync`, `BackupOperationRuntime.WrapTaskWithPreflight`, and `BackupManager.StartRestore`.

Focused queue/restore contract coverage is `28 passed, 0 failed, 0 skipped`; default full Net10 is `1922 passed, 0 failed, 26 skipped`. Tests cover idempotent disposal, real hosted-service cancellation with one running and one pending request, post-shutdown enqueue rejection, and archive binding cleanup before execution. This closes deterministic queued cleanup only; running-task cancellation still depends on the task honoring its token, and service/COM end-to-end restore remains blocked. Next slice: preserve archived account credential/encryption type during restore.
## Current Completed Slice (2026-08-08, QUEUE SHUTDOWN ADMISSION FENCE)

Code/test commit `ba8390f2c` closes the race where backup/restore dispatch could be accepted between service cancellation and the prior pending-drain call. `BackupTaskQueue.StopAccepting` and `TryEnqueue` share a lifecycle lock; shutdown marks the queue stopping and completes the channel before awaiting the active worker. Pending requests are drained after worker shutdown, while requests dequeued after cancellation are aborted before execution. COM identities, direct activation, authenticated boundaries, SQL schema, SMTP trust, and live reconfiguration remain unchanged.

Legacy `WorkQueue::Stop` calls `io_context_.stop()` before interrupt/join (`hmailserver/source/Server/Common/Threading/WorkQueue.cpp:128-181`), and `WorkQueueManager::RemoveQueue` stops before erase (`hmailserver/source/Server/Common/Threading/WorkQueueManager.cpp:68-107`). Current anchors are `BackupTaskQueue.StopAccepting`, `BackupTaskQueue.TryEnqueue`, and `BackupTaskHostedService.StopAsync`. Focused queue/restore coverage is `28 passed, 0 failed, 0 skipped`; default full Net10 is `1922 passed, 0 failed, 26 skipped`.

This closes admission linearization only. A non-cooperative active task can still outlive the host stop timeout; abort callback exception isolation and explicit archive-binding ownership for duplicate/denied dispatch remain open. Next slice: fence or explicitly retain non-cooperative active restore during shutdown.
## Current Completed Slice (2026-08-08, ACTIVE RESTORE SHUTDOWN COMPLETION FENCE)

Code/test commit `3599ce44d` prevents `BackupTaskHostedService.StopAsync` from returning while a dequeued backup/restore delegate is still executing. The service tracks the active request with a completion source, closes queue admission first, propagates cancellation, and waits for the request’s execute/abort path and idempotent `ThreadStopped` notification to finish before returning. This is a lifecycle fence, not forced thread termination; a delegate that never completes can still hold shutdown open, which is explicit and safer than returning while restore mutation continues.

Legacy anchors are `WorkQueue::Stop` (`hmailserver/source/Server/Common/Threading/WorkQueue.cpp:128-181`) and `BackupTask::DoWork` (`hmailserver/source/Server/Common/Application/BackupTask.cpp:27-41`). Current symbols are `BackupTaskHostedService.ExecuteAsync`, `BackupTaskHostedService.StopAsync`, `BackupTaskQueue.StopAccepting`, and `BackupTaskRequest.NotifyThreadStopped`. Focused queue/restore coverage is `29 passed, 0 failed, 0 skipped`; default full Net10 is `1923 passed, 0 failed, 26 skipped`.

The completion fence closes the active-task-after-shutdown-return gap without changing COM contracts or production resources. Remaining queue work is per-request abort exception isolation, followed by non-queued archive-binding ownership on duplicate/denied dispatch. Next slice: isolate pending-abort callback failures while draining.
## Current Completed Slice (2026-08-08, QUEUED ABORT FAILURE ISOLATION)

Code/test commit `4864a4dba` makes queued restore cleanup continue after one abort callback fails. `BackupTaskQueue.CompleteAndAbortPending` catches and traces each callback failure before reading the next request; the hosted service logs the analogous failure for a request dequeued after cancellation. `BackupTaskRequest.AbortPending` retains its idempotent abort and `ThreadStopped` guarantees. COM identities, direct activation, authentication boundaries, SQL schema, SMTP trust, and live reconfiguration remain unchanged.

Legacy anchors are `WorkQueue::Stop` and `BackupTask::DoWork` (`hmailserver/source/Server/Common/Threading/WorkQueue.cpp:128-181`, `hmailserver/source/Server/Common/Application/BackupTask.cpp:27-41`); legacy pending tasks have no cleanup callback and are discarded. Current symbols are `BackupTaskQueue.CompleteAndAbortPending`, `BackupTaskRequest.AbortPending`, and `BackupTaskHostedService.ExecuteAsync`. Focused queue/restore coverage is `30 passed, 0 failed, 0 skipped`; default full Net10 is `1924 passed, 0 failed, 26 skipped`.

This closes the drain-continuation gap only. Non-queued restore archive binding ownership on duplicate/denied dispatch, credential encryption semantics, restore atomicity/order, service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Next slice: close non-queued archive binding ownership on duplicate/denied dispatch.
## Current Completed Slice (2026-08-08, REJECTED RESTORE ARCHIVE OWNERSHIP)

Code/test commit `d1fa4a6a5` closes non-queued restore snapshot leaks without breaking same-object duplicate dispatch. `Backup.TryClaimRestoreDispatch` gives the first queued request ownership; `BackupManager.StartRestore` cleans and releases only a distinct rejected backup on `AlreadyRunning`, queue unavailable, dispatch exception, or pre-dispatch authorization denial. The queued request and its abort callback release the claim after execution or cleanup. Installed COM IID/CLSID/ProgID/DISPID/vtable/type-library identity, direct activation boundaries, SQL schema, SMTP trust, and live reconfiguration are unchanged.

Legacy anchors are `BackupManager::StartRestore` (`hmailserver/source/Server/Common/Application/BackupManager.cpp:75-98`), `BackupTask::SetBackupToRestore` (`hmailserver/source/Server/Common/Application/BackupTask.cpp:44-49`), and `BackupTask::DoWork` (`hmailserver/source/Server/Common/Application/BackupTask.cpp:27-41`). Current symbols are `Backup.TryClaimRestoreDispatch`, `Backup.ReleaseRestoreDispatchClaim`, `Backup.StartRestore`, and `BackupManager.StartRestore`. Tests prove same-object duplicate preservation, distinct `AlreadyRunning` cleanup, denied unclaimed cleanup, and existing queued cleanup. Focused manager/COM coverage is `26 passed, 0 failed, 0 skipped`; default full Net10 is `1926 passed, 0 failed, 26 skipped`.

This closes ownership on the bounded dispatch outcomes only. Credential encryption semantics, restore atomicity and full ordering, service/COM, SEC-18, installer, AD/DC, and lifecycle gates remain open. Next slice: preserve archived account credential/encryption type during restore.
## Current Next Slice (2026-08-08, PUBLIC RESTORE MANIFEST CAPABILITY)

Code/test commit `4cc66396a` adds an additive transaction-scoped manifest API beside `DeleteAllPublicFoldersForRestoreAsync`. The SQL batch captures `messagefilename`, `messageaccountid`, `messagefolderid`, nullable `accountaddress`, and `messagetype` for legacy public messages (`messageaccountid = 0`) before deleting recipients, search rows, metadata, ACLs, messages, and non-Inbox public folders. The `messagetype <> 2` recipient rule remains unchanged, and the transaction owner still controls commit/rollback. Legacy references are `BackupExecuter::StartRestore`/`RestoreDataDirectory_` (`hmailserver/source/Server/Common/Application/BackupExecuter.cpp:230-388`), `PersistentIMAPFolder::DeleteObject`, and `PersistentMessage::DeleteObject`/`DeleteFile`.

Focused static SQL coverage is `11 passed, 0 failed, 0 skipped`; the disposable SQL integration set is `3 skipped` because the approved local connection and database-create opt-in are unavailable. Default full Net10 is `1939 passed, 0 failed, 31 skipped`. The capability is intentionally not called by `BackupRestoreExecution`, does not restore settings/public-folder XML, does not delete files, and does not invoke reinitialization. Security review found no new injection, authorization, COM, or scope issue; the capability is not release evidence until commit-path SQL integration runs. Next slice: run the approved populated SQL/Data acceptance for this manifest/no-return cleanup, then design the separately gated full public-folder/settings orchestration.

Release remains RED: full restore ordering, archive public-folder metadata/message parity, physical cleanup and rollback, SQL/filesystem atomicity, native destination/copy/delete containment, process-kill/power-loss, service/COM, SEC-18, installer, migration, AD/DC, and soak evidence remain open.
## Current Audit Note (2026-08-09, RETAINED DRAFT AND ACCOUNT ID AUTHORIZATION PARITY)

Code/test commit `1bbffd74d` completes one bounded retained-object authorization slice. Legacy `InterfaceAccount::SetAuthentication` (`hmailserver/source/Server/COM/InterfaceAccount.cpp:53-56`) is attached by `get_Messages` (`:420-445`), `get_FetchAccounts` (`:694-721`), `get_Rules` (`:790-815`), and `get_IMAPFolders` (`:817-845`); scalar `get_AdminLevel` (`:723-738`) also rechecks the shared authentication state. The .NET path now denies direct parameterless `Account.ID` access and rechecks authentication before retained `FetchAccount` and `Message` draft setter staging, while `Save()` remains guarded.

Focused Account/FetchAccount/Message coverage is `81 passed, 0 failed, 0 skipped`; `git diff --check` passed. The filtered full assembly is `1944 passed, 0 failed, 31 skipped`. The default full run remains `1947 passed, 2 failed, 31 skipped`; `ClamWinScannerTestRuntimeTests` and `CustomScannerTestRuntimeTests` fail cleanup with `UnauthorizedAccessException` when host AV locks generated `.eml` files. Installed COM identity, direct activation boundaries other than the corrected Account identity read, SQL ownership, SMTP trust, and live reconfiguration are unchanged.

Next code slice: propagate retained authorization into Message `Attachments`, `Recipients`, and `Headers`, plus Rule `Criterias` and their child facades, with focused stale-object denial tests. Nested Domain collections, SQL/Data restore, service/COM, SEC-18, live performance, and soak gates remain open; release is RED.
## Current Audit Note (2026-08-09, RETAINED MESSAGE AND RULE CHILD AUTHORIZATION PARITY)

Code/test commit `5542ced99` completes one bounded retained-child authorization slice. Legacy `InterfaceMessage::get_Attachments` (`hmailserver/source/Server/COM/InterfaceMessage.cpp:336-357`) and `get_Recipients` (`:736-755`) attach `COMAuthentication` to returned collections and child facades. Legacy `InterfaceRule::get_Criterias` (`hmailserver/source/Server/COM/InterfaceRule.cpp:168-189`) and `InterfaceRuleCriterias::{get_Item,get_ItemByDBID,Add}` (`hmailserver/source/Server/COM/InterfaceRuleCriterias.cpp:15-72,90-118`) attach the same authentication state. The .NET path now carries the live callback through Message Attachments/Recipients and RuleCriterias/RuleCriteria, denying retained reads and mutations after failed reauthentication.

Focused Message/RuleCriteria coverage is `60 passed, 0 failed, 0 skipped`; the filtered full assembly is `1945 passed, 0 failed, 31 skipped`; `git diff --check` passed. The default full run remains `1947 passed, 2 failed, 31 skipped` because host AV locks generated scanner `.eml` files during cleanup. Legacy `InterfaceMessage::get_Headers` (`InterfaceMessage.cpp:363-388`) does not call `SetAuthentication`, so MessageHeaders were deliberately left unchanged. Installed COM identity, IDL/vtable/DISPID shape, direct activation, SQL owner scope, SMTP trust, and live reconfiguration are unchanged.

Next code slice: parity-confirm and propagate retained authorization through nested Domain collections, with owner-scope and stale-child denial tests. Approved disposable SQL/Data restore acceptance, SEC-18, service/out-of-process COM, live performance, and soak gates remain open; release is RED.

## Current Audit Note (2026-08-09, RELEASE-GATE REVALIDATION; NO CODE GAP)

The retained Domain child-collection audit found no safe production code slice. Legacy `InterfaceDomain::get_Accounts`, `get_Aliases`, `get_DomainAliases`, and `get_DistributionLists` (`hmailserver/source/Server/COM/InterfaceDomain.cpp:308-478`) attach the shared `COMAuthentication`; the .NET `Domain` adapter (`hmailserver/source/Server.Net10/src/HMailServer.ComInterop/Domains.cs:811-821,882-889`) evaluates the guarded snapshot before child-adapter creation and propagates the live callback. Existing owner-scoped child adapters therefore deny retained access after failed authentication without changing legacy read behavior. `DomainsComContractTests`, `LinksComContractTests`, and `WebAdminRoutePostOnlySourceTests` pass `27/27`; no production code changed.

The historical `background_route_save.php` POST-only/CSRF item is already complete in `8d684e638` and covered by `WebAdminRoutePostOnlySourceTests`; do not restart it. Reality review remains RED: the approved disposable SQL/Data connection and isolated-create opt-in are unset; live SQL/FTS, protocol/load, service/COM, SEC-18, installer, AD/DC, and 24-hour soak evidence remain unavailable. The default suite still has the two known host-AV scanner cleanup failures. The untracked benchmark directory contains an older `d7d5cb6c4` artifact and must not be used as release evidence; the newer `565175aff` run was temporary and unstaged. Next executable action is to provision an explicitly disposable SQL/Data target, not to alter production code on this host.
