Add to a text area control and confirm that the text still exists after the udpate.
</textarea>

Add to a text area control and confirm that no message box is shown after save.
</textarea><script>alert('hej');</script>

Account deletion must use a POST form containing page=background_account_save, action=delete, domainid, accountid, and csrftoken.
Confirm that a GET request to background_account_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the account.
Confirm that the corresponding valid POST deletes the selected non-current account and returns to the domain account list.

Domain deletion must use a POST form containing page=background_domain_save, action=delete, domainid, and csrftoken.
Confirm that a GET request to background_domain_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the domain.
Confirm that a non-server-admin POST cannot delete a domain, while a valid server-admin POST deletes the selected domain and returns to the domain list.

Alias deletion must use a POST form containing page=background_alias_save, action=delete, domainid, aliasid, and csrftoken.
Confirm that a GET request to background_alias_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the alias.
Confirm that a valid same-domain administrator POST deletes the selected alias and returns to the alias list, while cross-domain or user requests remain denied.

Distribution list deletion must use a POST form containing page=background_distributionlist_save, action=delete, domainid, distributionlistid, and csrftoken.
Confirm that a GET request to background_distributionlist_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the distribution list.
Confirm that a valid same-domain administrator POST deletes the selected distribution list and returns to the distribution list, while cross-domain or user requests remain denied.

Distribution list recipient deletion must use a POST form containing page=background_distributionlist_recipient_save, action=delete, domainid, distributionlistid, recipientid, and csrftoken.
Confirm that a GET request to background_distributionlist_recipient_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the recipient.
Confirm that a valid same-domain administrator POST deletes the selected recipient and returns to that distribution list's recipient list, while cross-domain or user requests remain denied.

Domain alias deletion must use a POST form containing page=background_domain_name_save, action=delete, domainid, aliasid, and csrftoken.
Confirm that a GET request to background_domain_name_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the domain alias.
Confirm that a non-server-admin POST cannot delete a domain alias, while a valid server-admin POST deletes the selected alias and returns to its domain.

Route deletion must use a POST form containing page=background_route_save, action=delete, routeid, and csrftoken.
Confirm that a GET request to background_route_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the route.
Confirm that a non-server-admin POST cannot delete a route, while a valid server-admin POST deletes the selected route and returns to the route list.

Route address deletion must use a POST form containing page=background_route_address_save, action=delete, routeid, routeaddressid, and csrftoken.
Confirm that a GET request to background_route_address_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the route address.
Confirm that a non-server-admin POST cannot delete a route address, while a valid server-admin POST deletes the selected address and returns to its parent route's address list.

Security range deletion must use a POST form containing page=background_securityrange_save, action=delete, securityrangeid, and csrftoken.
Confirm that a GET request to background_securityrange_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the security range.
Confirm that a non-server-admin POST cannot delete a security range, while a valid server-admin POST follows the selected range's existing legacy delete/save control flow.

Incoming relay deletion must use a POST form containing page=background_incomingrelay_save, action=delete, relayid, and csrftoken.
Confirm that a GET request to background_incomingrelay_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the relay.
Confirm that a non-server-admin POST cannot delete an incoming relay, while a valid server-admin POST follows the selected relay's existing legacy delete/save control flow without changing SMTP trust behavior.

SSL certificate deletion must use a POST form containing page=background_sslcertificate_save, action=delete, id, and csrftoken.
Confirm that a GET request to background_sslcertificate_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the certificate.
Confirm that a non-server-admin POST cannot delete a certificate, while a valid server-admin POST follows the selected certificate's existing legacy delete/save control flow without changing TLS or TCP/IP port behavior.

TCP/IP port deletion must use a POST form containing page=background_tcpipport_save, action=delete, tcpipportid, and csrftoken.
Confirm that a GET request to background_tcpipport_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the TCP/IP port.
Confirm that a non-server-admin POST cannot delete a TCP/IP port, while a valid server-admin POST deletes the selected port and returns to the TCP/IP port list without restarting listeners.

DNS blacklist deletion must use a POST form containing page=background_dnsblacklist_save, action=delete, id, and csrftoken.
Confirm that a GET request to background_dnsblacklist_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the DNS blacklist.
Confirm that a non-server-admin POST cannot delete a DNS blacklist, while a valid server-admin POST preserves the selected blacklist's legacy delete/save control flow without changing anti-spam DNS-blacklist behavior.

SURBL server deletion must use a POST form containing page=background_surblserver_save, action=delete, id, and csrftoken.
Confirm that a GET request to background_surblserver_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the SURBL server.
Confirm that a non-server-admin POST cannot delete a SURBL server, while a valid server-admin POST preserves the selected server's legacy delete/save control flow without changing anti-spam SURBL behavior.

Whitelist address deletion must use a POST form containing page=background_whitelistaddress_save, action=delete, ID, and csrftoken.
Confirm that a GET request to background_whitelistaddress_save with action=delete and a valid CSRF token returns HTTP 405 without deleting the whitelist address.
Confirm that a non-server-admin POST cannot delete a whitelist address, while a valid server-admin POST deletes the selected address and returns to the whitelist-address list without changing anti-spam whitelist behavior.
